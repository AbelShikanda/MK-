is it just me or is this still processing trend packages in a confirmed range?
and still confriming that its not a range?

2026.01.19 02:58:47.035	2025.07.23 09:24:23   [PACKAGE_FRESHNESS] [2025.07.23 09:24:23] Interface for XAUUSD: age=0 sec, maxAge=300 sec, fresh=true
2026.01.19 02:58:47.035	2025.07.23 09:24:23   [CHECK_CLOSED_POSITIONS_START] [2025.07.23 09:24:23] Checking 1 symbols for closed positions
2026.01.19 02:58:47.035	2025.07.23 09:24:23   [DEBUG-EA-PACKAGE_INTERVAL] [2025.07.23 09:24:23] Package update interval reached
2026.01.19 02:58:47.035	2025.07.23 09:24:23   [PACKAGE_FRESHNESS] [2025.07.23 09:24:23] Interface for XAUUSD: age=0 sec, maxAge=300 sec, fresh=true
2026.01.19 02:58:47.035	2025.07.23 09:24:23   [CHECK_CLOSED_POSITIONS_START] [2025.07.23 09:24:23] Checking 1 symbols for closed positions
2026.01.19 02:58:47.035	2025.07.23 09:24:24   [PACKAGE_FRESHNESS] [2025.07.23 09:24:24] Interface for XAUUSD: age=1 sec, maxAge=300 sec, fresh=true
2026.01.19 02:58:47.035	2025.07.23 09:24:24   [CHECK_CLOSED_POSITIONS_START] [2025.07.23 09:24:24] Checking 1 symbols for closed positions
2026.01.19 02:58:47.035	2025.07.23 09:24:24   [PACKAGE_FRESHNESS] [2025.07.23 09:24:24] Interface for XAUUSD: age=1 sec, maxAge=300 sec, fresh=true
2026.01.19 02:58:47.035	2025.07.23 09:24:24   [CHECK_CLOSED_POSITIONS_START] [2025.07.23 09:24:24] Checking 1 symbols for closed positions
2026.01.19 02:58:47.035	2025.07.23 09:24:25   [ONTIMER] [2025.07.23 09:24:24] OnTimer called
2026.01.19 02:58:47.035	2025.07.23 09:24:25   [PACKAGE_FRESHNESS] [2025.07.23 09:24:24] Interface for XAUUSD: age=1 sec, maxAge=300 sec, fresh=true
2026.01.19 02:58:47.035	2025.07.23 09:24:25   [CHECK_CLOSED_POSITIONS_START] [2025.07.23 09:24:24] Checking 1 symbols for closed positions
2026.01.19 02:58:47.035	2025.07.23 09:24:25   Fixed Range Initialized: 3416.35000 - 3438.99000 (Width: 0.66%)
2026.01.19 02:58:47.035	2025.07.23 09:24:25   [MarketRegime] [2025.07.23 09:24:25] Fixed Range Initialized: 3416.35000 - 3438.99000 (Width: 0.66%)
2026.01.19 02:58:47.035	2025.07.23 09:24:25   [Range] [2025.07.23 09:24:25] RANGE XAUUSD 0.00 lots @ 3427.67000
2026.01.19 02:58:47.036	2025.07.23 09:24:25   Range Invalidated: MA alignment confirms trending market (Cooldown: 8 bars)
2026.01.19 02:58:47.036	2025.07.23 09:24:25   [MarketRegime] [2025.07.23 09:24:25] Range Invalidated: MA alignment confirms trending market (Cooldown: 8 bars)
2026.01.19 02:58:47.036	2025.07.23 09:24:25   [Range] [2025.07.23 09:24:25] INVALIDATE XAUUSD 0.00 lots @ 3424.07000
2026.01.19 02:58:47.036	2025.07.23 09:24:25   [Recommendation] [2025.07.23 09:24:25] HOLD XAUUSD 0.00 lots @ 3424.07000
2026.01.19 02:58:47.036	2025.07.23 09:24:25   [MarketRegime] [2025.07.23 09:24:25] ANALYSIS: Ranging High Vol | Confidence: 30% | Action: Fade carefully with tight stops | R/R: 2.0
2026.01.19 02:58:47.036	2025.07.23 09:24:25   [DEBUG-EA-MARKET_REGIME] [2025.07.23 09:24:25] XAUUSD: Ranging High Vol (30% confidence) | Root: RANGING
2026.01.19 02:58:47.036	2025.07.23 09:24:25   [PACKAGE_FRESHNESS] [2025.07.23 09:24:25] Interface for XAUUSD: age=2 sec, maxAge=300 sec, fresh=true
2026.01.19 02:58:47.036	2025.07.23 09:24:25   [CHECK_CLOSED_POSITIONS_START] [2025.07.23 09:24:25] Checking 1 symbols for closed positions
2026.01.19 02:58:47.036	2025.07.23 09:24:25   [PACKAGE_FRESHNESS] [2025.07.23 09:24:25] Interface for XAUUSD: age=2 sec, maxAge=300 sec, fresh=true
2026.01.19 02:58:47.036	2025.07.23 09:24:25   [CHECK_CLOSED_POSITIONS_START] [2025.07.23 09:24:25] Checking 1 symbols for closed positions
2026.01.19 02:58:47.036	2025.07.23 09:24:26   [AUTO_PACKAGE_CREATION] [2025.07.23 09:24:26] Creating package for XAUUSD
2026.01.19 02:58:47.036	2025.07.23 09:24:26   [PACKAGE_CREATION] [2025.07.23 09:24:26] === AUTO-PACKAGE CREATION ===
2026.01.19 02:58:47.036	2025.07.23 09:24:26   [PACKAGE_CREATION] [2025.07.23 09:24:26] Symbol: XAUUSD
2026.01.19 02:58:47.036	2025.07.23 09:24:26   Fixed Range Initialized: 3416.35000 - 3438.99000 (Width: 0.66%)
2026.01.19 02:58:47.036	2025.07.23 09:24:26   [MarketRegime] [2025.07.23 09:24:26] Fixed Range Initialized: 3416.35000 - 3438.99000 (Width: 0.66%)
2026.01.19 02:58:47.036	2025.07.23 09:24:26   [Range] [2025.07.23 09:24:26] RANGE XAUUSD 0.00 lots @ 3427.67000
2026.01.19 02:58:47.037	2025.07.23 09:24:26   Range Invalidated: MA alignment confirms trending market (Cooldown: 8 bars)
2026.01.19 02:58:47.037	2025.07.23 09:24:26   [MarketRegime] [2025.07.23 09:24:26] Range Invalidated: MA alignment confirms trending market (Cooldown: 8 bars)
2026.01.19 02:58:47.037	2025.07.23 09:24:26   [Range] [2025.07.23 09:24:26] INVALIDATE XAUUSD 0.00 lots @ 3424.05000
2026.01.19 02:58:47.037	2025.07.23 09:24:26   [Recommendation] [2025.07.23 09:24:26] HOLD XAUUSD 0.00 lots @ 3424.05000
2026.01.19 02:58:47.037	2025.07.23 09:24:26   [MarketRegime] [2025.07.23 09:24:26] ANALYSIS: Ranging High Vol | Confidence: 30% | Action: Fade carefully with tight stops | R/R: 2.0
2026.01.19 02:58:47.037	2025.07.23 09:24:26   [MARKET_REGIME_ANALYSIS] [2025.07.23 09:24:26] Market Regime: RANGING | State: Ranging High Vol | Confidence: 30%
2026.01.19 02:58:47.037	2025.07.23 09:24:26   [PACKAGE_TYPE] [2025.07.23 09:24:26] 📊 MARKET IS RANGING - Creating RANGE package
2026.01.19 02:58:47.037	2025.07.23 09:24:26   [RANGE_ANALYSIS_START] [2025.07.23 09:24:26] Analyzing XAUUSD on M15
2026.01.19 02:58:47.037	2025.07.23 09:24:26   [PRICE_TREND_CHECK] [2025.07.23 09:24:26] XAUUSD: Price change=0.01%, ATR=0.09%, TrendScore=0.1
2026.01.19 02:58:47.037	2025.07.23 09:24:26   [VALID_RANGE_DETECTED] [2025.07.23 09:24:26] XAUUSD: SUCCESS - Support=3416.35000 (2 touches), Resistance=3424.58000 (6 touches), Width=0.24%, Inside=100.0%
2026.01.19 02:58:47.037	2025.07.23 09:24:26   [FAILED_BREAKOUTS] [2025.07.23 09:24:26] XAUUSD: 1 failed breakouts
2026.01.19 02:58:47.037	2025.07.23 09:24:26   [RANGE_ANALYSIS_COMPLETE] [2025.07.23 09:24:26] Overall: BEARISH (100%) | Range: 0.2% width | Trap: 56% | Bias: BEARISH (84%) | Action: FADE_EDGE_STRONG | Context: PRICE_NEAR_RESISTANCE | Touch: RESISTANCE_DOMINANT (0.33:1)
2026.01.19 02:58:47.037	2025.07.23 09:24:26   [PACKAGE_CREATION_COMPLETE] [2025.07.23 09:24:26] 🎯 Package created: Type=RANGE, Regime=RANGING, State=Ranging High Vol, Action=FADE_EDGE_STRONG, Dir=BEARISH, Conf=86.0%
2026.01.19 02:58:47.037	2025.07.23 09:24:26   [PROCESS_PACKAGE_START] [2025.07.23 09:24:26] =====================================
2026.01.19 02:58:47.037	2025.07.23 09:24:26   [PROCESS_PACKAGE_START] [2025.07.23 09:24:26] >>> STARTING PACKAGE PROCESSING | Symbol: XAUUSD
2026.01.19 02:58:47.037	2025.07.23 09:24:26   [PACKAGE_DETAILS] [2025.07.23 09:24:26] Package Details: Type=RANGE, Confidence=86.0%, Dir=BEARISH, Regime=RANGING, Action=FADE_EDGE_STRONG, Trap=56.3%
2026.01.19 02:58:47.037	2025.07.23 09:24:26   [MARKET_REGIME_CHECK] [2025.07.23 09:24:26] --- MARKET REGIME ANALYSIS ---
2026.01.19 02:58:47.037	2025.07.23 09:24:26   Fixed Range Initialized: 3416.35000 - 3438.99000 (Width: 0.66%)
2026.01.19 02:58:47.037	2025.07.23 09:24:26   [MarketRegime] [2025.07.23 09:24:26] Fixed Range Initialized: 3416.35000 - 3438.99000 (Width: 0.66%)
2026.01.19 02:58:47.037	2025.07.23 09:24:26   [Range] [2025.07.23 09:24:26] RANGE XAUUSD 0.00 lots @ 3427.67000
2026.01.19 02:58:47.038	2025.07.23 09:24:26   Range Invalidated: MA alignment confirms trending market (Cooldown: 8 bars)
2026.01.19 02:58:47.038	2025.07.23 09:24:26   [MarketRegime] [2025.07.23 09:24:26] Range Invalidated: MA alignment confirms trending market (Cooldown: 8 bars)
2026.01.19 02:58:47.038	2025.07.23 09:24:26   [Range] [2025.07.23 09:24:26] INVALIDATE XAUUSD 0.00 lots @ 3424.05000
2026.01.19 02:58:47.038	2025.07.23 09:24:26   [Recommendation] [2025.07.23 09:24:26] HOLD XAUUSD 0.00 lots @ 3424.05000
2026.01.19 02:58:47.038	2025.07.23 09:24:26   [MarketRegime] [2025.07.23 09:24:26] ANALYSIS: Ranging High Vol | Confidence: 30% | Action: Fade carefully with tight stops | R/R: 2.0
2026.01.19 02:58:47.038	2025.07.23 09:24:26   [MARKET_REGIME_RESULT] [2025.07.23 09:24:26] Market Regime Analysis Complete:
2026.01.19 02:58:47.038	2025.07.23 09:24:26   [MARKET_REGIME_RESULT] [2025.07.23 09:24:26]   - Root State: RANGING
2026.01.19 02:58:47.038	2025.07.23 09:24:26   [MARKET_REGIME_RESULT] [2025.07.23 09:24:26]   - Market State: Ranging High Vol
2026.01.19 02:58:47.038	2025.07.23 09:24:26   [ROUTING_START] [2025.07.23 09:24:26] --- STRICT REGIME-BASED ROUTING ---
2026.01.19 02:58:47.038	2025.07.23 09:24:26   [PACKAGE_TYPE] [2025.07.23 09:24:26] Package type: RANGE
2026.01.19 02:58:47.038	2025.07.23 09:24:26   [ROUTING_DECISION] [2025.07.23 09:24:26] 📊 STRICT: Market is RANGING - using RANGE processor
2026.01.19 02:58:47.038	2025.07.23 09:24:26   [PROCESS_RANGE_PACKAGE] [2025.07.23 09:24:26] === PROCESSING RANGE PACKAGE === | Symbol: XAUUSD | Regime: RANGING | Action: FADE_EDGE_STRONG | Trap: 56.3%
2026.01.19 02:58:47.038	2025.07.23 09:24:26   [FIND_SYMBOL_INDEX] [2025.07.23 09:24:26] Found symbol XAUUSD at index 0
2026.01.19 02:58:47.038	2025.07.23 09:24:26   [HAS_SYMBOL] [2025.07.23 09:24:26] Checking if symbol XAUUSD exists: YES
2026.01.19 02:58:47.038	2025.07.23 09:24:26   [FIND_SYMBOL_INDEX] [2025.07.23 09:24:26] Found symbol XAUUSD at index 0
2026.01.19 02:58:47.038	2025.07.23 09:24:26   [RANGE_DATA] [2025.07.23 09:24:26] Regime: RANGING | Action: FADE_EDGE_STRONG | Trap: 56.3% | Avoid: NO
2026.01.19 02:58:47.038	2025.07.23 09:24:26   [PACKAGE_FRESHNESS] [2025.07.23 09:24:26] Interface for XAUUSD: age=0 sec, maxAge=300 sec, fresh=true
2026.01.19 02:58:47.038	2025.07.23 09:24:26   [IS_PACKAGE_FRESH] [2025.07.23 09:24:26] Package freshness for XAUUSD: FRESH
2026.01.19 02:58:47.038	2025.07.23 09:24:26   [ANALYZE_POSITIONS_START] [2025.07.23 09:24:26] Analyzing positions for symbol index 0
2026.01.19 02:58:47.038	2025.07.23 09:24:26   [ANALYZE_POSITIONS] [2025.07.23 09:24:26] Analyzing positions for XAUUSD (magic: 16582)
2026.01.19 02:58:47.038	2025.07.23 09:24:26   [POSITIONS_TOTAL] [2025.07.23 09:24:26] Total positions in terminal: 0
2026.01.19 02:58:47.039	2025.07.23 09:24:26   [ANALYZE_POSITIONS_RESULT] [2025.07.23 09:24:26] Position analysis for XAUUSD: State: NO_POSITION | Buy: 0 | Sell: 0 | Profit: 0.00 | Volume: 0.00
2026.01.19 02:58:47.039	2025.07.23 09:24:26   [MAKE_RANGE_DECISION] [2025.07.23 09:24:26] =====================================
2026.01.19 02:58:47.039	2025.07.23 09:24:26   [MAKE_RANGE_DECISION] [2025.07.23 09:24:26] === MAKING RANGE DECISION === | Symbol: XAUUSD | Index: 0
2026.01.19 02:58:47.039	2025.07.23 09:24:26   [RANGE_CONTEXT_ANALYSIS] [2025.07.23 09:24:26] --- Getting enhanced range analysis with context ---
2026.01.19 02:58:47.039	2025.07.23 09:24:26   [RANGE_ANALYSIS_START] [2025.07.23 09:24:26] Analyzing XAUUSD on M15
2026.01.19 02:58:47.039	2025.07.23 09:24:26   [PRICE_TREND_CHECK] [2025.07.23 09:24:26] XAUUSD: Price change=0.01%, ATR=0.09%, TrendScore=0.1
2026.01.19 02:58:47.039	2025.07.23 09:24:26   [VALID_RANGE_DETECTED] [2025.07.23 09:24:26] XAUUSD: SUCCESS - Support=3416.35000 (2 touches), Resistance=3424.58000 (6 touches), Width=0.24%, Inside=100.0%
2026.01.19 02:58:47.039	2025.07.23 09:24:26   [FAILED_BREAKOUTS] [2025.07.23 09:24:26] XAUUSD: 1 failed breakouts
2026.01.19 02:58:47.039	2025.07.23 09:24:26   [RANGE_ANALYSIS_COMPLETE] [2025.07.23 09:24:26] Overall: BEARISH (100%) | Range: 0.2% width | Trap: 56% | Bias: BEARISH (84%) | Action: FADE_EDGE_STRONG | Context: PRICE_NEAR_RESISTANCE | Touch: RESISTANCE_DOMINANT (0.33:1)
2026.01.19 02:58:47.039	2025.07.23 09:24:26   [RANGE_CONTEXT_INFO] [2025.07.23 09:24:26] Context: PRICE_NEAR_RESISTANCE | Tactical: SELL_AT_RESISTANCE | Touches: RESISTANCE_DOMINANT (0.33:1) | Edge: RESISTANCE
2026.01.19 02:58:47.039	2025.07.23 09:24:26   [TOUCH_DETAILS] [2025.07.23 09:24:26] Support touches: 2 | Resistance touches: 6 | Ratio: 0.33
2026.01.19 02:58:47.039	2025.07.23 09:24:26   [RANGE_REGIME_ANALYSIS] [2025.07.23 09:24:26] --- Getting market regime analysis ---
2026.01.19 02:58:47.039	2025.07.23 09:24:26   Fixed Range Initialized: 3416.35000 - 3438.99000 (Width: 0.66%)
2026.01.19 02:58:47.039	2025.07.23 09:24:26   [MarketRegime] [2025.07.23 09:24:26] Fixed Range Initialized: 3416.35000 - 3438.99000 (Width: 0.66%)
2026.01.19 02:58:47.039	2025.07.23 09:24:26   [Range] [2025.07.23 09:24:26] RANGE XAUUSD 0.00 lots @ 3427.67000
2026.01.19 02:58:47.039	2025.07.23 09:24:26   Range Invalidated: MA alignment confirms trending market (Cooldown: 8 bars)
2026.01.19 02:58:47.039	2025.07.23 09:24:26   [MarketRegime] [2025.07.23 09:24:26] Range Invalidated: MA alignment confirms trending market (Cooldown: 8 bars)
2026.01.19 02:58:47.039	2025.07.23 09:24:26   [Range] [2025.07.23 09:24:26] INVALIDATE XAUUSD 0.00 lots @ 3424.05000
2026.01.19 02:58:47.039	2025.07.23 09:24:26   [Recommendation] [2025.07.23 09:24:26] HOLD XAUUSD 0.00 lots @ 3424.05000
2026.01.19 02:58:47.040	2025.07.23 09:24:26   [MarketRegime] [2025.07.23 09:24:26] ANALYSIS: Ranging High Vol | Confidence: 30% | Action: Fade carefully with tight stops | R/R: 2.0
2026.01.19 02:58:47.040	2025.07.23 09:24:26   [RANGE_REGIME_DETAILS] [2025.07.23 09:24:26] Range Analysis: Root: RANGING | State: Ranging High Vol (30%) | Next: Ranging Low Vol
2026.01.19 02:58:47.040	2025.07.23 09:24:26   Action: Fade carefully with tight stops | Position: VERY SMALL | Dir: Neutral (dangerous)
2026.01.19 02:58:47.040	2025.07.23 09:24:26   Stop: 4000000.0 pips | TP: 7500000.0 pips | R/R: 2.0
2026.01.19 02:58:47.040	2025.07.23 09:24:26   Description: High volatility range. Emotional swings, frequent stop hunts.
2026.01.19 02:58:47.040	2025.07.23 09:24:26   [RANGE_VALIDATION_FAILED] [2025.07.23 09:24:26] ❌ Not in ranging market! RootState: RANGING, MarketState: Ranging High Vol
2026.01.19 02:58:47.040	2025.07.23 09:24:26   [RANGE_DECISION_MADE] [2025.07.23 09:24:26] Range decision: HOLD
2026.01.19 02:58:47.040	2025.07.23 09:24:26   [ROUTING_COMPLETE] [2025.07.23 09:24:26] ✅ Routed to RANGE processor | Reason: Ranging market - optimal routing
2026.01.19 02:58:47.040	2025.07.23 09:24:26   [SYMBOL_CHECK_START] [2025.07.23 09:24:26] --- SYMBOL REGISTRATION ---
2026.01.19 02:58:47.040	2025.07.23 09:24:26   [FIND_SYMBOL_INDEX] [2025.07.23 09:24:26] Found symbol XAUUSD at index 0
2026.01.19 02:58:47.040	2025.07.23 09:24:26   [HAS_SYMBOL] [2025.07.23 09:24:26] Checking if symbol XAUUSD exists: YES
2026.01.19 02:58:47.040	2025.07.23 09:24:26   [FIND_SYMBOL_INDEX] [2025.07.23 09:24:26] Found symbol XAUUSD at index 0
2026.01.19 02:58:47.040	2025.07.23 09:24:26   [REGIME_UPDATE] [2025.07.23 09:24:26] Package updated: Regime=RANGING→RANGING, State=Ranging High Vol
2026.01.19 02:58:47.040	2025.07.23 09:24:26   [PACKAGE_RESULT] [2025.07.23 09:24:26] [2025.07.23 09:24:26] XAUUSD | Processor: RANGE | Decision: HOLD | Status: COMPLETED | MarketState: Ranging High Vol | Mismatch: NO
2026.01.19 02:58:47.040	2025.07.23 09:24:26   [PACKAGE_PROCESSING] [2025.07.23 09:24:26] [2025.07.23 09:24:26] XAUUSD | Processor: RANGE | Decision: HOLD | Status: COMPLETED | MarketState: Ranging High Vol | Mismatch: NO
2026.01.19 02:58:47.040	2025.07.23 09:24:26   [NO_ACTION_TAKEN] [2025.07.23 09:24:26] ⚠️ No action taken for XAUUSD | Decision: HOLD | MarketState: Ranging High Vol
2026.01.19 02:58:47.040	2025.07.23 09:24:26   [PROCESS_PACKAGE_END] [2025.07.23 09:24:26] <<< PACKAGE PROCESSING COMPLETE | Symbol: XAUUSD | Decision: HOLD | Regime: RANGING | State: Ranging High Vol | Processor: RANGE
2026.01.19 02:58:47.040	2025.07.23 09:24:26   [PROCESS_PACKAGE_END] [2025.07.23 09:24:26] =====================================
2026.01.19 02:58:47.040	2025.07.23 09:24:26   
2026.01.19 02:58:47.040	2025.07.23 09:24:26   [PACKAGE_FRESHNESS] [2025.07.23 09:24:26] Interface for XAUUSD: age=0 sec, maxAge=300 sec, fresh=true
2026.01.19 02:58:47.040	2025.07.23 09:24:26   [CHECK_CLOSED_POSITIONS_START] [2025.07.23 09:24:26] Checking 1 symbols for closed positions
2026.01.19 02:58:47.040	2025.07.23 09:24:26   [DEBUG-EA-PACKAGE_INTERVAL] [2025.07.23 09:24:26] Package update interval reached


______________________________________________________________________________________

for your reference

//+------------------------------------------------------------------+
//|                                                   MarketRegime.mq5|
//|             Simplified Market Regime & Lifecycle Detection       |
//+------------------------------------------------------------------+
// Logger Integration Status: FULLY INTEGRATED
// All logging uses static Logger::Log() calls with debug wrapper
//+------------------------------------------------------------------+

#property copyright "Copyright 2024"
#property version "1.00"
#property strict

#include <Math\Stat\Math.mqh>

#include "../Headers/Enums.mqh"
#include "../Headers/Structures.mqh"
#include "../Data/IndicatorManager.mqh"
#include "../Utils/Logger.mqh"

// ====================== DEBUG SETTINGS ======================
bool DEBUG_REGIME_ENABLED = true;

// Simple debug function using Logger
void DebugRegimeLogFile(string context, string message)
{
   if (DEBUG_REGIME_ENABLED)
   {
      Logger::Log("MarketRegime", StringFormat("%s: %s", context, message), true, false);
   }
}

//+------------------------------------------------------------------+
//| Market Regime Detector Class                                     |
//+------------------------------------------------------------------+
class MarketRegimeDetector
{
private:
   string m_symbol;
   ENUM_TIMEFRAMES m_timeframe;
   double m_fixedRangeTop;
   double m_fixedRangeBottom;
   datetime m_rangeStartTime;
   bool m_rangeActive;
   int m_lookbackBars;

   // Account info for position sizing
   double m_accountBalance;
   double m_accountRiskPercent;

   // Indicator Manager instance
   IndicatorManager *m_indicatorManager;
   bool m_useIndicatorManager;

   int m_rangeLookbackBars;       // How many bars to look back for the range
   int m_barsSinceLastRangeInit;  // Bars since last range initialization
   datetime m_lastRangeCheckTime; // Last time we checked for reinitialization

   // NEW: Range invalidation tracking
   datetime m_lastInvalidationTime; // Track when range was invalidated
   int m_invalidationCooldownBars;  // Bars to wait before new range

public:
   MarketRegimeDetector(string symbol = NULL,
                        ENUM_TIMEFRAMES tf = PERIOD_H1,
                        double accountBalance = 10000,
                        double riskPercent = 1.0,
                        bool useIndicatorManager = true)
   {
      m_symbol = (symbol == NULL) ? Symbol() : symbol;
      m_timeframe = tf;
      m_rangeActive = false;
      m_lookbackBars = 10;
      m_accountBalance = accountBalance;
      m_accountRiskPercent = riskPercent;
      m_useIndicatorManager = useIndicatorManager;

      m_rangeLookbackBars = 10; // Default: reinitialize every 8 bars
      m_barsSinceLastRangeInit = 0;
      m_lastRangeCheckTime = 0;

      // NEW: Initialize invalidation tracking
      m_lastInvalidationTime = 0;
      m_invalidationCooldownBars = 8; // Wait 8 bars after invalidation

      DebugRegimeLogFile("Constructor", StringFormat("Initializing detector for %s timeframe %d",
                                                     m_symbol, m_timeframe));

      // Initialize indicator manager if requested
      if (m_useIndicatorManager)
      {
         m_indicatorManager = new IndicatorManager(m_symbol);
         if (!m_indicatorManager.Initialize())
         {
            Logger::LogError("MarketRegime", "IndicatorManager failed to initialize. Falling back to direct indicators.");
            m_useIndicatorManager = false;
         }
         else
         {
            DebugRegimeLogFile("Constructor", "IndicatorManager initialized successfully");
         }
      }
      else
      {
         m_indicatorManager = NULL;
         DebugRegimeLogFile("Constructor", "Using direct indicators (no IndicatorManager)");
      }

      // Initialize fixed range
      InitializeFixedRange();
   }

   ~MarketRegimeDetector()
   {
      DebugRegimeLogFile("Destructor", "Cleaning up MarketRegimeDetector");

      if (m_indicatorManager != NULL)
      {
         m_indicatorManager.Deinitialize();
         delete m_indicatorManager;
         DebugRegimeLogFile("Destructor", "IndicatorManager cleaned up");
      }

      Logger::Log("MarketRegime", "Detector shutdown complete", true, false);
   }

   //+------------------------------------------------------------------+
   //| MAIN PUBLIC METHOD: Get complete market analysis                |
   //+------------------------------------------------------------------+
   MarketAnalysis GetMarketRegime()
   {
      DebugRegimeLogFile("GetMarketRegime", "Starting market analysis");

      MarketAnalysis analysis;

      // 1. Check if we should reinitialize the range
      CheckAndReinitializeRange();

      // 2. Initialize fixed range if not active AND conditions are right
      if (!m_rangeActive && ShouldCreateNewRange())
      {
         InitializeFixedRange();
      }

      // 3. Get current state using indicators
      analysis.state = DetectCurrentState();
      analysis.rootState = GetRootState(analysis.state);
      analysis.confidence = CalculateConfidence(analysis.state);

      DebugRegimeLogFile("GetMarketRegime", StringFormat("Detected state: %s with %.1f%% confidence",
                                                         MarketAnalysis::GetStateString(analysis.state), analysis.confidence));

      // 4. Determine next likely state
      analysis.nextLikelyState = PredictNextState(analysis.state);

      DebugRegimeLogFile("GetMarketRegime", StringFormat("Next likely state: %s",
                                                         MarketAnalysis::GetStateString(analysis.nextLikelyState)));

      // 5. Generate trading recommendations
      GenerateRecommendations(analysis);

      DebugRegimeLogFile("GetMarketRegime", StringFormat("Recommendation: %s (Position: %s)",
                                                         analysis.action, GetPositionSizeString(analysis.positionSize)));

      // 6. Set description
      analysis.description = GenerateDescription(analysis);

      DebugRegimeLogFile("GetMarketRegime", "Analysis complete");

      // Log final analysis summary
      Logger::Log("MarketRegime", StringFormat("ANALYSIS: %s | Confidence: %.0f%% | Action: %s | R/R: %.1f", MarketAnalysis::GetStateString(analysis.state), analysis.confidence, analysis.action, analysis.riskRewardRatio),
                  true, true);

      return analysis;
   }

private:
   string GetPositionSizeString(ENUM_POSITION_SIZE size)
   {
      switch (size)
      {
      case SIZE_ZERO:
         return "ZERO";
      case SIZE_VERY_SMALL:
         return "VERY_SMALL";
      case SIZE_SMALL:
         return "SMALL";
      case SIZE_MEDIUM:
         return "MEDIUM";
      case SIZE_LARGE:
         return "LARGE";
      default:
         return "UNKNOWN";
      }
   }

   void ReinitializeRange()
   {
      DebugRegimeLogFile("ReinitializeRange", "Reinitializing fixed range...");
      Logger::Log("MarketRegime", "Reinitializing fixed range", true, false);
      InitializeFixedRange();
      m_barsSinceLastRangeInit = 0;
   }

   //+------------------------------------------------------------------+
   //| SMART RANGE REINITIALIZATION                                    |
   //+------------------------------------------------------------------+
   bool ShouldSmartReinitialize()
   {
      DebugRegimeLogFile("ShouldSmartReinitialize", "Checking if smart reinitialization is needed");

      // Get current market state
      ENUM_MARKET_STATE state = DetectCurrentState();
      double confidence = CalculateConfidence(state);

      // Condition 1: Clear ranging state with high confidence
      bool isClearRanging = (state == STATE_RANGING_LOW_VOL || state == STATE_RANGING_HIGH_VOL) &&
                            confidence > 50;

      // Condition 2: Range is too old (more than X bars)
      bool rangeIsOld = m_barsSinceLastRangeInit >= m_rangeLookbackBars;

      // Condition 3: Price has moved slightly outside the range
      bool priceOutOfRange = false;
      if (m_rangeActive)
      {
         double currentPrice = iClose(m_symbol, m_timeframe, 0);
         double rangeWidth = m_fixedRangeTop - m_fixedRangeBottom;
         double atr = GetATR(14);

         if (currentPrice > m_fixedRangeTop + (rangeWidth * 0.3) ||
             currentPrice < m_fixedRangeBottom - (rangeWidth * 0.3))
         {
            priceOutOfRange = true;
         }
      }

      // Decision matrix
      if (rangeIsOld)
      {
         if (isClearRanging || priceOutOfRange)
         {
            DebugRegimeLogFile("ShouldSmartReinitialize", "Conditions met for smart reinitialization");
            return true;
         }

         // Reinitialize if market is in contraction/expansion phase
         if (state == STATE_CONTRACTION || state == STATE_EXPANSION)
         {
            DebugRegimeLogFile("ShouldSmartReinitialize", "Contraction/Expansion phase detected");
            return true;
         }
      }

      DebugRegimeLogFile("ShouldSmartReinitialize", "No need for smart reinitialization");
      return false;
   }

   void CheckAndReinitializeRange()
   {
      datetime currentTime = iTime(m_symbol, m_timeframe, 0);

      if (m_lastRangeCheckTime == currentTime)
         return;

      m_lastRangeCheckTime = currentTime;
      m_barsSinceLastRangeInit++;

      DebugRegimeLogFile("CheckAndReinitializeRange",
                         StringFormat("Bar count since last init: %d", m_barsSinceLastRangeInit));

      // Use smart reinitialization logic
      if (ShouldSmartReinitialize())
      {
         ReinitializeRange();
      }
   }

   //+------------------------------------------------------------------+
   //| SHOULD CREATE NEW RANGE METHOD                                   |
   //+------------------------------------------------------------------+
   bool ShouldCreateNewRange()
   {
      DebugRegimeLogFile("ShouldCreateNewRange", "Checking if new range should be created");

      // 1. Check cooldown period
      if (m_lastInvalidationTime > 0)
      {
         int barsSinceInvalidation = iBarShift(m_symbol, m_timeframe, m_lastInvalidationTime);
         if (barsSinceInvalidation < m_invalidationCooldownBars)
         {
            DebugRegimeLogFile("ShouldCreateNewRange",
                               StringFormat("Still in cooldown: %d/%d bars",
                                            barsSinceInvalidation, m_invalidationCooldownBars));
            return false; // Still in cooldown
         }
      }

      // 2. Market must be clearly ranging (not trending)
      double adx = GetADX(14);
      if (adx >= 25) // ADX too high = trending
      {
         DebugRegimeLogFile("ShouldCreateNewRange",
                            StringFormat("ADX too high for new range: %.1f (>=25)", adx));
         return false;
      }

      // 3. Check MA alignment - price should be near MA50
      double ma50 = GetMAValue(50, 0);
      double currentPrice = iClose(m_symbol, m_timeframe, 0);

      // FIX: Add validation for MA50
      if (ma50 <= 0)
      {
         DebugRegimeLogFile("ShouldCreateNewRange", "Invalid MA50 value");
         return false; // Invalid MA value
      }

      double priceFromMA = MathAbs(currentPrice - ma50) / ma50 * 100;

      if (priceFromMA > 0.8) // Price too far from MA50
      {
         DebugRegimeLogFile("ShouldCreateNewRange",
                            StringFormat("Price too far from MA50: %.2f%% (>0.8%%)", priceFromMA));
         return false;
      }

      // 4. Check for consolidation (low volatility)
      double atr = GetATR(14);
      double volatilityScore = CalculateVolatilityScore(atr, GetBollingerWidth(20, 2));
      if (volatilityScore > 0.5) // Too volatile
      {
         DebugRegimeLogFile("ShouldCreateNewRange",
                            StringFormat("Volatility too high: %.2f (>0.5)", volatilityScore));
         return false;
      }

      // 5. Look for price compression (last 5 bars)
      double high = iHigh(m_symbol, m_timeframe, 0);
      double low = iLow(m_symbol, m_timeframe, 0);

      for (int i = 1; i < 5; i++)
      {
         high = MathMax(high, iHigh(m_symbol, m_timeframe, i));
         low = MathMin(low, iLow(m_symbol, m_timeframe, i));
      }

      double rangeHeight = high - low;
      double avgPrice = (high + low) / 2;

      // FIX: Add validation for avgPrice
      if (avgPrice <= 0)
      {
         DebugRegimeLogFile("ShouldCreateNewRange", "Invalid average price");
         return false;
      }

      double rangePercent = (rangeHeight / avgPrice) * 100;

      if (rangePercent > 0.5) // Range too wide
      {
         DebugRegimeLogFile("ShouldCreateNewRange",
                            StringFormat("Range too wide: %.2f%% (>0.5%%)", rangePercent));
         return false;
      }

      DebugRegimeLogFile("ShouldCreateNewRange", "All conditions met for new range creation");
      return true; // All conditions met, can create new range
   }

   double GetMAValue(int period, int shift = 0)
   {
      if (m_useIndicatorManager && m_indicatorManager != NULL && m_indicatorManager.IsInitialized())
      {
         // Use IndicatorManager for MA values
         double ma9, ma21, ma50, ma89;
         if (m_indicatorManager.GetMAValuesForRange(m_timeframe, ma9, ma21, ma50, ma89, shift))
         {
            if (period == 50)
               return ma50;
            if (period == 89)
               return ma89;
         }
         else
         {
            double ma_fast, ma_slow, ma_medium;
            if (m_indicatorManager.GetMAValues(m_timeframe, ma_fast, ma_slow, ma_medium, shift))
            {
               if (period == 9)
                  return ma_fast;
               if (period == 21)
                  return ma_medium;
               if (period == 89)
                  return ma_slow;
            }
         }
      }

      // Fallback to direct MA calculation
      int handle = iMA(m_symbol, m_timeframe, period, 0, MODE_SMA, PRICE_CLOSE);
      if (handle == INVALID_HANDLE)
         return 0;

      double values[];
      ArraySetAsSeries(values, true);

      if (CopyBuffer(handle, 0, shift, 1, values) > 0)
      {
         IndicatorRelease(handle);
         return values[0];
      }

      IndicatorRelease(handle);
      return 0;
   }

   bool IsMATrendAligned()
   {
      if (m_useIndicatorManager && m_indicatorManager != NULL && m_indicatorManager.IsInitialized())
      {
         // Use IndicatorManager for MA values
         double ma9, ma21, ma50, ma89;
         if (m_indicatorManager.GetMAValuesForRange(m_timeframe, ma9, ma21, ma50, ma89, 0))
         {
            double ma50_10 = GetMAValue(50, 8);
            double ma89_10 = GetMAValue(89, 8);

            if (ma50 <= 0 || ma89 <= 0)
               return false;

            // Calculate separation percentage
            double separation = MathAbs(ma50 - ma89) / ((ma50 + ma89) / 2) * 100;

            // Check alignment and slope
            bool maAlignedBullish = (ma50 > ma89) && (ma50_10 > ma89_10);
            bool maAlignedBearish = (ma50 < ma89) && (ma50_10 < ma89_10);
            bool maSlopeBullish = (ma50 > ma50_10);
            bool maSlopeBearish = (ma50 < ma50_10);

            // Significant separation with same slope = strong trend
            if (separation > 0.5) // 0.5% separation threshold
            {
               if ((maAlignedBullish && maSlopeBullish) ||
                   (maAlignedBearish && maSlopeBearish))
               {
                  DebugRegimeLogFile("IsMATrendAligned", "MA trend alignment confirmed");
                  return true;
               }
            }
         }
      }

      return false;
   }

   void InitializeFixedRange()
   {
      DebugRegimeLogFile("InitializeFixedRange", "Initializing new fixed range");

      int bars = 10; // Fixed 5-bar lookback for initial range
      double highest = 0;
      double lowest = DBL_MAX;

      for (int i = 0; i < bars; i++)
      {
         double high = iHigh(m_symbol, m_timeframe, i);
         double low = iLow(m_symbol, m_timeframe, i);

         if (high > highest)
            highest = high;
         if (low < lowest)
            lowest = low;
      }

      if (highest > lowest)
      {
         m_fixedRangeTop = highest;
         m_fixedRangeBottom = lowest;
         m_rangeStartTime = iTime(m_symbol, m_timeframe, 0);
         m_rangeActive = true;
         m_barsSinceLastRangeInit = 0;

         string rangeInfo = StringFormat("Fixed Range Initialized: %.5f - %.5f (Width: %.2f%%)",
                                         lowest, highest,
                                         ((highest - lowest) / ((highest + lowest) / 2)) * 100);

         Print(rangeInfo);
         Logger::Log("MarketRegime", rangeInfo, true, true);
         DebugRegimeLogFile("InitializeFixedRange", rangeInfo);

         // Log range details
         Logger::LogTrade("Range", m_symbol, "RANGE", 0, (highest + lowest) / 2);
      }
      else
      {
         Logger::LogError("MarketRegime", "Failed to initialize fixed range");
         DebugRegimeLogFile("InitializeFixedRange", "Failed to initialize fixed range");
      }
   }

   bool IsPriceInFixedRange(double price)
   {
      if (!m_rangeActive)
         return false;
      return price >= m_fixedRangeBottom && price <= m_fixedRangeTop;
   }

   //+------------------------------------------------------------------+
   //| CHECK AND INVALIDATE RANGE METHOD                                |
   //+------------------------------------------------------------------+
   void CheckAndInvalidateRange()
   {
      if (!m_rangeActive)
      {
         DebugRegimeLogFile("CheckAndInvalidateRange", "Range not active, nothing to invalidate");
         return;
      }

      DebugRegimeLogFile("CheckAndInvalidateRange", "Checking if range should be invalidated");

      double currentPrice = iClose(m_symbol, m_timeframe, 0);

      // Rule 1: Consecutive closes outside range
      int consecutiveOutside = 0;
      for (int i = 0; i < 3; i++) // Check last 3 bars
      {
         double price = iClose(m_symbol, m_timeframe, i);
         if (!IsPriceInFixedRange(price))
            consecutiveOutside++;
         else
            break; // Break streak if price returns to range
      }

      if (consecutiveOutside >= 2)
      {
         InvalidateRange("2+ consecutive closes outside range");
         return;
      }

      // Rule 2: Strong momentum break with ATR expansion
      double atr = GetATR(14);
      double atr_5 = GetATRValue(14, 5);
      double priceChange = GetPriceChange(3); // Last 3 bars change

      bool strongBreakout = HasRangeBroken(currentPrice) &&
                            (atr > atr_5 * 1.2) &&            // ATR expanded 20%
                            MathAbs(priceChange) > atr * 1.5; // Strong momentum

      if (strongBreakout)
      {
         InvalidateRange("Strong momentum breakout with ATR expansion");
         return;
      }

      // Rule 3: MA confirms trend (not range)
      double ma50 = GetMAValue(50, 0);
      double ma200 = GetMAValue(200, 0);
      double adx = GetADX(14);

      // FIX: Add validation check for MA values
      if (ma50 > 0 && ma200 > 0) // Only proceed if we have valid MA values
      {
         double maDistance = MathAbs(ma50 - ma200) / ((ma50 + ma200) / 2) * 100;

         bool maTrending = (maDistance > 0.5) &&                     // MAs separated
                           ((ma50 > ma200 && currentPrice > ma50) || // Bullish alignment
                            (ma50 < ma200 && currentPrice < ma50));  // Bearish alignment

         if (maTrending && adx >= 20)
         {
            InvalidateRange("MA alignment confirms trending market");
            return;
         }
      }

      DebugRegimeLogFile("CheckAndInvalidateRange", "Range validation passed, keeping range active");
   }

   //+------------------------------------------------------------------+
   //| INVALIDATE RANGE HELPER                                          |
   //+------------------------------------------------------------------+
   void InvalidateRange(string reason)
   {
      DebugRegimeLogFile("InvalidateRange", StringFormat("Invalidating range: %s", reason));

      m_rangeActive = false;
      m_lastInvalidationTime = iTime(m_symbol, m_timeframe, 0);

      string invalidationMsg = StringFormat("Range Invalidated: %s (Cooldown: %d bars)",
                                            reason, m_invalidationCooldownBars);

      Print(invalidationMsg);
      Logger::Log("MarketRegime", invalidationMsg, true, true);
      DebugRegimeLogFile("InvalidateRange", invalidationMsg);

      // Log trade event for range invalidation
      Logger::LogTrade("Range", m_symbol, "INVALIDATE", 0, iClose(m_symbol, m_timeframe, 0));
   }

   //+------------------------------------------------------------------+
   //| MODIFIED HasRangeBroken()                                        |
   //+------------------------------------------------------------------+
   bool HasRangeBroken(double price)
   {
      if (!m_rangeActive)
         return false;

      double atr = GetATR(14);
      double margin = atr * 0.5; // INCREASED buffer from 0.3 ATR to 0.5 ATR

      bool broken = (price > m_fixedRangeTop + margin) ||
                    (price < m_fixedRangeBottom - margin);

      if (broken)
      {
         DebugRegimeLogFile("HasRangeBroken", StringFormat("Range broken at price %.5f (ATR margin: %.5f)", price, margin));
      }

      return broken;
   }

   //+------------------------------------------------------------------+
   //| STATE DETECTION LOGIC - NO UNKNOWN STATES                        |
   //+------------------------------------------------------------------+
   ENUM_MARKET_STATE DetectCurrentState()
   {
      DebugRegimeLogFile("DetectCurrentState", "Starting state detection - NO UNKNOWN states");

      // NEW: Check if current range should be invalidated
      CheckAndInvalidateRange();

      // Get key indicator values
      double adx = GetADX(14);
      double atr = GetATR(14);
      double bbWidth = GetBollingerWidth(20, 2);
      double priceChange = GetPriceChange(8);
      double rangeTouches = CountRangeTouches();

      DebugRegimeLogFile("DetectCurrentState",
                         StringFormat("Indicators: ADX=%.1f, ATR=%.5f, BBWidth=%.4f, PriceChange=%.2f%%, RangeTouches=%d",
                                      adx, atr, bbWidth, priceChange, rangeTouches));

      // ========== FIXED: MA SEPARATION as CONFIDENCE BOOST, not VETO ==========
      double ma50 = GetMAValue(50, 0);
      double ma89 = GetMAValue(89, 0);
      double maDistancePercent = 0;

      if (ma50 > 0 && ma89 > 0)
      {
         maDistancePercent = MathAbs(ma50 - ma89) / ((ma50 + ma89) / 2) * 100;
         DebugRegimeLogFile("DetectCurrentState",
                            StringFormat("MA50=%.5f, MA89=%.5f, Distance=%.2f%%",
                                         ma50, ma89, maDistancePercent));
      }

      // ========== TREND BIAS: If MAs are far apart, INCREASE trend confidence ==========
      double trendBias = 0; // 0 = no bias, 1.0 = strong trend bias
      double rangeBias = 0; // 0 = no bias, 1.0 = strong range bias

      if (maDistancePercent > 0.5) // 0.5% separation = moderate trend bias
      {
         trendBias = MathMin(1.0, (maDistancePercent - 0.5) / 1.0); // Scales 0.5-1.5% to 0-1.0

         // Get MA slope (5 bars ago vs now)
         double ma50_10 = GetMAValue(50, 10);
         bool maSlopeUp = (ma50 > ma50_10);
         bool maSlopeDown = (ma50 < ma50_10);

         // If MAs are diverging (getting further apart), stronger trend bias
         if ((maDistancePercent > 0.75) && (maSlopeUp || maSlopeDown))
         {
            trendBias = MathMin(1.0, trendBias + 0.3); // Extra bias for strong separation with slope
         }

         DebugRegimeLogFile("DetectCurrentState",
                            StringFormat("Trend bias: %.2f, Range bias: %.2f", trendBias, rangeBias));
      }

      double volatilityScore = CalculateVolatilityScore(atr, bbWidth);
      bool volatilityLow = volatilityScore < 0.3;
      bool volatilityHigh = volatilityScore > 0.7;
      bool volatilityRising = IsVolatilityRising();

      DebugRegimeLogFile("DetectCurrentState",
                         StringFormat("Volatility: Score=%.2f, Low=%s, High=%s, Rising=%s",
                                      volatilityScore, volatilityLow ? "true" : "false",
                                      volatilityHigh ? "true" : "false", volatilityRising ? "true" : "false"));

      // Price structure analysis
      bool isTrendingStructure = IsTrendingStructure();
      bool isRangingStructure = IsRangingStructure();

      DebugRegimeLogFile("DetectCurrentState",
                         StringFormat("Structure: Trending=%s, Ranging=%s",
                                      isTrendingStructure ? "true" : "false",
                                      isRangingStructure ? "true" : "false"));

      double currentPrice = iClose(m_symbol, m_timeframe, 0);

      // NEW: State confidence check before ranging logic
      if (m_rangeActive)
      {
         // Check if range is still valid before using it
         if (rangeTouches < 2) // Not enough touches
         {
            // Range exists but isn't being respected
            DebugRegimeLogFile("DetectCurrentState",
                               StringFormat("Range weak: only %d touches, likely breaking out", rangeTouches));
            CheckAndInvalidateRange(); // Mark as invalid
            // Return expansion (trend-like state) instead of unknown
            DebugRegimeLogFile("DetectCurrentState", "Range weak, returning STATE_EXPANSION (trend-like)");
            Logger::Log("MarketRegime", "Market State: EXPANSION (Weak range breaking out)", true, false);
            return STATE_EXPANSION;
         }
      }

      // Decision matrix - FIRST PASS: Clear cases
      // 1. Check for Contraction/Squeeze (STATE_CONTRACTION) - treat as RANGE
      if (volatilityLow && !volatilityRising && adx < 15 && rangeTouches < 2)
      {
         DebugRegimeLogFile("DetectCurrentState", "Detected STATE_CONTRACTION (treat as RANGE)");
         Logger::Log("MarketRegime", "Market State: CONTRACTION (Volatility squeeze - RANGE)", true, false);
         return STATE_CONTRACTION; // Contraction is a ranging state
      }

      // 2. Check for Expansion/Breakout (STATE_EXPANSION) - treat as TREND
      if (HasRangeBroken(currentPrice) &&
          MathAbs(priceChange) > atr * 2)
      {
         DebugRegimeLogFile("DetectCurrentState", "Detected STATE_EXPANSION (treat as TREND)");
         Logger::Log("MarketRegime", "Market State: EXPANSION (Breakout detected - TREND)", true, false);
         return STATE_EXPANSION; // Expansion is a trend state
      }

      // 3. Check for Churn/Exhaustion (STATE_CHURN) - treat as RANGE
      if (volatilityHigh && volatilityRising && adx > 40 &&
          MathAbs(priceChange) < atr * 0.5)
      {
         DebugRegimeLogFile("DetectCurrentState", "Detected STATE_CHURN (treat as RANGE)");
         Logger::Log("MarketRegime", "Market State: CHURN (Exhaustion phase - RANGE)", true, false);
         return STATE_CHURN; // Churn is a ranging state
      }

      // ========== ADD RANGE STRENGTH CHECK ==========
      // Check if price is respecting range boundaries
      bool priceNearRangeBoundary = false;
      if (m_rangeActive)
      {
         double rangeHeight = m_fixedRangeTop - m_fixedRangeBottom;
         double pricePosition = (currentPrice - m_fixedRangeBottom) / rangeHeight;

         // Price near top or bottom (within 15% of boundary)
         if (pricePosition < 0.15 || pricePosition > 0.85)
         {
            priceNearRangeBoundary = true;
            rangeBias = MathMin(1.0, rangeBias + 0.4);
         }

         // Multiple range touches = stronger range
         if (rangeTouches >= 3)
         {
            rangeBias = MathMin(1.0, rangeBias + 0.3);
         }
      }

      // ========== BALANCED DECISION MAKING ==========

      // 4. Check trending states WITH BIAS CONSIDERATION
      if (isTrendingStructure)
      {
         // RESTORE ADX THRESHOLD to 25 (from 22)
         if (adx >= 25) // increase makes trends harder to qualify
         {
            // Apply trend bias if MAs are separated
            double trendStrength = adx / 100.0;                                               // Normalize ADX 0-1
            double finalTrendScore = trendStrength + (trendBias * 0.25) - (rangeBias * 0.35); // consider trend biases.

            // Need reasonable trend strength even with MA bias
            if (finalTrendScore > 0.45) // higher score to make harder for trend to qualify
            {
               // Healthy trend
               if (volatilityLow || volatilityScore < 0.5)
               {
                  DebugRegimeLogFile("DetectCurrentState", "Detected STATE_TRENDING_LOW_VOL");
                  Logger::Log("MarketRegime", "Market State: TRENDING_LOW_VOL (Healthy trend)", true, false);
                  return STATE_TRENDING_LOW_VOL;
               }
               else
               {
                  DebugRegimeLogFile("DetectCurrentState", "Detected STATE_TRENDING_HIGH_VOL");
                  Logger::Log("MarketRegime", "Market State: TRENDING_HIGH_VOL (High vol trend)", true, false);
                  return STATE_TRENDING_HIGH_VOL;
               }
            }
         }
         else if (adx > 40) // Keep this for exhaustion trend
         {
            // Exhaustion trend
            DebugRegimeLogFile("DetectCurrentState", "Detected STATE_TRENDING_HIGH_VOL (Exhaustion)");
            Logger::Log("MarketRegime", "Market State: TRENDING_HIGH_VOL (Exhaustion)", true, false);
            return STATE_TRENDING_HIGH_VOL;
         }
      }

      // 5. Check ranging states WITH BIAS CONSIDERATION
      // SIMPLIFIED RANGING LOGIC
      if (m_rangeActive && IsPriceInFixedRange(currentPrice))
      {
         // Check range strength
         if (rangeTouches >= 2) // Range has been respected
         {
            // Simple: If in range and price is within it, we're ranging
            if (volatilityHigh)
            {
               DebugRegimeLogFile("DetectCurrentState", "Detected STATE_RANGING_HIGH_VOL");
               Logger::Log("MarketRegime", "Market State: RANGING_HIGH_VOL (High volatility range)", true, false);
               return STATE_RANGING_HIGH_VOL;
            }
            else
            {
               DebugRegimeLogFile("DetectCurrentState", "Detected STATE_RANGING_LOW_VOL");
               Logger::Log("MarketRegime", "Market State: RANGING_LOW_VOL (Low volatility range)", true, false);
               return STATE_RANGING_LOW_VOL;
            }
         }
      }

      // ========== TIE-BREAKER: If both trend and range have similar scores ==========
      if (isTrendingStructure && m_rangeActive)
      {
         DebugRegimeLogFile("DetectCurrentState", "Running tie-breaker logic (both trend and range possible)");

         // ========== BALANCED SCORING ==========
         // Trend factors (0-1 scale)
         double trendADX = MathMin(1.0, adx / 100.0);              // ADX contribution
         double trendMA = MathMin(0.3, maDistancePercent / 500.0); // REDUCED: MA contributes max 0.3 (was 0.75)
         double trendStructure = isTrendingStructure ? 0.4 : 0.1;  // Price structure

         // Range factors (0-1 scale)
         double rangeActive = m_rangeActive ? 0.5 : 0.0;              // Range is active
         double rangeTouchesScore = MathMin(0.3, rangeTouches * 0.1); // Range touches
         double rangePositionScore = 0.0;

         // Calculate price position in range (if in range)
         if (m_rangeActive)
         {
            double rangeHeight = m_fixedRangeTop - m_fixedRangeBottom;
            double pricePosition = (currentPrice - m_fixedRangeBottom) / rangeHeight;
            // Price in middle of range = stronger range evidence
            if (pricePosition > 0.3 && pricePosition < 0.7)
               rangePositionScore = 0.2;
         }

         // Composite scores with BALANCED weights
         double trendScore = (trendADX * 0.5) + (trendMA * 0.3) + (trendStructure * 0.2);
         double rangeScore = (rangeActive * 0.4) + (rangeTouchesScore * 0.3) + (rangePositionScore * 0.3);

         DebugRegimeLogFile("DetectCurrentState",
                            StringFormat("Tie-breaker scores: Trend=%.2f, Range=%.2f", trendScore, rangeScore));

         // ========== DECISION WITH THRESHOLDS ==========
         // Both need reasonable minimum scores
         bool trendValid = trendScore >= 0.3;
         bool rangeValid = rangeScore >= 0.3;

         if (trendValid && !rangeValid)
         {
            // Clear trend
            if (volatilityLow || volatilityScore < 0.5)
            {
               DebugRegimeLogFile("DetectCurrentState", "Tie-breaker: Clear trend (TRENDING_LOW_VOL)");
               return STATE_TRENDING_LOW_VOL;
            }
            else
            {
               DebugRegimeLogFile("DetectCurrentState", "Tie-breaker: Clear trend (TRENDING_HIGH_VOL)");
               return STATE_TRENDING_HIGH_VOL;
            }
         }
         else if (rangeValid && !trendValid)
         {
            // Clear range
            if (volatilityHigh)
            {
               DebugRegimeLogFile("DetectCurrentState", "Tie-breaker: Clear range (RANGING_HIGH_VOL)");
               return STATE_RANGING_HIGH_VOL;
            }
            else
            {
               DebugRegimeLogFile("DetectCurrentState", "Tie-breaker: Clear range (RANGING_LOW_VOL)");
               return STATE_RANGING_LOW_VOL;
            }
         }
         else if (trendValid && rangeValid)
         {
            // Both valid - check dominance
            double scoreDifference = trendScore - rangeScore;

            // Trend needs to be SIGNIFICANTLY stronger (>0.15) to win
            if (scoreDifference > 0.15) // Changed from 0.2 to 0.15
            {
               if (volatilityLow || volatilityScore < 0.5)
               {
                  DebugRegimeLogFile("DetectCurrentState", "Tie-breaker: Trend dominant (TRENDING_LOW_VOL)");
                  return STATE_TRENDING_LOW_VOL;
               }
               else
               {
                  DebugRegimeLogFile("DetectCurrentState", "Tie-breaker: Trend dominant (TRENDING_HIGH_VOL)");
                  return STATE_TRENDING_HIGH_VOL;
               }
            }
            // Range needs to be SIGNIFICANTLY stronger to win
            else if (scoreDifference < -0.15) // Range needs larger numbers for advantage
            {
               if (volatilityHigh)
               {
                  DebugRegimeLogFile("DetectCurrentState", "Tie-breaker: Range dominant (RANGING_HIGH_VOL)");
                  return STATE_RANGING_HIGH_VOL;
               }
               else
               {
                  DebugRegimeLogFile("DetectCurrentState", "Tie-breaker: Range dominant (RANGING_LOW_VOL)");
                  return STATE_RANGING_LOW_VOL;
               }
            }
            // Close scores = check additional factors
            else
            {
               DebugRegimeLogFile("DetectCurrentState", "Tie-breaker: Close scores, checking additional factors");

               // Close call - check additional factors
               // 1. Check ADX strength
               if (adx >= 30)
               {
                  // Strong ADX favors trend
                  if (volatilityLow || volatilityScore < 0.5)
                  {
                     DebugRegimeLogFile("DetectCurrentState", "Tie-breaker: ADX favors trend (TRENDING_LOW_VOL)");
                     return STATE_TRENDING_LOW_VOL;
                  }
                  else
                  {
                     DebugRegimeLogFile("DetectCurrentState", "Tie-breaker: ADX favors trend (TRENDING_HIGH_VOL)");
                     return STATE_TRENDING_HIGH_VOL;
                  }
               }
               // 2. Check volatility context
               else if (volatilityHigh)
               {
                  // High volatility favors range (noisy)
                  DebugRegimeLogFile("DetectCurrentState", "Tie-breaker: High vol favors range (RANGING_HIGH_VOL)");
                  return STATE_RANGING_HIGH_VOL;
               }
               // 3. Check recent price action
               else
               {
                  // Default to range for safety (as requested)
                  DebugRegimeLogFile("DetectCurrentState", "Tie-breaker: Default to range (RANGING_LOW_VOL)");
                  return STATE_RANGING_LOW_VOL;
               }
            }
         }
      }

      // ========== FINAL DECISION: NO UNKNOWN STATES ==========
      // If we get here, we need to make a definitive choice

      // Check if we have ANY range evidence
      bool hasRangeEvidence = m_rangeActive || rangeTouches > 0 || volatilityHigh;

      // Check if we have ANY trend evidence
      bool hasTrendEvidence = adx > 20 || isTrendingStructure || maDistancePercent > 0.3;

      DebugRegimeLogFile("DetectCurrentState",
                         StringFormat("Final decision: RangeEvidence=%s, TrendEvidence=%s, Volatility=%.2f, ADX=%.1f",
                                      hasRangeEvidence ? "YES" : "NO",
                                      hasTrendEvidence ? "YES" : "NO",
                                      volatilityScore, adx));

      // DECISION RULES (prioritize RANGE when unclear):
      // 1. If we have ANY range evidence, favor RANGE
      // 2. If ADX is low (<25) and volatility is not high, favor RANGE
      // 3. Only return TREND if we have strong evidence

      if (hasRangeEvidence || adx < 25 || volatilityScore < 0.6)
      {
         // Favor RANGE (safer default)
         if (volatilityHigh)
         {
            DebugRegimeLogFile("DetectCurrentState", "Ambiguous: Favoring STATE_RANGING_HIGH_VOL (safety default)");
            Logger::Log("MarketRegime", "Market State: RANGING_HIGH_VOL (ambiguous, safety default)", true, false);
            return STATE_RANGING_HIGH_VOL;
         }
         else
         {
            DebugRegimeLogFile("DetectCurrentState", "Ambiguous: Favoring STATE_RANGING_LOW_VOL (safety default)");
            Logger::Log("MarketRegime", "Market State: RANGING_LOW_VOL (ambiguous, safety default)", true, false);
            return STATE_RANGING_LOW_VOL;
         }
      }
      else
      {
         // Strong trend evidence
         if (volatilityLow || volatilityScore < 0.5)
         {
            DebugRegimeLogFile("DetectCurrentState", "Ambiguous but strong trend evidence: STATE_TRENDING_LOW_VOL");
            Logger::Log("MarketRegime", "Market State: TRENDING_LOW_VOL (strong evidence)", true, false);
            return STATE_TRENDING_LOW_VOL;
         }
         else
         {
            DebugRegimeLogFile("DetectCurrentState", "Ambiguous but strong trend evidence: STATE_TRENDING_HIGH_VOL");
            Logger::Log("MarketRegime", "Market State: TRENDING_HIGH_VOL (strong evidence)", true, false);
            return STATE_TRENDING_HIGH_VOL;
         }
      }
   }

   ENUM_ROOT_REGIME GetRootState(ENUM_MARKET_STATE state)
   {
      ENUM_ROOT_REGIME rootState;

      switch (state)
      {
      case STATE_TRENDING_LOW_VOL:
      case STATE_TRENDING_HIGH_VOL:
         rootState = REGIME_TRENDING;
         break;

      case STATE_RANGING_LOW_VOL:
      case STATE_RANGING_HIGH_VOL:
         rootState = REGIME_RANGING;
         break;

      default:
         rootState = REGIME_UNKNOWN;
      }

      DebugRegimeLogFile("GetRootState",
                         StringFormat("Root state for %s: %s",
                                      MarketAnalysis::GetStateString(state),
                                      rootState == REGIME_TRENDING ? "TRENDING" : rootState == REGIME_RANGING ? "RANGING"
                                                                                                              : "UNKNOWN"));

      return rootState;
   }

   //+------------------------------------------------------------------+
   //| INDICATOR CALCULATIONS (using IndicatorManager when available)  |
   //+------------------------------------------------------------------+
   double GetADX(int period)
   {
      if (m_useIndicatorManager && m_indicatorManager != NULL && m_indicatorManager.IsInitialized())
      {
         double adx, plus_di, minus_di;
         if (m_indicatorManager.GetADXValues(m_timeframe, adx, plus_di, minus_di))
         {
            return adx;
         }
      }

      // Fallback to direct calculation
      int handle = iADX(m_symbol, m_timeframe, period);
      if (handle == INVALID_HANDLE)
         return 0;

      double adx[];
      ArraySetAsSeries(adx, true);

      if (CopyBuffer(handle, 0, 0, 1, adx) > 0)
      {
         IndicatorRelease(handle);
         return adx[0];
      }

      IndicatorRelease(handle);
      return 0;
   }

   double GetATR(int period)
   {
      if (m_useIndicatorManager && m_indicatorManager != NULL && m_indicatorManager.IsInitialized())
      {
         return m_indicatorManager.GetATR(m_timeframe);
      }

      // Fallback to direct calculation
      int handle = iATR(m_symbol, m_timeframe, period);
      if (handle == INVALID_HANDLE)
         return 0;

      double atr[];
      ArraySetAsSeries(atr, true);

      if (CopyBuffer(handle, 0, 0, 1, atr) > 0)
      {
         IndicatorRelease(handle);
         return atr[0];
      }

      IndicatorRelease(handle);
      return 0;
   }

   double GetBollingerWidth(int period, double deviations)
   {
      if (m_useIndicatorManager && m_indicatorManager != NULL && m_indicatorManager.IsInitialized())
      {
         double upper, middle, lower;
         if (m_indicatorManager.GetBollingerBandsValues(m_timeframe, upper, middle, lower))
         {
            double currentPrice = iClose(m_symbol, m_timeframe, 0);
            if (currentPrice > 0)
               return (upper - lower) / currentPrice;
         }
      }

      // Fallback to direct calculation
      int handle = iBands(m_symbol, m_timeframe, period, 0, deviations, PRICE_CLOSE);
      if (handle == INVALID_HANDLE)
         return 0;

      double upper[], lower[];
      ArraySetAsSeries(upper, true);
      ArraySetAsSeries(lower, true);

      if (CopyBuffer(handle, 1, 0, 1, upper) > 0 &&
          CopyBuffer(handle, 2, 0, 1, lower) > 0)
      {
         IndicatorRelease(handle);
         double currentPrice = iClose(m_symbol, m_timeframe, 0);
         if (currentPrice > 0)
            return (upper[0] - lower[0]) / currentPrice;
      }

      IndicatorRelease(handle);
      return 0;
   }

   double GetPriceChange(int bars)
   {
      double current = iClose(m_symbol, m_timeframe, 0);
      double past = iClose(m_symbol, m_timeframe, bars);

      if (past > 0)
         return ((current - past) / past) * 100;

      return 0;
   }

   int CountRangeTouches()
   {
      if (!m_rangeActive)
         return 0;

      int touches = 0;
      double atr = GetATR(14);
      double tolerance = atr * 0.1;

      for (int i = 0; i < 20; i++)
      {
         double high = iHigh(m_symbol, m_timeframe, i);
         double low = iLow(m_symbol, m_timeframe, i);

         if (MathAbs(high - m_fixedRangeTop) < tolerance ||
             MathAbs(low - m_fixedRangeBottom) < tolerance)
         {
            touches++;
         }
      }

      DebugRegimeLogFile("CountRangeTouches", StringFormat("Range touches: %d", touches));
      return touches;
   }

   // In CalculateVolatilityScore() function:
   double CalculateVolatilityScore(double atr, double bbWidth)
   {
      // Use longer period for smoothing (increase from 20 to 50)
      double atrValues[10];
      int lookback = 10; // Increased from 20

      for (int i = 0; i < lookback; i++)
      {
         double price = iClose(m_symbol, m_timeframe, i);
         if (price > 0)
            atrValues[i] = GetATRValue(14, i) / price;
      }

      double currentAtrNorm = atr / iClose(m_symbol, m_timeframe, 0);
      double avgAtrNorm = ArrayAverage(atrValues, lookback);

      if (avgAtrNorm > 0)
      {
         // Apply smoothing to reduce noise
         static double smoothedScore = 0.5;
         double rawScore = MathMin(1.0, currentAtrNorm / avgAtrNorm);
         smoothedScore = smoothedScore * 0.3 + rawScore * 0.7; // 50% weight to previous value
         return smoothedScore;
      }

      return 0.5;
   }

   bool IsVolatilityRising()
   {
      double atrNow = GetATR(14);
      double atrBefore = GetATRValue(14, 5);

      bool rising = atrNow > atrBefore * 1.1;
      DebugRegimeLogFile("IsVolatilityRising", StringFormat("ATR now=%.5f, before=%.5f, rising=%s",
                                                            atrNow, atrBefore, rising ? "true" : "false"));
      return rising;
   }

   double GetATRValue(int period, int shift)
   {
      if (m_useIndicatorManager && m_indicatorManager != NULL && m_indicatorManager.IsInitialized())
      {
         return m_indicatorManager.GetATR(m_timeframe, shift);
      }

      // Fallback to direct calculation
      int handle = iATR(m_symbol, m_timeframe, period);
      if (handle == INVALID_HANDLE)
         return 0;

      double atr[];
      ArraySetAsSeries(atr, true);

      if (CopyBuffer(handle, 0, shift, 1, atr) > 0)
      {
         IndicatorRelease(handle);
         return atr[0];
      }

      IndicatorRelease(handle);
      return 0;
   }

   bool IsTrendingStructure()
   {
      DebugRegimeLogFile("IsTrendingStructure", "Checking for trending price structure");

      // ========== NEW: ADD MOVING AVERAGE CONFIRMATION ==========
      // Get MA values using IndicatorManager or direct
      double ma50 = GetMAValue(50, 0);
      double ma50_10 = GetMAValue(50, 8);
      double ma89 = GetMAValue(89, 0);
      double ma89_10 = GetMAValue(89, 8);
      double ma200 = GetMAValue(200, 0);

      // Check if we got valid values
      if (ma50 <= 0 || ma89 <= 0 || ma200 <= 0)
      {
         DebugRegimeLogFile("IsTrendingStructure", "Invalid MA values, falling back to basic structure check");
         // Fall back to original price structure logic
         return CheckBasicTrendStructure();
      }

      // Calculate MA separation percentage
      double maDistancePercent = 0;
      double avgPrice = (ma50 + ma89) / 2;
      if (avgPrice > 0)
      {
         maDistancePercent = MathAbs(ma50 - ma89) / avgPrice * 100;
      }

      // Check MA alignment
      bool maAlignedBullish = (ma50 > ma89) && (ma50_10 > ma89_10);
      bool maAlignedBearish = (ma50 < ma89) && (ma50_10 < ma89_10);

      // ========== ENHANCED PRICE STRUCTURE (more forgiving) ==========
      // Look at 8 bars instead of 5
      int lookback = 10;
      double highs[], lows[];
      ArrayResize(highs, lookback);
      ArrayResize(lows, lookback);

      for (int i = 0; i < lookback; i++)
      {
         highs[i] = iHigh(m_symbol, m_timeframe, i);
         lows[i] = iLow(m_symbol, m_timeframe, i);
      }

      // More forgiving trend detection (60% rule instead of 100%)
      int uptrendSignals = 0;
      int downtrendSignals = 0;

      for (int i = 1; i < lookback; i++)
      {
         // Check HH pattern
         if (highs[i] > highs[i - 1])
            uptrendSignals++;
         if (lows[i] > lows[i - 1])
            uptrendSignals++;

         // Check LH pattern
         if (highs[i] < highs[i - 1])
            downtrendSignals++;
         if (lows[i] < lows[i - 1])
            downtrendSignals++;
      }

      int totalChecks = (lookback - 1) * 2;
      double uptrendRatio = totalChecks > 0 ? (double)uptrendSignals / totalChecks : 0;
      double downtrendRatio = totalChecks > 0 ? (double)downtrendSignals / totalChecks : 0;

      DebugRegimeLogFile("IsTrendingStructure",
                         StringFormat("Price structure: Uptrend ratio=%.2f, Downtrend ratio=%.2f",
                                      uptrendRatio, downtrendRatio));

      // ========== DECISION LOGIC (BALANCED) ==========

      // CRITICAL: If MAs are significantly separated (>0.75%), GENTLE trend bias
      // Changed from 0.5% to 0.75% for more selective bias
      if (maDistancePercent > 0.6) // Increase to 0.5 - need clearer MA separation
      {
         // Strong MA separation = trend gets MODERATE priority
         // Changed from 0.4 to 0.5 threshold - need clearer trend structure
         if (maAlignedBullish && uptrendRatio > 0.45)
         {
            DebugRegimeLogFile("IsTrendingStructure", "MA aligned bullish with uptrend structure");
            return true; // Need 50% confirmation (was 40%)
         }
         if (maAlignedBearish && downtrendRatio > 0.45)
         {
            DebugRegimeLogFile("IsTrendingStructure", "MA aligned bearish with downtrend structure");
            return true;
         }
      }

      // Original logic - keep as primary
      if (uptrendRatio > 0.6 || downtrendRatio > 0.6) // 70% threshold
      {
         DebugRegimeLogFile("IsTrendingStructure", "Strong price structure detected");
         return true;
      }

      // Additional check: If price consistently above/below key MAs
      double currentPrice = iClose(m_symbol, m_timeframe, 0);

      // Price far from 200 MA suggests trend
      double distanceFromMA = MathAbs(currentPrice - ma200) / ma200 * 100;
      if (distanceFromMA > 1.0) // Changed from 1.0% to 1.5% - more conservative
      {
         DebugRegimeLogFile("IsTrendingStructure",
                            StringFormat("Price far from MA200 (%.2f%%), forcing trend classification", distanceFromMA));
         return true; // Force trend classification
      }

      DebugRegimeLogFile("IsTrendingStructure", "No trending structure detected");
      return false;
   }

   bool CheckBasicTrendStructure()
   {
      DebugRegimeLogFile("CheckBasicTrendStructure", "Running basic 5-bar trend structure check");

      // Your original 5-bar HH/HL or LH/LL logic
      double highs[5], lows[5];

      for (int i = 0; i < 5; i++)
      {
         highs[i] = iHigh(m_symbol, m_timeframe, i);
         lows[i] = iLow(m_symbol, m_timeframe, i);
      }

      // Check for uptrend structure
      bool uptrend = true;
      for (int i = 1; i < 5; i++)
      {
         if (highs[i] <= highs[i - 1] || lows[i] <= lows[i - 1])
         {
            uptrend = false;
            break;
         }
      }

      // Check for downtrend structure
      bool downtrend = true;
      for (int i = 1; i < 5; i++)
      {
         if (highs[i] >= highs[i - 1] || lows[i] >= lows[i - 1])
         {
            downtrend = false;
            break;
         }
      }

      bool result = uptrend || downtrend;
      DebugRegimeLogFile("CheckBasicTrendStructure",
                         StringFormat("Basic trend check result: %s (Uptrend: %s, Downtrend: %s)",
                                      result ? "true" : "false", uptrend ? "true" : "false", downtrend ? "true" : "false"));

      return result;
   }

   bool IsRangingStructure()
   {
      if (!m_rangeActive)
      {
         DebugRegimeLogFile("IsRangingStructure", "Range not active");
         return false;
      }

      double currentPrice = iClose(m_symbol, m_timeframe, 0);
      bool inRange = IsPriceInFixedRange(currentPrice);

      DebugRegimeLogFile("IsRangingStructure",
                         StringFormat("Price %.5f in range %.5f-%.5f: %s",
                                      currentPrice, m_fixedRangeBottom, m_fixedRangeTop, inRange ? "true" : "false"));

      return inRange;
   }

   double CalculateConfidence(ENUM_MARKET_STATE state)
   {
      DebugRegimeLogFile("CalculateConfidence",
                         StringFormat("Calculating confidence for state: %s", MarketAnalysis::GetStateString(state)));

      double adx = GetADX(14);
      double atrScore = CalculateVolatilityScore(GetATR(14), GetBollingerWidth(20, 2));
      double structureConfidence = 0;

      if (state == STATE_TRENDING_LOW_VOL || state == STATE_TRENDING_HIGH_VOL)
      {
         structureConfidence = IsTrendingStructure() ? 80 : 40;
      }
      else if (state == STATE_RANGING_LOW_VOL || state == STATE_RANGING_HIGH_VOL)
      {
         structureConfidence = IsRangingStructure() ? 80 : 40;
      }

      double confidence = 0;

      switch (state)
      {
      case STATE_CONTRACTION:
         confidence = (1.0 - atrScore) * 100 * 0.7 + (adx < 20 ? 30 : 0);
         break;

      case STATE_EXPANSION:
         confidence = (HasRangeBroken(iClose(m_symbol, m_timeframe, 0)) ? 80 : 30);
         break;

      case STATE_TRENDING_LOW_VOL:
         confidence = MathMin(100, adx * 2) * 0.6 + structureConfidence * 0.4;
         break;

      case STATE_TRENDING_HIGH_VOL:
         confidence = MathMin(100, adx) * 0.7 + structureConfidence * 0.3;
         break;

      case STATE_RANGING_LOW_VOL:
      case STATE_RANGING_HIGH_VOL:
         confidence = (m_rangeActive ? 70 : 30) + (CountRangeTouches() * 5);
         break;

      case STATE_CHURN:
         confidence = (atrScore > 0.7 ? 70 : 30) + (adx > 40 ? 30 : 0);
         break;
      }

      confidence = MathMin(100, MathMax(0, confidence));

      DebugRegimeLogFile("CalculateConfidence",
                         StringFormat("Final confidence: %.1f%% (ADX=%.1f, ATRScore=%.2f, StructureConf=%.0f)",
                                      confidence, adx, atrScore, structureConfidence));

      return confidence;
   }

   //+------------------------------------------------------------------+
   //| STATE PREDICTION                                                |
   //+------------------------------------------------------------------+
   ENUM_MARKET_STATE PredictNextState(ENUM_MARKET_STATE currentState)
   {
      DebugRegimeLogFile("PredictNextState",
                         StringFormat("Predicting next state from: %s", MarketAnalysis::GetStateString(currentState)));

      ENUM_MARKET_STATE nextState;

      // State transition probabilities
      switch (currentState)
      {
      case STATE_RANGING_LOW_VOL:
         // Could compress further or expand into high volatility range
         nextState = STATE_CONTRACTION;
         break;

      case STATE_CONTRACTION:
         // Compression leads to expansion
         nextState = STATE_EXPANSION;
         break;

      case STATE_EXPANSION:
         // Breakout leads to trending or failed breakout leads back to range
         nextState = STATE_TRENDING_LOW_VOL;
         break;

      case STATE_TRENDING_LOW_VOL:
         // Healthy trend matures into high volatility trend
         nextState = STATE_TRENDING_HIGH_VOL;
         break;

      case STATE_TRENDING_HIGH_VOL:
         // Exhaustion leads to churn
         nextState = STATE_CHURN;
         break;

      case STATE_CHURN:
         // Churn leads to new range (high vol)
         nextState = STATE_RANGING_HIGH_VOL;
         break;

      case STATE_RANGING_HIGH_VOL:
         // High vol range stabilizes into low vol range
         nextState = STATE_RANGING_LOW_VOL;
         break;

      default:
         nextState = STATE_UNKNOWN;
      }

      DebugRegimeLogFile("PredictNextState",
                         StringFormat("Predicted next state: %s", MarketAnalysis::GetStateString(nextState)));

      return nextState;
   }

   //+------------------------------------------------------------------+
   //| TRADING RECOMMENDATIONS                                         |
   //+------------------------------------------------------------------+
   void GenerateRecommendations(MarketAnalysis &analysis)
   {
      DebugRegimeLogFile("GenerateRecommendations", "Generating trading recommendations");

      double currentPrice = iClose(m_symbol, m_timeframe, 0);
      double atr = GetATR(14);

      // Default values
      analysis.action = "Wait for clarity";
      analysis.positionSize = SIZE_ZERO;
      analysis.stopDistance = atr * 1.5;
      analysis.takeProfitDistance = atr * 3.0;
      analysis.riskRewardRatio = 2.0;
      analysis.direction = "Neutral";

      // State-specific recommendations
      switch (analysis.state)
      {
      case STATE_RANGING_LOW_VOL:
         analysis.action = "Fade range extremes";
         analysis.positionSize = SIZE_SMALL;
         analysis.stopDistance = (m_fixedRangeTop - m_fixedRangeBottom) * 0.2;
         analysis.takeProfitDistance = (m_fixedRangeTop - m_fixedRangeBottom) * 0.6;
         analysis.riskRewardRatio = 3.0;
         analysis.direction = "Mean reversion";
         DebugRegimeLogFile("GenerateRecommendations", "RANGING_LOW_VOL: Fade extremes, mean reversion");
         break;

      case STATE_RANGING_HIGH_VOL:
         analysis.action = "Fade carefully with tight stops";
         analysis.positionSize = SIZE_VERY_SMALL;
         analysis.stopDistance = atr * 0.8;
         analysis.takeProfitDistance = atr * 1.5;
         analysis.riskRewardRatio = 2.0;
         analysis.direction = "Neutral (dangerous)";
         DebugRegimeLogFile("GenerateRecommendations", "RANGING_HIGH_VOL: Fade carefully, tight stops");
         break;

      case STATE_CONTRACTION:
         analysis.action = "Prepare for breakout - no trades";
         analysis.positionSize = SIZE_ZERO;
         analysis.stopDistance = 0;
         analysis.takeProfitDistance = 0;
         analysis.riskRewardRatio = 0;
         analysis.direction = "Neutral (waiting)";
         DebugRegimeLogFile("GenerateRecommendations", "CONTRACTION: No trades, prepare for breakout");
         break;

      case STATE_EXPANSION:
         analysis.action = "Test breakout entry";
         analysis.positionSize = SIZE_MEDIUM;
         analysis.stopDistance = atr * 1.2;
         analysis.takeProfitDistance = atr * 3.6;
         analysis.riskRewardRatio = 3.0;

         if (currentPrice > m_fixedRangeTop)
         {
            analysis.direction = "Bullish breakout";
            DebugRegimeLogFile("GenerateRecommendations", "EXPANSION: Bullish breakout entry");
         }
         else if (currentPrice < m_fixedRangeBottom)
         {
            analysis.direction = "Bearish breakout";
            DebugRegimeLogFile("GenerateRecommendations", "EXPANSION: Bearish breakout entry");
         }
         else
         {
            analysis.direction = "Testing";
            DebugRegimeLogFile("GenerateRecommendations", "EXPANSION: Testing breakout");
         }
         break;

      case STATE_TRENDING_LOW_VOL:
         analysis.action = "Add to winning positions";
         analysis.positionSize = SIZE_LARGE;
         analysis.stopDistance = atr * 2.0;
         analysis.takeProfitDistance = atr * 6.0;
         analysis.riskRewardRatio = 3.0;
         analysis.direction = IsTrendingStructure() ? "With trend" : "Neutral";
         DebugRegimeLogFile("GenerateRecommendations", "TRENDING_LOW_VOL: Add to winning positions");
         break;

      case STATE_TRENDING_HIGH_VOL:
         analysis.action = "Take partial profits";
         analysis.positionSize = SIZE_MEDIUM;
         analysis.stopDistance = atr * 1.0;
         analysis.takeProfitDistance = atr * 2.0;
         analysis.riskRewardRatio = 2.0;
         analysis.direction = "With trend (cautious)";
         DebugRegimeLogFile("GenerateRecommendations", "TRENDING_HIGH_VOL: Take partial profits");
         break;

      case STATE_CHURN:
         analysis.action = "Exit positions, wait for clarity";
         analysis.positionSize = SIZE_ZERO;
         analysis.stopDistance = 0;
         analysis.takeProfitDistance = 0;
         analysis.riskRewardRatio = 0;
         analysis.direction = "Neutral (avoid)";
         DebugRegimeLogFile("GenerateRecommendations", "CHURN: Exit positions, wait for clarity");
         break;
      }

      // Adjust position size based on account
      analysis.positionSize = AdjustPositionSize(analysis.positionSize);

      DebugRegimeLogFile("GenerateRecommendations",
                         StringFormat("Final recommendation: %s, Position: %s, Stop: %.2f, TP: %.2f, R/R: %.1f",
                                      analysis.action, GetPositionSizeString(analysis.positionSize),
                                      analysis.stopDistance, analysis.takeProfitDistance, analysis.riskRewardRatio));

      // Log trading recommendation
      Logger::LogTrade("Recommendation", m_symbol,
                       analysis.direction == "Bullish breakout" ? "BUY" : analysis.direction == "Bearish breakout" ? "SELL"
                                                                                                                   : "HOLD",
                       0, currentPrice);

      // Convert to pips for display if needed
      double point = SymbolInfoDouble(m_symbol, SYMBOL_POINT);
      if (point > 0)
      {
         analysis.stopDistance = analysis.stopDistance / point;
         analysis.takeProfitDistance = analysis.takeProfitDistance / point;
      }
   }

   ENUM_POSITION_SIZE AdjustPositionSize(ENUM_POSITION_SIZE baseSize)
   {
      // Adjust based on account size and risk
      double riskAmount = m_accountBalance * (m_accountRiskPercent / 100);

      ENUM_POSITION_SIZE adjustedSize = baseSize;

      if (riskAmount < 100) // Very small account
      {
         switch (baseSize)
         {
         case SIZE_LARGE:
            adjustedSize = SIZE_MEDIUM;
            DebugRegimeLogFile("AdjustPositionSize", "Account small: LARGE -> MEDIUM");
            break;
         case SIZE_MEDIUM:
            adjustedSize = SIZE_SMALL;
            DebugRegimeLogFile("AdjustPositionSize", "Account small: MEDIUM -> SMALL");
            break;
         default:
            adjustedSize = baseSize;
         }
      }

      DebugRegimeLogFile("AdjustPositionSize",
                         StringFormat("Position size: %s -> %s (Account: $%.2f, Risk: $%.2f)",
                                      GetPositionSizeString(baseSize),
                                      GetPositionSizeString(adjustedSize),
                                      m_accountBalance, riskAmount));

      return adjustedSize;
   }

   //+------------------------------------------------------------------+
   //| UTILITY FUNCTIONS                                               |
   //+------------------------------------------------------------------+
   double ArrayAverage(double &arr[], int count)
   {
      double sum = 0;
      int validCount = 0;

      for (int i = 0; i < count && i < ArraySize(arr); i++)
      {
         sum += arr[i];
         validCount++;
      }

      double avg = validCount > 0 ? sum / validCount : 0;
      return avg;
   }

   string GenerateDescription(MarketAnalysis &analysis)
   {
      string desc = "";

      switch (analysis.state)
      {
      case STATE_RANGING_LOW_VOL:
         desc = "Low volatility consolidation. Market is balanced, awaiting catalyst.";
         break;

      case STATE_RANGING_HIGH_VOL:
         desc = "High volatility range. Emotional swings, frequent stop hunts.";
         break;

      case STATE_CONTRACTION:
         desc = "Volatility squeeze/compression. Energy building for next move.";
         break;

      case STATE_EXPANSION:
         desc = "Breakout/expansion phase. New trend may be forming.";
         break;

      case STATE_TRENDING_LOW_VOL:
         desc = "Healthy institutional trend. Ideal trading conditions.";
         break;

      case STATE_TRENDING_HIGH_VOL:
         desc = "Parabolic/exhaustion trend. Late stage, reversal risk high.";
         break;

      case STATE_CHURN:
         desc = "Exhaustion/distribution. Directionless volatility, avoid trading.";
         break;

      default:
         desc = "Market state unclear. Wait for better signals.";
      }

      DebugRegimeLogFile("GenerateDescription", StringFormat("Description: %s", desc));

      return desc;
   }

public:
   //+------------------------------------------------------------------+
   //| MANUAL RANGE REINITIALIZATION                                   |
   //+------------------------------------------------------------------+
   void ResetRange()
   {
      DebugRegimeLogFile("ResetRange", "Manual range reset requested");
      Logger::Log("MarketRegime", "Manual range reset", true, true);
      ReinitializeRange();
   }

   //+------------------------------------------------------------------+
   //| GET/SET RANGE CONFIGURATION                                     |
   //+------------------------------------------------------------------+
   int GetBarsSinceLastRangeInit() const { return m_barsSinceLastRangeInit; }
   int GetRangeLookbackBars() const { return m_rangeLookbackBars; }

   void SetRangeLookbackBars(int bars)
   {
      m_rangeLookbackBars = bars;
      string msg = "Range lookback bars set to: " + IntegerToString(bars);
      Print(msg);
      Logger::Log("MarketRegime", msg, true, false);
      DebugRegimeLogFile("SetRangeLookbackBars", msg);
   }

   //+------------------------------------------------------------------+
   //| GETTER METHODS                                                  |
   //+------------------------------------------------------------------+
   double GetRangeTop() const { return m_fixedRangeTop; }
   double GetRangeBottom() const { return m_fixedRangeBottom; }
   bool IsRangeActive() const { return m_rangeActive; }

   void SetAccountInfo(double balance, double riskPercent)
   {
      m_accountBalance = balance;
      m_accountRiskPercent = riskPercent;

      DebugRegimeLogFile("SetAccountInfo",
                         StringFormat("Account info updated: Balance=$%.2f, Risk=%.1f%%",
                                      balance, riskPercent));
   }

   // Access to IndicatorManager for other uses
   IndicatorManager *GetIndicatorManager() const { return m_indicatorManager; }

   // Method to get IndicatorManager initialization status
   bool IsIndicatorManagerInitialized() const
   {
      bool initialized = (m_indicatorManager != NULL && m_indicatorManager.IsInitialized());
      DebugRegimeLogFile("IsIndicatorManagerInitialized",
                         StringFormat("IndicatorManager initialized: %s", initialized ? "true" : "false"));
      return initialized;
   }
};

//+------------------------------------------------------------------+
//| SIMPLE CHART DRAWING FUNCTIONS                                   |
//+------------------------------------------------------------------+
void DrawMarketRegime(MarketAnalysis &analysis, string symbol = NULL, ENUM_TIMEFRAMES tf = PERIOD_H1)
{
   if (symbol == NULL)
      symbol = Symbol();

   long chartId = ChartID();

   // Remove previous drawings
   RemoveRegimeDrawings(chartId);

   // Draw fixed range if active
   MarketRegimeDetector detector(symbol, tf);
   if (detector.IsRangeActive())
   {
      double top = detector.GetRangeTop();
      double bottom = detector.GetRangeBottom();

      // Draw range lines
      DrawHorizontalLine(chartId, "Regime_Range_Top", top, clrBlue, STYLE_DASH, 2);
      DrawHorizontalLine(chartId, "Regime_Range_Bottom", bottom, clrBlue, STYLE_DASH, 2);
   }

   // Draw regime info box
   string infoText = StringFormat(
       "State: %s (%.0f%%)\n" +
           "Action: %s\n" +
           "Position: %s\n" +
           "Direction: %s\n" +
           "R/R: %.1f",
       MarketAnalysis::GetStateString(analysis.state),
       analysis.confidence,
       analysis.action,
       MarketAnalysis::GetStateString(analysis.nextLikelyState),
       analysis.direction,
       analysis.riskRewardRatio);

   DrawText(chartId, "Regime_Info", TimeCurrent() + PeriodSeconds(tf) * 15,
            iClose(symbol, tf, 0), infoText, clrWhite, 10, "Arial");

   ChartRedraw(chartId);
}

void RemoveRegimeDrawings(long chartId)
{
   int total = ObjectsTotal(chartId);
   for (int i = total - 1; i >= 0; i--)
   {
      string name = ObjectName(chartId, i);
      if (StringFind(name, "Regime_") == 0)
      {
         ObjectDelete(chartId, name);
      }
   }
}

void DrawHorizontalLine(long chartId, string name, double price, color clr,
                        ENUM_LINE_STYLE style, int width)
{
   if (ObjectFind(chartId, name) >= 0)
      ObjectDelete(chartId, name);

   ObjectCreate(chartId, name, OBJ_HLINE, 0, 0, price);
   ObjectSetInteger(chartId, name, OBJPROP_COLOR, clr);
   ObjectSetInteger(chartId, name, OBJPROP_STYLE, style);
   ObjectSetInteger(chartId, name, OBJPROP_WIDTH, width);
   ObjectSetInteger(chartId, name, OBJPROP_BACK, true);
}

void DrawText(long chartId, string name, datetime time, double price,
              string text, color clr, int fontSize, string font)
{
   if (ObjectFind(chartId, name) >= 0)
      ObjectDelete(chartId, name);

   ObjectCreate(chartId, name, OBJ_TEXT, 0, time, price);
   ObjectSetString(chartId, name, OBJPROP_TEXT, text);
   ObjectSetInteger(chartId, name, OBJPROP_COLOR, clr);
   ObjectSetInteger(chartId, name, OBJPROP_FONTSIZE, fontSize);
   ObjectSetString(chartId, name, OBJPROP_FONT, font);
   ObjectSetInteger(chartId, name, OBJPROP_BACK, false);
}

//+------------------------------------------------------------------+
//| ENHANCED CHART DISPLAY FUNCTIONS                                |
//+------------------------------------------------------------------+
//+------------------------------------------------------------------+
//| SIMPLIFIED: Show range ONLY when market is ranging              |
//+------------------------------------------------------------------+
void DisplayMarketRegimeOnChart(MarketAnalysis &analysis,
                                string symbol = NULL,
                                ENUM_TIMEFRAMES tf = PERIOD_H1,
                                int corner = CORNER_RIGHT_UPPER)
{
   DebugRegimeLogFile("DisplayMarketRegimeOnChart", "Updating chart display");

   if (symbol == NULL)
      symbol = Symbol();

   long chartId = ChartID();
   string prefix = "MarketRegime_";

   // Remove ALL previous drawings first
   RemoveAllRegimeDrawings(chartId, prefix);

   // ==================== CRITICAL CHECK: Only show in ranging states ====================
   bool shouldShowRange = false;

   switch (analysis.state)
   {
   case STATE_RANGING_LOW_VOL:
   case STATE_RANGING_HIGH_VOL:
   case STATE_CONTRACTION: // Contraction/squeeze is also a ranging state
      shouldShowRange = true;
      DebugRegimeLogFile("DisplayMarketRegimeOnChart", "Showing range (ranging state detected)");
      break;

   case STATE_TRENDING_LOW_VOL:
   case STATE_TRENDING_HIGH_VOL:
   case STATE_EXPANSION: // Expansion/breakout = trending
   case STATE_CHURN:     // Churn/exhaustion = unclear
   case STATE_UNKNOWN:
   default:
      shouldShowRange = false;
      DebugRegimeLogFile("DisplayMarketRegimeOnChart", "Hiding range (non-ranging state)");
   }

   // Only proceed if we should show range
   if (!shouldShowRange)
   {
      // Remove any existing drawings and exit
      ChartRedraw(chartId);
      return;
   }

   // Draw fixed range on chart if active
   MarketRegimeDetector detector(symbol, tf);

   // Double-check: Is range actually active?
   if (detector.IsRangeActive() && shouldShowRange)
   {
      double top = detector.GetRangeTop();
      double bottom = detector.GetRangeBottom();

      // Only draw if range is meaningful (not too narrow)
      double rangeHeight = top - bottom;
      double avgPrice = (top + bottom) / 2;

      if (avgPrice > 0 && (rangeHeight / avgPrice) * 100 > 0.05) // 0.05% minimum width
      {
         // Draw ONLY the range lines (no text, no labels)
         DrawHorizontalLine(chartId, prefix + "RangeTop", top, clrDodgerBlue, STYLE_DASH, 2);
         DrawHorizontalLine(chartId, prefix + "RangeBottom", bottom, clrDodgerBlue, STYLE_DASH, 2);

         DebugRegimeLogFile("DisplayMarketRegimeOnChart",
                            StringFormat("Range drawn: Top=%.5f, Bottom=%.5f, Height=%.2f%%",
                                         top, bottom, (rangeHeight / avgPrice) * 100));
      }
   }

   // If we get here and range shouldn't be shown, the RemoveAllRegimeDrawings
   // at the beginning already cleared any existing drawings

   ChartRedraw(chartId);
}

//+------------------------------------------------------------------+
//| PIXEL-BASED DRAWING FUNCTIONS (for panel)                       |
//+------------------------------------------------------------------+

void DrawTextPixel(long chartId, string name, int x, int y, string text,
                   color clr, int fontSize, string font, int corner)
{
   if (ObjectFind(chartId, name) >= 0)
      ObjectDelete(chartId, name);

   ObjectCreate(chartId, name, OBJ_LABEL, 0, 0, 0);
   ObjectSetInteger(chartId, name, OBJPROP_CORNER, corner);
   ObjectSetInteger(chartId, name, OBJPROP_XDISTANCE, x);
   ObjectSetInteger(chartId, name, OBJPROP_YDISTANCE, y);
   ObjectSetString(chartId, name, OBJPROP_TEXT, text);
   ObjectSetInteger(chartId, name, OBJPROP_COLOR, clr);
   ObjectSetInteger(chartId, name, OBJPROP_FONTSIZE, fontSize);
   ObjectSetString(chartId, name, OBJPROP_FONT, font);
   ObjectSetInteger(chartId, name, OBJPROP_BACK, false);
   ObjectSetInteger(chartId, name, OBJPROP_SELECTABLE, false);
}

void DrawLinePixel(long chartId, string name, int x1, int y1, int x2, int y2,
                   color clr, int width)
{
   if (ObjectFind(chartId, name) >= 0)
      ObjectDelete(chartId, name);

   ObjectCreate(chartId, name, OBJ_TREND, 0, 0, 0);
   ObjectSetInteger(chartId, name, OBJPROP_CORNER, CORNER_LEFT_UPPER);
   ObjectSetInteger(chartId, name, OBJPROP_XDISTANCE, x1);
   ObjectSetInteger(chartId, name, OBJPROP_YDISTANCE, y1);
   ObjectSetInteger(chartId, name, OBJPROP_XSIZE, x2 - x1);
   ObjectSetInteger(chartId, name, OBJPROP_YSIZE, y2 - y1);
   ObjectSetInteger(chartId, name, OBJPROP_COLOR, clr);
   ObjectSetInteger(chartId, name, OBJPROP_WIDTH, width);
   ObjectSetInteger(chartId, name, OBJPROP_BACK, false);
   ObjectSetInteger(chartId, name, OBJPROP_SELECTABLE, false);
   ObjectSetInteger(chartId, name, OBJPROP_RAY, false);
}

void DrawTextOnChart(long chartId, string name, datetime time, double price,
                     string text, color clr, int fontSize, string font,
                     ENUM_ANCHOR_POINT anchor = ANCHOR_LEFT_UPPER)
{
   if (ObjectFind(chartId, name) >= 0)
      ObjectDelete(chartId, name);

   ObjectCreate(chartId, name, OBJ_TEXT, 0, time, price);
   ObjectSetString(chartId, name, OBJPROP_TEXT, text);
   ObjectSetInteger(chartId, name, OBJPROP_COLOR, clr);
   ObjectSetInteger(chartId, name, OBJPROP_FONTSIZE, fontSize);
   ObjectSetString(chartId, name, OBJPROP_FONT, font);
   ObjectSetInteger(chartId, name, OBJPROP_ANCHOR, anchor);
   ObjectSetInteger(chartId, name, OBJPROP_BACK, false);
   ObjectSetInteger(chartId, name, OBJPROP_SELECTABLE, false);
}

void RemoveAllRegimeDrawings(long chartId, string prefix = "MarketRegime_")
{
   int total = ObjectsTotal(chartId);
   for (int i = total - 1; i >= 0; i--)
   {
      string name = ObjectName(chartId, i);
      if (StringFind(name, prefix) == 0)
      {
         ObjectDelete(chartId, name);
      }
   }
}

//+------------------------------------------------------------------+
//| COLOR FUNCTIONS FOR VISUAL FEEDBACK                             |
//+------------------------------------------------------------------+

color GetStateColor(ENUM_MARKET_STATE state)
{
   switch (state)
   {
   case STATE_RANGING_LOW_VOL:
      return clrSteelBlue; // Blue for consolidation
   case STATE_RANGING_HIGH_VOL:
      return clrOrangeRed; // Red-orange for dangerous range
   case STATE_TRENDING_LOW_VOL:
      return clrLimeGreen; // Green for healthy trend
   case STATE_TRENDING_HIGH_VOL:
      return clrGold; // Gold/yellow for exhaustion
   case STATE_CONTRACTION:
      return clrDarkSlateGray; // Dark gray for compression
   case STATE_EXPANSION:
      return clrDodgerBlue; // Bright blue for expansion
   case STATE_CHURN:
      return clrDarkOrange; // Orange for churn
   default:
      return clrGray;
   }
}

color GetTransitionColor(ENUM_MARKET_STATE current, ENUM_MARKET_STATE next)
{
   // Color transitions based on expected move
   if ((current == STATE_CONTRACTION && next == STATE_EXPANSION) ||
       (current == STATE_EXPANSION && next == STATE_TRENDING_LOW_VOL))
      return clrLime; // Green for positive transitions

   if ((current == STATE_TRENDING_HIGH_VOL && next == STATE_CHURN) ||
       (current == STATE_CHURN && next == STATE_RANGING_HIGH_VOL))
      return clrOrangeRed; // Red for negative transitions

   return clrYellow; // Yellow for neutral transitions
}

color GetActionColor(string action)
{
   if (StringFind(action, "Fade") >= 0)
      return clrDodgerBlue; // Blue for mean reversion
   if (StringFind(action, "Add") >= 0 || StringFind(action, "Test") >= 0)
      return clrLime; // Green for trend following
   if (StringFind(action, "Take") >= 0)
      return clrGold; // Yellow for profit taking
   if (StringFind(action, "Exit") >= 0 || StringFind(action, "Wait") >= 0)
      return clrOrangeRed; // Red for exiting/avoiding

   return clrWhite;
}

color GetPositionSizeColor(ENUM_POSITION_SIZE size)
{
   switch (size)
   {
   case SIZE_ZERO:
      return clrGray;
   case SIZE_VERY_SMALL:
      return clrOrangeRed;
   case SIZE_SMALL:
      return clrOrange;
   case SIZE_MEDIUM:
      return clrYellow;
   case SIZE_LARGE:
      return clrLime;
   default:
      return clrWhite;
   }
}

color GetDirectionColor(string direction)
{
   if (StringFind(direction, "Bullish") >= 0 || StringFind(direction, "Up") >= 0)
      return clrLime;
   if (StringFind(direction, "Bearish") >= 0 || StringFind(direction, "Down") >= 0)
      return clrRed;
   if (StringFind(direction, "With trend") >= 0)
      return clrLimeGreen;
   if (StringFind(direction, "Mean reversion") >= 0)
      return clrDodgerBlue;
   if (StringFind(direction, "Neutral") >= 0)
      return clrGray;

   return clrYellow;
}

color GetRRColor(double rrRatio)
{
   if (rrRatio >= 3.0)
      return clrLime;
   if (rrRatio >= 2.0)
      return clrYellow;
   if (rrRatio > 0)
      return clrOrange;

   return clrGray;
}

color GetRangePositionColor(double positionPercent)
{
   if (positionPercent < 20)
      return clrRed; // Oversold near bottom
   if (positionPercent > 80)
      return clrLime; // Overbought near top
   if (positionPercent < 40)
      return clrOrange; // Lower half
   if (positionPercent > 60)
      return clrDodgerBlue; // Upper half

   return clrYellow; // Middle
}

//+------------------------------------------------------------------+
//| ENHANCED GLOBAL HELPER FUNCTION                                  |
//+------------------------------------------------------------------+
MarketAnalysis GetMarketRegimeWithDisplay(string symbol = NULL,
                                          ENUM_TIMEFRAMES tf = PERIOD_H1,
                                          bool showPanel = true,
                                          int panelCorner = CORNER_RIGHT_UPPER,
                                          bool useIndicatorManager = true)
{
   static MarketRegimeDetector *detector = NULL;

   if (detector == NULL)
   {
      detector = new MarketRegimeDetector(symbol, tf, 10000, 1.0, useIndicatorManager);
      Logger::Log("MarketRegime", StringFormat("Global detector initialized for %s", symbol), true, false);
   }

   // Get analysis
   MarketAnalysis analysis = detector.GetMarketRegime();

   // Display on chart if requested
   if (showPanel)
   {
      DisplayMarketRegimeOnChart(analysis, symbol, tf, panelCorner);
   }

   return analysis;
}
//+------------------------------------------------------------------+

+++++++++++++++++++++++++++++++++++++++++++++++++


and also

// ++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++ //
// ++++++++++++++++++++++++++       DECISION ENGINE v4.1           ++++++++++++++++++++++++++++ //
// ++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++ //

#property copyright "Copyright 2024"
#property strict
#property version "4.10"

/*
==============================================================
DECISION ENGINE v4.1 - INTELLIGENT ROUTING ARCHITECTURE
==============================================================
Core Improvements:
1. Automatic processor routing based on package type & market regime
2. Dynamic threshold adjustment for ranging/trending markets
3. Clear separation between trend-following and range-fading logic
4. Enhanced regime detection with automatic switching
5. Auto-package creation and processing
==============================================================
*/

// ================= INCLUDES =================

#include "../Headers/Enums.mqh"
#include "../Headers/Structures.mqh"

#include "../Utils/Logger.mqh"
#include "../Utils/TimeUtils.mqh"

#include "../Execution/PositionManager.mqh"

#include "../Data/RangePackage.mqh"
#include "../Data/TrendPackage.mqh"
#include "../Data/MarketRegime.mqh"

#include "../Core/PackageManager.mqh"

// ================= FORWARD DECLARATIONS =================

// ====================== DEBUG SETTINGS ======================
bool DEBUG_ENABLED = true;

// Simple debug function using Logger
void DebugLogFile(string context, string message)
{
    if (DEBUG_ENABLED)
    {
        Logger::Log(context, message, true, true);
    }
}

// ================= ENUMS =================
enum DECISION_ACTION
{
    ACTION_NONE,
    ACTION_OPEN_BUY,
    ACTION_OPEN_SELL,
    ACTION_CLOSE_BUY,
    ACTION_CLOSE_SELL,
    ACTION_CLOSE_ALL,
    ACTION_HOLD,
    ACTION_WAITING_FOR_PACKAGE
};

enum POSITION_STATE
{
    STATE_NO_POSITION,
    STATE_HAS_BUY,
    STATE_HAS_SELL,
    STATE_HAS_BOTH
};

// Add this missing enum from MarketRegime.mqh
enum ENUM_MARKET_LIFECYCLE
{
    LIFECYCLE_UNKNOWN,
    LIFECYCLE_TREND_CONFIRMED,
    LIFECYCLE_TREND_RESUMING,
    LIFECYCLE_TREND_WEAKENING,
    LIFECYCLE_BREAKOUT_DETECTED,
    LIFECYCLE_RANGE_ACTIVE,
    LIFECYCLE_RANGE_FORMING,
    LIFECYCLE_PULLBACK_ACTIVE,
    LIFECYCLE_PULLBACK_FORMING
};

// ====================== TRADE PACKAGE INTERFACE ======================

// ================= STRUCTURES =================
struct DecisionParams
{
    double buyConfidenceThreshold;
    double sellConfidenceThreshold;
    double closePositionThreshold;
    double closeAllThreshold;
    int cooldownMinutes;
    int maxPositionsPerSymbol;
    double riskPercent;
    double minRiskRewardRatio;

    // New: Dynamic thresholds for different market regimes
    double rangeBuyThreshold;
    double rangeSellThreshold;
    double trendBuyThreshold;
    double trendSellThreshold;
    double trapProbabilityThreshold;

    DecisionParams(double buyThresh = 65.0,
                   double sellThresh = 65.0,
                   double closeThresh = 40.0,
                   double closeAllThresh = 40.0,
                   int cooldownMins = 30,
                   int maxPositions = 2,
                   double riskPct = 20.0,
                   double minRR = 1.0,
                   double rangeBuy = 75.0,
                   double rangeSell = 75.0,
                   double trendBuy = 65.0,
                   double trendSell = 65.0,
                   double trapProb = 65.0)
    {
        buyConfidenceThreshold = buyThresh;
        sellConfidenceThreshold = sellThresh;
        closePositionThreshold = closeThresh;
        closeAllThreshold = closeAllThresh;
        cooldownMinutes = cooldownMins;
        maxPositionsPerSymbol = maxPositions;
        riskPercent = riskPct;
        minRiskRewardRatio = minRR;
        rangeBuyThreshold = rangeBuy;
        rangeSellThreshold = rangeSell;
        trendBuyThreshold = trendBuy;
        trendSellThreshold = trendSell;
        trapProbabilityThreshold = trapProb;
    }

    // Added: Log params structure
    string ToString() const
    {
        return StringFormat("BuyThresh: %.1f%%, SellThresh: %.1f%%, CloseThresh: %.1f%%, CloseAllThresh: %.1f%%, Cooldown: %dm, MaxPos: %d, Risk: %.1f%%, RR: %.1f",
                            buyConfidenceThreshold, sellConfidenceThreshold, closePositionThreshold,
                            closeAllThreshold, cooldownMinutes, maxPositionsPerSymbol, riskPercent, minRiskRewardRatio);
    }

    // Updated: Get threshold based on market regime
    double GetBuyThreshold(const string &marketRegime) const
    {
        // Check for ALL ranging regime variations
        if (marketRegime == "RANGING" ||
            marketRegime == "TIGHT_RANGE" ||
            marketRegime == "CONSOLIDATION")
            return rangeBuyThreshold;
        return trendBuyThreshold;
    }

    double GetSellThreshold(const string &marketRegime) const
    {
        // Check for ALL ranging regime variations
        if (marketRegime == "RANGING" ||
            marketRegime == "TIGHT_RANGE" ||
            marketRegime == "CONSOLIDATION")
            return rangeSellThreshold;
        return trendSellThreshold;
    }
};

struct CooldownRecord
{
    datetime lastBuyTime;
    datetime lastSellTime;
    int buyCount;
    int sellCount;

    CooldownRecord()
    {
        lastBuyTime = 0;
        lastSellTime = 0;
        buyCount = 0;
        sellCount = 0;
    }

    bool IsInCooldown(bool isBuy, int cooldownMinutes)
    {
        if (cooldownMinutes <= 0)
            return false;

        datetime currentTime = TimeCurrent();
        datetime lastTime = isBuy ? lastBuyTime : lastSellTime;

        bool inCooldown = ((currentTime - lastTime) < (cooldownMinutes * 60));
        if (inCooldown)
        {
            DebugLogFile("COOLDOWN_CHECK", StringFormat("In cooldown: isBuy=%s, lastTime=%s, cooldownMins=%d, remaining=%d sec",
                                                        isBuy ? "true" : "false", TimeToString(lastTime), cooldownMinutes, (cooldownMinutes * 60) - (currentTime - lastTime)));
        }
        return inCooldown;
    }

    void Update(bool isBuy)
    {
        if (isBuy)
        {
            lastBuyTime = TimeCurrent();
            buyCount++;
            DebugLogFile("COOLDOWN_UPDATE", StringFormat("Buy cooldown updated. LastBuyTime: %s, BuyCount: %d",
                                                         TimeToString(lastBuyTime), buyCount));
        }
        else
        {
            lastSellTime = TimeCurrent();
            sellCount++;
            DebugLogFile("COOLDOWN_UPDATE", StringFormat("Sell cooldown updated. LastSellTime: %s, SellCount: %d",
                                                         TimeToString(lastSellTime), sellCount));
        }
    }

    void Reset()
    {
        lastBuyTime = 0;
        lastSellTime = 0;
        buyCount = 0;
        sellCount = 0;
        DebugLogFile("COOLDOWN_RESET", "Cooldown record reset");
    }

    // Added: Log cooldown status
    string GetStatus() const
    {
        return StringFormat("Buy: %s (count: %d), Sell: %s (count: %d)",
                            (lastBuyTime > 0 ? TimeToString(lastBuyTime) : "Never"), buyCount,
                            (lastSellTime > 0 ? TimeToString(lastSellTime) : "Never"), sellCount);
    }
};

struct SymbolState
{
    string symbol;
    DecisionParams params;
    CooldownRecord cooldown;
    DecisionEngineInterface lastPackage;
    DECISION_ACTION lastDecision;
    datetime lastDecisionTime;
    int magicNumber;
    RangeAnalysisResult lastRangeResult;
    bool usingRangePackage;
    string lastProcessorUsed;

    // POSITION TRACKING FIELDS:
    datetime positionOpenTime;
    datetime positionCloseTime;
    DECISION_ACTION positionOpenDecision;
    double positionOpenConfidence;
    bool awaitingProfitabilityCheck;

    string actualProcessorUsed; // "TREND", "RANGE", etc.
    datetime lastProcessingTime;
    string lastProcessingReason;

    SymbolState()
    {
        symbol = "";
        lastDecision = ACTION_NONE;
        lastDecisionTime = 0;
        magicNumber = 0;
        usingRangePackage = false;
        lastProcessorUsed = "NONE";

        // Initialize position tracking fields
        positionOpenTime = 0;
        positionCloseTime = 0;
        positionOpenDecision = ACTION_NONE;
        positionOpenConfidence = 0;
        awaitingProfitabilityCheck = false;

        actualProcessorUsed = "NONE";
        lastProcessingTime = 0;
        lastProcessingReason = "";
    }

    bool HasValidPackage()
    {
        bool isValid = (lastPackage.analysisTime > 0);
        if (!isValid)
        {
            DebugLogFile("PACKAGE_CHECK", StringFormat("No valid interface for %s: analysisTime=%s",
                                                       symbol, TimeToString(lastPackage.analysisTime)));
        }
        return isValid;
    }

    bool IsPackageFresh(int maxAgeSeconds = 300)
    {
        if (!HasValidPackage())
        {
            DebugLogFile("PACKAGE_FRESHNESS", StringFormat("No valid interface for %s", symbol));
            return false;
        }

        int age = (int)(TimeCurrent() - lastPackage.analysisTime);
        bool isFresh = (age <= maxAgeSeconds);

        DebugLogFile("PACKAGE_FRESHNESS", StringFormat("Interface for %s: age=%d sec, maxAge=%d sec, fresh=%s",
                                                       symbol, age, maxAgeSeconds, isFresh ? "true" : "false"));

        return isFresh;
    }

    string GetStatus() const
    {
        return StringFormat("Symbol: %s, Magic: %d, LastDecision: %s, LastDecisionTime: %s, HasInterface: %s, Processor: %s",
                            symbol, magicNumber,
                            (lastDecision == ACTION_NONE ? "NONE" : "SET"),
                            (lastDecisionTime > 0 ? TimeToString(lastDecisionTime) : "Never"),
                            (lastPackage.IsValid() ? "YES" : "NO"),
                            lastProcessorUsed);
    }
};

struct PositionAnalysis
{
    POSITION_STATE state;
    int buyCount;
    int sellCount;
    double totalProfit;
    double totalVolume;
    double maxRiskExposure;

    PositionAnalysis()
    {
        state = STATE_NO_POSITION;
        buyCount = 0;
        sellCount = 0;
        totalProfit = 0;
        totalVolume = 0;
        maxRiskExposure = 0;
    }

    string ToString() const
    {
        string stateStr = "";
        switch (state)
        {
        case STATE_NO_POSITION:
            stateStr = "NO_POSITION";
            break;
        case STATE_HAS_BUY:
            stateStr = "HAS_BUY";
            break;
        case STATE_HAS_SELL:
            stateStr = "HAS_SELL";
            break;
        case STATE_HAS_BOTH:
            stateStr = "HAS_BOTH";
            break;
        }
        return StringFormat("State: %s | Buy: %d | Sell: %d | Profit: %.2f | Volume: %.2f",
                            stateStr, buyCount, sellCount, totalProfit, totalVolume);
    }
};

struct DecisionMetrics
{
    int totalDecisions;
    int profitableDecisions;
    double accuracyRate;
    double averageConfidence;
    datetime startTime;

    int trendProcessorCount;
    int rangeProcessorCount;
    int autoRoutedCount;

    DecisionMetrics()
    {
        totalDecisions = 0;
        profitableDecisions = 0;
        accuracyRate = 0;
        averageConfidence = 0;
        startTime = TimeCurrent();
        trendProcessorCount = 0;
        rangeProcessorCount = 0;
        autoRoutedCount = 0;
        DebugLogFile("METRICS_INIT", "Decision metrics initialized");
    }

    void Update(DECISION_ACTION decision, double confidence, bool wasProfitable = false, string processorType = "")
    {
        // Only count OPEN decisions, not CLOSE decisions
        bool isOpenDecision = (decision == ACTION_OPEN_BUY || decision == ACTION_OPEN_SELL);

        if (!isOpenDecision)
        {
            DebugLogFile("METRICS_SKIP",
                         StringFormat("Skipping non-open decision in metrics: %s (Conf: %.1f%%)",
                                      DecisionToStringStatic(decision), confidence));
            return;
        }

        // ⚠️ IMPORTANT: This method should ONLY be called from ProcessClosedTradeProfitability()
        // after we know if the trade was profitable or not

        // The counting of totalDecisions and profitableDecisions is now done in ProcessClosedTradeProfitability()
        // We just track processor usage here

        // Track processor usage
        if (processorType == "TREND")
            trendProcessorCount++;
        else if (processorType == "RANGE")
            rangeProcessorCount++;
        else if (processorType == "AUTO_ROUTED")
            autoRoutedCount++;

        DebugLogFile("METRICS_PROCESSOR_ONLY",
                     StringFormat("Updated processor stats: Trend: %d | Range: %d | Auto: %d",
                                  trendProcessorCount, rangeProcessorCount, autoRoutedCount));
    }

    // ✅ CORRECTED - KEEP THIS ONE (REMOVE THE OTHER ONE)
    string ToString() const
    {
        // Calculate accuracy based on ONLY OPEN decisions that have closed
        double accuracy = 0;
        if (totalDecisions > 0)
        {
            accuracy = ((double)profitableDecisions / totalDecisions) * 100.0;
        }

        int losses = totalDecisions - profitableDecisions;
        int runningHours = (int)((TimeCurrent() - startTime) / 3600);

        return StringFormat("Trades: %d | W:%d L:%d | Acc: %.1f%% | AvgConf: %.1f%% | Running: %dh",
                            totalDecisions,
                            profitableDecisions,
                            losses,
                            accuracy,
                            averageConfidence,
                            runningHours);
    }

    string GetProcessorStats() const
    {
        return StringFormat("Trend: %d (%.1f%%) | Range: %d (%.1f%%) | Auto: %d (%.1f%%)",
                            trendProcessorCount, totalDecisions > 0 ? (double)trendProcessorCount / totalDecisions * 100 : 0,
                            rangeProcessorCount, totalDecisions > 0 ? (double)rangeProcessorCount / totalDecisions * 100 : 0,
                            autoRoutedCount, totalDecisions > 0 ? (double)autoRoutedCount / totalDecisions * 100 : 0);
    }

    static string DecisionToStringStatic(DECISION_ACTION decision)
    {
        switch (decision)
        {
        case ACTION_NONE:
            return "NONE";
        case ACTION_OPEN_BUY:
            return "OPEN_BUY";
        case ACTION_OPEN_SELL:
            return "OPEN_SELL";
        case ACTION_CLOSE_BUY:
            return "CLOSE_BUY";
        case ACTION_CLOSE_SELL:
            return "CLOSE_SELL";
        case ACTION_CLOSE_ALL:
            return "CLOSE_ALL";
        case ACTION_HOLD:
            return "HOLD";
        case ACTION_WAITING_FOR_PACKAGE:
            return "WAITING";
        default:
            return "UNKNOWN";
        }
    }
};

// ================= CLASS DEFINITION =================
class DecisionEngine
{
private:
    // Configuration
    string m_engineName;
    int m_engineMagicBase;
    bool m_initialized;
    bool m_debugEnabled;

    // Symbol management
    SymbolState m_symbolStates[];
    int m_totalSymbols;

    // Performance tracking
    DecisionMetrics m_metrics;

    // Configuration
    bool m_allowMultiplePositions;
    bool m_useRiskManagement;
    bool m_enforceCooldown;
    int m_maxPackageAgeSeconds;

    // Routing configuration
    bool m_autoDetectProcessor;
    bool m_useMarketRegimeRouting;
    double m_trapThresholdForRange;

    // Auto-package creation
    bool m_autoPackageCreation;
    int m_packageUpdateInterval;
    datetime m_lastPackageCreation;
    PackageManager *m_packageManager;

public:
    // ================= CONSTRUCTOR/DESTRUCTOR =================
    DecisionEngine()
    {
        m_engineName = "DecisionEngine";
        m_engineMagicBase = 10000;
        m_initialized = false;
        m_debugEnabled = false;
        m_totalSymbols = 0;

        m_allowMultiplePositions = true;
        m_useRiskManagement = true;
        m_enforceCooldown = true;
        m_maxPackageAgeSeconds = 300;

        // Routing defaults
        m_autoDetectProcessor = true;
        m_useMarketRegimeRouting = true;
        m_trapThresholdForRange = 50.0;

        // Auto-package creation defaults
        m_autoPackageCreation = false;
        m_packageUpdateInterval = 10;
        m_lastPackageCreation = 0;
        m_packageManager = NULL;

        ArrayResize(m_symbolStates, 0);

        DebugLogFile("CONSTRUCTOR", "DecisionEngine constructor called");
    }

    ~DecisionEngine()
    {
        DebugLogFile("DESTRUCTOR", "DecisionEngine destructor called");
        Deinitialize();
    }

    // ================= INITIALIZATION =================
    bool Initialize(string engineName = "DecisionEngine",
                    int magicBase = 10000,
                    bool debug = false)
    {
        DebugLogFile("INIT_START", StringFormat("Initializing DecisionEngine: name=%s, magicBase=%d, debug=%s",
                                                engineName, magicBase, debug ? "true" : "false"));

        if (m_initialized)
        {
            DebugLogFile("INIT_WARNING", "Already initialized, calling Deinitialize first");
            Deinitialize();
        }

        m_engineName = engineName;
        m_engineMagicBase = magicBase;
        m_debugEnabled = debug;

        // Initialize logger with default settings
        Logger::Initialize();

        m_initialized = true;

        DebugLogFile("INIT_SUCCESS", "Decision Engine v4.1 Initialized Successfully");
        DebugLogFile("CONFIG", StringFormat("Mode: Intelligent Router | Max Package Age: %d seconds", m_maxPackageAgeSeconds));
        DebugLogFile("CONFIG", StringFormat("AllowMultiplePositions: %s | UseRiskManagement: %s | EnforceCooldown: %s",
                                            m_allowMultiplePositions ? "true" : "false",
                                            m_useRiskManagement ? "true" : "false",
                                            m_enforceCooldown ? "true" : "false"));
        DebugLogFile("CONFIG", StringFormat("AutoDetectProcessor: %s | UseRegimeRouting: %s | TrapThreshold: %.1f%%",
                                            m_autoDetectProcessor ? "true" : "false",
                                            m_useMarketRegimeRouting ? "true" : "false",
                                            m_trapThresholdForRange));
        DebugLogFile("CONFIG", StringFormat("AutoPackageCreation: %s | UpdateInterval: %d sec",
                                            m_autoPackageCreation ? "ENABLED" : "DISABLED",
                                            m_packageUpdateInterval));

        return true;
    }

    void Deinitialize()
    {
        DebugLogFile("DEINIT_START", "Deinitializing DecisionEngine");

        if (!m_initialized)
        {
            DebugLogFile("DEINIT_WARNING", "Not initialized, skipping deinitialization");
            return;
        }

        // Log shutdown statistics
        DebugLogFile("SHUTDOWN", "=== SHUTDOWN STATISTICS ===");
        DebugLogFile("STATS", m_metrics.ToString());
        DebugLogFile("PROCESSOR_STATS", m_metrics.GetProcessorStats());
        DebugLogFile("SYMBOLS", StringFormat("Total symbols monitored: %d", m_totalSymbols));

        // Log each symbol's final state
        for (int i = 0; i < m_totalSymbols; i++)
        {
            DebugLogFile("SYMBOL_FINAL_STATE", m_symbolStates[i].GetStatus());
        }

        ArrayFree(m_symbolStates);
        m_totalSymbols = 0;
        m_initialized = false;

        Logger::Shutdown();

        DebugLogFile("DEINIT_SUCCESS", "DecisionEngine deinitialized successfully");
    }

    // ================= AUTO-PACKAGE CREATION SETTINGS =================
    void SetAutoPackageCreation(bool enabled)
    {
        m_autoPackageCreation = enabled;
        DebugLogFile("CONFIG_CHANGE", StringFormat("Auto-package creation: %s", enabled ? "ENABLED" : "DISABLED"));
    }

    void SetPackageUpdateInterval(int seconds)
    {
        m_packageUpdateInterval = MathMax(1, seconds);
        DebugLogFile("CONFIG_CHANGE", StringFormat("Package update interval: %d seconds", m_packageUpdateInterval));
    }

    void SetPackageManager(PackageManager *manager)
    {
        m_packageManager = manager;
        DebugLogFile("CONFIG_CHANGE", "PackageManager set for auto-package creation");
    }

    // ================= SYMBOL MANAGEMENT =================
    bool RegisterSymbol(string symbol, DecisionParams &params)
    {
        DebugLogFile("REGISTER_SYMBOL_START", StringFormat("Registering symbol: %s with params: %s", symbol, params.ToString()));

        if (!m_initialized)
        {
            DebugLogFile("ERROR", "Engine not initialized");
            return false;
        }

        if (HasSymbol(symbol))
        {
            DebugLogFile("WARNING", "Symbol already registered: " + symbol);
            return true;
        }

        // Resize array if needed
        if (m_totalSymbols >= ArraySize(m_symbolStates))
        {
            int newSize = m_totalSymbols + 10;
            ArrayResize(m_symbolStates, newSize);
            DebugLogFile("ARRAY_RESIZE", StringFormat("Resized symbol states array to %d", newSize));
        }

        // Get market regime for this symbol
        MarketAnalysis regime = GetMarketRegimeAnalysis(symbol, PERIOD_H1);

        // Apply regime adaptation
        DecisionParams adaptedParams = CreateRegimeBasedParams(regime, params);

        // Initialize symbol state
        SymbolState state;
        state.symbol = symbol;
        state.params = adaptedParams;

        // Generate unique magic number
        state.magicNumber = GenerateMagicNumber(symbol);

        // Store in array
        m_symbolStates[m_totalSymbols] = state;
        m_totalSymbols++;

        DebugLogFile("REGISTER_SYMBOL_SUCCESS", StringFormat("Symbol registered: %s | Magic: %d | Buy: %.1f%% | Sell: %.1f%%",
                                                             symbol, state.magicNumber,
                                                             state.params.buyConfidenceThreshold,
                                                             state.params.sellConfidenceThreshold));

        DebugLogFile("SYMBOL_COUNT", StringFormat("Total symbols: %d", m_totalSymbols));

        return true;
    }

    // Factory function for mk$ EA parameters - APPLIES MK$ PROFILE
    static DecisionParams CreateMKParams(double maxRiskPerTrade = 20.0,
                                         int positionCooldownMinutes = 60)
    {
        // Start with minimal defaults
        DecisionParams params;

        // OVERRIDE with mk$ specific settings
        params.buyConfidenceThreshold = 60.0;  // mk$ needs higher confidence
        params.sellConfidenceThreshold = 60.0; // Even higher for sells
        params.closePositionThreshold = 40.0;
        params.closeAllThreshold = 40.0;
        params.cooldownMinutes = positionCooldownMinutes;
        params.maxPositionsPerSymbol = 2; // mk$ only trades 1 position
        params.riskPercent = maxRiskPerTrade;
        params.minRiskRewardRatio = 1.0;

        // Regime-specific thresholds for mk$
        params.rangeBuyThreshold = 75.0; // Higher for range trading
        params.rangeSellThreshold = 75.0;
        params.trendBuyThreshold = 60.0; // Lower for trend following
        params.trendSellThreshold = 60.0;
        params.trapProbabilityThreshold = 65.0; // mk$ avoids traps

        DebugLogFile("MK_PARAMS_CREATED",
                     StringFormat("Created mk$ params: Risk=%.1f%%, Cooldown=%dm",
                                  maxRiskPerTrade, positionCooldownMinutes));

        return params;
    }

    bool SetupForMKEA(string symbol,
                      double maxRiskPerTrade,
                      int positionCooldownMinutes,
                      bool useAutoExecution,
                      int magicBase = 10000)
    {
        // Initialize engine
        if (!Initialize("mk$", magicBase, useAutoExecution))
            return false;

        // 1. Create mk$ profile
        DecisionParams mkParams = CreateMKParams(maxRiskPerTrade, positionCooldownMinutes);

        // 2. Get current market regime
        MarketAnalysis regime = GetMarketRegimeAnalysis(symbol, PERIOD_H1);

        // 3. Adapt mk$ profile to current regime
        DecisionParams finalParams = CreateRegimeBasedParams(regime, mkParams);

        // 4. Register with adapted parameters
        return RegisterSymbol(symbol, finalParams);
    }

    bool RegisterSymbolWithDefaults(string symbol,
                                    double buyThreshold = 60.0,
                                    double sellThreshold = 60.0,
                                    double riskPercent = 5.0)
    {
        DebugLogFile("REGISTER_SYMBOL_DEFAULTS", StringFormat("Registering %s with defaults: Buy=%.1f%%, Sell=%.1f%%, Risk=%.1f%%",
                                                              symbol, buyThreshold, sellThreshold, riskPercent));

        DecisionParams params;
        params.buyConfidenceThreshold = buyThreshold;
        params.sellConfidenceThreshold = sellThreshold;
        params.riskPercent = riskPercent;

        return RegisterSymbol(symbol, params);
    }

    bool UnregisterSymbol(string symbol)
    {
        DebugLogFile("UNREGISTER_SYMBOL_START", "Unregistering symbol: " + symbol);

        int index = FindSymbolIndex(symbol);
        if (index < 0)
        {
            DebugLogFile("UNREGISTER_ERROR", "Symbol not found: " + symbol);
            return false;
        }

        // Log symbol state before removal
        DebugLogFile("SYMBOL_REMOVAL", StringFormat("Removing symbol %s at index %d, state: %s",
                                                    symbol, index, m_symbolStates[index].GetStatus()));

        // Shift array elements
        for (int i = index; i < m_totalSymbols - 1; i++)
        {
            m_symbolStates[i] = m_symbolStates[i + 1];
        }

        m_totalSymbols--;
        DebugLogFile("UNREGISTER_SUCCESS", "Symbol unregistered: " + symbol);
        DebugLogFile("SYMBOL_COUNT", StringFormat("Total symbols after removal: %d", m_totalSymbols));

        return true;
    }

    // ================= MAIN PUBLIC INTERFACE =================
    DECISION_ACTION ProcessPackage(DecisionEngineInterface &package)
    {
        DebugLogFile("PROCESS_PACKAGE_START", "=====================================");
        DebugLogFile("PROCESS_PACKAGE_START", StringFormat(">>> STARTING PACKAGE PROCESSING | Symbol: %s", package.symbol));

        if (!m_initialized)
        {
            DebugLogFile("ERROR", "❌ Engine not initialized - cannot process package");
            return ACTION_NONE;
        }

        string symbol = package.symbol;
        DebugLogFile("PACKAGE_DETAILS", StringFormat("Package Details: Type=%s, Confidence=%.1f%%, Dir=%s, Regime=%s, Action=%s, Trap=%.1f%%",
                                                     package.packageType,
                                                     package.overallConfidence,
                                                     package.dominantDirection,
                                                     package.marketRegime,
                                                     package.recommendedAction,
                                                     package.trapProbability));

        // ============ STEP 1: GET MARKET REGIME ANALYSIS ============
        DebugLogFile("MARKET_REGIME_CHECK", "--- MARKET REGIME ANALYSIS ---");

        MarketAnalysis regimeAnalysis = GetMarketRegimeAnalysis(symbol, PERIOD_H1);

        DebugLogFile("MARKET_REGIME_RESULT", StringFormat("Market Regime Analysis Complete:", ""));
        DebugLogFile("MARKET_REGIME_RESULT", StringFormat("  - Root State: %s", regimeAnalysis.GetRootStateString(regimeAnalysis.rootState)));
        DebugLogFile("MARKET_REGIME_RESULT", StringFormat("  - Market State: %s", regimeAnalysis.GetStateString(regimeAnalysis.state)));

        // ============ STEP 2: STRICT REGIME-BASED ROUTING ============
        DebugLogFile("ROUTING_START", "--- STRICT REGIME-BASED ROUTING ---");

        DECISION_ACTION decision = ACTION_NONE;
        string processorType = "UNKNOWN";
        string routingReason = "";

        // Get package type from packageType field
        string packageType = package.packageType;
        if (packageType == "NONE" || packageType == "")
        {
            // Fallback detection if packageType not set
            if (StringFind(package.marketRegime, "RANGING") >= 0 ||
                StringFind(package.recommendedAction, "FADE") >= 0 ||
                package.trapProbability > 40)
            {
                packageType = "RANGE";
            }
            else
            {
                packageType = "TREND";
            }
            DebugLogFile("PACKAGE_TYPE_FALLBACK", StringFormat("Fallback: Package determined as %s", packageType));
        }

        DebugLogFile("PACKAGE_TYPE", StringFormat("Package type: %s", packageType));

        // STRICT ROUTING: Use REGIME to decide processor, NOT package type
        if (regimeAnalysis.IsTrending())
        {
            DebugLogFile("ROUTING_DECISION", StringFormat("📈 STRICT: Market is TRENDING - using TREND processor", ""));

            // Log mismatch if package was marked as range type
            if (packageType == "RANGE")
            {
                DebugLogFile("ROUTING_WARNING", "⚠️ Received RANGE package in TRENDING market - adjusting");
                routingReason = "Trending market - range package adjusted for trend following";

                // Add mismatch flag
                package.extraInfo3 += "|ADJUSTED:RANGE_IN_TREND";
            }
            else
            {
                routingReason = "Trending market - optimal routing";
            }

            decision = ProcessTrendPackage(package);
            processorType = "TREND";
        }
        else if (regimeAnalysis.IsRanging())
        {
            DebugLogFile("ROUTING_DECISION", StringFormat("📊 STRICT: Market is RANGING - using RANGE processor", ""));

            // Log mismatch if package was marked as trend type
            if (packageType == "TREND")
            {
                DebugLogFile("ROUTING_WARNING", "⚠️ Received TREND package in RANGING market - adjusting");
                routingReason = "Ranging market - trend package adjusted for range fading";

                // Add mismatch flag
                package.extraInfo3 += "|ADJUSTED:TREND_IN_RANGE";
            }
            else
            {
                routingReason = "Ranging market - optimal routing";
            }

            decision = ProcessRangePackage(package);
            processorType = "RANGE";
        }
        else
        {
            DebugLogFile("ROUTING_DECISION", "❓ Market regime unclear - using package type");

            if (packageType == "RANGE")
            {
                routingReason = "Unknown regime - using RANGE processor (package type)";
                decision = ProcessRangePackage(package);
                processorType = "RANGE";
            }
            else
            {
                routingReason = "Unknown regime - using TREND processor (package type)";
                decision = ProcessTrendPackage(package);
                processorType = "TREND";
            }
        }

        DebugLogFile("ROUTING_COMPLETE", StringFormat("✅ Routed to %s processor | Reason: %s", processorType, routingReason));

        // ============ STEP 3: SYMBOL REGISTRATION CHECK ============
        DebugLogFile("SYMBOL_CHECK_START", "--- SYMBOL REGISTRATION ---");
        if (!HasSymbol(symbol))
        {
            DebugLogFile("AUTO_REGISTRATION", StringFormat("Symbol %s not registered - auto-registering with regime-based defaults", symbol));

            DecisionParams minimalParams;
            DecisionParams params = CreateRegimeBasedParams(regimeAnalysis, minimalParams);

            if (!RegisterSymbol(symbol, params))
            {
                DebugLogFile("AUTO_REG_ERROR", "❌ Failed to auto-register symbol");
                LogPackageProcessingResult(symbol, processorType, decision, "FAILED - Auto-registration error");
                return ACTION_NONE;
            }
            DebugLogFile("AUTO_REG_SUCCESS", "✅ Symbol auto-registered with regime-based params");
        }

        // ============ STEP 4: GET SYMBOL INDEX ============
        int symbolIndex = FindSymbolIndex(symbol);
        if (symbolIndex < 0)
        {
            DebugLogFile("SYMBOL_INDEX_ERROR", "❌ Symbol index not found - critical error");
            LogPackageProcessingResult(symbol, processorType, decision, "FAILED - Symbol index error");
            return ACTION_NONE;
        }

        // ============ STEP 5: UPDATE SYMBOL STATE ============
        m_symbolStates[symbolIndex].lastProcessorUsed = processorType;
        m_symbolStates[symbolIndex].actualProcessorUsed = processorType;
        m_symbolStates[symbolIndex].lastProcessingTime = TimeCurrent();
        m_symbolStates[symbolIndex].lastProcessingReason = routingReason;

        // Update package with regime info
        string previousRegime = package.marketRegime;
        package.marketRegime = regimeAnalysis.GetRootStateString(regimeAnalysis.rootState);

        // Add additional regime info to package
        package.extraInfo1 = StringFormat("MarketState:%s", regimeAnalysis.GetStateString(regimeAnalysis.state));
        package.extraInfo2 = StringFormat("PositionSize:%s", GetPositionSizeString(regimeAnalysis.positionSize));

        DebugLogFile("REGIME_UPDATE", StringFormat("Package updated: Regime=%s→%s, State=%s",
                                                   previousRegime, package.marketRegime,
                                                   regimeAnalysis.GetStateString(regimeAnalysis.state)));

        // ============ STEP 6: LOG AND RETURN ============
        string decisionStr = DecisionToString(decision);

        // Check if package type matched regime
        bool wasMismatch = (regimeAnalysis.IsTrending() && packageType == "RANGE") ||
                           (regimeAnalysis.IsRanging() && packageType == "TREND");

        LogPackageProcessingResult(symbol, processorType, decision,
                                   StringFormat("COMPLETED | MarketState: %s | Mismatch: %s",
                                                regimeAnalysis.GetStateString(regimeAnalysis.state),
                                                wasMismatch ? "YES" : "NO"));

        if (decision == ACTION_NONE || decision == ACTION_HOLD)
        {
            DebugLogFile("NO_ACTION_TAKEN", StringFormat("⚠️ No action taken for %s | Decision: %s | MarketState: %s",
                                                         symbol, decisionStr,
                                                         regimeAnalysis.GetStateString(regimeAnalysis.state)));
        }
        else if (decision == ACTION_WAITING_FOR_PACKAGE)
        {
            DebugLogFile("WAITING_FOR_PACKAGE", StringFormat("⏳ Waiting for fresh package for %s | MarketState: %s",
                                                             symbol, regimeAnalysis.GetStateString(regimeAnalysis.state)));
        }
        else
        {
            DebugLogFile("ACTIONABLE_DECISION", StringFormat("🎯 Actionable decision for %s: %s | Regime: %s | Processor: %s",
                                                             symbol, decisionStr,
                                                             regimeAnalysis.GetRootStateString(regimeAnalysis.rootState),
                                                             processorType));
        }

        DebugLogFile("PROCESS_PACKAGE_END", StringFormat("<<< PACKAGE PROCESSING COMPLETE | Symbol: %s | Decision: %s | Regime: %s | State: %s | Processor: %s",
                                                         symbol, decisionStr,
                                                         regimeAnalysis.GetRootStateString(regimeAnalysis.rootState),
                                                         regimeAnalysis.GetStateString(regimeAnalysis.state),
                                                         processorType));
        DebugLogFile("PROCESS_PACKAGE_END", "=====================================\n");

        return decision;
    }

    // ================= AUTO-PACKAGE CREATION =================
    void CreateAndProcessPackages()
    {
        if (!m_autoPackageCreation || m_packageManager == NULL)
            return;

        if (TimeCurrent() - m_lastPackageCreation < m_packageUpdateInterval)
            return;

        m_lastPackageCreation = TimeCurrent();

        // Create packages for all registered symbols
        for (int i = 0; i < m_totalSymbols; i++)
        {
            string symbol = m_symbolStates[i].symbol;
            DebugLogFile("AUTO_PACKAGE_CREATION", StringFormat("Creating package for %s", symbol));

            // Create appropriate package based on market regime
            DecisionEngineInterface package = CreateAppropriatePackage(symbol);

            if (package.isValid)
            {
                // Process the package
                ProcessPackage(package);
            }
        }
    }

    // ================= PACKAGE CREATION FUNCTION =================
    DecisionEngineInterface CreateAppropriatePackage(string symbol)
    {
        DecisionEngineInterface package;
        package.symbol = symbol;
        package.analysisTime = TimeCurrent();

        DebugLogFile("PACKAGE_CREATION", "=== AUTO-PACKAGE CREATION ===");
        DebugLogFile("PACKAGE_CREATION", "Symbol: " + symbol);

        // ========== STEP 1: GET MARKET REGIME ANALYSIS ==========
        MarketAnalysis regimeAnalysis = GetMarketRegimeAnalysis(symbol, PERIOD_H1);

        DebugLogFile("MARKET_REGIME_ANALYSIS",
                     StringFormat("Market Regime: %s | State: %s | Confidence: %.0f%%",
                                  regimeAnalysis.GetRootStateString(regimeAnalysis.rootState),
                                  regimeAnalysis.GetStateString(regimeAnalysis.state),
                                  regimeAnalysis.confidence));

        // ========== STEP 2: CREATE PACKAGE BASED ON MARKET REGIME ==========
        if (regimeAnalysis.IsTrending())
        {
            // ========== CREATE TREND PACKAGE ==========
            DebugLogFile("PACKAGE_TYPE", "📈 MARKET IS TRENDING - Creating TREND package");

            if (m_packageManager == NULL || !m_packageManager.IsInitialized())
            {
                DebugLogFile("ERROR", "❌ PackageManager not available");
                package.isValid = false;
                return package;
            }

            TrendPackage trendPackage = m_packageManager.GetTrendPackage(true);

            if (!trendPackage.isValid)
            {
                DebugLogFile("ERROR", "❌ No valid TrendPackage available");
                package.isValid = false;
                return package;
            }

            // Populate TREND package with regime info
            package.marketRegime = "TRENDING";
            package.packageType = "TREND"; // Set explicit package type
            package.overallConfidence = (regimeAnalysis.confidence * 0.2) + (trendPackage.overallConfidence * 0.8);
            package.dominantDirection = trendPackage.directionAnalysis.dominantDirection;
            package.weightedScore = trendPackage.weightedScore;
            package.isValid = true;
            package.signalReason = StringFormat("Market Trending (%s): %s",
                                                regimeAnalysis.GetStateString(regimeAnalysis.state),
                                                regimeAnalysis.description);

            // Set trend-specific fields
            package.recommendedAction = "TREND_FOLLOW";
            package.trapProbability = 0; // Trend packages typically have low trap probability
            package.isTrapZone = false;
            package.isAvoidSignal = false;
            package.rangeAction = "NO_RANGE"; // Not a range trade
        }
        else if (regimeAnalysis.IsRanging())
        {
            // ========== CREATE RANGE PACKAGE ==========
            DebugLogFile("PACKAGE_TYPE", "📊 MARKET IS RANGING - Creating RANGE package");

            // Use RangeIntelligence::AnalyzeRange
            RangeAnalysisResult rangeResult = RangeIntelligence::AnalyzeRange(symbol, PERIOD_M15);

            // Populate RANGE package with regime info
            package.marketRegime = "RANGING";
            package.packageType = "RANGE"; // Set explicit package type
            package.trapProbability = rangeResult.trapProbability;
            package.recommendedAction = rangeResult.rangeAction;
            package.overallConfidence = (regimeAnalysis.confidence * 0.2) + (rangeResult.overallConfidence * 0.8);
            package.dominantDirection = rangeResult.rangeBiasDirection;
            package.isTrapZone = rangeResult.isTrapZone;
            package.isAvoidSignal = (regimeAnalysis.action == "Wait for clarity" ||
                                     rangeResult.rangeAction == "AVOID_HIGH_TRAP");
            package.trapReason = StringFormat("Trap: %.1f%%, Bias: %s",
                                              rangeResult.trapProbability, rangeResult.rangeBiasDirection);
            package.isValid = true;
            package.signalReason = StringFormat("Market Ranging (%s): %s",
                                                regimeAnalysis.GetStateString(regimeAnalysis.state),
                                                regimeAnalysis.description);

            // Copy range-specific fields
            package.entryQualityScore = rangeResult.entryQualityScore;
            package.riskRewardScore = rangeResult.riskRewardScore;
            package.rangeBiasDirection = rangeResult.rangeBiasDirection;
            package.rangeBiasConfidence = rangeResult.rangeBiasConfidence;
            package.rangeAction = rangeResult.rangeAction;
            package.supportLevel = rangeResult.supportLevel;
            package.resistanceLevel = rangeResult.resistanceLevel;
            package.supportTouches = rangeResult.supportTouches;
            package.resistanceTouches = rangeResult.resistanceTouches;
        }
        else
        {
            // ========== CREATE DEFAULT PACKAGE ==========
            DebugLogFile("PACKAGE_TYPE", "❓ MARKET REGIME UNKNOWN - Creating default package");

            package.marketRegime = "UNKNOWN";
            package.packageType = "RANGE"; // Default to range for safety
            package.overallConfidence = regimeAnalysis.confidence;
            package.dominantDirection = regimeAnalysis.direction;
            package.isValid = true;
            package.signalReason = "Market Regime Unknown: " + regimeAnalysis.description;

            // Set RANGE-style defaults for safety
            package.recommendedAction = "Wait for clarity";
            package.trapProbability = 50.0;
            package.isTrapZone = true;
            package.isAvoidSignal = true;
            package.rangeAction = "AVOID_HIGH_TRAP";
        }

        // ========== STEP 3: SET COMMON FIELDS WITH REGIME INFO ==========
        package.entryPrice = SymbolInfoDouble(symbol, SYMBOL_BID);
        package.orderType = (package.dominantDirection == "BULLISH") ? ORDER_TYPE_BUY : (package.dominantDirection == "BEARISH") ? ORDER_TYPE_SELL
                                                                                                                                 : ORDER_TYPE_BUY_LIMIT;
        package.signalConfidence = package.overallConfidence;

        // Use regime-based stop and TP distances
        package.stopLoss = regimeAnalysis.stopDistance;
        package.takeProfit1 = regimeAnalysis.takeProfitDistance;

        // Use regime-based position size
        switch (regimeAnalysis.positionSize)
        {
        case SIZE_ZERO:
            package.positionSize = 0.0;
            break;
        case SIZE_VERY_SMALL:
            package.positionSize = 0.01;
            break;
        case SIZE_SMALL:
            package.positionSize = 0.08;
            break;
        case SIZE_MEDIUM:
            package.positionSize = 2.05;
            break;
        case SIZE_LARGE:
            package.positionSize = 5.1;
            break;
        default:
            package.positionSize = 0.03;
            break;
        }

        package.timeframe = Period();

        // Store regime info in extra fields
        package.extraInfo1 = StringFormat("Regime:%s", regimeAnalysis.GetRootStateString(regimeAnalysis.rootState));
        package.extraInfo2 = StringFormat("State:%s", regimeAnalysis.GetStateString(regimeAnalysis.state));
        package.extraInfo3 = StringFormat("Conf:%.0f|Action:%s", regimeAnalysis.confidence, regimeAnalysis.action);

        DebugLogFile("PACKAGE_CREATION_COMPLETE",
                     StringFormat("🎯 Package created: Type=%s, Regime=%s, State=%s, Action=%s, Dir=%s, Conf=%.1f%%",
                                  package.packageType,
                                  package.marketRegime,
                                  regimeAnalysis.GetStateString(regimeAnalysis.state),
                                  package.recommendedAction,
                                  package.dominantDirection,
                                  package.overallConfidence));

        return package;
    }

    // ================= ADDITIONAL HELPER METHODS =================
    void LogPackageProcessingResult(string symbol, string processorType, DECISION_ACTION decision, string status)
    {
        string decisionStr = DecisionToString(decision);
        string timestamp = TimeToString(TimeCurrent(), TIME_DATE | TIME_SECONDS);

        string logEntry = StringFormat("[%s] %s | Processor: %s | Decision: %s | Status: %s",
                                       timestamp, symbol, processorType, decisionStr, status);

        DebugLogFile("PACKAGE_RESULT", logEntry);
        Logger::Log("PACKAGE_PROCESSING", logEntry, true, true);
    }

    string TimeframeToString(ENUM_TIMEFRAMES tf)
    {
        switch (tf)
        {
        case PERIOD_M1:
            return "M1";
        case PERIOD_M5:
            return "M5";
        case PERIOD_M15:
            return "M15";
        case PERIOD_M30:
            return "M30";
        case PERIOD_H1:
            return "H1";
        case PERIOD_H4:
            return "H4";
        case PERIOD_D1:
            return "D1";
        case PERIOD_W1:
            return "W1";
        case PERIOD_MN1:
            return "MN1";
        default:
            return "UNKNOWN";
        }
    }

    // ================= PROCESS TREND PACKAGE =================
    DECISION_ACTION ProcessTrendPackage(DecisionEngineInterface &package)
    {
        DebugLogFile("PROCESS_TREND_PACKAGE", StringFormat("=== PROCESSING TREND PACKAGE === | Symbol: %s | Dir: %s | Conf: %.1f%% | Regime: %s",
                                                           package.symbol, package.dominantDirection,
                                                           package.overallConfidence, package.marketRegime));

        if (!m_initialized)
        {
            DebugLogFile("ERROR", "Engine not initialized");
            return ACTION_NONE;
        }

        string symbol = package.symbol;

        // Validate symbol is registered
        if (!HasSymbol(symbol))
        {
            DebugLogFile("AUTO-REG", "Auto-registering symbol: " + symbol);
            if (!RegisterSymbolWithDefaults(symbol))
            {
                DebugLogFile("AUTO-REG_ERROR", "Failed to auto-register symbol: " + symbol);
                return ACTION_NONE;
            }
        }

        int symbolIndex = FindSymbolIndex(symbol);
        if (symbolIndex < 0)
        {
            DebugLogFile("ERROR", "Symbol index not found: " + symbol);
            return ACTION_NONE;
        }

        // Store the package as trend
        m_symbolStates[symbolIndex].lastPackage = package;
        m_symbolStates[symbolIndex].lastDecisionTime = TimeCurrent();
        m_symbolStates[symbolIndex].usingRangePackage = false;

        DebugLogFile("TREND_PACKAGE_STORED", StringFormat("Trend package stored for %s | Dir: %s | Conf: %.1f%%",
                                                          symbol, package.dominantDirection, package.overallConfidence));

        // Validate package
        if (!package.IsValid())
        {
            LogDecision(symbol, ACTION_NONE, package, "Invalid trend package");
            return ACTION_NONE;
        }

        // Check package freshness
        if (!IsPackageFresh(symbolIndex))
        {
            LogDecision(symbol, ACTION_WAITING_FOR_PACKAGE, package, "Trend package too old");
            return ACTION_WAITING_FOR_PACKAGE;
        }

        DebugLogFile("TREND_PROCESS", "✅ Trend package valid and fresh");

        // Analyze current positions
        PositionAnalysis positions = AnalyzePositions(symbolIndex);
        DebugLogFile("TREND_POSITIONS", StringFormat("Current positions: %s", positions.ToString()));

        // Make decision using PURE trend-following logic
        DECISION_ACTION decision = MakeTrendDecision(symbolIndex, package, positions);
        DebugLogFile("TREND_DECISION_MADE", StringFormat("Trend decision made: %s", DecisionToString(decision)));

        // Validate and potentially execute
        if (decision != ACTION_NONE && decision != ACTION_HOLD && decision != ACTION_WAITING_FOR_PACKAGE)
        {
            DebugLogFile("TREND_VALIDATION_START", "Starting trend decision validation");
            if (ValidateDecision(symbolIndex, decision, package))
            {
                DebugLogFile("TREND_VALIDATION", "✅ Trend decision validated");
                ExecuteDecision(symbolIndex, decision, package);
            }
            else
            {
                DebugLogFile("TREND_VALIDATION", "❌ Trend decision validation failed");
                decision = ACTION_HOLD;
                DebugLogFile("TREND_FALLBACK", "Falling back to HOLD due to validation failure");
            }
        }
        else
        {
            DebugLogFile("TREND_NO_EXECUTION", StringFormat("No execution needed: %s", DecisionToString(decision)));
        }

        // Store and log decision
        m_symbolStates[symbolIndex].lastDecision = decision;

        DebugLogFile("TREND_PROCESS_COMPLETE", StringFormat("=== TREND PROCESS COMPLETE === | Final decision: %s", DecisionToString(decision)));
        return decision;
    }

    // ================= RANGE PACKAGE PROCESSING =================
    DECISION_ACTION ProcessRangePackage(DecisionEngineInterface &package)
    {
        DebugLogFile("PROCESS_RANGE_PACKAGE", StringFormat("=== PROCESSING RANGE PACKAGE === | Symbol: %s | Regime: %s | Action: %s | Trap: %.1f%%",
                                                           package.symbol, package.marketRegime, package.recommendedAction, package.trapProbability));

        if (!m_initialized)
        {
            DebugLogFile("ERROR", "Engine not initialized");
            return ACTION_NONE;
        }

        string symbol = package.symbol;

        // Validate symbol is registered
        if (!HasSymbol(symbol))
        {
            DebugLogFile("AUTO-REG", "Auto-registering symbol: " + symbol);
            if (!RegisterSymbolWithDefaults(symbol))
            {
                DebugLogFile("AUTO-REG_ERROR", "Failed to auto-register symbol: " + symbol);
                return ACTION_NONE;
            }
        }

        int symbolIndex = FindSymbolIndex(symbol);
        if (symbolIndex < 0)
        {
            DebugLogFile("ERROR", "Symbol index not found: " + symbol);
            return ACTION_NONE;
        }

        // Mark this as a range package
        m_symbolStates[symbolIndex].usingRangePackage = true;
        m_symbolStates[symbolIndex].lastPackage = package;
        m_symbolStates[symbolIndex].lastDecisionTime = TimeCurrent();

        // Range packages should have these fields populated:
        DebugLogFile("RANGE_DATA", StringFormat("Regime: %s | Action: %s | Trap: %.1f%% | Avoid: %s",
                                                package.marketRegime, package.recommendedAction,
                                                package.trapProbability, package.ShouldAvoid() ? "YES" : "NO"));

        // Validate package
        if (!package.IsValid())
        {
            LogDecision(symbol, ACTION_NONE, package, "Invalid RangePackage");
            return ACTION_NONE;
        }

        // Check package freshness
        if (!IsPackageFresh(symbolIndex))
        {
            LogDecision(symbol, ACTION_WAITING_FOR_PACKAGE, package, "Range package too old");
            return ACTION_WAITING_FOR_PACKAGE;
        }

        // Analyze current positions
        PositionAnalysis positions = AnalyzePositions(symbolIndex);

        // Make decision based on RANGE package
        DECISION_ACTION decision = MakeRangeDecision(symbolIndex, package, positions);

        DebugLogFile("RANGE_DECISION_MADE", StringFormat("Range decision: %s", DecisionToString(decision)));

        // Execute if needed
        if (decision != ACTION_NONE && decision != ACTION_HOLD && decision != ACTION_WAITING_FOR_PACKAGE)
        {
            if (ValidateDecision(symbolIndex, decision, package))
            {
                ExecuteDecision(symbolIndex, decision, package);
            }
            else
            {
                decision = ACTION_HOLD;
            }
        }

        m_symbolStates[symbolIndex].lastDecision = decision;

        return decision;
    }

    // ================= SETTERS =================
    void SetDebugMode(bool enabled)
    {
        DebugLogFile("CONFIG_CHANGE", StringFormat("Debug mode changing from %s to %s",
                                                   m_debugEnabled ? "ENABLED" : "DISABLED", enabled ? "ENABLED" : "DISABLED"));
        m_debugEnabled = enabled;
        DebugLogFile("CONFIG", "Debug mode: " + (enabled ? "ENABLED" : "DISABLED"));
    }

    void SetAllowMultiplePositions(bool allowed)
    {
        DebugLogFile("CONFIG_CHANGE", StringFormat("Multiple positions changing from %s to %s",
                                                   m_allowMultiplePositions ? "ALLOWED" : "NOT ALLOWED", allowed ? "ALLOWED" : "NOT ALLOWED"));
        m_allowMultiplePositions = allowed;
        DebugLogFile("CONFIG", "Multiple positions: " + (allowed ? "ALLOWED" : "NOT ALLOWED"));
    }

    void SetUseRiskManagement(bool use)
    {
        DebugLogFile("CONFIG_CHANGE", StringFormat("Risk management changing from %s to %s",
                                                   m_useRiskManagement ? "ENABLED" : "DISABLED", use ? "ENABLED" : "DISABLED"));
        m_useRiskManagement = use;
        DebugLogFile("CONFIG", "Risk management: " + (use ? "ENABLED" : "DISABLED"));
    }

    void SetEnforceCooldown(bool enforce)
    {
        DebugLogFile("CONFIG_CHANGE", StringFormat("Cooldown enforcement changing from %s to %s",
                                                   m_enforceCooldown ? "ENABLED" : "DISABLED", enforce ? "ENABLED" : "DISABLED"));
        m_enforceCooldown = enforce;
        DebugLogFile("CONFIG", "Cooldown enforcement: " + (enforce ? "ENABLED" : "DISABLED"));
    }

    void SetMaxPackageAge(int seconds)
    {
        int oldValue = m_maxPackageAgeSeconds;
        m_maxPackageAgeSeconds = MathMax(60, seconds);
        DebugLogFile("CONFIG_CHANGE", StringFormat("Max package age changing from %d to %d seconds", oldValue, m_maxPackageAgeSeconds));
        DebugLogFile("CONFIG", StringFormat("Max package age: %d seconds", m_maxPackageAgeSeconds));
    }

    void SetAutoDetectProcessor(bool autoDetect)
    {
        m_autoDetectProcessor = autoDetect;
        DebugLogFile("CONFIG_CHANGE", StringFormat("Auto-detect processor: %s", autoDetect ? "ENABLED" : "DISABLED"));
    }

    void SetUseMarketRegimeRouting(bool useRegimeRouting)
    {
        m_useMarketRegimeRouting = useRegimeRouting;
        DebugLogFile("CONFIG_CHANGE", StringFormat("Market regime routing: %s", useRegimeRouting ? "ENABLED" : "DISABLED"));
    }

    void SetTrapThresholdForRange(double threshold)
    {
        m_trapThresholdForRange = MathMax(0, MathMin(100, threshold));
        DebugLogFile("CONFIG_CHANGE", StringFormat("Trap threshold for range routing: %.1f%%", m_trapThresholdForRange));
    }

    void UpdateSymbolParams(string symbol, DecisionParams &params)
    {
        DebugLogFile("UPDATE_PARAMS_START", StringFormat("Updating params for %s: %s", symbol, params.ToString()));

        int index = FindSymbolIndex(symbol);
        if (index >= 0)
        {
            DecisionParams oldParams = m_symbolStates[index].params;
            m_symbolStates[index].params = params;

            DebugLogFile("UPDATE_PARAMS_SUCCESS", StringFormat("Updated params for %s", symbol));
            DebugLogFile("PARAMS_CHANGE", StringFormat("Buy: %.1f%% -> %.1f%% | Sell: %.1f%% -> %.1f%%",
                                                       oldParams.buyConfidenceThreshold, params.buyConfidenceThreshold,
                                                       oldParams.sellConfidenceThreshold, params.sellConfidenceThreshold));
        }
        else
        {
            DebugLogFile("UPDATE_PARAMS_ERROR", "Symbol not found: " + symbol);
        }
    }

    // ================= GETTERS =================
    string GetStatus() const
    {
        if (!m_initialized)
        {
            DebugLogFile("GET_STATUS", "DecisionEngine: NOT INITIALIZED");
            return "DecisionEngine: NOT INITIALIZED";
        }

        string status = StringFormat("DecisionEngine v4.1 | Symbols: %d | %s",
                                     m_totalSymbols, m_metrics.ToString());
        DebugLogFile("GET_STATUS", status);
        return status;
    }

    string GetProcessorStatistics() const
    {
        if (!m_initialized)
            return "Engine not initialized";

        return m_metrics.GetProcessorStats();
    }

    DecisionEngineInterface GetLastPackage(string symbol)
    {
        DebugLogFile("GET_LAST_PACKAGE", "Getting last package for: " + symbol);

        int index = FindSymbolIndex(symbol);
        if (index >= 0)
        {
            DebugLogFile("GET_LAST_PACKAGE_SUCCESS", StringFormat("Found package for %s, isValid=%s",
                                                                  symbol, m_symbolStates[index].lastPackage.IsValid() ? "true" : "false"));
            return m_symbolStates[index].lastPackage;
        }

        DebugLogFile("GET_LAST_PACKAGE_ERROR", "Symbol not found: " + symbol);
        DecisionEngineInterface empty;
        return empty;
    }

    DECISION_ACTION GetLastDecision(string symbol)
    {
        DebugLogFile("GET_LAST_DECISION", "Getting last decision for: " + symbol);

        int index = FindSymbolIndex(symbol);
        if (index >= 0)
        {
            DebugLogFile("GET_LAST_DECISION_SUCCESS", StringFormat("Last decision for %s: %s",
                                                                   symbol, DecisionToString(m_symbolStates[index].lastDecision)));
            return m_symbolStates[index].lastDecision;
        }

        DebugLogFile("GET_LAST_DECISION_ERROR", "Symbol not found: " + symbol);
        return ACTION_NONE;
    }

    string GetLastProcessorUsed(string symbol)
    {
        int index = FindSymbolIndex(symbol);
        if (index >= 0)
        {
            return m_symbolStates[index].lastProcessorUsed;
        }
        return "NONE";
    }

    DecisionMetrics GetMetrics() const
    {
        DebugLogFile("GET_METRICS", "Getting decision metrics");
        return m_metrics;
    }

    int GetSymbolCount() const
    {
        DebugLogFile("GET_SYMBOL_COUNT", StringFormat("Symbol count: %d", m_totalSymbols));
        return m_totalSymbols;
    }

    bool HasSymbol(string symbol)
    {
        bool hasSymbol = (FindSymbolIndex(symbol) >= 0);
        DebugLogFile("HAS_SYMBOL", StringFormat("Checking if symbol %s exists: %s", symbol, hasSymbol ? "YES" : "NO"));
        return hasSymbol;
    }

    int GetMagicNumber(string symbol)
    {
        int index = FindSymbolIndex(symbol);
        if (index >= 0)
        {
            return m_symbolStates[index].magicNumber;
        }
        return 0;
    }

    // ================= EVENT HANDLERS =================
    void OnTick()
    {
        if (!m_initialized)
        {
            DebugLogFile("ONTICK_WARNING", "Engine not initialized, skipping OnTick");
            return;
        }

        // Auto-create and process packages
        if (m_autoPackageCreation)
        {
            CreateAndProcessPackages();
        }

        // Check for expired packages
        int expiredCount = 0;
        for (int i = 0; i < m_totalSymbols; i++)
        {
            if (m_symbolStates[i].HasValidPackage() && !m_symbolStates[i].IsPackageFresh())
            {
                expiredCount++;
                DebugLogFile("PACKAGE_EXPIRED", StringFormat("Package expired for %s (age: %d seconds)",
                                                             m_symbolStates[i].symbol,
                                                             (int)(TimeCurrent() - m_symbolStates[i].lastPackage.analysisTime)));
            }
        }

        if (expiredCount > 0)
        {
            DebugLogFile("PACKAGE_CHECK_SUMMARY", StringFormat("%d packages expired out of %d total symbols", expiredCount, m_totalSymbols));
        }

        // NEW: Check closed positions for profitability
        CheckClosedPositions();
    }

    void OnTimer()
    {
        DebugLogFile("ONTIMER", "OnTimer called");
        OnTick();
    }

    void OnTradeTransaction(const MqlTradeTransaction &trans,
                            const MqlTradeRequest &request,
                            const MqlTradeResult &result)
    {
        DebugLogFile("TRADE_TRANSACTION", StringFormat("Trade transaction: type=%d, symbol=%s, result=%d",
                                                       trans.type, trans.symbol, result.retcode));

        if (!m_initialized)
        {
            DebugLogFile("TRADE_TRANSACTION_WARNING", "Engine not initialized, skipping");
            return;
        }

        // Update cooldown on successful trades
        if (result.retcode == TRADE_RETCODE_DONE)
        {
            DebugLogFile("TRADE_SUCCESS", "Trade transaction successful");

            bool isBuy = false;

            // Check if it's a deal addition
            if (trans.type == TRADE_TRANSACTION_DEAL_ADD)
            {
                // Get deal information
                ulong dealTicket = trans.deal;

                // Check if this is a buy deal
                if (HistoryDealSelect(dealTicket))
                {
                    long dealType = HistoryDealGetInteger(dealTicket, DEAL_TYPE);
                    if (dealType == DEAL_TYPE_BUY)
                    {
                        isBuy = true;
                        DebugLogFile("TRADE_DIRECTION", "BUY deal detected");
                    }
                    else if (dealType == DEAL_TYPE_SELL)
                    {
                        DebugLogFile("TRADE_DIRECTION", "SELL deal detected");
                    }
                }
            }

            // Update cooldown for the symbol
            int index = FindSymbolIndex(trans.symbol);
            if (index >= 0)
            {
                m_symbolStates[index].cooldown.Update(isBuy);
                DebugLogFile("COOLDOWN_UPDATE_TRADE", StringFormat("Cooldown updated for %s: %s",
                                                                   trans.symbol, isBuy ? "BUY" : "SELL"));
            }
        }
    }

    // ================= UTILITY =================
    string DecisionToString(DECISION_ACTION decision) const
    {
        return DecisionMetrics::DecisionToStringStatic(decision);
    }

    string GetSymbolAtIndex(int index) const
    {
        if (index >= 0 && index < m_totalSymbols)
        {
            return m_symbolStates[index].symbol;
        }
        return "";
    }

    string GetRegimeInfo(string symbol)
    {
        string regime, source, direction;
        double confidence;
        return GetRegimeInfoDetailed(symbol, regime, source, direction, confidence);
    }

    // Configure market regime detector settings
    void SetMarketRegimeSettings(int lookbackBars = 8,
                                 double accountBalance = 10000,
                                 double riskPercent = 1.0)
    {
        // These settings will be used when creating MarketRegimeDetector instances
        DebugLogFile("REGIME_CONFIG",
                     StringFormat("Market Regime Settings: Lookback=%d bars, Balance=$%.2f, Risk=%.1f%%",
                                  lookbackBars, accountBalance, riskPercent));
    }

    // Get current market regime as string
    string GetCurrentMarketRegimeString(string symbol)
    {
        MarketAnalysis analysis = GetMarketRegimeAnalysis(symbol, PERIOD_H1);
        return analysis.ToString();
    }

private:
    // ==================== FIXED: PROFITABILITY CHECKER ====================
    void CheckClosedPositions()
    {
        DebugLogFile("CHECK_CLOSED_POSITIONS_START",
                     StringFormat("Checking %d symbols for closed positions", m_totalSymbols));

        for (int i = 0; i < m_totalSymbols; i++)
        {
            string symbol = m_symbolStates[i].symbol;
            int magic = m_symbolStates[i].magicNumber;

            // METHOD 1: Check if we have an open position that should be tracked
            if (m_symbolStates[i].positionOpenTime > 0 &&
                !m_symbolStates[i].awaitingProfitabilityCheck)
            {
                // Check if position is still open
                bool positionStillOpen = IsPositionStillOpen(symbol, magic);

                if (!positionStillOpen)
                {
                    // Position WAS open (we tracked it) but NOW it's closed!
                    // Must have closed via SL/TP/Manual outside system
                    DebugLogFile("AUTO_CLOSE_DETECTED",
                                 StringFormat("Position for %s (Magic: %d) auto-closed! Opened at %s",
                                              symbol, magic, TimeToString(m_symbolStates[i].positionOpenTime)));

                    // Process it now
                    ProcessClosedTradeProfitability(i);
                }
            }

            // METHOD 2: Original logic for manual closes
            if (m_symbolStates[i].awaitingProfitabilityCheck &&
                m_symbolStates[i].positionOpenTime > 0)
            {
                // Wait for the trade to fully close (give it 10 seconds)
                if ((TimeCurrent() - m_symbolStates[i].positionCloseTime) > 10)
                {
                    bool positionStillOpen = IsPositionStillOpen(symbol, magic);

                    if (!positionStillOpen)
                    {
                        ProcessClosedTradeProfitability(i);
                    }
                    else
                    {
                        DebugLogFile("PROFIT_CHECK_DEFERRED",
                                     StringFormat("Position still open for %s (Magic: %d), waiting...",
                                                  symbol, magic));
                    }
                }
            }
        }
    }

    bool IsPositionStillOpen(string symbol, int magicNumber)
    {
        int total = PositionsTotal();
        for (int i = 0; i < total; i++)
        {
            ulong ticket = PositionGetTicket(i);
            if (ticket > 0)
            {
                string posSymbol = PositionGetString(POSITION_SYMBOL);
                long posMagic = PositionGetInteger(POSITION_MAGIC);

                if (posSymbol == symbol && posMagic == magicNumber)
                {
                    return true; // Position is still open
                }
            }
        }
        return false; // Position is closed
    }

    void ProcessClosedTradeProfitability(int symbolIndex)
    {
        string symbol = m_symbolStates[symbolIndex].symbol;
        int magic = m_symbolStates[symbolIndex].magicNumber;

        DebugLogFile("PROCESS_CLOSED_TRADE",
                     StringFormat("Processing closed trade for %s (Magic: %d) opened at %s",
                                  symbol, magic, TimeToString(m_symbolStates[symbolIndex].positionOpenTime)));

        // Look for the CLOSED trade in history
        bool wasProfitable = CheckClosedTradeInHistory(symbol, magic, m_symbolStates[symbolIndex].positionOpenTime);

        // Get processor type
        string processorType = m_symbolStates[symbolIndex].lastProcessorUsed;

        // ⚠️ CRITICAL: UPDATE METRICS WITH ACTUAL RESULT!
        DebugLogFile("METRICS_UPDATE_START",
                     StringFormat("Updating metrics for %s: wasProfitable=%s | OpenDecision: %s | Conf: %.1f%%",
                                  symbol, wasProfitable ? "true" : "false",
                                  DecisionToString(m_symbolStates[symbolIndex].positionOpenDecision),
                                  m_symbolStates[symbolIndex].positionOpenConfidence));

        // Only count if it was an OPEN decision
        if (m_symbolStates[symbolIndex].positionOpenDecision == ACTION_OPEN_BUY ||
            m_symbolStates[symbolIndex].positionOpenDecision == ACTION_OPEN_SELL)
        {
            // Update total decisions
            int prevTotal = m_metrics.totalDecisions;
            m_metrics.totalDecisions++;

            // Update profitable decisions if it was profitable
            if (wasProfitable)
            {
                m_metrics.profitableDecisions++;
                DebugLogFile("METRICS_PROFITABLE_ADD",
                             StringFormat("✅ Added profitable trade: %s | Total profitable now: %d",
                                          symbol, m_metrics.profitableDecisions));
            }
            else
            {
                DebugLogFile("METRICS_UNPROFITABLE_ADD",
                             StringFormat("❌ Added unprofitable trade: %s | Profitable total still: %d",
                                          symbol, m_metrics.profitableDecisions));
            }

            // Update rolling average confidence
            if (m_metrics.totalDecisions == 1)
            {
                m_metrics.averageConfidence = m_symbolStates[symbolIndex].positionOpenConfidence;
            }
            else
            {
                m_metrics.averageConfidence = ((m_metrics.averageConfidence * prevTotal) +
                                               m_symbolStates[symbolIndex].positionOpenConfidence) /
                                              m_metrics.totalDecisions;
            }

            // Update accuracy rate
            if (m_metrics.totalDecisions > 0)
            {
                m_metrics.accuracyRate = ((double)m_metrics.profitableDecisions / m_metrics.totalDecisions) * 100.0;

                DebugLogFile("METRICS_UPDATED",
                             StringFormat("Metrics updated: Trades: %d | Profitable: %d | Accuracy: %.1f%% | AvgConf: %.1f%%",
                                          m_metrics.totalDecisions,
                                          m_metrics.profitableDecisions,
                                          m_metrics.accuracyRate,
                                          m_metrics.averageConfidence));
            }
        }
        else
        {
            DebugLogFile("METRICS_SKIP",
                         StringFormat("Skipping non-open decision in metrics: %s",
                                      DecisionToString(m_symbolStates[symbolIndex].positionOpenDecision)));
        }

        // Track processor usage (call Update with wasProfitable flag)
        m_metrics.Update(m_symbolStates[symbolIndex].positionOpenDecision,
                         m_symbolStates[symbolIndex].positionOpenConfidence,
                         wasProfitable,
                         processorType);

        DebugLogFile("CLOSED_TRADE_PROCESSED",
                     StringFormat("%s | Open: %s | Final Result: %s | Processor: %s",
                                  symbol,
                                  DecisionToString(m_symbolStates[symbolIndex].positionOpenDecision),
                                  wasProfitable ? "WIN" : "LOSS",
                                  processorType));

        // Reset the stored position info
        ResetPositionTracking(symbolIndex);
    }

    bool CheckClosedTradeInHistory(string symbol, int magicNumber, datetime openTime)
    {
        int totalDeals = HistoryDealsTotal();
        double netProfit = 0;
        bool foundCloseDeal = false;

        DebugLogFile("CHECK_HISTORY_FOR_CLOSED_TRADE",
                     StringFormat("Checking history for closed trade: %s (Magic: %d)", symbol, magicNumber));

        for (int i = totalDeals - 1; i >= 0; i--)
        {
            ulong dealTicket = HistoryDealGetTicket(i);
            if (dealTicket <= 0)
                continue;

            datetime dealTime = (datetime)HistoryDealGetInteger(dealTicket, DEAL_TIME);

            // Only check deals that happened after position open
            if (dealTime < openTime - 60) // Allow 1 minute buffer
                continue;

            string dealSymbol = HistoryDealGetString(dealTicket, DEAL_SYMBOL);
            long dealMagic = HistoryDealGetInteger(dealTicket, DEAL_MAGIC);
            long dealEntry = HistoryDealGetInteger(dealTicket, DEAL_ENTRY);

            // Check if this deal belongs to our position
            if (dealSymbol == symbol && dealMagic == magicNumber)
            {
                // Look for OUT/OUT_BY deals (closing deals)
                if (dealEntry == DEAL_ENTRY_OUT || dealEntry == DEAL_ENTRY_OUT_BY)
                {
                    foundCloseDeal = true;

                    double profit = HistoryDealGetDouble(dealTicket, DEAL_PROFIT);
                    double commission = HistoryDealGetDouble(dealTicket, DEAL_COMMISSION);
                    double swap = HistoryDealGetDouble(dealTicket, DEAL_SWAP);
                    double dealTotal = profit + commission + swap;

                    netProfit += dealTotal;

                    DebugLogFile("CLOSE_DEAL_FOUND",
                                 StringFormat("Close deal %llu: Time: %s, Profit: $%.2f, Total: $%.2f",
                                              dealTicket, TimeToString(dealTime), profit, dealTotal));
                }
            }
        }

        if (foundCloseDeal)
        {
            bool isProfitable = (netProfit > 0);

            DebugLogFile("CLOSED_TRADE_RESULT",
                         StringFormat("Closed trade for %s: Net Profit: $%.2f | Profitable: %s",
                                      symbol, netProfit, isProfitable ? "YES (WIN)" : "NO (LOSS)"));

            return isProfitable;
        }
        else
        {
            // No close deals found yet - trade might still be processing
            DebugLogFile("NO_CLOSE_DEALS_FOUND",
                         StringFormat("No close deals found for %s yet, might still be processing", symbol));
            return false; // Don't count it yet
        }
    }

    void ResetPositionTracking(int symbolIndex)
    {
        m_symbolStates[symbolIndex].positionOpenTime = 0;
        m_symbolStates[symbolIndex].positionCloseTime = 0;
        m_symbolStates[symbolIndex].positionOpenDecision = ACTION_NONE;
        m_symbolStates[symbolIndex].positionOpenConfidence = 0;
        m_symbolStates[symbolIndex].awaitingProfitabilityCheck = false;
    }

    // ==================== NEW: ENHANCED PROFITABILITY CHECK ====================
    bool CheckPositionNetProfitability(string symbol, int magicNumber, datetime openTime)
    {
        // Find ALL deals for this position (including partial closes)
        int totalDeals = HistoryDealsTotal();
        double netProfit = 0;
        bool positionFound = false;

        DebugLogFile("ENHANCED_PROFIT_TRACKING",
                     StringFormat("Checking enhanced profitability for %s (Magic: %d) opened at %s",
                                  symbol, magicNumber, TimeToString(openTime)));

        // We need to check deals from openTime until now
        datetime endTime = TimeCurrent();

        for (int i = totalDeals - 1; i >= 0; i--)
        {
            ulong dealTicket = HistoryDealGetTicket(i);
            if (dealTicket <= 0)
                continue;

            datetime dealTime = (datetime)HistoryDealGetInteger(dealTicket, DEAL_TIME);

            // Only check deals that happened after position open
            if (dealTime < openTime)
                continue;

            string dealSymbol = HistoryDealGetString(dealTicket, DEAL_SYMBOL);
            long dealMagic = HistoryDealGetInteger(dealTicket, DEAL_MAGIC);
            long dealEntry = HistoryDealGetInteger(dealTicket, DEAL_ENTRY);

            // Check if this deal belongs to our position
            if (dealSymbol == symbol && dealMagic == magicNumber)
            {
                double profit = HistoryDealGetDouble(dealTicket, DEAL_PROFIT);
                double commission = HistoryDealGetDouble(dealTicket, DEAL_COMMISSION);
                double swap = HistoryDealGetDouble(dealTicket, DEAL_SWAP);
                double dealTotal = profit + commission + swap;

                netProfit += dealTotal;
                positionFound = true;

                DebugLogFile("POSITION_DEAL_DETAIL_ENHANCED",
                             StringFormat("Deal %llu: Time: %s, Entry: %d, Profit: $%.2f, Commission: $%.2f, Swap: $%.2f, Total: $%.2f",
                                          dealTicket, TimeToString(dealTime), dealEntry,
                                          profit, commission, swap, dealTotal));
            }
        }

        if (positionFound)
        {
            bool isProfitable = (netProfit > 0);

            DebugLogFile("POSITION_NET_RESULT_ENHANCED",
                         StringFormat("Position for %s: Net Profit: $%.2f | Profitable: %s",
                                      symbol, netProfit, isProfitable ? "YES (WIN)" : "NO (LOSS)"));

            return isProfitable;
        }

        DebugLogFile("POSITION_NOT_FOUND_ENHANCED", "No position deals found in history");
        return false;
    }

    ulong GetLastTradeTicket(string symbol, int magicNumber)
    {
        int totalDeals = HistoryDealsTotal();

        for (int i = totalDeals - 1; i >= 0; i--)
        {
            ulong dealTicket = HistoryDealGetTicket(i);
            if (dealTicket <= 0)
                continue;

            string dealSymbol = HistoryDealGetString(dealTicket, DEAL_SYMBOL);
            long dealMagic = HistoryDealGetInteger(dealTicket, DEAL_MAGIC);
            long dealType = HistoryDealGetInteger(dealTicket, DEAL_TYPE);

            if (dealSymbol == symbol && dealMagic == magicNumber &&
                (dealType == DEAL_TYPE_BUY || dealType == DEAL_TYPE_SELL))
            {
                DebugLogFile("LAST_TRADE_TICKET",
                             StringFormat("Found last trade for %s (Magic: %d): Ticket %llu",
                                          symbol, magicNumber, dealTicket));
                return dealTicket;
            }
        }

        DebugLogFile("LAST_TRADE_TICKET",
                     StringFormat("No trade found for %s (Magic: %d)", symbol, magicNumber));
        return 0;
    }

    // ================= TRADE PROFITABILITY CHECKING =================
    bool CheckTradeProfitability(ulong tradeTicket)
    {
        if (tradeTicket <= 0)
        {
            DebugLogFile("PROFIT_CHECK", "Invalid trade ticket");
            return false;
        }

        if (!HistoryDealSelect(tradeTicket))
        {
            DebugLogFile("PROFIT_CHECK", StringFormat("Failed to select deal with ticket: %llu", tradeTicket));
            return false;
        }

        // Get profit components
        double profit = HistoryDealGetDouble(tradeTicket, DEAL_PROFIT);
        double commission = HistoryDealGetDouble(tradeTicket, DEAL_COMMISSION);
        double swap = HistoryDealGetDouble(tradeTicket, DEAL_SWAP);
        double total = profit + commission + swap;

        // Log detailed info
        DebugLogFile("PROFIT_DETAILS",
                     StringFormat("Ticket: %llu | Profit: $%.2f | Commission: $%.2f | Swap: $%.2f | Total: $%.2f | Profitable: %s",
                                  tradeTicket, profit, commission, swap, total, total > 0 ? "YES" : "NO"));

        return (total > 0);
    }

    // Enhanced version to check position profitability
    bool CheckPositionProfitability(string symbol, int magicNumber, datetime sinceTime = 0)
    {
        int totalDeals = HistoryDealsTotal();
        double totalProfit = 0;
        int tradeCount = 0;
        int profitableTrades = 0;

        for (int i = totalDeals - 1; i >= 0; i--)
        {
            ulong dealTicket = HistoryDealGetTicket(i);
            if (dealTicket <= 0)
                continue;

            datetime dealTime = (datetime)HistoryDealGetInteger(dealTicket, DEAL_TIME);
            string dealSymbol = HistoryDealGetString(dealTicket, DEAL_SYMBOL);
            long dealMagic = HistoryDealGetInteger(dealTicket, DEAL_MAGIC);
            long dealType = HistoryDealGetInteger(dealTicket, DEAL_TYPE);

            // Filter by symbol, magic, and time
            if (dealSymbol == symbol && dealMagic == magicNumber &&
                (sinceTime == 0 || dealTime >= sinceTime) &&
                (dealType == DEAL_TYPE_BUY || dealType == DEAL_TYPE_SELL))
            {
                double profit = HistoryDealGetDouble(dealTicket, DEAL_PROFIT);
                double commission = HistoryDealGetDouble(dealTicket, DEAL_COMMISSION);
                double swap = HistoryDealGetDouble(dealTicket, DEAL_SWAP);
                double netProfit = profit + commission + swap;

                totalProfit += netProfit;
                tradeCount++;
                if (netProfit > 0)
                    profitableTrades++;

                DebugLogFile("POSITION_PROFIT_DETAILS",
                             StringFormat("Deal: %llu | Time: %s | Type: %s | Net: $%.2f",
                                          dealTicket, TimeToString(dealTime),
                                          dealType == DEAL_TYPE_BUY ? "BUY" : "SELL",
                                          netProfit));
            }
        }

        DebugLogFile("POSITION_PROFIT_SUMMARY",
                     StringFormat("%s (Magic: %d) | Trades: %d | Profitable: %d | Total P/L: $%.2f | Win Rate: %.1f%%",
                                  symbol, magicNumber, tradeCount, profitableTrades, totalProfit,
                                  tradeCount > 0 ? ((double)profitableTrades / tradeCount * 100) : 0));

        return (totalProfit > 0);
    }

    // Get market regime analysis
    MarketAnalysis GetMarketRegimeAnalysis(string symbol, ENUM_TIMEFRAMES timeframe = PERIOD_H1)
    {
        MarketRegimeDetector detector(symbol, timeframe);
        return detector.GetMarketRegime();
    }

    // Regime-based parameter adjustment - ADAPTS TO MARKET
    static DecisionParams CreateRegimeBasedParams(const MarketAnalysis &regime,
                                                  const DecisionParams &baseParams)
    {
        // Start with the base parameters (either minimal or mk$ profile)
        DecisionParams params = baseParams;

        DebugLogFile("REGIME_ADAPT_START",
                     StringFormat("Adapting params for regime: %s",
                                  regime.GetRootStateString(regime.rootState)));

        if (regime.IsTrending())
        {
            DebugLogFile("TRENDING_ADAPT", "Adapting for TRENDING market");

            // Trending market adjustments
            params.cooldownMinutes = MathMax(30, baseParams.cooldownMinutes / 2);            // Faster entries
            params.maxPositionsPerSymbol = MathMin(3, baseParams.maxPositionsPerSymbol + 1); // More positions

            // Adjust thresholds for trend following
            if (regime.direction == "Bullish")
            {
                params.buyConfidenceThreshold = MathMax(55.0, baseParams.trendBuyThreshold);
                params.sellConfidenceThreshold = MathMax(60.0, baseParams.trendSellThreshold);
            }
            else if (regime.direction == "Bearish")
            {
                params.buyConfidenceThreshold = MathMax(60.0, baseParams.trendBuyThreshold);
                params.sellConfidenceThreshold = MathMax(60.0, baseParams.trendSellThreshold);
            }

            // Risk management for trends
            params.minRiskRewardRatio = MathMax(2.0, baseParams.minRiskRewardRatio);

            DebugLogFile("TRENDING_ADAPT_DONE",
                         StringFormat("Cooldown: %d→%d, MaxPos: %d→%d, RR: %.1f→%.1f",
                                      baseParams.cooldownMinutes, params.cooldownMinutes,
                                      baseParams.maxPositionsPerSymbol, params.maxPositionsPerSymbol,
                                      baseParams.minRiskRewardRatio, params.minRiskRewardRatio));
        }
        else if (regime.IsRanging())
        {
            DebugLogFile("RANGING_ADAPT", "Adapting for RANGING market");

            // Ranging market adjustments
            params.cooldownMinutes = MathMin(120, baseParams.cooldownMinutes * 2);           // Slower entries
            params.maxPositionsPerSymbol = MathMax(1, baseParams.maxPositionsPerSymbol - 1); // Fewer positions

            // Higher thresholds for range trading
            params.buyConfidenceThreshold = MathMax(60.0, baseParams.rangeBuyThreshold);
            params.sellConfidenceThreshold = MathMax(60.0, baseParams.rangeSellThreshold);

            // More conservative in ranges
            params.minRiskRewardRatio = MathMax(1.0, baseParams.minRiskRewardRatio - 0.5);
            params.trapProbabilityThreshold = MathMax(40.0, baseParams.trapProbabilityThreshold - 10.0);

            DebugLogFile("RANGING_ADAPT_DONE",
                         StringFormat("Cooldown: %d→%d, MaxPos: %d→%d, Trap: %.1f→%.1f",
                                      baseParams.cooldownMinutes, params.cooldownMinutes,
                                      baseParams.maxPositionsPerSymbol, params.maxPositionsPerSymbol,
                                      baseParams.trapProbabilityThreshold, params.trapProbabilityThreshold));
        }
        else
        {
            DebugLogFile("UNKNOWN_REGIME", "Keeping base params for unknown regime");
        }

        return params;
    }

    // Apply regime-based position size
    void ApplyRegimePositionSize(int symbolIndex, const MarketAnalysis &regimeAnalysis, bool isBuy)
    {
        // Store the recommended position size for execution
        ENUM_POSITION_SIZE recommendedSize = regimeAnalysis.positionSize;

        DebugLogFile("REGIME_POSITION_SIZE",
                     StringFormat("Market regime recommends %s position size for %s",
                                  GetPositionSizeString(recommendedSize),
                                  isBuy ? "BUY" : "SELL"));

        // Adjust position size multiplier based on market state
        double sizeMultiplier = 1.0;

        switch (regimeAnalysis.state)
        {
        case STATE_TRENDING_LOW_VOL:
        case STATE_EXPANSION:
            sizeMultiplier = 1.2; // Larger positions in healthy trends/expansions
            break;
        case STATE_TRENDING_HIGH_VOL:
        case STATE_RANGING_HIGH_VOL:
        case STATE_CHURN:
            sizeMultiplier = 0.5; // Smaller positions in volatile/churn states
            break;
        case STATE_CONTRACTION:
            sizeMultiplier = 0.3; // Very small positions during contractions
            break;
        }

        DebugLogFile("POSITION_SIZE_ADJUSTMENT",
                     StringFormat("Applying %.1fx multiplier for %s state",
                                  sizeMultiplier, regimeAnalysis.GetStateString(regimeAnalysis.state)));
    }

    // position sizing string
    string GetPositionSizeString(ENUM_POSITION_SIZE size)
    {
        switch (size)
        {
        case SIZE_ZERO:
            return "ZERO";
        case SIZE_VERY_SMALL:
            return "VERY_SMALL";
        case SIZE_SMALL:
            return "SMALL";
        case SIZE_MEDIUM:
            return "MEDIUM";
        case SIZE_LARGE:
            return "LARGE";
        default:
            return "UNKNOWN";
        }
    }

    // Get root state for decision making
    ENUM_ROOT_REGIME GetRootRegimeForSymbol(string symbol)
    {
        MarketAnalysis analysis = GetMarketRegimeAnalysis(symbol, PERIOD_H1);
        return analysis.rootState;
    }

    // Get market state for decision making
    ENUM_MARKET_STATE GetMarketStateForSymbol(string symbol)
    {
        MarketAnalysis analysis = GetMarketRegimeAnalysis(symbol, PERIOD_H1);
        return analysis.state;
    }

    // Get position size recommendation based on market regime
    ENUM_POSITION_SIZE GetRecommendedPositionSize(string symbol)
    {
        MarketAnalysis analysis = GetMarketRegimeAnalysis(symbol, PERIOD_H1);
        return analysis.positionSize;
    }

    // Get stop distance based on market regime
    double GetRecommendedStopDistance(string symbol)
    {
        MarketAnalysis analysis = GetMarketRegimeAnalysis(symbol, PERIOD_H1);
        return analysis.stopDistance;
    }

    // Get TP distance based on market regime
    double GetRecommendedTakeProfitDistance(string symbol)
    {
        MarketAnalysis analysis = GetMarketRegimeAnalysis(symbol, PERIOD_H1);
        return analysis.takeProfitDistance;
    }

    // Convert lifecycle enum to string
    string GetLifecycleString(ENUM_MARKET_LIFECYCLE lifecycle)
    {
        switch (lifecycle)
        {
        case LIFECYCLE_TREND_CONFIRMED:
            return "TREND_CONFIRMED";
        case LIFECYCLE_TREND_RESUMING:
            return "TREND_RESUMING";
        case LIFECYCLE_TREND_WEAKENING:
            return "TREND_WEAKENING";
        case LIFECYCLE_BREAKOUT_DETECTED:
            return "BREAKOUT_DETECTED";
        case LIFECYCLE_RANGE_ACTIVE:
            return "RANGE_ACTIVE";
        case LIFECYCLE_RANGE_FORMING:
            return "RANGE_FORMING";
        case LIFECYCLE_PULLBACK_ACTIVE:
            return "PULLBACK_ACTIVE";
        case LIFECYCLE_PULLBACK_FORMING:
            return "PULLBACK_FORMING";
        case LIFECYCLE_UNKNOWN:
        default:
            return "UNKNOWN";
        }
    }

    // ================= PRIVATE HELPER METHODS =================
    int FindSymbolIndex(string symbol)
    {
        for (int i = 0; i < m_totalSymbols; i++)
        {
            if (m_symbolStates[i].symbol == symbol)
            {
                DebugLogFile("FIND_SYMBOL_INDEX", StringFormat("Found symbol %s at index %d", symbol, i));
                return i;
            }
        }
        DebugLogFile("FIND_SYMBOL_INDEX", "Symbol not found: " + symbol);
        return -1;
    }

    int GenerateMagicNumber(string symbol)
    {
        DebugLogFile("GENERATE_MAGIC", "Generating magic number for: " + symbol);

        int hash = 0;
        for (int i = 0; i < StringLen(symbol); i++)
        {
            hash = hash * 31 + StringGetCharacter(symbol, i);
        }

        int magic = m_engineMagicBase + (MathAbs(hash) % 10000);
        DebugLogFile("GENERATE_MAGIC_RESULT", StringFormat("Generated magic %d for symbol %s", magic, symbol));

        return magic;
    }

    bool IsPackageFresh(int symbolIndex)
    {
        if (symbolIndex < 0 || symbolIndex >= m_totalSymbols)
        {
            DebugLogFile("IS_PACKAGE_FRESH_ERROR", StringFormat("Invalid symbol index: %d", symbolIndex));
            return false;
        }

        bool isFresh = m_symbolStates[symbolIndex].IsPackageFresh(m_maxPackageAgeSeconds);
        DebugLogFile("IS_PACKAGE_FRESH", StringFormat("Package freshness for %s: %s",
                                                      m_symbolStates[symbolIndex].symbol, isFresh ? "FRESH" : "STALE"));

        return isFresh;
    }

    int GetActiveSymbolCount()
    {
        int count = 0;
        for (int i = 0; i < m_totalSymbols; i++)
        {
            if (m_symbolStates[i].HasValidPackage() && m_symbolStates[i].IsPackageFresh())
            {
                count++;
            }
        }
        return count;
    }

    // ================= REGIME CHECKING =================ENUM_ROOT_REGIME
    struct EnhancedRegimeInfo
    {
        ENUM_ROOT_REGIME basicRegime;
        ENUM_MARKET_LIFECYCLE lifecycle;
        string regimeString;
        string lifecycleString;
        double confidence;
        string description;

        EnhancedRegimeInfo()
        {
            basicRegime = REGIME_UNKNOWN;
            lifecycle = LIFECYCLE_UNKNOWN;
            regimeString = "UNKNOWN";
            lifecycleString = "UNKNOWN";
            confidence = 0.0;
            description = "";
        }

        string ToString() const
        {
            return StringFormat("Regime: %s | Lifecycle: %s | Conf: %.0f%% | %s",
                                regimeString, lifecycleString, confidence, description);
        }

        bool IsTrending() const
        {
            return (basicRegime == REGIME_TRENDING || basicRegime == REGIME_TRENDING);
        }

        bool IsRanging() const
        {
            return (basicRegime == REGIME_RANGING);
        }

        bool IsInTrendPhase() const
        {
            return (lifecycle == LIFECYCLE_TREND_CONFIRMED ||
                    lifecycle == LIFECYCLE_TREND_RESUMING ||
                    lifecycle == LIFECYCLE_BREAKOUT_DETECTED);
        }

        bool IsInRangePhase() const
        {
            return (lifecycle == LIFECYCLE_RANGE_ACTIVE ||
                    lifecycle == LIFECYCLE_RANGE_FORMING ||
                    lifecycle == LIFECYCLE_PULLBACK_ACTIVE ||
                    lifecycle == LIFECYCLE_PULLBACK_FORMING);
        }
    };

    string RegimeToString(ENUM_ROOT_REGIME regime)
    {
        switch (regime)
        {
        case REGIME_TRENDING:
            return "TRENDING";
        case REGIME_RANGING:
            return "RANGING";
        case REGIME_UNKNOWN:
            return "UNKNOWN";
        default:
            return "UNKNOWN";
        }
    }

    string ExtractBaseRegime(const string &packageRegime)
    {
        int underscorePos = StringFind(packageRegime, "_LIFECYCLE_");
        if (underscorePos > 0)
        {
            return StringSubstr(packageRegime, 0, underscorePos);
        }

        if (StringFind(packageRegime, "TRENDING") >= 0)
            return "TRENDING";
        if (StringFind(packageRegime, "RANGING") >= 0)
            return "RANGING";

        return packageRegime;
    }

    // ================= ENHANCED TREND DECISION WITH LIFECYCLE =================
    DECISION_ACTION MakeTrendDecision(int symbolIndex, const DecisionEngineInterface &package, PositionAnalysis &positions)
    {
        DebugLogFile("MAKE_TREND_DECISION", "=====================================");
        DebugLogFile("MAKE_TREND_DECISION", StringFormat("=== MAKING TREND DECISION === | Symbol: %s | Index: %d",
                                                         m_symbolStates[symbolIndex].symbol, symbolIndex));

        if (symbolIndex < 0 || symbolIndex >= m_totalSymbols)
        {
            DebugLogFile("ERROR", "❌ Invalid symbol index");
            return ACTION_NONE;
        }

        string symbol = m_symbolStates[symbolIndex].symbol;

        // ============ STEP 1: GET MARKET REGIME ANALYSIS ============
        DebugLogFile("TREND_REGIME_ANALYSIS", "--- Getting market regime analysis ---");
        MarketAnalysis regimeAnalysis = GetMarketRegimeAnalysis(symbol, PERIOD_H1);

        DebugLogFile("TREND_REGIME_DETAILS", StringFormat("Trend Analysis: %s", regimeAnalysis.ToString()));

        // Validate we're actually in a trending market
        if (!regimeAnalysis.IsTrending())
        {
            DebugLogFile("TREND_VALIDATION_FAILED",
                         StringFormat("❌ Not in trending market! RootState: %s, MarketState: %s",
                                      regimeAnalysis.GetRootStateString(regimeAnalysis.rootState),
                                      regimeAnalysis.GetStateString(regimeAnalysis.state)));

            if (regimeAnalysis.IsRanging() && regimeAnalysis.state == STATE_CONTRACTION)
            {
                DebugLogFile("CONTRACTION_WARNING", "⚠️ Market in contraction - might be preparing for trend");
            }

            return ACTION_HOLD;
        }

        // ============ STEP 2: SET DYNAMIC THRESHOLDS BASED ON MARKET STATE ============
        DebugLogFile("TREND_THRESHOLDS", "--- Setting dynamic thresholds ---");
        DecisionParams params = m_symbolStates[symbolIndex].params;

        double baseBuyThreshold = params.trendBuyThreshold;
        double baseSellThreshold = params.trendSellThreshold;

        DebugLogFile("TREND_BASE_THRESHOLDS", StringFormat("Base thresholds - Buy: %.1f%%, Sell: %.1f%%",
                                                           baseBuyThreshold, baseSellThreshold));

        // ============ STEP 3: APPLY MARKET STATE ADJUSTMENTS ============
        DebugLogFile("TREND_STATE_ADJUSTMENTS", "--- Applying market state adjustments ---");

        double stateMultiplier = 1.0;
        string adjustmentReason = "No state adjustment";

        switch (regimeAnalysis.state)
        {
        case STATE_TRENDING_LOW_VOL:
            stateMultiplier = 1.0; // 0.85; // More aggressive in healthy trends
            adjustmentReason = "Healthy low-volatility trend - 15% more aggressive";
            DebugLogFile("TREND_STATE_ADJ", "✅ HEALTHY TREND: Using 15% more aggressive thresholds");
            break;

        case STATE_TRENDING_HIGH_VOL:
            stateMultiplier = 1.0; // 1.25; // More conservative in exhaustion
            adjustmentReason = "High-volatility/Exhaustion trend - 25% more conservative";
            DebugLogFile("TREND_STATE_ADJ", "⚠️ EXHAUSTION TREND: Using 25% more conservative thresholds");
            break;

        case STATE_EXPANSION:
            stateMultiplier = 1.0; // 0.8; // Very aggressive in expansions
            adjustmentReason = "Trend expansion/breakout - 20% more aggressive";
            DebugLogFile("TREND_STATE_ADJ", "🚀 EXPANSION: Using 20% more aggressive thresholds");
            break;

        case STATE_CONTRACTION:
            stateMultiplier = 1.0; // 1.5; // Very conservative during contractions
            adjustmentReason = "Contraction before trend - 50% more conservative";
            DebugLogFile("TREND_STATE_ADJ", "⚡ CONTRACTION: Using 50% more conservative thresholds");
            break;

        default:
            stateMultiplier = 1.0; // 1.0;
            adjustmentReason = "Standard trending market";
            DebugLogFile("TREND_STATE_ADJ", "📈 STANDARD TREND: Using normal thresholds");
            break;
        }

        double adjustedBuyThreshold = baseBuyThreshold * stateMultiplier;
        double adjustedSellThreshold = baseSellThreshold * stateMultiplier;

        DebugLogFile("TREND_ADJUSTED_THRESHOLDS", StringFormat("Adjusted - Buy: %.1f%% (was %.1f%%), Sell: %.1f%% (was %.1f%%) | Reason: %s",
                                                               adjustedBuyThreshold, baseBuyThreshold,
                                                               adjustedSellThreshold, baseSellThreshold,
                                                               adjustmentReason));

        // ============ SPECIAL HANDLING FOR UNKNOWN REGIME ============
        if (regimeAnalysis.rootState == REGIME_UNKNOWN)
        {
            DebugLogFile("TREND_UNKNOWN_REGIME",
                         "⚠️ UNKNOWN regime - increasing threshold by 20% for safety");
            adjustedBuyThreshold *= 1;  // adjustedBuyThreshold *= 1.2;
            adjustedSellThreshold *= 1; // adjustedSellThreshold *= 1.2;

            DebugLogFile("TREND_UNKNOWN_ADJUSTED",
                         StringFormat("UNKNOWN regime adjusted thresholds: Buy=%.1f%%, Sell=%.1f%%",
                                      adjustedBuyThreshold, adjustedSellThreshold));
        }

        // ============ STEP 4: GET PACKAGE VALUES ============
        DebugLogFile("TREND_PACKAGE_VALUES", "--- Analyzing package values ---");
        double confidence = package.overallConfidence;
        string direction = package.dominantDirection;

        DebugLogFile("TREND_PACKAGE_INFO", StringFormat("Direction: %s, Confidence: %.1f%%, RegimeDirection: %s",
                                                        direction, confidence, regimeAnalysis.direction));

        // Check direction alignment with market regime
        bool directionAligned = false;
        bool shouldPreferPackage = false; // NEW: Flag to track if we should prefer package direction
        string alignmentNote = "";        // NEW: Track alignment reason

        if (regimeAnalysis.direction == "Bullish" && direction == "BULLISH")
        {
            directionAligned = true;
            alignmentNote = "✅ BULLISH alignment: Package=BULLISH, Regime=Bullish";
        }
        else if (regimeAnalysis.direction == "Bearish" && direction == "BEARISH")
        {
            directionAligned = true;
            alignmentNote = "✅ BEARISH alignment: Package=BEARISH, Regime=Bearish";
        }
        else
        {
            // CONTRADICTION DETECTED - prefer trend package direction
            if (regimeAnalysis.IsTrending())
            {
                // In trending markets, PREFER TREND PACKAGE over regime
                shouldPreferPackage = true;

                if (direction == "BULLISH")
                {
                    alignmentNote = "⚠️ CONTRADICTION - Preferring TREND PACKAGE BULLISH (Regime suggests: " +
                                    regimeAnalysis.direction + ")";
                    directionAligned = true; // Override: accept package direction
                }
                else if (direction == "BEARISH")
                {
                    alignmentNote = "⚠️ CONTRADICTION - Preferring TREND PACKAGE BEARISH (Regime suggests: " +
                                    regimeAnalysis.direction + ")";
                    directionAligned = true; // Override: accept package direction
                }
            }
            else
            {
                // In ranging or unknown markets, respect the contradiction
                directionAligned = false;
                alignmentNote = "❌ Direction mismatch: Package=" + direction +
                                ", Regime=" + regimeAnalysis.direction +
                                " (Not trending, respecting contradiction)";
            }
        }

        DebugLogFile("DIRECTION_ALIGNMENT", alignmentNote);

        // ============ STEP 5: HANDLE NO POSITION CASE ============
        if (positions.state == STATE_NO_POSITION)
        {
            DebugLogFile("TREND_NO_POSITION", "No positions open - evaluating new trend position");

            // Check for adverse conditions
            if (regimeAnalysis.state == STATE_TRENDING_HIGH_VOL && package.trapProbability > 65)
            {
                DebugLogFile("TREND_EXHAUSTION_WARNING",
                             StringFormat("⚠️ High trap probability (%.1f%%) during exhaustion - avoiding entry",
                                          package.trapProbability));
                return ACTION_HOLD;
            }

            // Check BUY conditions
            if (direction == "BULLISH" && confidence >= adjustedBuyThreshold)
            {
                if (!directionAligned)
                {
                    DebugLogFile("TREND_DIRECTION_MISMATCH",
                                 StringFormat("⚠️ Package bullish but regime direction: %s", regimeAnalysis.direction));
                    return ACTION_HOLD;
                }

                DebugLogFile("TREND_BUY_CONDITION_MET",
                             StringFormat("✅ TREND BUY condition met: Dir=%s, Conf=%.1f%% >= %.1f%%, RegimeDir=%s",
                                          direction, confidence, adjustedBuyThreshold, regimeAnalysis.direction));

                if (!CheckCooldown(symbolIndex, true))
                {
                    DebugLogFile("TREND_BUY_COOLDOWN", "❌ Trend buy blocked by cooldown");
                    return ACTION_HOLD;
                }

                if (!CheckPositionLimits(symbolIndex, true))
                {
                    DebugLogFile("TREND_BUY_LIMITS", "❌ Trend buy position limits reached");
                    return ACTION_HOLD;
                }

                // Apply regime-based position size
                ApplyRegimePositionSize(symbolIndex, regimeAnalysis, true);

                DebugLogFile("TREND_DECISION_FINAL", "🎯 Trend decision: OPEN_BUY");
                return ACTION_OPEN_BUY;
            }

            // Check SELL conditions
            if (direction == "BEARISH" && confidence >= adjustedSellThreshold)
            {
                if (!directionAligned)
                {
                    DebugLogFile("TREND_DIRECTION_MISMATCH",
                                 StringFormat("⚠️ Package bearish but regime direction: %s", regimeAnalysis.direction));
                    return ACTION_HOLD;
                }

                DebugLogFile("TREND_SELL_CONDITION_MET",
                             StringFormat("✅ TREND SELL condition met: Dir=%s, Conf=%.1f%% >= %.1f%%, RegimeDir=%s",
                                          direction, confidence, adjustedSellThreshold, regimeAnalysis.direction));

                if (!CheckCooldown(symbolIndex, false))
                {
                    DebugLogFile("TREND_SELL_COOLDOWN", "❌ Trend sell blocked by cooldown");
                    return ACTION_HOLD;
                }

                if (!CheckPositionLimits(symbolIndex, false))
                {
                    DebugLogFile("TREND_SELL_LIMITS", "❌ Trend sell position limits reached");
                    return ACTION_HOLD;
                }

                // Apply regime-based position size
                ApplyRegimePositionSize(symbolIndex, regimeAnalysis, false);

                DebugLogFile("TREND_DECISION_FINAL", "🎯 Trend decision: OPEN_SELL");
                return ACTION_OPEN_SELL;
            }

            DebugLogFile("TREND_NO_ENTRY", "⏸️ No trend entry conditions met");
            return ACTION_HOLD;
        }

        // ============ STEP 6: HANDLE EXISTING POSITIONS ============
        DebugLogFile("TREND_EXISTING_POSITIONS", StringFormat("Existing positions: %s", positions.ToString()));

        // Apply market state-based position management
        return DecideWithPositionTrend(symbolIndex, package, positions, params, direction, confidence, regimeAnalysis);
    }

    //+------------------------------------------------------------------+
    //| Make range decision with context integration                    |
    //+------------------------------------------------------------------+
    DECISION_ACTION MakeRangeDecision(int symbolIndex, const DecisionEngineInterface &package,
                                      PositionAnalysis &positions)
    {
        DebugLogFile("MAKE_RANGE_DECISION", "=====================================");
        DebugLogFile("MAKE_RANGE_DECISION", StringFormat("=== MAKING RANGE DECISION === | Symbol: %s | Index: %d",
                                                         m_symbolStates[symbolIndex].symbol, symbolIndex));

        if (symbolIndex < 0 || symbolIndex >= m_totalSymbols)
        {
            DebugLogFile("ERROR", "❌ Invalid symbol index");
            return ACTION_NONE;
        }

        string symbol = m_symbolStates[symbolIndex].symbol;

        // ============ STEP 1: GET ENHANCED RANGE ANALYSIS WITH CONTEXT ============
        DebugLogFile("RANGE_CONTEXT_ANALYSIS", "--- Getting enhanced range analysis with context ---");
        RangeAnalysisResult rangeResult = RangeIntelligence::AnalyzeRange(symbol, PERIOD_M15);

        // Extract context information
        string positionContext = rangeResult.positionContext;
        string touchDominance = rangeResult.touchDominance;
        string tacticalDirection = rangeResult.tacticalDirection;
        double touchRatio = rangeResult.touchRatio;

        DebugLogFile("RANGE_CONTEXT_INFO",
                     StringFormat("Context: %s | Tactical: %s | Touches: %s (%.2f:1) | Edge: %s",
                                  positionContext, tacticalDirection, touchDominance,
                                  touchRatio, rangeResult.strongestEdge));

        DebugLogFile("TOUCH_DETAILS",
                     StringFormat("Support touches: %d | Resistance touches: %d | Ratio: %.2f",
                                  rangeResult.supportTouches, rangeResult.resistanceTouches, touchRatio));

        // ============ STEP 2: GET MARKET REGIME ANALYSIS ============
        DebugLogFile("RANGE_REGIME_ANALYSIS", "--- Getting market regime analysis ---");
        MarketAnalysis regimeAnalysis = GetMarketRegimeAnalysis(symbol, PERIOD_H1);

        DebugLogFile("RANGE_REGIME_DETAILS", StringFormat("Range Analysis: %s", regimeAnalysis.ToString()));

        // Validate we're actually in a ranging market
        if (!regimeAnalysis.IsRanging() || regimeAnalysis.rootState != REGIME_UNKNOWN)
        {
            DebugLogFile("RANGE_VALIDATION_FAILED",
                         StringFormat("❌ Not in ranging market! RootState: %s, MarketState: %s",
                                      regimeAnalysis.GetRootStateString(regimeAnalysis.rootState),
                                      regimeAnalysis.GetStateString(regimeAnalysis.state)));

            // Special case: Contraction might be preparing for range
            if (regimeAnalysis.state == STATE_CONTRACTION)
            {
                DebugLogFile("CONTRACTION_SPECIAL", "⚡ Market in contraction - might be preparing for range formation");
            }
            else
            {
                return ACTION_HOLD;
            }
        }

        // ============ STEP 3: SET DYNAMIC THRESHOLDS ============
        DebugLogFile("RANGE_THRESHOLDS", "--- Setting dynamic thresholds ---");
        DecisionParams params = m_symbolStates[symbolIndex].params;

        double baseBuyThreshold = params.rangeBuyThreshold;
        double baseSellThreshold = params.rangeSellThreshold;

        // ============ CONTEXT-BASED THRESHOLD ADJUSTMENTS ============
        double confidenceMultiplier = 1.0;
        string confidenceAdjustReason = "No adjustment";

        // Adjust based on touch dominance alignment with price position
        if (positionContext == "PRICE_NEAR_SUPPORT")
        {
            if (touchDominance == "SUPPORT_DOMINANT" || touchDominance == "SUPPORT_LEANING")
            {
                // Strong alignment: Price at support + support-dominant touches
                confidenceMultiplier = 0.8; // 20% easier to trigger (more aggressive)
                confidenceAdjustReason = "Strong alignment: Price at support + support-dominant touches";
            }
            else if (touchDominance == "RESISTANCE_DOMINANT" || touchDominance == "RESISTANCE_LEANING")
            {
                // Contradiction: Price at support but resistance-dominant touches
                confidenceMultiplier = 1.2; // 30% harder to trigger (more conservative)
                confidenceAdjustReason = "Contradiction: Price at support but resistance-dominant touches";
            }
        }
        else if (positionContext == "PRICE_NEAR_RESISTANCE")
        {
            if (touchDominance == "RESISTANCE_DOMINANT" || touchDominance == "RESISTANCE_LEANING")
            {
                // Strong alignment: Price at resistance + resistance-dominant touches
                confidenceMultiplier = 0.8; // 20% easier to trigger (more aggressive)
                confidenceAdjustReason = "Strong alignment: Price at resistance + resistance-dominant touches";
            }
            else if (touchDominance == "SUPPORT_DOMINANT" || touchDominance == "SUPPORT_LEANING")
            {
                // Contradiction: Price at resistance but support-dominant touches
                confidenceMultiplier = 1.1; // 30% harder to trigger (more conservative)
                confidenceAdjustReason = "Contradiction: Price at resistance but support-dominant touches";
            }
        }

        // Apply market state adjustments
        double stateMultiplier = 1.0;
        switch (regimeAnalysis.state)
        {
        case STATE_RANGING_LOW_VOL:
            stateMultiplier = 1; // stateMultiplier = 0.9;
            break;
        case STATE_RANGING_HIGH_VOL:
            stateMultiplier = 1; // stateMultiplier = 1.3;
            break;
        case STATE_CONTRACTION:
            stateMultiplier = 1; // stateMultiplier = 1.5;
            break;
        case STATE_CHURN:
            stateMultiplier = 1; // stateMultiplier = 2.0;
            break;
        }

        double finalMultiplier = confidenceMultiplier * stateMultiplier;
        double adjustedBuyThreshold = baseBuyThreshold * finalMultiplier;
        double adjustedSellThreshold = baseSellThreshold * finalMultiplier;

        DebugLogFile("THRESHOLD_ADJUSTMENTS",
                     StringFormat("Base: Buy=%.1f%%, Sell=%.1f%% | Context Mult: %.2f | State Mult: %.2f | Final: Buy=%.1f%%, Sell=%.1f%%",
                                  baseBuyThreshold, baseSellThreshold, confidenceMultiplier,
                                  stateMultiplier, adjustedBuyThreshold, adjustedSellThreshold));
        DebugLogFile("ADJUSTMENT_REASON", StringFormat("Reason: %s", confidenceAdjustReason));

        // ============ STEP 4: GET PACKAGE VALUES ============
        DebugLogFile("RANGE_PACKAGE_VALUES", "--- Analyzing package values ---");
        double confidence = package.overallConfidence;
        string direction = package.dominantDirection;
        string action = package.recommendedAction;

        DebugLogFile("RANGE_PACKAGE_INFO",
                     StringFormat("Direction: %s, Confidence: %.1f%%, Action: %s, Trap: %.1f%%, Avoid: %s",
                                  direction, confidence, action,
                                  package.trapProbability, package.ShouldAvoid() ? "YES" : "NO"));

        // ============ STEP 5: CONTEXT-BASED DECISION MATRIX ============
        DebugLogFile("CONTEXT_DECISION_MATRIX", "--- Applying context-based decision matrix ---");

        // Get current price for validation
        double currentPrice = SymbolInfoDouble(symbol, SYMBOL_BID);

        // ============ CASE 1: PRICE NEAR SUPPORT ============
        if (positionContext == "PRICE_NEAR_SUPPORT")
        {
            DebugLogFile("DECISION_CONTEXT", "🎯 CONTEXT: Price near support - evaluating BUY for fade");

            // Check if package direction aligns with context
            if (direction != "BULLISH")
            {
                DebugLogFile("DIRECTION_MISMATCH",
                             StringFormat("⚠️ Context says BUY (price at support) but package says %s", direction));

                // Override for strong context alignment
                if (touchDominance == "SUPPORT_DOMINANT")
                {
                    DebugLogFile("CONTEXT_OVERRIDE", "✅ Overriding package direction due to strong support-dominant context");
                    direction = "BULLISH";
                    confidence *= 1.1; // Boost confidence for context override
                }
            }

            // Apply context-based confidence adjustments
            if (touchDominance == "SUPPORT_DOMINANT")
            {
                confidence *= 1.2; // 20% confidence boost for strong alignment
                DebugLogFile("CONFIDENCE_BOOST", "➕ 20% confidence boost: Price at support + support-dominant touches");
            }
            else if (touchDominance == "RESISTANCE_DOMINANT")
            {
                confidence *= 0.7; // 30% confidence reduction for contradiction
                DebugLogFile("CONFIDENCE_REDUCTION", "➖ 30% confidence reduction: Price at support but resistance-dominant touches");
            }

            // Check BUY conditions with context-adjusted thresholds
            if (direction == "BULLISH" && confidence >= adjustedBuyThreshold)
            {
                DebugLogFile("CONTEXT_BUY_CONDITION_MET",
                             StringFormat("✅ CONTEXT-ALIGNED BUY: Price at support + %s touches | Conf=%.1f%% >= %.1f%%",
                                          touchDominance, confidence, adjustedBuyThreshold));

                if (!CheckCooldown(symbolIndex, true))
                {
                    DebugLogFile("RANGE_BUY_COOLDOWN", "❌ Range buy blocked by cooldown");
                    return ACTION_HOLD;
                }

                if (!CheckPositionLimits(symbolIndex, true))
                {
                    DebugLogFile("RANGE_BUY_LIMITS", "❌ Range buy position limits reached");
                    return ACTION_HOLD;
                }

                // Apply regime-based position size with context adjustment
                ApplyRegimePositionSize(symbolIndex, regimeAnalysis, true);

                // Adjust position size based on context strength
                if (touchDominance == "SUPPORT_DOMINANT")
                {
                    DebugLogFile("POSITION_SIZE_BOOST", "➕ Increasing position size for strong context alignment");
                    // You could add position size adjustment here
                }

                DebugLogFile("RANGE_DECISION_FINAL", "🎯 Range decision: OPEN_BUY (context-aligned)");
                return ACTION_OPEN_BUY;
            }
        }
        // ============ CASE 2: PRICE NEAR RESISTANCE ============
        else if (positionContext == "PRICE_NEAR_RESISTANCE")
        {
            DebugLogFile("DECISION_CONTEXT", "🎯 CONTEXT: Price near resistance - evaluating SELL for fade");

            // Check if package direction aligns with context
            if (direction != "BEARISH")
            {
                DebugLogFile("DIRECTION_MISMATCH",
                             StringFormat("⚠️ Context says SELL (price at resistance) but package says %s", direction));

                // Override for strong context alignment
                if (touchDominance == "RESISTANCE_DOMINANT")
                {
                    DebugLogFile("CONTEXT_OVERRIDE", "✅ Overriding package direction due to strong resistance-dominant context");
                    direction = "BEARISH";
                    confidence *= 1.1; // Boost confidence for context override
                }
            }

            // Apply context-based confidence adjustments
            if (touchDominance == "RESISTANCE_DOMINANT")
            {
                confidence *= 1.2; // 20% confidence boost for strong alignment
                DebugLogFile("CONFIDENCE_BOOST", "➕ 20% confidence boost: Price at resistance + resistance-dominant touches");
            }
            else if (touchDominance == "SUPPORT_DOMINANT")
            {
                confidence *= 0.7; // 30% confidence reduction for contradiction
                DebugLogFile("CONFIDENCE_REDUCTION", "➖ 30% confidence reduction: Price at resistance but support-dominant touches");
            }

            // Check SELL conditions with context-adjusted thresholds
            if (direction == "BEARISH" && confidence >= adjustedSellThreshold)
            {
                DebugLogFile("CONTEXT_SELL_CONDITION_MET",
                             StringFormat("✅ CONTEXT-ALIGNED SELL: Price at resistance + %s touches | Conf=%.1f%% >= %.1f%%",
                                          touchDominance, confidence, adjustedSellThreshold));

                if (!CheckCooldown(symbolIndex, false))
                {
                    DebugLogFile("RANGE_SELL_COOLDOWN", "❌ Range sell blocked by cooldown");
                    return ACTION_HOLD;
                }

                if (!CheckPositionLimits(symbolIndex, false))
                {
                    DebugLogFile("RANGE_SELL_LIMITS", "❌ Range sell position limits reached");
                    return ACTION_HOLD;
                }

                // Apply regime-based position size with context adjustment
                ApplyRegimePositionSize(symbolIndex, regimeAnalysis, false);

                // Adjust position size based on context strength
                if (touchDominance == "RESISTANCE_DOMINANT")
                {
                    DebugLogFile("POSITION_SIZE_BOOST", "➕ Increasing position size for strong context alignment");
                    // You could add position size adjustment here
                }

                DebugLogFile("RANGE_DECISION_FINAL", "🎯 Range decision: OPEN_SELL (context-aligned)");
                return ACTION_OPEN_SELL;
            }
        }
        // ============ CASE 3: PRICE IN MIDDLE ============
        else if (positionContext == "PRICE_IN_MIDDLE")
        {
            DebugLogFile("DECISION_CONTEXT", "⏸️ CONTEXT: Price in middle of range - no edge trade");
            return ACTION_HOLD;
        }

        // ============ STEP 6: FALLBACK TO ORIGINAL LOGIC ============
        DebugLogFile("CONTEXT_FALLBACK", "--- Falling back to original logic ---");

        // Original trap check
        if (package.trapProbability > 50)
        {
            DebugLogFile("RANGE_TRAP_WARNING",
                         StringFormat("⚠️ High trap probability (%.1f%%) for range entry",
                                      package.trapProbability));
            return ACTION_HOLD;
        }

        // Original BUY/SELL logic without context
        if (direction == "BULLISH" && confidence >= adjustedBuyThreshold)
        {
            DebugLogFile("FALLBACK_BUY",
                         StringFormat("Fallback BUY: Dir=%s, Conf=%.1f%% >= %.1f%%",
                                      direction, confidence, adjustedBuyThreshold));

            if (!CheckCooldown(symbolIndex, true) || !CheckPositionLimits(symbolIndex, true))
                return ACTION_HOLD;

            return ACTION_OPEN_BUY;
        }

        if (direction == "BEARISH" && confidence >= adjustedSellThreshold)
        {
            DebugLogFile("FALLBACK_SELL",
                         StringFormat("Fallback SELL: Dir=%s, Conf=%.1f%% >= %.1f%%",
                                      direction, confidence, adjustedSellThreshold));

            if (!CheckCooldown(symbolIndex, false) || !CheckPositionLimits(symbolIndex, false))
                return ACTION_HOLD;

            return ACTION_OPEN_SELL;
        }

        DebugLogFile("NO_ENTRY_CONDITIONS", "⏸️ No entry conditions met with or without context");
        return ACTION_HOLD;
    }

    // ================= SUPPORTING FUNCTIONS =================
    DECISION_ACTION DecideWithPositionTrend(int symbolIndex, const DecisionEngineInterface &source,
                                            PositionAnalysis &positions, DecisionParams &params,
                                            string direction, double confidence, const MarketAnalysis &regimeAnalysis)
    {
        DebugLogFile("DECIDE_TREND_WITH_POSITION", "--- Making trend decision with existing positions ---");

        double closeThreshold = params.closePositionThreshold;

        DebugLogFile("TREND_CLOSE_THRESHOLD", StringFormat("Base close threshold: %.1f%% | Market State: %s",
                                                           closeThreshold, MarketAnalysis::GetStateString(regimeAnalysis.state)));

        // Adjust close threshold based on market state
        if (regimeAnalysis.state == STATE_TRENDING_HIGH_VOL)
        {
            closeThreshold += 0.0;
            DebugLogFile("TREND_WEAKENING_CLOSE", StringFormat("High volatility trend - increasing close threshold to %.1f%%", closeThreshold));
        }
        else if (regimeAnalysis.state == STATE_CONTRACTION)
        {
            closeThreshold += 0.0; // Be very conservative about closing during contractions
            DebugLogFile("TREND_CONTRACTION_CLOSE", StringFormat("Contraction phase - increasing close threshold to %.1f%%", closeThreshold));
        }
        else if (regimeAnalysis.state == STATE_EXPANSION)
        {
            closeThreshold -= 0.0; // Be more aggressive about closing during expansions
            DebugLogFile("TREND_EXPANSION_CLOSE", StringFormat("Expansion phase - decreasing close threshold to %.1f%%", closeThreshold));
        }

        if (confidence < params.closeAllThreshold)
        {
            DebugLogFile("TREND_EMERGENCY_CLOSE_ALL", StringFormat("Emergency close-all: Confidence %.1f%% < %.1f%%",
                                                                   confidence, params.closeAllThreshold));
            return ACTION_CLOSE_ALL;
        }

        if (confidence >= MathMax(params.buyConfidenceThreshold, params.sellConfidenceThreshold) * 1.2 &&
            (regimeAnalysis.state == STATE_TRENDING_LOW_VOL || regimeAnalysis.state == STATE_EXPANSION))
        {
            DebugLogFile("TREND_ADD_TO_POSITION", StringFormat("High confidence %.1f%% in %s trend phase - checking to add",
                                                               confidence, MarketAnalysis::GetStateString(regimeAnalysis.state)));
            return DecideAdding(symbolIndex, source, positions, direction);
        }

        return DecideClosing(symbolIndex, source, positions, direction, closeThreshold);
    }

    DECISION_ACTION DecideWithPositionRange(int symbolIndex, const DecisionEngineInterface &source,
                                            PositionAnalysis &positions, DecisionParams &params,
                                            string direction, double confidence, const MarketAnalysis &regimeAnalysis)
    {
        DebugLogFile("DECIDE_RANGE_WITH_POSITION", "--- Making range decision with existing positions ---");

        double rangeCloseThreshold = params.closePositionThreshold + 15.0;

        DebugLogFile("RANGE_CLOSE_THRESHOLD", StringFormat("Range close threshold: %.1f%% (base: %.1f%%) | Market State: %s",
                                                           rangeCloseThreshold, params.closePositionThreshold,
                                                           MarketAnalysis::GetStateString(regimeAnalysis.state)));

        // Adjust close threshold based on market state
        if (regimeAnalysis.state == STATE_RANGING_HIGH_VOL || regimeAnalysis.state == STATE_CHURN)
        {
            rangeCloseThreshold += 0.0; // Be more aggressive in closing positions during high volatility/churn
            DebugLogFile("RANGE_STATE_ADJUSTMENT", StringFormat("High volatility/churn state - increasing close threshold to %.1f%%", rangeCloseThreshold));
        }
        else if (regimeAnalysis.state == STATE_CONTRACTION)
        {
            rangeCloseThreshold -= 0.0; // Be more conservative during contractions
            DebugLogFile("RANGE_STATE_ADJUSTMENT", StringFormat("Contraction state - decreasing close threshold to %.1f%%", rangeCloseThreshold));
        }

        if (confidence > rangeCloseThreshold)
        {
            if (positions.state == STATE_HAS_BUY && direction == "BEARISH")
            {
                DebugLogFile("RANGE_CLOSE_BUY", "🔻 Closing BUY in range (bearish signal)");
                return ACTION_CLOSE_BUY;
            }

            if (positions.state == STATE_HAS_SELL && direction == "BULLISH")
            {
                DebugLogFile("RANGE_CLOSE_SELL", "🔺 Closing SELL in range (bullish signal)");
                return ACTION_CLOSE_SELL;
            }
        }

        // Be more aggressive about closing all during dangerous market states
        double closeAllThreshold = params.closeAllThreshold;
        if (regimeAnalysis.state == STATE_RANGING_HIGH_VOL || regimeAnalysis.state == STATE_CHURN)
        {
            closeAllThreshold += 0.0; // Close all positions sooner in dangerous states
            DebugLogFile("RANGE_DANGEROUS_STATE", StringFormat("High vol/churn state - increasing close-all threshold to %.1f%%", closeAllThreshold));
        }

        if (confidence < closeAllThreshold)
        {
            DebugLogFile("RANGE_CLOSE_ALL", StringFormat("Low confidence %.1f%% in %s range - closing all",
                                                         confidence, MarketAnalysis::GetStateString(regimeAnalysis.state)));
            return ACTION_CLOSE_ALL;
        }

        DebugLogFile("RANGE_HOLD_POSITIONS", StringFormat("⏸️ Holding existing range positions | Market State: %s",
                                                          MarketAnalysis::GetStateString(regimeAnalysis.state)));
        return ACTION_HOLD;
    }

    DECISION_ACTION HandleFadeSignal(int symbolIndex, const DecisionEngineInterface &package,
                                     PositionAnalysis &positions, double buyThreshold, double sellThreshold)
    {
        DebugLogFile("HANDLE_FADE_SIGNAL", "--- Handling FADE (counter-trend) signal ---");

        string direction = package.dominantDirection;
        double confidence = package.overallConfidence;
        string fadeDirection = (direction == "BULLISH") ? "BEARISH" : "BULLISH";

        DebugLogFile("FADE_LOGIC", StringFormat("Package Dir: %s → FADE Dir: %s | Conf: %.1f%%",
                                                direction, fadeDirection, confidence));

        if (positions.state == STATE_NO_POSITION)
        {
            if (fadeDirection == "BULLISH" && confidence >= buyThreshold)
            {
                DebugLogFile("FADE_BUY_SIGNAL", StringFormat("🎯 FADE BUY: Buying against bearish signal with %.1f%% confidence", confidence));

                if (!CheckCooldown(symbolIndex, true) || !CheckPositionLimits(symbolIndex, true))
                    return ACTION_HOLD;

                return ACTION_OPEN_BUY;
            }

            if (fadeDirection == "BEARISH" && confidence >= sellThreshold)
            {
                DebugLogFile("FADE_SELL_SIGNAL", StringFormat("🎯 FADE SELL: Selling against bullish signal with %.1f%% confidence", confidence));

                if (!CheckCooldown(symbolIndex, false) || !CheckPositionLimits(symbolIndex, false))
                    return ACTION_HOLD;

                return ACTION_OPEN_SELL;
            }
        }
        else
        {
            if (positions.state == STATE_HAS_BUY && fadeDirection == "BEARISH")
            {
                DebugLogFile("FADE_CLOSE_BUY", "Closing BUY for FADE SELL signal");
                return ACTION_CLOSE_BUY;
            }

            if (positions.state == STATE_HAS_SELL && fadeDirection == "BULLISH")
            {
                DebugLogFile("FADE_CLOSE_SELL", "Closing SELL for FADE BUY signal");
                return ACTION_CLOSE_SELL;
            }
        }

        return ACTION_HOLD;
    }

    DECISION_ACTION DecideAdding(int symbolIndex, const DecisionEngineInterface &source,
                                 PositionAnalysis &positions, string direction)
    {
        DebugLogFile("DECIDE_ADDING", StringFormat("Evaluating adding to positions. Direction: %s, Positions: %s",
                                                   direction, positions.ToString()));

        if (direction == "BULLISH" && (positions.state == STATE_HAS_BUY || positions.state == STATE_HAS_BOTH))
        {
            DebugLogFile("ADD_BUY_CONSIDERED", "Considering adding to BUY position");

            if (!CheckCooldown(symbolIndex, true))
            {
                DebugLogFile("ADD_BUY_COOLDOWN_BLOCKED", "❌ Adding BUY blocked by cooldown");
                return ACTION_HOLD;
            }

            DebugLogFile("ADD_BUY_COOLDOWN_PASSED", "Buy cooldown check passed for adding");

            if (!CheckPositionLimits(symbolIndex, true))
            {
                DebugLogFile("ADD_BUY_LIMITS_FAILED", "Position limits failed for adding BUY");
                return ACTION_HOLD;
            }

            DebugLogFile("ADD_BUY_LIMITS_PASSED", "Position limits passed for adding BUY");
            DebugLogFile("DECISION_FINAL", "Decision: OPEN_BUY (adding)");
            return ACTION_OPEN_BUY;
        }

        if (direction == "BEARISH" && (positions.state == STATE_HAS_SELL || positions.state == STATE_HAS_BOTH))
        {
            DebugLogFile("ADD_SELL_CONSIDERED", "Considering adding to SELL position");

            if (!CheckCooldown(symbolIndex, false))
            {
                DebugLogFile("ADD_SELL_COOLDOWN_BLOCKED", "❌ Adding SELL blocked by cooldown");
                return ACTION_HOLD;
            }

            DebugLogFile("ADD_SELL_COOLDOWN_PASSED", "Sell cooldown check passed for adding");

            if (!CheckPositionLimits(symbolIndex, false))
            {
                DebugLogFile("ADD_SELL_LIMITS_FAILED", "Position limits failed for adding SELL");
                return ACTION_HOLD;
            }

            DebugLogFile("ADD_SELL_LIMITS_PASSED", "Position limits passed for adding SELL");
            DebugLogFile("DECISION_FINAL", "Decision: OPEN_SELL (adding)");
            return ACTION_OPEN_SELL;
        }

        DebugLogFile("DECISION_FINAL", "No adding conditions met, Decision: HOLD");
        return ACTION_HOLD;
    }

    DECISION_ACTION DecideClosing(int symbolIndex, const DecisionEngineInterface &source,
                                  PositionAnalysis &positions, string direction, double closeThreshold = 40.0)
    {
        DebugLogFile("DECIDE_CLOSING_START", StringFormat("Evaluating closing positions. Direction: %s, Positions: %s",
                                                          direction, positions.ToString()));

        double confidence = source.overallConfidence;

        DebugLogFile("CLOSING_LOGIC", StringFormat("Using fixed close threshold: %.1f%% (Position profit: %.2f)",
                                                   closeThreshold, positions.totalProfit));

        DebugLogFile("CLOSING_THRESHOLD", StringFormat("Conf: %.1f%%, CloseThreshold: %.1f%%, Profit: $%.2f, Dir: %s",
                                                       confidence, closeThreshold, positions.totalProfit, direction));

        if (confidence < closeThreshold)
        {
            DebugLogFile("CLOSING_DECISION", "❌ Confidence below threshold - HOLD (no close)");
            return ACTION_HOLD;
        }

        DebugLogFile("CLOSING_DECISION", "✅ Signal strong enough for closing consideration");

        if (positions.state == STATE_HAS_BUY && direction == "BEARISH")
        {
            DebugLogFile("CLOSE_BUY_SIGNAL", "🔻 Closing BUY (strong BEARISH signal)");
            return ACTION_CLOSE_BUY;
        }

        if (positions.state == STATE_HAS_SELL && direction == "BULLISH")
        {
            DebugLogFile("CLOSE_SELL_SIGNAL", "🔺 Closing SELL (strong BULLISH signal)");
            return ACTION_CLOSE_SELL;
        }

        if (positions.state == STATE_HAS_BOTH)
        {
            DebugLogFile("CLOSE_BOTH_POSITIONS", "🔀 Has BOTH positions, closing opposite side");
            if (direction == "BULLISH")
            {
                DebugLogFile("CLOSE_SELL_FOR_BULLISH", "Closing SELL for BULLISH signal");
                return ACTION_CLOSE_SELL;
            }
            else if (direction == "BEARISH")
            {
                DebugLogFile("CLOSE_BUY_FOR_BEARISH", "Closing BUY for BEARISH signal");
                return ACTION_CLOSE_BUY;
            }
        }

        DebugLogFile("CLOSING_DECISION", "⏸️ No closing needed, Decision: HOLD");
        return ACTION_HOLD;
    }

    // ================= POSITION ANALYSIS =================
    PositionAnalysis AnalyzePositions(int symbolIndex)
    {
        DebugLogFile("ANALYZE_POSITIONS_START", StringFormat("Analyzing positions for symbol index %d", symbolIndex));

        PositionAnalysis analysis;

        if (symbolIndex < 0 || symbolIndex >= m_totalSymbols)
        {
            DebugLogFile("ANALYZE_POSITIONS_ERROR", StringFormat("Invalid symbol index: %d", symbolIndex));
            return analysis;
        }

        string symbol = m_symbolStates[symbolIndex].symbol;
        int magic = m_symbolStates[symbolIndex].magicNumber;

        DebugLogFile("ANALYZE_POSITIONS", StringFormat("Analyzing positions for %s (magic: %d)", symbol, magic));

        int positionsTotal = PositionsTotal();
        DebugLogFile("POSITIONS_TOTAL", StringFormat("Total positions in terminal: %d", positionsTotal));

        for (int i = 0; i < positionsTotal; i++)
        {
            ulong ticket = PositionGetTicket(i);
            if (ticket > 0)
            {
                string positionSymbol = PositionGetString(POSITION_SYMBOL);
                long positionMagic = PositionGetInteger(POSITION_MAGIC);

                if (positionSymbol == symbol && positionMagic == magic)
                {
                    long positionType = PositionGetInteger(POSITION_TYPE);
                    double positionVolume = PositionGetDouble(POSITION_VOLUME);
                    double positionProfit = PositionGetDouble(POSITION_PROFIT);
                    double positionSwap = PositionGetDouble(POSITION_SWAP);

                    double positionCommission = GetPositionCommission(ticket);
                    analysis.totalProfit += positionProfit + positionSwap + positionCommission;

                    if (positionType == POSITION_TYPE_BUY)
                    {
                        analysis.buyCount++;
                    }
                    else if (positionType == POSITION_TYPE_SELL)
                    {
                        analysis.sellCount++;
                    }
                }
            }
        }

        if (analysis.buyCount > 0 && analysis.sellCount > 0)
        {
            analysis.state = STATE_HAS_BOTH;
        }
        else if (analysis.buyCount > 0)
        {
            analysis.state = STATE_HAS_BUY;
        }
        else if (analysis.sellCount > 0)
        {
            analysis.state = STATE_HAS_SELL;
        }
        else
        {
            analysis.state = STATE_NO_POSITION;
        }

        DebugLogFile("ANALYZE_POSITIONS_RESULT", StringFormat("Position analysis for %s: %s", symbol, analysis.ToString()));

        return analysis;
    }

    double GetPositionCommission(ulong positionTicket)
    {
        if (positionTicket == 0)
        {
            DebugLogFile("GET_COMMISSION", "Position ticket is 0");
            return 0.0;
        }

        double commission = 0.0;

        if (HistoryDealSelect(positionTicket))
        {
            commission = HistoryDealGetDouble(positionTicket, DEAL_COMMISSION);
        }
        else
        {
            datetime positionTime = 0;
            string positionSymbol = "";

            if (PositionSelectByTicket(positionTicket))
            {
                positionTime = (datetime)PositionGetInteger(POSITION_TIME);
                positionSymbol = PositionGetString(POSITION_SYMBOL);
            }

            int totalDeals = HistoryDealsTotal();
            for (int i = totalDeals - 1; i >= 0; i--)
            {
                ulong dealTicket = HistoryDealGetTicket(i);
                if (dealTicket <= 0)
                    continue;

                long dealType = HistoryDealGetInteger(dealTicket, DEAL_TYPE);
                datetime dealTime = (datetime)HistoryDealGetInteger(dealTicket, DEAL_TIME);
                string dealSymbol = HistoryDealGetString(dealTicket, DEAL_SYMBOL);

                if ((dealType == DEAL_TYPE_BUY || dealType == DEAL_TYPE_SELL) &&
                    dealSymbol == positionSymbol &&
                    MathAbs(dealTime - positionTime) < 60)
                {
                    commission = HistoryDealGetDouble(dealTicket, DEAL_COMMISSION);
                    break;
                }
            }
        }

        return commission;
    }

    bool CheckPositionLimits(int symbolIndex, bool isBuy)
    {
        if (symbolIndex < 0)
        {
            DebugLogFile("CHECK_POSITION_LIMITS_ERROR", StringFormat("Invalid symbol index: %d", symbolIndex));
            return false;
        }

        if (!m_allowMultiplePositions)
        {
            PositionAnalysis analysis = AnalyzePositions(symbolIndex);
            bool hasPosition = (analysis.state != STATE_NO_POSITION);

            if (isBuy && hasPosition && analysis.state != STATE_HAS_SELL)
            {
                DebugLogFile("CHECK_POSITION_LIMITS", "Cannot open BUY - already have position");
                return false;
            }

            if (!isBuy && hasPosition && analysis.state != STATE_HAS_BUY)
            {
                DebugLogFile("CHECK_POSITION_LIMITS", "Cannot open SELL - already have position");
                return false;
            }

            DebugLogFile("CHECK_POSITION_LIMITS", "Position limit check passed");
            return true;
        }

        PositionAnalysis analysis = AnalyzePositions(symbolIndex);
        DecisionParams params = m_symbolStates[symbolIndex].params;

        int currentCount = isBuy ? analysis.buyCount : analysis.sellCount;
        int maxAllowed = params.maxPositionsPerSymbol;

        bool withinLimits = (currentCount < maxAllowed);

        DebugLogFile("CHECK_POSITION_LIMITS", StringFormat("Symbol: %s, isBuy: %s, Current: %d, Max: %d, WithinLimits: %s",
                                                           m_symbolStates[symbolIndex].symbol, isBuy ? "true" : "false", currentCount, maxAllowed, withinLimits ? "true" : "false"));

        return withinLimits;
    }

    // ================= VALIDATION =================
    bool ValidateDecision(int symbolIndex, DECISION_ACTION decision, const DecisionEngineInterface &package)
    {
        DebugLogFile("VALIDATE_DECISION_START", StringFormat("=== VALIDATING DECISION === | %s | Symbol: %s | Conf: %.1f%% | Processor: %s",
                                                             DecisionToString(decision), package.symbol, package.overallConfidence,
                                                             m_symbolStates[symbolIndex].lastProcessorUsed));

        if (decision == ACTION_NONE || decision == ACTION_HOLD || decision == ACTION_WAITING_FOR_PACKAGE)
        {
            DebugLogFile("VALIDATE_NON_ACTION", "Non-action decision - always valid");
            return true;
        }

        if (symbolIndex < 0)
        {
            DebugLogFile("VALIDATE_ERROR", "Invalid symbol index");
            return false;
        }

        DecisionParams params = m_symbolStates[symbolIndex].params;
        double confidence = package.overallConfidence;
        string direction = package.dominantDirection;

        DebugLogFile("VALIDATION_PARAMS", StringFormat("Params: Buy=%.1f%%, Sell=%.1f%%, Close=%.1f%%, CloseAll=%.1f%%",
                                                       params.buyConfidenceThreshold, params.sellConfidenceThreshold,
                                                       params.closePositionThreshold, params.closeAllThreshold));

        bool confidenceValid = false;
        switch (decision)
        {
        case ACTION_OPEN_BUY:
            DebugLogFile("VALIDATE_OPEN_BUY", StringFormat("Checking OPEN_BUY: Conf=%.1f%% >= %.1f%% && Dir=%s == BULLISH",
                                                           confidence, params.buyConfidenceThreshold, direction));
            if (confidence < params.buyConfidenceThreshold)
            {
                DebugLogFile("VALIDATE_FAIL", "❌ Confidence too low for BUY");
                return false;
            }
            if (direction != "BULLISH")
            {
                DebugLogFile("VALIDATE_FAIL", StringFormat("❌ Wrong direction for BUY: %s != BULLISH", direction));
                return false;
            }
            confidenceValid = true;
            break;

        case ACTION_OPEN_SELL:
            DebugLogFile("VALIDATE_OPEN_SELL", StringFormat("Checking OPEN_SELL: Conf=%.1f%% >= %.1f%% && Dir=%s == BEARISH",
                                                            confidence, params.sellConfidenceThreshold, direction));
            if (confidence < params.sellConfidenceThreshold)
            {
                DebugLogFile("VALIDATE_FAIL", "❌ Confidence too low for SELL");
                return false;
            }
            if (direction != "BEARISH")
            {
                DebugLogFile("VALIDATE_FAIL", StringFormat("❌ Wrong direction for SELL: %s != BEARISH", direction));
                return false;
            }
            confidenceValid = true;
            break;

        case ACTION_CLOSE_BUY:
        case ACTION_CLOSE_SELL:
            DebugLogFile("VALIDATE_CLOSE", StringFormat("Checking CLOSE: Conf=%.1f%% <= %.1f%%",
                                                        confidence, params.closePositionThreshold));
            if (confidence > params.closePositionThreshold)
            {
                DebugLogFile("VALIDATE_FAIL", "❌ Confidence too high for close");
                return false;
            }
            confidenceValid = true;
            break;

        case ACTION_CLOSE_ALL:
            DebugLogFile("VALIDATE_CLOSE_ALL", StringFormat("Checking CLOSE_ALL: Conf=%.1f%% <= %.1f%%",
                                                            confidence, params.closeAllThreshold));
            if (confidence > params.closeAllThreshold)
            {
                DebugLogFile("VALIDATE_FAIL", "❌ Confidence too high for close all");
                return false;
            }
            confidenceValid = true;
            break;
        }

        if (!confidenceValid)
        {
            DebugLogFile("VALIDATE_FAIL", "❌ Confidence validation failed");
            return false;
        }

        DebugLogFile("VALIDATE_SUCCESS", "✅ Confidence validation passed");

        if (m_useRiskManagement)
        {
            DebugLogFile("VALIDATE_RISK", "Checking risk conditions...");
            DebugLogFile("VALIDATE_SUCCESS", "✅ Risk conditions met");
        }

        DebugLogFile("VALIDATE_TRADING_HOURS", "Checking trading hours...");
        if (!TimeUtils::IsTradingSession(m_symbolStates[symbolIndex].symbol))
        {
            DebugLogFile("VALIDATE_FAIL", "❌ Not in trading session");
            return false;
        }
        DebugLogFile("VALIDATE_SUCCESS", "✅ Trading hours OK");

        bool isBuy = (decision == ACTION_OPEN_BUY || decision == ACTION_CLOSE_SELL || decision == ACTION_CLOSE_ALL);
        DebugLogFile("VALIDATE_COOLDOWN", StringFormat("Checking cooldown (%s)...", isBuy ? "BUY" : "SELL"));
        if (!CheckCooldown(symbolIndex, isBuy))
        {
            DebugLogFile("VALIDATE_FAIL", "❌ In cooldown");
            return false;
        }
        DebugLogFile("VALIDATE_SUCCESS", "✅ Cooldown OK");

        if (decision == ACTION_OPEN_BUY || decision == ACTION_OPEN_SELL)
        {
            DebugLogFile("UPDATE_COOLDOWN_VALIDATION", StringFormat("Updating cooldown on validation for %s decision",
                                                                    isBuy ? "BUY" : "SELL"));
            m_symbolStates[symbolIndex].cooldown.Update(isBuy);
        }

        if (decision == ACTION_OPEN_BUY || decision == ACTION_OPEN_SELL)
        {
            DebugLogFile("VALIDATE_POSITION_LIMITS", "Checking position limits...");
            if (!CheckPositionLimits(symbolIndex, isBuy))
            {
                DebugLogFile("VALIDATE_FAIL", "❌ Position limit reached");
                return false;
            }
            DebugLogFile("VALIDATE_SUCCESS", "✅ Position limits OK");
        }

        DebugLogFile("VALIDATE_SUCCESS_ALL", "✅ All validation passed");
        return true;
    }

    bool CheckCooldown(int symbolIndex, bool isBuy)
    {
        if (!m_enforceCooldown)
        {
            DebugLogFile("COOLDOWN_CHECK_SKIP", "Cooldown enforcement disabled");
            return true;
        }

        if (symbolIndex < 0)
        {
            DebugLogFile("COOLDOWN_CHECK_ERROR", "Invalid symbol index");
            return false;
        }

        DecisionParams params = m_symbolStates[symbolIndex].params;
        CooldownRecord cooldown = m_symbolStates[symbolIndex].cooldown;

        DebugLogFile("COOLDOWN_STATE", StringFormat("Symbol: %s, isBuy: %s, LastBuy: %s, LastSell: %s, CooldownMins: %d",
                                                    m_symbolStates[symbolIndex].symbol,
                                                    isBuy ? "true" : "false",
                                                    TimeToString(cooldown.lastBuyTime),
                                                    TimeToString(cooldown.lastSellTime),
                                                    params.cooldownMinutes));

        bool inCooldown = cooldown.IsInCooldown(isBuy, params.cooldownMinutes);

        DebugLogFile("COOLDOWN_CHECK", StringFormat("Symbol: %s, isBuy: %s, InCooldown: %s",
                                                    m_symbolStates[symbolIndex].symbol, isBuy ? "true" : "false",
                                                    inCooldown ? "BLOCKED" : "ALLOWED"));

        return !inCooldown;
    }

    void ExecuteDecision(int symbolIndex, DECISION_ACTION decision, DecisionEngineInterface &package)
    {
        DebugLogFile("EXECUTE_DECISION_START", StringFormat("=== EXECUTING DECISION === | %s | Symbol: %s | Conf: %.1f%% | Processor: %s",
                                                            DecisionToString(decision), package.symbol, package.overallConfidence,
                                                            m_symbolStates[symbolIndex].lastProcessorUsed));

        if (symbolIndex < 0)
        {
            DebugLogFile("EXECUTE_ERROR", "❌ Invalid symbol index");
            return;
        }

        string symbol = m_symbolStates[symbolIndex].symbol;
        int magic = m_symbolStates[symbolIndex].magicNumber;

        DebugLogFile("EXECUTE_PARAMS", StringFormat("Symbol: %s | Magic: %d", symbol, magic));

        bool executed = false;
        string actionStr = DecisionToString(decision);

        switch (decision)
        {
        case ACTION_OPEN_BUY:
            DebugLogFile("EXECUTE_BUY", "Opening BUY position...");
            executed = PositionManager::OpenPositionWithTrendPackage(symbol, true, package, magic);
            if (executed)
            {
                m_symbolStates[symbolIndex].cooldown.Update(true);
                DebugLogFile("EXECUTE_COOLDOWN", "Buy cooldown updated after execution");
            }
            break;

        case ACTION_OPEN_SELL:
            DebugLogFile("EXECUTE_SELL", "Opening SELL position...");
            executed = PositionManager::OpenPositionWithTrendPackage(symbol, false, package, magic);
            if (executed)
            {
                m_symbolStates[symbolIndex].cooldown.Update(false);
                DebugLogFile("EXECUTE_COOLDOWN", "Sell cooldown updated after execution");
            }
            break;

        case ACTION_CLOSE_BUY:
            DebugLogFile("EXECUTE_CLOSE_BUY", "Calling PositionManager::CloseAllPositions (BUY)...");
            executed = PositionManager::CloseAllPositions(symbol, magic, "DecisionEngine: Close BUY");
            if (executed)
            {
                m_symbolStates[symbolIndex].cooldown.Update(true);
                DebugLogFile("EXECUTE_COOLDOWN", "Buy cooldown updated after closing BUY");
            }
            break;

        case ACTION_CLOSE_SELL:
            DebugLogFile("EXECUTE_CLOSE_SELL", "Calling PositionManager::CloseAllPositions (SELL)...");
            executed = PositionManager::CloseAllPositions(symbol, magic, "DecisionEngine: Close SELL");
            if (executed)
            {
                m_symbolStates[symbolIndex].cooldown.Update(false);
                DebugLogFile("EXECUTE_COOLDOWN", "Sell cooldown updated after closing SELL");
            }
            break;

        case ACTION_CLOSE_ALL:
            DebugLogFile("EXECUTE_CLOSE_ALL", "Calling PositionManager::CloseAllPositions (ALL)...");
            executed = PositionManager::CloseAllPositions(symbol, magic, "DecisionEngine: Close ALL");
            if (executed)
            {
                m_symbolStates[symbolIndex].cooldown.Update(true);
                m_symbolStates[symbolIndex].cooldown.Update(false);
                DebugLogFile("EXECUTE_COOLDOWN", "Both cooldowns updated after closing ALL");
            }
            break;

        default:
            DebugLogFile("EXECUTE_SKIP", "No execution needed for this decision type");
            return;
        }

        if (executed)
        {
            DebugLogFile("EXECUTE_SUCCESS", "✅ Execution successful");
            LogExecution(symbol, decision, true, package);
        }
        else
        {
            DebugLogFile("EXECUTE_FAILED", "❌ Execution failed");
            LogExecution(symbol, decision, false, package);
            m_symbolStates[symbolIndex].cooldown.Reset();
            DebugLogFile("COOLDOWN_RESET", "Cooldown reset due to failed execution");
        }
    }

    // ================= LOGGING =================
    void LogInfo(string message)
    {
        DebugLogFile("INFO", message);
        Logger::Log(m_engineName, message, true, true);
    }

    void LogWarning(string message)
    {
        DebugLogFile("WARNING", message);
        Logger::LogError(m_engineName, message);
    }

    void LogError(string message)
    {
        DebugLogFile("ERROR", message);
        Logger::LogError(m_engineName, message);
    }

    void LogDebug(string message)
    {
        if (m_debugEnabled)
        {
            DebugLogFile("DEBUG", message);
            Logger::Log("DEBUG-" + m_engineName, message, true, true);
        }
    }

    void LogDecision(string symbol, DECISION_ACTION decision, const DecisionEngineInterface &package, string reason)
    {
        if (decision == ACTION_NONE || decision == ACTION_HOLD)
        {
            DebugLogFile("LOG_DECISION_SKIP", StringFormat("Skipping log for non-action decision: %s", DecisionToString(decision)));
            return;
        }

        string decisionStr = DecisionToString(decision);
        string logMsg = StringFormat("%s | %s | Conf: %.1f%% | Dir: %s | Reason: %s",
                                     symbol, decisionStr, package.overallConfidence,
                                     package.dominantDirection, reason);

        logMsg += StringFormat(" | Regime: %s | Action: %s | Type: %s",
                               package.marketRegime, package.recommendedAction,
                               package.IsRangePackage() ? "RANGE" : "TREND");

        if (decision == ACTION_WAITING_FOR_PACKAGE)
        {
            DebugLogFile("DECISION_WAITING", logMsg);
        }
        else
        {
            LogInfo(logMsg);
        }

        DebugLogFile("LOG_DECISION", StringFormat("Logged decision: %s", logMsg));
    }

    void LogExecution(string symbol, DECISION_ACTION decision, bool success, const DecisionEngineInterface &package)
    {
        string decisionStr = DecisionToString(decision);
        string logType = success ? "EXECUTION_SUCCESS" : "EXECUTION_FAILURE";

        DebugLogFile(logType, StringFormat("%s for %s | Conf: %.1f%%", decisionStr, symbol, package.overallConfidence));

        if (success)
        {
            if (decision == ACTION_OPEN_BUY || decision == ACTION_OPEN_SELL)
            {
                // Store position open info for later profitability check
                int symbolIndex = FindSymbolIndex(symbol);
                if (symbolIndex >= 0)
                {
                    m_symbolStates[symbolIndex].positionOpenTime = TimeCurrent();
                    m_symbolStates[symbolIndex].positionOpenDecision = decision;
                    m_symbolStates[symbolIndex].positionOpenConfidence = package.overallConfidence;
                    m_symbolStates[symbolIndex].awaitingProfitabilityCheck = false;

                    // DON'T update metrics here! Wait for close.

                    DebugLogFile("POSITION_OPEN_RECORDED",
                                 StringFormat("Recorded open for %s | Decision: %s | Conf: %.1f%% | Waiting for close...",
                                              symbol,
                                              DecisionToString(decision),
                                              package.overallConfidence));
                }
            }
            else if (decision == ACTION_CLOSE_BUY || decision == ACTION_CLOSE_SELL || decision == ACTION_CLOSE_ALL)
            {
                // This is a CLOSE signal - mark that we're waiting for the position to actually close
                int symbolIndex = FindSymbolIndex(symbol);
                if (symbolIndex >= 0 && m_symbolStates[symbolIndex].positionOpenTime > 0)
                {
                    m_symbolStates[symbolIndex].positionCloseTime = TimeCurrent();
                    m_symbolStates[symbolIndex].awaitingProfitabilityCheck = true;

                    DebugLogFile("CLOSE_SIGNAL_RECEIVED",
                                 StringFormat("Close signal for %s | Open was: %s | Awaiting actual close...",
                                              symbol,
                                              DecisionToString(m_symbolStates[symbolIndex].positionOpenDecision)));
                }
            }
        }
        else
        {
            LogError(StringFormat("FAILED: %s for %s", decisionStr, symbol));
            DebugLogFile("FAILURE_LOG", StringFormat("Execution failure logged: %s %s",
                                                     decisionStr, symbol));
        }
    }

    void ResetCooldown(string symbol)
    {
        DebugLogFile("RESET_COOLDOWN_START", "Resetting cooldown for: " + symbol);

        int index = FindSymbolIndex(symbol);
        if (index >= 0)
        {
            m_symbolStates[index].cooldown.Reset();
            DebugLogFile("RESET_COOLDOWN_SUCCESS", "Cooldown reset for " + symbol);
        }
        else
        {
            DebugLogFile("RESET_COOLDOWN_ERROR", "Symbol not found: " + symbol);
        }
    }

    void ResetAllCooldowns()
    {
        DebugLogFile("RESET_ALL_COOLDOWNS_START", "Resetting all cooldowns");

        for (int i = 0; i < m_totalSymbols; i++)
        {
            m_symbolStates[i].cooldown.Reset();
            DebugLogFile("COOLDOWN_RESET_INDIVIDUAL", StringFormat("Reset cooldown for %s", m_symbolStates[i].symbol));
        }

        DebugLogFile("RESET_ALL_COOLDOWNS_SUCCESS", "All cooldowns reset");
    }

    // ================= NEW: Get detailed regime info =================
    string GetRegimeInfoDetailed(string symbol, string &outRegime, string &outSource,
                                 string &outDirection, double &outConfidence)
    {
        int index = FindSymbolIndex(symbol);
        if (index < 0)
            return "";

        if (m_symbolStates[index].lastPackage.IsValid())
        {
            outRegime = m_symbolStates[index].lastPackage.marketRegime;
            outSource = m_symbolStates[index].usingRangePackage ? "RangePackage" : "TrendPackage";
            outDirection = m_symbolStates[index].lastPackage.dominantDirection;
            outConfidence = m_symbolStates[index].lastPackage.overallConfidence;

            return StringFormat("%s|%s|%s|%.0f%%",
                                outRegime, outSource, outDirection, outConfidence);
        }

        return "";
    }

    string GetActualProcessorUsed(string symbol)
    {
        int index = FindSymbolIndex(symbol);
        if (index >= 0)
        {
            return m_symbolStates[index].actualProcessorUsed;
        }
        return "NONE";
    }
};

// ================= END OF FILE =================












































































    
give me the most minimal code to invalidate a range.
i love your simple diagram
use this structure to solve this file issues
=====================================================================
START
   ↓
[1] DETECT RANGE (InitializeFixedRange() - only if ShouldCreateNewRange() true)
   ↓
[2] MONITOR FOR BREAKOUT (HasRangeBroken() + CheckAndInvalidateRange())
   ↓
[3] BREAKOUT OCCURS → [4] CLASSIFY BREAKOUT:
        │                    ↓
        │           CheckAndInvalidateRange() runs FIRST
        │                    ↓
        │           [5] INVALIDATE OLD RANGE (m_rangeActive = false)
        │                    ↓
        │           [6] DECISION: TREND or NEW RANGE?
        │                    │
        │           ┌─────────┴─────────┐
        │           ↓                   ↓
        │   [7A] IF TREND:         [7B] IF NEW RANGE:
        │   • Range stays invalid  • Wait for cooldown  if cooldown exists(8-10 bars)
        │   • DetectCurrentState() • Market must settle:
        │     returns trending       - ADX < 25
        │   • No new range created   - Price near MA50 (<0.8%)
        │                            • THEN create new range
        │                               (InitializeFixedRange())
        └───────────────────────────────────┘
   ↓
[8] TREND CONTINUES:
   • Range stays invalid (m_rangeActive = false)
   • ShouldCreateNewRange() returns false
   • System return trending market
   ↓
[9] TREND EXHAUSTS:
   • ADX drops < 25
   • Price returns near MA50
   • ShouldCreateNewRange() returns true
   • NEW RANGE CREATED
   ↓
[10] BACK TO [1] with new range boundaries
=====================================================================


i also want that you consider these for invalidation:

1. use bars as suggested. then add strength if volume
    - Price closes outside range for 2+ consecutive bars
2. use atr that will not confict with this function unless you will also tune HasRangeBroken() which i think helps determine weather to expand or not.
    - Strong momentum break with high volume/ATR expansion
3. factor in Ma into this function to help.



so go ahead and :
=====================================================================================
1. Add Cooldown After Range Invalidation
=====================================================================================
mql5
// Add to class:
private:
    datetime m_lastInvalidationTime;  // Track when range was invalidated
    int m_invalidationCooldownBars;   // Bars to wait before new range

// In constructor:
m_lastInvalidationTime = 0;
m_invalidationCooldownBars = 8;  // Wait 8 bars after invalidation

the goal is to Prevents range from immediately reincarnating after invalidation.
=====================================================================================

2. Modified GetMarketRegime() - Line 98-101
=====================================================================================
mql5
// REPLACE current lines 98-101:
if (!m_rangeActive)
{
    InitializeFixedRange();  // OLD: Always recreates
}

// WITH:
if (!m_rangeActive && ShouldCreateNewRange())  // NEW: Conditional
{
    InitializeFixedRange();
}

The goal being to Only creates new range when market conditions are right.
=====================================================================================

3. Add ShouldCreateNewRange() Method
=====================================================================================
mql5
bool ShouldCreateNewRange()
{
    // 1. Check cooldown period
    
    // 2. Market must be clearly ranging (not trending)
}


add from the structure proposed above as well
goal is to Ensures new ranges only form in genuine ranging conditions.
=====================================================================================

4. Add Clear Range Invalidation at Start of DetectCurrentState()
=====================================================================================
mql5
// ADD at beginning of DetectCurrentState() (Line 269):
ENUM_MARKET_STATE DetectCurrentState()
{
    // NEW: Check if current range should be invalidated
    CheckAndInvalidateRange();  // Add this line
    
    // Existing code continues...
    double adx = GetADX(14);
    // ...
}
Purpose: Makes range invalidation part of the state detection process.
=====================================================================================

5. Add CheckAndInvalidateRange() Method
=====================================================================================
mql5
void CheckAndInvalidateRange()
{
    if (!m_rangeActive) return;
}
Purpose: Clear, simple rules for when a range is no longer valid.
=====================================================================================

8. Modify HasRangeBroken() for Clearer Breakouts
=====================================================================================

bool HasRangeBroken(double price)
{
    if (!m_rangeActive) return false;
    
    // INCREASE buffer from 0.3 ATR to 0.5 ATR
    double atr = GetATR(14);
    double margin = atr * 0.5;  // Changed from 0.3
    
    return (price > m_fixedRangeTop + margin) ||
           (price < m_fixedRangeBottom - margin);
}
Purpose: Reduces false breakout signals. also correct the current use in the file now.
=====================================================================================

9. Add State Confidence Check Before Ranging Logic
=====================================================================================
mql5
// BEFORE line 355 (if (isRangingStructure || m_rangeActive)):
if (m_rangeActive)
{
    // Check if range is still valid before using it
    double rangeTouches = CountRangeTouches();
    if (rangeTouches < 2)  // Not enough touches
    {
        // Range exists but isn't being respected
        return STATE_EXPANSION;  // Likely breaking out
    }
}
Purpose: Prevents using weak/invalid ranges in decisions.
=====================================================================================

10. Clean Up Conflicting Logic
=====================================================================================
mql5
// REMOVE or simplify lines 355-380 complex scoring system
// Replace with:
if (m_rangeActive && IsPriceInFixedRange(currentPrice))
{
    // Simple: If in range and price is within it, we're ranging
    if (volatilityHigh) return STATE_RANGING_HIGH_VOL;
    else return STATE_RANGING_LOW_VOL;
}
Purpose: Eliminates conflicting signals between range and trend.
=====================================================================================



































so i want a more comprehensive and yet copmact display: inspired by the dicesion engine:
1. Avoid using emojis, better to use special characters if you even have to.
2. Let direction be described as LONG/SHORT and action or recommended action be BUY/SELL or B/S respectively
3. this is the final format i would like to display
  
|---------------- |     DASSHBOARD      | -----------------------------
Account Info    : Balance | Equity | Margin Levels % | Session 
Signals         : RootState (Ranging) | State (High Vol Range) --> Next Likely (Low VOl Range)
Trading Info    : Range/Trend Package | SHORT | Confidence %age
|---------------------these will be conditional------------------------ 
[if trending]   : MTF/B/80% | POI/S/30% | RSI/S/40% | MACD/B/30% | etc.. 
[else if range] : Range (Top&Bottom Prices) | Price Position in % |
[else if range] : COMP1/B/40% | COMP2/B/40% | COMP3/B/40% | etc..
|---------------------------------------------------------------------- 
Setup           : Position Size | SL | TP | RR 
Action          : Caution | High VOlatility | Small Stops | Fade/Follow-->[(in what direction)bull/bear?] 
Description     : State | Reasons | Additional Information (i basically wnat to know the exact reason a decision is made to secure or miss a position)
Decision        : Direction (like BUY) | 2 Positions | LONG
Statistics      : Decisions (like positions taken) | Accuracy (like how many ended up with net profit/ total positions taken) in %age
|----------------------------------------------------------------------













place settings on the top of the file


++++++++++++++++++++++
ONLY PROCEED IF THIS PROVIDED FILE NEEDS THESE THINGS 
PLEASE NOTE THAT BEFOR EVERYTHING, YOU SHOULD NOT CHANGE FUNCTIONSLTY, NAMES OF FUNCTIONS OR REMOVE FUNCTIONS
AT THE TOP AFTER THE FILE NAME LET IT STATE THAT ITS ALREADY INTERGRATED THIS IF YOU THIS PROCESS IS APPLICABLE
THE LOGGER AND COMMENT FILES AND UTILS ARE ALL STATIC AND STATELESS so use static functions for these
++++++++++++++++++++++
give me this file back with (ADD these instruction ONLY OF NECESSAYR, IF NOT JUST STOP AND TELL ME):
- prefer my files below for all your utils needs to avoid redundance.
- prefer the logs and chart comments privided also for uniformity and to avoid confusion.

I WANT:
- only essential logs, like those that would display incase of an errro. still minimal to avoid perfomance issues
- chart comments with scores. also minimal to avoid perfomance issues
- create these functions for it:
    CONSTRUCTOR - sets default values, reserving memory only.
    INITIALIZE() - Takes all dependencies as parameters, Creates actual resources, Sets up internal state, Returns bool and Uses a flag m_initialized
    DEINITIALIZE() - Closes/frees resources, Resets m_initialized flag, Does NOT delete the module itself (thats for the initializer)
    Plus event handlers: OnTick(), OnTimer(), OnTradeTransaction() - ONLY process if m_initialized = true, and only use initialized resources 
- a step-by-step Minimal EA Intergration example.
- a list of all the functions in the file in this manner
        File.mqh
        ++++++++++++++++++++++++++++
        function1(param1, param2)
        function2(param1, param2)
- Proper comments to be able to follow up on the code slowly





Update this module to:
	RETURN their own module-specific data structures
	ADD helper methods that return raw data, NOT TradePackage objects
	Modules should NOT know about TradePackage at all!
WHAT MODULES MUST PROVIDE:
	Their own data structures (e.g., MTFScore, POISignal)
	Analysis methods that return those structures
	NO TradePackage includes or references





+++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++
AT THE TOP AFTER THE FILE NAME LET IT STATE THAT ITS ALREADY INTERGRATED THIS IF YOU THIS PROCESS IS APPLICABLE
FOR UTILS PLEASE PREFER MY FILES AND FUNCITONS:
all utils are static files with static functions only, no classes.
use as static functions only.
prefer this for all your utils needs to avoid redundance.

these are the functions in all my utils files
+++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++
consider mathutils
++++++++++++++++++++++
PipsToPrice(string symbol, double pips)
PriceToPips(string symbol, double price)
CalculatePipValue(string symbol)
CalculatePositionRisk(string symbol, double entryPrice, double stopLoss, double lotSize)
CalculateRiskRewardRatio(double entryPrice, double stopLoss, double takeProfit)
CalculatePercentageChange(double oldValue, double newValue)
CalculateValueFromPercentage(double baseValue, double percentage)
CalculatePercentageOfValue(double part, double whole)
CalculateSimpleMovingAverage(const double &values[], int period)
CalculateWeightedAverage(const double &values[], const double &weights[])
CalculateATR(string symbol, ENUM_TIMEFRAMES timeframe, int period, int shift = 0)
CalculateStandardDeviation(const double &data[])
CalculateDistanceInPips(string symbol, double price1, double price2)
CalculateDistanceAsPercentage(double price1, double price2, double referencePrice)
NormalizePrice(string symbol, double price)
NormalizePriceToTick(string symbol, double price)
NormalizeLotSize(string symbol, double lotSize)
CalculateProfitInPips(string symbol, double entryPrice, double exitPrice, bool isBuy)
CalculateProfitInMoney(string symbol, double entryPrice, double exitPrice, double lotSize, bool isBuy)
CalculateWinProbability(int totalTrades, int winningTrades)
CalculateExpectedValue(double winRatePercent, double avgWin, double avgLoss)
CalculateKellyCriterion(double winRatePercent, double avgWinToLossRatio)
CalculateFibonacciLevel(double high, double low, double level)
CalculateGeometricMean(const double &values[])
CalculateAnnualizedReturn(double totalReturnPercent, double days)
CalculateCompoundedGrowth(double initialAmount, double ratePercent, int periods)
IsValidPrice(string symbol, double price)
IsValidLotSize(string symbol, double lotSize)
CalculatePositionSizeByRisk(string symbol, double entryPrice, double stopLoss, double riskPercent, double accountBalance)
CalculatePositionSize() find out params in indicator file for atr based position sizing
CalculateBreakevenPrice(double entryPrice, bool isBuy, double spreadPips)
CalculateMarginRequired(string symbol, double lotSize, int orderType = ORDER_TYPE_BUY)
CalculateSwap(string symbol, double lotSize, int orderType, int days = 1)
RoundToTick(string symbol, double value)
CalculateCommission(string symbol, double lotSize, double commissionPerLot = 0)
CalculateTotalTradeCost(string symbol, double lotSize, bool isBuy, double commissionPerLot = 0)
CalculatePositionScore()

consider errorutils
++++++++++++++++++++++
CheckError(int errorCode)
GetErrorDescription(int errorCode)
HandleOrderError(int errorCode, Logger &logger)
HandleOrderError(int errorCode)
HandleMarketError(int errorCode, Logger &logger)
HandleMarketError(int errorCode)
GetLastError(Logger &logger)
GetLastError()
CheckErrorWithTime(int errorCode, Logger &logger)
IsRecoverableError(int errorCode)
IsFatalError(int errorCode)
GetRecoverySuggestion(int errorCode)
ResetLastError()
GetErrorDetails(int errorCode)
LogErrorWithDetails(int errorCode, Logger &logger, string context)
HandleErrorWithRetry(int errorCode, Logger &logger, int maxRetries)

consider loggerutils
++++++++++++++++++++++
GetTimestamp()
GetTimeOnly()
BuildMessage(string module, string timestamp, string reason)
LogInternal(string module, string reason, bool logToFile = true, bool logToConsole = true)
Initialize(string fileName = "", bool logToFile = true, bool logToConsole = true)
Shutdown()
Log(string module, string reason, bool logToFile = true, bool logToConsole = true)
LogError(string module, string reason, int errorCode = 0)
LogTrade(string module, string symbol, string operation, double volume, double price = 0.0)
LogFast(string module, string reason)
LogUltraFast(const string &module, const string &reason)
LogTradeFast(const string &module, const string &symbol, const string &operation, double volume)
IsFileLoggingAvailable()
GetLogFileName()
GetFileHandleStatus()
LogMemoryUsage(string module)
Flush()
LogWithTimestamp(string module, string reason, datetime customTime)

consider configutils
++++++++++++++++++++++
ReadDatetime(string key, datetime defaultValue)
ReadColor(string key, color defaultValue)
ReadEnum(string key, int defaultValue)
ReadInt(string key, int defaultValue, string section)
ReadDouble(string key, double defaultValue, string section)
ReadBool(string key, bool defaultValue, string section)
ReadString(string key, string defaultValue, string section)
WriteInt(string key, int value, string section)
WriteDouble(string key, double value, string section)
WriteBool(string key, bool value, string section)
WriteString(string key, string value, string section)
WriteDatetime(string key, datetime value, string section)
WriteColor(string key, color value, string section)
ConfigExists()
GetConfigPath(bool common)

consider timeutils
++++++++++++++++++++++
IsTradingSession(const string symbol = NULL)
GetTradingSession(const string symbol, datetime &startTime, datetime &endTime)
IsNewBar(const string symbol, const ENUM_TIMEFRAMES timeframe)
IsMarketOpen(const string symbol = NULL)
IsEndOfMonth(const string symbol = NULL)
IsStartOfMonth(const string symbol = NULL)
MinutesUntilSession(const string symbol = NULL, bool nextDay = false)
IsHighVolatilityPeriod()
IsTimeInRange(int startHour, int startMinute, int endHour, int endMinute)
TradingDaysBetween(datetime startDate, datetime endDate)
IsPreMarket(const string symbol = NULL)
IsAfterHours(const string symbol = NULL)
NextTradingDay(datetime fromDate = 0)
TimeOfDayToString()
IsRolloverTime()
GetTimestamp()
TimeframeToMinutes(ENUM_TIMEFRAMES tf)
GetBarOpenTime(const string symbol, ENUM_TIMEFRAMES timeframe, int shift = 0)
GetBarCloseTime(const string symbol, ENUM_TIMEFRAMES timeframe, int shift = 0)
+++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++


















+++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++
AT THE TOP AFTER THE FILE NAME LET IT STATE THAT ITS ALREADY INTERGRATED THIS IF YOU THIS PROCESS IS APPLICABLE
FOR LOGS PLEASE PREFER MY FILES AND FUNCITONS:
+++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++
Prefer my logger functions instead, only where necessary LOGGERS ARE STATIC SO USE STATIC CALLS IE Logger::Log(...)
++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++
application example: in a file always wrap inside of a debug true false.
// ====================== DEBUG SETTINGS ======================
bool DEBUG_ENABLED = true;

// Simple debug function using Logger
void DebugLogFile(string context, string message) {
   if(DEBUG_ENABLED) {
      Logger::Log(log);
   }
}

//then all the logs and wrapped around the degub
// not chart comments.

so

// Initialize with different configurations
Logger::Initialize();                          // Default: file logging ON, chart ON, 2s updates
Logger::Initialize("MyBot.log", true, true, 1); // Custom settings
Logger::Initialize("", false, true, 5);        // No file logging, chart only, 5s updates
Logger::Shutdown();                           // Clean shutdown
// Runtime control
Logger::EnableChart(false);                   // Disable chart updates temporarily
Logger::SetChartFrequency(3);                 // Change update frequency (seconds)
Logger::ClearChart();                         // Clear all chart comments
Logger::Flush();                              // Flush file buffer
// Status checks
bool canLog = Logger::IsFileLoggingAvailable(); // Check if file is open
string fileName = Logger::GetLogFileName();   // Get current log filename
bool chartOn = Logger::IsChartEnabled();      // Check chart status
// Standard logging (console + file)
Logger::Log("Module", "Message");
Logger::Log("Strategy", "Entry signal detected", true, true); // logToFile, logToConsole
// Error logging
Logger::LogError("API", "Failed to connect");
Logger::LogError("Trade", "Order rejected", 10013); // With error code
// Trade logging
Logger::LogTrade("Portfolio", "EURUSD", "BUY", 0.1, 1.08542); // With price
Logger::LogTrade("Risk", "GBPUSD", "SELL", 0.05);             // Without price
// Performance logging
Logger::LogMemoryUsage("System");            // Log memory usage (MQL5 only)
// Faster with minimal formatting
Logger::LogFast("Module", "Fast message");  // Quick timestamp
Logger::LogUltraFast("Ticker", "Price update: 1.0850"); // No timestamp
// Fast trade logging
Logger::LogTradeFast("Scalper", "EURUSD", "BUY", 0.1);
// Custom timestamp logging
Logger::LogWithTimestamp("Backtest", "Strategy executed", D'2024.01.15 10:30:00');
// Single symbol score display
Logger::ShowScoreFast("EURUSD", 0.85, "BUY", 0.9);
Logger::ShowScoreFast("GBPUSD", 0.42, "SELL", 0.6);
Logger::ShowScoreFast("USDJPY", 0.15, "HOLD", 0.3); // Low score example
Logger::ShowScoreFast("XAUUSD", 0.92, "BUY", 0.95); // High confidence
// Trading decisions
Logger::ShowDecisionFast("EURUSD", 1, 0.92, "Strong bullish divergence on 4H");
Logger::ShowDecisionFast("GBPUSD", -1, 0.75, "Bearish breakout below support");
Logger::ShowDecisionFast("AUDUSD", 0, 0.60, "Waiting for confirmation"); // HOLD decision
// Portfolio overviews
string symbols[] = {"EURUSD", "GBPUSD", "USDJPY", "AUDUSD", "USDCAD", "XAUUSD"};
double scores[] = {0.85, 0.42, 0.73, 0.61, 0.29, 0.92};
int directions[] = {1, -1, 0, 1, -1, 1}; // 1=BUY, -1=SELL, 0=HOLD
Logger::ShowPortfolioFast(symbols, scores, directions);
// Risk metrics display
Logger::ShowRiskMetrics(3.2, 1.8, 1.4, 5); // risk%, drawdown%, sharpe, positions
Logger::ShowRiskMetrics(5.7, 3.2, 0.8, 8); // High risk example
Logger::ShowRiskMetrics(1.5, 0.9, 2.1, 3); // Low risk example
// Mixed use cases
string forexPairs[] = {"EURUSD", "GBPUSD", "USDJPY"};
double forexScores[] = {0.85, 0.42, 0.73};
int forexDirections[] = {1, -1, 0};
Logger::ShowPortfolioFast(forexPairs, forexScores, forexDirections);
// Backtesting scenarios
Logger::LogWithTimestamp("Backtest", "Entry: BUY EURUSD @ 1.0850", D'2024.01.15 10:30:00');
Logger::LogWithTimestamp("Backtest", "Exit: SELL EURUSD @ 1.0900 (+50 pips)", D'2024.01.15 14:45:00');
// Multi-timeframe analysis
Logger::Log("Analysis", "4H: Bullish | 1H: Neutral | 15M: Bearish");
Logger::ShowDecisionFast("EURUSD", 1, 0.82, "4H trend up, 1H pullback to support");
// Correlation analysis
Logger::Log("Correlation", "EURUSD-GBPUSD correlation: 0.72 (High)");
Logger::ShowScoreFast("EURUSD", 0.85, "BUY", 0.9);
Logger::ShowScoreFast("GBPUSD", 0.65, "HOLD", 0.7); // Lower due to correlation
// Position sizing and risk
Logger::Log("Risk", "Position size: 0.15 lots, Risk: $150 (1.5% of account)");
Logger::LogTrade("Execution", "EURUSD", "BUY", 0.15, 1.08542);
Logger::ShowRiskMetrics(1.5, 2.1, 1.2, 4);
// Performance tracking
Logger::Log("Performance", "Win Rate: 65%, Profit Factor: 1.8, Avg Win: $85");
Logger::LogMemoryUsage("Monitor");
// News/Event reactions
Logger::Log("News", "NFP release in 15 minutes - reducing position sizes");
Logger::ShowDecisionFast("USD pairs", 0, 0.40, "Waiting for NFP data");
// System alerts
Logger::LogError("System", "High latency detected: 250ms");
Logger::LogError("Connection", "Feed disconnected", 4065);
Logger::ShowDecisionFast("ALL", 0, 0.10, "Connection issues - pausing trading");
// Portfolio rebalancing
Logger::Log("Rebalance", "Closing 2 positions to reduce correlation risk");
Logger::LogTrade("Rebalance", "EURUSD", "CLOSE", 0.1);
Logger::LogTrade("Rebalance", "GBPUSD", "CLOSE", 0.05);
Logger::ShowRiskMetrics(2.8, 1.5, 1.6, 3); // Updated risk after rebalance
// Strategy parameter optimization
Logger::Log("Optimization", "Testing params: MA1=10, MA2=20, StopLoss=50");
Logger::ShowScoreFast("EURUSD", 0.78, "BUY", 0.8);
Logger::Log("Optimization", "Testing params: MA1=14, MA2=28, StopLoss=60");
Logger::ShowScoreFast("EURUSD", 0.82, "BUY", 0.85);
// Market condition analysis
Logger::Log("Market", "High volatility detected: ATR = 0.0085");
Logger::ShowDecisionFast("EURUSD", 1, 0.68, "High vol - using wider stops");
+++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++


























+++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++
ONLY PROCEED IF THIS FINE NEEDS INDICATORS PLEASE PREFER MY FILES AND FUNCITONS:
AT THE TOP AFTER THE FILE NAME LET IT STATE THAT ITS ALREADY INTERGRATED THIS IF YOU THIS PROCESS IS APPLICABLE
FOR INDICATORS PLEASE PREFER MY FILES AND FUNCITONS:
give the provided file back using this file as reference to build it properly.
the indicator manager primary needs to be able to provide all necessary indicator values.
+++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++
IndicatorManager(string symbol = NULL)
~IndicatorManager()
Initialize()
Deinitialize()
OnTick()
OnTimer()
OnTradeTransaction(const MqlTradeTransaction& trans, const MqlTradeRequest& request, const MqlTradeResult& result)
GetMAValues(ENUM_TIMEFRAMES tf, double &ma_fast, double &ma_slow, double &ma_medium, int shift = 0)
GetRSI(ENUM_TIMEFRAMES tf, int shift = 0)
GetMACDValues(ENUM_TIMEFRAMES tf, double &macd_main, double &macd_signal, int shift = 0)
GetADXValues(ENUM_TIMEFRAMES tf, double &adx, double &plus_di, double &minus_di, int shift = 0)
GetStochasticValues(ENUM_TIMEFRAMES tf, double &stoch_main, double &stoch_signal, int shift = 0)
GetATR(ENUM_TIMEFRAMES tf, int shift = 0)
GetVolume(ENUM_TIMEFRAMES tf, int shift = 0)
GetBollingerBandsValues(ENUM_TIMEFRAMES tf, double &upper, double &middle, double &lower, int shift = 0)
IsTrendBullish(ENUM_TIMEFRAMES tf)
IsTrendBearish(ENUM_TIMEFRAMES tf)
IsOverbought(ENUM_TIMEFRAMES tf)
IsOversold(ENUM_TIMEFRAMES tf)
IsStrongTrend(ENUM_TIMEFRAMES tf, int threshold = 25)
GetADXTrendDirection(ENUM_TIMEFRAMES tf)
GetMACDCrossover(ENUM_TIMEFRAMES tf)
GetMultiTimeframeConfirmation(int &bullish_tf_count, int &bearish_tf_count)
GetBBandsPosition(ENUM_TIMEFRAMES tf, double price)
CalculatePositionSize(double risk_percent, double stop_loss_pips, ENUM_TIMEFRAMES tf = PERIOD_H1)
GetMarketScore()
+++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++



















+++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++
ONLY PROCEED IF THIS FINE NEEDS TRADEPACKAGE PLEASE PREFER MY FILES AND FUNCITONS:
AT THE TOP AFTER THE FILE NAME LET IT STATE THAT ITS ALREADY INTERGRATED THIS IF YOU THIS PROCESS IS APPLICABLE
FOR TRADING PACKAGE PLEASE PREFER MY FILES AND FUNCITONS:
give me a way to populate the tradepackage given the file provided
rebuild this file to be able to populate my  tradepackage properly.
the trade package primary needs a re bull and bear bias and score and the confidence in that score
then any unique variables that the file can profide.
this functions constitute the trade package file
+++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++
TradePackage.mqh
++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++
File-level functions:
DebugLogTP(string context, string message)

Struct ComponentDisplay:
ComponentDisplay()
ComponentDisplay(string n, string d, double s, double c, double w, bool a, string dt = "")
string GetFormattedLine(bool useIcons = true, bool showDetails = false)
string GetDirectionIcon(string dir, bool useIcons)

Struct DirectionAnalysis:
string GetDisplayString() const

Struct TradeSignal:
string GetOrderTypeString() const
string GetSimpleSignal() const

Struct TradeSetup:
bool IsValid() const
string GetRRRString() const

Struct MTFData:
string GetAlignmentString() const

Struct RiskManagement:
string GetSettingsString() const

Struct POISignal (nested in TradePackage):
POISignal()
string GetSimpleSignal() const
string GetConfidenceString() const
bool IsActionable() const
string GetDisplayString() const

Struct TradePackage:
TradePackage()
void CalculateWeightedScore()
double CalculateOverallConfidence()
void NormalizeWeights()
bool ValidatePackage(double minConfidence = 60.0)
void DisplayTabular()
string GenerateTabularDisplay()
static void DisplayMultiSymbol(const TradePackage &packages[], bool showAllComponents = false)
string GetTabularHeader()
string GetSymbolHeader() const
void CollectComponents(ComponentDisplay &components[]) const
string GetOverallSummary()
string GetSetupInfo()
string GetValidationStatus()
string GetMTFDirection() const
string GetSignalIcon() const
string GetDirectionIcon(string dir, bool useIcons)
void ConfigureDisplay(bool tabularFormat = true, bool useColors = true, bool showInactive = false, bool showDetails = false)
void SetMaxComponentsPerLine(int max)
void DisplayOnChart()
string GenerateChartDisplay()
void LogCompletePackage()
string GenerateLogEntry()
void LogKeyMetrics()
void CalculatePositionSize(double accountBalance)
void CalculateRiskReward()
string GetSummary()
bool HasMTFData() const
bool HasSetup() const
double GetPositionSizeMultiplier() const
int GetTradeDecision() const
double GetConfidenceDecimal() const
void Display()
string RepeatString(string str, int count)
void SetDecisionEngine(DecisionEngine* de)
bool ProcessAndExecute()
bool UpdateAndExecute()
bool Validate() const
int StringCount(const string text, const string search)
+++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++




































+++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++
ONLY PROCEED IF THIS FINE NEEDS INDICATORS PLEASE PREFER MY FILES AND FUNCITONS:
AT THE TOP AFTER THE FILE NAME LET IT STATE THAT ITS ALREADY INTERGRATED THIS IF YOU THIS PROCESS IS APPLICABLE
FOR MTFANALYZER MANAGEMENT PLEASE PREFER MY FILES AND FUNCITONS:
+++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++
MTFAnalyser() - Constructor, sets default values
Initialize(symbol, primaryTF, indicatorManager) - Initializes the analyzer with dependencies
Deinitialize() - Cleans up resources
OnTick() - Handles tick events
OnTimer() - Handles timer events
OnTradeTransaction(trans, request, result) - Handles trade transaction events
AnalyzeMultiTimeframe(symbol) - Analyzes alignment across multiple timeframes
CheckAlignment(symbol, minScore) - Checks if timeframes are aligned above minimum score
GetDominantTF(symbol) - Gets the timeframe with strongest trend
IsInitialized() - Returns initialization status
GetSymbol() - Gets current symbol
GetPrimaryTF() - Gets primary timeframe
AnalyzeTrend(symbol, timeframe) - PRIVATE: Analyzes trend for specific timeframe
GetEMA(symbol, timeframe, period) - PRIVATE: Gets EMA value
CalculateTrendStrength(symbol, timeframe) - PRIVATE: Calculates trend strength using ADX
+++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++

+++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++
ONLY PROCEED IF THIS FINE NEEDS INDICATORS PLEASE PREFER MY FILES AND FUNCITONS:
AT THE TOP AFTER THE FILE NAME LET IT STATE THAT ITS ALREADY INTERGRATED THIS IF YOU THIS PROCESS IS APPLICABLE
FOR POSITION MANAGER PLEASE PREFER MY FILES AND FUNCITONS:
+++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++
PositionManager::OpenPosition(symbol, isBuy, comment, magic, stopMethod, riskPercent, rrRatio, reason)
PositionManager::OpenPositionWithTradePackage(symbol, isBuy, package)
PositionManager::CloseAllPositions(symbol, magic, reason)
PositionManager::SmartClosePosition(priority, magic, outClosedSymbol)
PositionManager::GetPositionCount(symbol, magic)
PositionManager::GetTotalProfit(symbol, magic)
PositionManager::UpdateTrailingStops(trailMethod, magic)
PositionManager::CheckMargin(symbol, lotSize, safetyBuffer)
+++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++

+++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++
ONLY PROCEED IF THIS FINE NEEDS INDICATORS PLEASE PREFER MY FILES AND FUNCITONS:
AT THE TOP AFTER THE FILE NAME LET IT STATE THAT ITS ALREADY INTERGRATED THIS IF YOU THIS PROCESS IS APPLICABLE
FOR DECISION ENGINE PLEASE PREFER MY FILES AND FUNCITONS:
+++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++
DecisionEngine() - Constructor
~DecisionEngine() - Destructor
Initialize(PositionManager, RiskManager, engineComment, engineMagicBase, slippage, chartUpdateSeconds)
Deinitialize()
MakeDecisionFromPackage(symbol, package)
ExecuteDecision(symbol, decision, package)
AddSymbol(symbol, params)
RemoveSymbol(symbol)
HasSymbol(symbol)
GetSymbolCount()
SetSymbolParameters(symbol, params)
SetTradePackageFunction(func)
SetDebugMode(debug)
SetUseComponentWeights(use)
SetMinConfidenceThreshold(threshold)
SetChartUpdateSeconds(seconds)
GetSymbolParameters(symbol)
GetLastPackage(symbol)
GetCurrentDecision(symbol)
GetDecisionAccuracy()
GetStatus()
DecisionToString(decision)
ResetStatistics()
QuickInitialize(symbol, buyThreshold, sellThreshold, riskPercent, cooldownMinutes, maxPositions)
OnTick()
OnTimer()
OnTradeTransaction(trans, request, result)
UpdateChartDisplay()
+++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++

+++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++
ONLY PROCEED IF THIS FINE NEEDS INDICATORS PLEASE PREFER MY FILES AND FUNCITONS:
AT THE TOP AFTER THE FILE NAME LET IT STATE THAT ITS ALREADY INTERGRATED THIS IF YOU THIS PROCESS IS APPLICABLE
FOR MARKET DATA PLEASE PREFER MY FILES AND FUNCITONS:
+++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++
MarketData(string symbol = NULL, ENUM_TIMEFRAMES timeframe = PERIOD_CURRENT)
double GetBid(string symbol = NULL)
double GetAsk(string symbol = NULL)
double GetSpread(string symbol = NULL)
MqlTick GetTick(string symbol = NULL)
bool GetOHLC(string symbol, ENUM_TIMEFRAMES timeframe, int shift, double &open, double &high, double &low, double &close)
long GetVolume(string symbol = NULL, ENUM_TIMEFRAMES timeframe = PERIOD_CURRENT, int shift = 0)
long GetVolume(string symbol = NULL)
bool IsFresh()
void Refresh()
+++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++

+++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++
ONLY PROCEED IF THIS FINE NEEDS INDICATORS PLEASE PREFER MY FILES AND FUNCITONS:
AT THE TOP AFTER THE FILE NAME LET IT STATE THAT ITS ALREADY INTERGRATED THIS IF YOU THIS PROCESS IS APPLICABLE
FOR POI PLEASE PREFER MY FILES AND FUNCITONS:
+++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++
POIModule() - Constructor
Initialize(string symbol, bool drawOnChart = false, double defaultBuffer = 2.0)
Deinitialize()
OnTick()
OnTimer()
OnTradeTransaction(...)
GetPOIScore(double currentPrice, ENUM_POI_TYPE &outZoneType, double &outDistanceToZone)
IsInsidePOIZone(double currentPrice, ENUM_POI_TYPE &outZoneType)
GetNearestZone(double currentPrice, POIZone &outZone)
IsInitialized()
GetZoneCount()
+++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++

+++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++
ONLY PROCEED IF THIS FINE NEEDS INDICATORS PLEASE PREFER MY FILES AND FUNCITONS:
AT THE TOP AFTER THE FILE NAME LET IT STATE THAT ITS ALREADY INTERGRATED THIS IF YOU THIS PROCESS IS APPLICABLE
FOR VOLUME PLEASE PREFER MY FILES AND FUNCITONS:
+++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++
VolumeModule() - Constructor
Initialize(string symbol, ENUM_TIMEFRAMES analysisTF = PERIOD_H1)
Deinitialize()
OnTick()
OnTimer()
OnTradeTransaction(...)
AnalyzeVolume(bool isBuyTrade, bool isInsidePOI, double distanceToPOI, double poiScore)
GetVolumeScore(bool isBuyTrade, bool isInsidePOI, double distanceToPOI, double poiScore)
HasVolumeDivergence(bool isBuyTrade, const double &prices, int period = 5)
IsInitialized()
+++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++

+++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++
ONLY PROCEED IF THIS FINE NEEDS INDICATORS PLEASE PREFER MY FILES AND FUNCITONS:
AT THE TOP AFTER THE FILE NAME LET IT STATE THAT ITS ALREADY INTERGRATED THIS IF YOU THIS PROCESS IS APPLICABLE
FOR RSI PLEASE PREFER MY FILES AND FUNCITONS:
+++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++
RSIModule() - Constructor
Initialize(string symbol, ENUM_TIMEFRAMES analysisTF = PERIOD_H1, 
          int rsiPeriod = 14, ENUM_APPLIED_PRICE appliedPrice = PRICE_CLOSE)
Deinitialize()
OnTick()
OnTimer()
OnTradeTransaction(...)
AnalyzeRSI(bool isBuyTrade, bool isInsidePOI, double distanceToPOI, double poiScore)
GetRSIScore(bool isBuyTrade, bool isInsidePOI, double distanceToPOI, double poiScore)
GetCurrentRSIValue()
GetRSITrend(int barsToCheck = 5)
HasFailureSwing(bool isBuyTrade)
IsInitialized()
GetAnalysisTimeframe()
+++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++

+++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++
ONLY PROCEED IF THIS FINE NEEDS INDICATORS PLEASE PREFER MY FILES AND FUNCITONS:
AT THE TOP AFTER THE FILE NAME LET IT STATE THAT ITS ALREADY INTERGRATED THIS IF YOU THIS PROCESS IS APPLICABLE
FOR RISK MANAGER PLEASE PREFER MY FILES AND FUNCITONS:
+++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++
RiskCalculator::CanOpenTrade(maxDailyLossPercent, maxDrawdownPercent)
RiskCalculator::CalculatePositionSize(symbol, entryPrice, stopLoss, riskPercent)
RiskCalculator::CalculatePositionSizeWithConfidence(symbol, entryPrice, stopLoss, confidence, baseRiskPercent)
RiskCalculator::CalculateStopLoss(symbol, isBuy, entryPrice, method, atrMultiplier)
RiskCalculator::CalculateTakeProfit(symbol, isBuy, entryPrice, stopLoss, rrRatio)
RiskCalculator::CalculateTakeProfitWithConfidence(symbol, isBuy, entryPrice, stopLoss, confidence, baseRR)
+++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++

+++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++
ONLY PROCEED IF THIS FINE NEEDS INDICATORS PLEASE PREFER MY FILES AND FUNCITONS:
AT THE TOP AFTER THE FILE NAME LET IT STATE THAT ITS ALREADY INTERGRATED THIS IF YOU THIS PROCESS IS APPLICABLE
FOR VolumeModule.mqh PLEASE PREFER MY FILES AND FUNCITONS:
+++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++
VolumeModule() - Constructor, sets default values
~VolumeModule() - Destructor, calls Deinitialize()
Initialize(IndicatorManager* indicatorMgr, string symbol = NULL) - Initialize module
Deinitialize() - Clean up resources
Analyze(ENUM_TIMEFRAMES tf = PERIOD_CURRENT, int lookback = 20, int fastPeriod = 5) - Comprehensive analysis
GetVolumeScore(ENUM_TIMEFRAMES tf = PERIOD_CURRENT, bool isBullishMove = true) - Simplified 0-100 score
IsVolumeConfirming(ENUM_TIMEFRAMES tf, bool expectingBullish) - Check volume confirmation
HasSpike(ENUM_TIMEFRAMES tf = PERIOD_CURRENT, double threshold = 2.0) - Check for volume spike
GetStatus(ENUM_TIMEFRAMES tf = PERIOD_CURRENT) - Get volume status string
HasDivergence(ENUM_TIMEFRAMES tf = PERIOD_CURRENT, int period = 5) - Check for divergence
IsClimaxVolume(ENUM_TIMEFRAMES tf = PERIOD_CURRENT, int lookback = 20) - Check for climax
SetSpikeThreshold(double threshold) - Set spike threshold
SetClimaxThreshold(double threshold) - Set climax threshold
SetDefaultTimeframe(ENUM_TIMEFRAMES tf) - Set default timeframe
IsInitialized() - Check if initialized
GetSymbol() - Get symbol
ConfigureTradePackageIntegration(bool enable = true, double bullWeight = 0.6, double bearWeight = 0.6) - Configure TP integration
DisplayOnChart(ENUM_TIMEFRAMES tf = PERIOD_CURRENT, int corner = 2, int x = 10, int y = 20) - Display on chart
GetTradePackageComponent(ENUM_TIMEFRAMES tf = PERIOD_CURRENT) - Get ComponentDisplay for TradePackage
GetVolumeScoreForTradePackage(ENUM_TIMEFRAMES tf = PERIOD_CURRENT, string expectedDirection = "") - Get TP-formatted score
GetDirectionalBias(double &bullScore, double &bearScore, double &overallConfidence, ENUM_TIMEFRAMES tf = PERIOD_CURRENT) - Get bias scores
GetTradeRecommendation(ENUM_TIMEFRAMES tf = PERIOD_CURRENT) - Get trade recommendation
GetConfirmationStatus(ENUM_TIMEFRAMES tf = PERIOD_CURRENT) - Get confirmation status
+++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++

+++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++
ONLY PROCEED IF THIS FINE NEEDS INDICATORS PLEASE PREFER MY FILES AND FUNCITONS:
AT THE TOP AFTER THE FILE NAME LET IT STATE THAT ITS ALREADY INTERGRATED THIS IF YOU THIS PROCESS IS APPLICABLE
FOR SimpleRSI.mqh PLEASE PREFER MY FILES AND FUNCITONS:
+++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++
SimpleRSI(string symbol, ENUM_TIMEFRAMES tf = PERIOD_H1, int period = 14, IndicatorManager* indicatorMgr = NULL)
RSIBias GetBiasAndConfidence(int lookback = 20)
void PopulateTradePackage(TradePackage &package, int lookback = 20)
ComponentDisplay GetComponentDisplay(int lookback = 15)
void AddToComponentsArray(ComponentDisplay &components[], int lookback = 15)
bool IsBullishBias(int lookback = 10)
bool IsBearishBias(int lookback = 10)
double GetNetBiasScore(int lookback = 10)
double GetConfidence(int lookback = 10)
double GetCurrentRSI()
void SetIndicatorManager(IndicatorManager* indicatorMgr)
bool IsUsingIndicatorManager() const

UltraSimpleRSI (Static Class)
++++++++++++++++++++++++++++
static void GetBias(string symbol, ENUM_TIMEFRAMES tf, double &biasScore, double &confidence, IndicatorManager* indicatorMgr = NULL)
static bool IsBullish(string symbol, ENUM_TIMEFRAMES tf, IndicatorManager* indicatorMgr = NULL)
+++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++

+++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++
ONLY PROCEED IF THIS FINE NEEDS INDICATORS PLEASE PREFER MY FILES AND FUNCITONS:
AT THE TOP AFTER THE FILE NAME LET IT STATE THAT ITS ALREADY INTERGRATED THIS IF YOU THIS PROCESS IS APPLICABLE
FOR MARKET DATA PLEASE PREFER MY FILES AND FUNCITONS:
+++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++
CandlestickPatterns.mqh
+++++++++++++++++++++++++++
CandlestickPatternAnalyzer() - Constructor
Initialize(string symbol, ENUM_TIMEFRAMES timeframe, int maxBars = 100)
Deinitialize()
OnTick()
OnTimer()
OnTradeTransaction()
AnalyzeCurrentPattern(int shift = 1)
GetPatternScore(int shift = 1)
GetPatternSignal(int shift = 1)
IsHammer(const CandleData &candle)
IsInvertedHammer(const CandleData &candle)
IsShootingStar(const CandleData &candle)
IsHangingMan(const CandleData &candle)
IsSpinningTop(const CandleData &candle)
IsMarubozuBullish(const CandleData &candle)
IsMarubozuBearish(const CandleData &candle)
IsDoji(const CandleData &candle, ENUM_CANDLE_PATTERN &dojiType)
CheckBullishEngulfing(CandleData &candle1, CandleData &candle2)
CheckBearishEngulfing(CandleData &candle1, CandleData &candle2)
CheckHarami(CandleData &candle1, CandleData &candle2, bool bullish)
CheckPiercingLine(CandleData &candle1, CandleData &candle2)
CheckDarkCloudCover(CandleData &candle1, CandleData &candle2)
CheckMorningStar(CandleData &candle1, CandleData &candle2, CandleData &candle3)
CheckEveningStar(CandleData &candle1, CandleData &candle2, CandleData &candle3)
CheckThreeWhiteSoldiers(CandleData &candle1, CandleData &candle2, CandleData &candle3)
CheckThreeBlackCrows(CandleData &candle1, CandleData &candle2, CandleData &candle3)
CheckSingleCandlePattern(CandleData &candle)
CheckTwoCandlePattern(CandleData &candle1, CandleData &candle2)
CheckThreeCandlePattern(CandleData &candle1, CandleData &candle2, CandleData &candle3)
GetCandleData(int shift)
GetDojiDescription(ENUM_CANDLE_PATTERN dojiType)
PatternToString(ENUM_CANDLE_PATTERN pattern)
IsInitialized()
GetSymbol()
GetTimeframe()
HasStrongPattern(int shift = 1)
GetSimpleDirection(int shift = 1)
+++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++

+++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++
ONLY PROCEED IF THIS FINE NEEDS INDICATORS PLEASE PREFER MY FILES AND FUNCITONS:
AT THE TOP AFTER THE FILE NAME LET IT STATE THAT ITS ALREADY INTERGRATED THIS IF YOU THIS PROCESS IS APPLICABLE
FOR MARKET DATA PLEASE PREFER MY FILES AND FUNCITONS:
+++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++
CandlestickPatterns.mqh
++++++++++++++++++++++++++++
CandlestickPatternAnalyzer() - Constructor
Initialize(string symbol, ENUM_TIMEFRAMES timeframe, int maxBars = 100)
Deinitialize()
OnTick()
OnTimer()
OnTradeTransaction(const MqlTradeTransaction& trans, const MqlTradeRequest& request, const MqlTradeResult& result)
AnalyzeCurrentPattern(int shift = 1)
GetPatternScore(int shift = 1)
GetPatternSignal(int shift = 1)
IsHammer(const CandleData &candle)
IsInvertedHammer(const CandleData &candle)
IsShootingStar(const CandleData &candle)
IsHangingMan(const CandleData &candle)
IsSpinningTop(const CandleData &candle)
IsMarubozuBullish(const CandleData &candle)
IsMarubozuBearish(const CandleData &candle)
IsDoji(const CandleData &candle, ENUM_CANDLE_PATTERN &dojiType)
CheckBullishEngulfing(CandleData &candle1, CandleData &candle2)
CheckBearishEngulfing(CandleData &candle1, CandleData &candle2)
CheckHarami(CandleData &candle1, CandleData &candle2, bool bullish)
CheckPiercingLine(CandleData &candle1, CandleData &candle2)
CheckDarkCloudCover(CandleData &candle1, CandleData &candle2)
CheckMorningStar(CandleData &candle1, CandleData &candle2, CandleData &candle3)
CheckEveningStar(CandleData &candle1, CandleData &candle2, CandleData &candle3)
CheckThreeWhiteSoldiers(CandleData &candle1, CandleData &candle2, CandleData &candle3)
CheckThreeBlackCrows(CandleData &candle1, CandleData &candle2, CandleData &candle3)
CheckSingleCandlePattern(CandleData &candle)
CheckTwoCandlePattern(CandleData &candle1, CandleData &candle2)
CheckThreeCandlePattern(CandleData &candle1, CandleData &candle2, CandleData &candle3)
UpdateChartComments()
ShowScoreOnChart(const PatternResult &result)
GetCandleData(int shift)
GetDojiDescription(ENUM_CANDLE_PATTERN dojiType)
PatternToString(ENUM_CANDLE_PATTERN pattern)
TimeframeToString(ENUM_TIMEFRAMES tf)
IsInitialized()
GetSymbol()
GetTimeframe()
HasStrongPattern(int shift = 1)
GetSimpleDirection(int shift = 1)
SetDebugEnabled(bool enabled)
SetChartUpdateFrequency(int seconds)

+++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++

+++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++
ONLY PROCEED IF THIS FINE NEEDS INDICATORS PLEASE PREFER MY FILES AND FUNCITONS:
AT THE TOP AFTER THE FILE NAME LET IT STATE THAT ITS ALREADY INTERGRATED THIS IF YOU THIS PROCESS IS APPLICABLE
FOR MARKET DATA PLEASE PREFER MY FILES AND FUNCITONS:
+++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++

+++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++

+++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++
ONLY PROCEED IF THIS FINE NEEDS INDICATORS PLEASE PREFER MY FILES AND FUNCITONS:
AT THE TOP AFTER THE FILE NAME LET IT STATE THAT ITS ALREADY INTERGRATED THIS IF YOU THIS PROCESS IS APPLICABLE
FOR MARKET DATA PLEASE PREFER MY FILES AND FUNCITONS:
+++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++

+++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++

+++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++
ONLY PROCEED IF THIS FINE NEEDS INDICATORS PLEASE PREFER MY FILES AND FUNCITONS:
AT THE TOP AFTER THE FILE NAME LET IT STATE THAT ITS ALREADY INTERGRATED THIS IF YOU THIS PROCESS IS APPLICABLE
FOR MARKET DATA PLEASE PREFER MY FILES AND FUNCITONS:
+++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++

+++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++

+++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++
ONLY PROCEED IF THIS FINE NEEDS INDICATORS PLEASE PREFER MY FILES AND FUNCITONS:
AT THE TOP AFTER THE FILE NAME LET IT STATE THAT ITS ALREADY INTERGRATED THIS IF YOU THIS PROCESS IS APPLICABLE
FOR MARKET DATA PLEASE PREFER MY FILES AND FUNCITONS:
+++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++

+++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++

+++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++
ONLY PROCEED IF THIS FINE NEEDS INDICATORS PLEASE PREFER MY FILES AND FUNCITONS:
AT THE TOP AFTER THE FILE NAME LET IT STATE THAT ITS ALREADY INTERGRATED THIS IF YOU THIS PROCESS IS APPLICABLE
FOR MARKET DATA PLEASE PREFER MY FILES AND FUNCITONS:
+++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++

+++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++

+++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++
ONLY PROCEED IF THIS FINE NEEDS INDICATORS PLEASE PREFER MY FILES AND FUNCITONS:
AT THE TOP AFTER THE FILE NAME LET IT STATE THAT ITS ALREADY INTERGRATED THIS IF YOU THIS PROCESS IS APPLICABLE
FOR MARKET DATA PLEASE PREFER MY FILES AND FUNCITONS:
+++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++

+++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++




BUILDING A UTIL FILE WITH STATIC, STATELESS FUNCTIONS
++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++
make this file static and make sure all functions are stateless
add the necessary time functions i may have missed if any 
make all functions static and mql5 friendly
make sure functions are stateless




FILE PERMORMANCE OPTIMIZATION INSTRUCTIONS
++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++
Please optimize the performance of this MQL5 code by removing 
excessive logging, debugging prints, and unnecessary console output 
while preserving essential error messages and critical information. 

Follow these guidelines:
- Remove ALL debug prints (Print()) from tight loops like OnTick(), OnTimer(), and position update functions
- Keep only essential logs for:
    - Initialization/deinitialization
    - Trade execution (open/close/modify)
    - Error conditions
    - Major state changes (emergency stop, risk level changes)
    - Configuration changes
- Optimize logging frequency:
    - Replace frequent prints with periodic summaries (e.g., every 1 minutes)
    - Use static timers to limit print frequency
    - Aggregate multiple messages into single prints
- Remove redundant information:
    - Dont log the same status repeatedly
    - Combine related information into single messages
    - Remove timestamp prefixes if MT5 already adds them
- Preserve critical information:
    - Keep trade execution confirmations
    - Keep error messages and warnings
    - Keep risk limit violations
    - Keep account/position state changes

ALSO:
- Cache position data to avoid repeated PositionGet calls in loops
- Use PrintFormat() instead of multiple Print() calls
- Remove logging from hot paths (functions called every tick)
- Add log levels enum RESOURCE_MANAGER
        {
            OBSERVE,
            AUTHORIZE,
            WARN,
            ENFORCE,
            AUDIT
        };
    with configurable verbosity
- Use static variables to track last log time and prevent spamming
- Move detailed logs to separate debug functions that are conditionally called
- Batch similar messages into periodic status reports instead of tick-by-tick logging
- Focus on the most performance-critical areas:
    - OnTick() and OnTimer() methods
    - Position update loops
    - Indicator calculations
    - Market data processing functions**

    
DO NOT CHANGE FUNCTIONALITY PLEASE
USE MINIMAL CODE CHANGE



CREATE AN MQL5 CHART COMMENT DASHBOARD
++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++
Create an optimized MQL5 chart comment file (.mqh) that displays essential account and trading information with maximum performance. This should be a visual dashboard that traders can place on their charts for at-a-glance monitoring.

## PERFORMANCE REQUIREMENTS:
- **Ultra-efficient updates**: Update only when values change, not every tick
- **Minimal CPU usage**: Use caching, static variables, and update throttling
- **Zero tight-loop operations**: No heavy calculations in OnTick()
- **Memory efficient**: Reuse string buffers, no string concatenation in loops
- **Clean rendering**: No flickering, smooth updates

## ESSENTIAL DISPLAY ELEMENTS (Creative & Visual):

### 1. ACCOUNT STATUS PANEL (Top Left)
```
╔══════════════════════════════════╗
║        ACCOUNT STATUS            ║
╠══════════════════════════════════╣
║ Balance:     $25,430.75  📊      ║
║ Equity:      $26,120.50  ↗️      ║
║ Margin:      $1,230.75   🔒      ║
║ Free Margin: $24,889.75  ✅      ║
║ Margin Level: 2,123%     🛡️      ║
║ Daily P/L:   +$689.75    📈      ║
╚══════════════════════════════════╝
```

### 2. TRADING SESSION INFO (Top Right)
```
╔══════════════════════════════════╗
║       TRADING SESSION            ║
╠══════════════════════════════════╣
║ Session:    LONDON/NY OVERLAP    ║
║ Time Left:  02:15:43     ⏳      ║
║ Volatility: HIGH          🌊      ║
║ Spread:     1.2 pips     ⚡      ║
║ Trend:      BULLISH      ↗️      ║
║ Market Hours: 09:00-17:00🕐      ║
╚══════════════════════════════════╝
```

### 3. POSITIONS OVERVIEW (Center Left)
```
╔══════════════════════════════════╗
║      ACTIVE POSITIONS            ║
╠══════════════════════════════════╣
║ EUR/USD:   BUY  1.0950  +$320    ║
║            SL: 1.0900   TP:1.1050║
║            R:R 3.2:1    🔥       ║
╠══════════════════════════════════╣
║ GBP/USD:   SELL 1.2650  -$45     ║
║            SL: 1.2700   TP:1.2550║
║            R:R 2.0:1    ⚠️       ║
╠══════════════════════════════════╣
║ TOTAL:     2 positions  +$275    ║
║            Risk: 1.8%    ✅      ║
╚══════════════════════════════════╝
```

### 4. PERFORMANCE METRICS (Center Right)
```
╔══════════════════════════════════╗
║    PERFORMANCE METRICS           ║
╠══════════════════════════════════╣
║ Win Rate:       68%      🎯      ║
║ Profit Factor:  2.4      💎      ║
║ Avg Win:       +$420     🚀      ║
║ Avg Loss:      -$180     🛡️      ║
║ Max DD:        -$1,200   ⚠️      ║
║ Recovery:      85%       📈      ║
║ Consecutive:   4 wins    🔥      ║
╚══════════════════════════════════╝
```

### 5. RISK MANAGEMENT (Bottom Left)
```
╔══════════════════════════════════╗
║      RISK MANAGEMENT             ║
╠══════════════════════════════════╣
║ Max Risk/Trade:   2.0%   ✅      ║
║ Daily Loss Limit: 5.0%   🛡️      ║
║ Max Positions:    5      🎯      ║
║ Current Risk:     1.8%   📊      ║
║ Available Risk:   3.2%   💰      ║
║ Volatility Adj:   85%    🌊      ║
║ Safety Level:     GREEN  ✅      ║
╚══════════════════════════════════╝
```

### 6. MARKET CONDITIONS (Bottom Right)
```
╔══════════════════════════════════╗
║     MARKET CONDITIONS            ║
╠══════════════════════════════════╣
║ Trend Strength:  85%     ↗️      ║
║ Market Phase:   ACCUMULATION    ║
║ Volatility:      HIGH    🌊      ║
║ Liquidity:       GOOD    💧      ║
║ News Impact:     MEDIUM  📰      ║
║ Bias:           BULLISH  🐂      ║
║ Confidence:      78%     ✅      ║
╚══════════════════════════════════╝
```

OnTick() (every 10s)
    ↓
IndicatorManager.UpdateAllModulesAndExecute()
    ↓
[Modules update TradePackage fields]
    ↓
TradePackage.UpdateAndExecute()
    ↓
TradePackage.ProcessAndExecute() → DecisionEngine → Trade Execution




















































































































































//+------------------------------------------------------------------+
//|                                              MarketRegime.mqh    |
//|           Advanced Market Regime & Lifecycle Detection System   |
//+------------------------------------------------------------------+
#property copyright "Copyright 2024"
#property version "1.00"
#property strict

#include <Math\Stat\Math.mqh>
#include <ChartObjects\ChartObjectsLines.mqh>
#include <ChartObjects\ChartObjectsShapes.mqh>
#include <ChartObjects\ChartObjectsTxtControls.mqh>

//+------------------------------------------------------------------+
//| Market Regime Types                                             |
//+------------------------------------------------------------------+
enum ENUM_MARKET_REGIME
{
    REGIME_TRENDING_UP,   // Strong uptrend
    REGIME_TRENDING_DOWN, // Strong downtrend
    REGIME_RANGING,       // Sideways/consolidation
    REGIME_UNKNOWN        // Unable to determine
};

//+------------------------------------------------------------------+
//| Market Lifecycle States                                         |
//+------------------------------------------------------------------+
enum ENUM_MARKET_LIFECYCLE
{
    LIFECYCLE_RANGE_FORMING,     // Consolidation forming (needs validation)
    LIFECYCLE_RANGE_ACTIVE,      // Validated range/consolidation
    LIFECYCLE_BREAKOUT_DETECTED, // Range broken, initial breakout
    LIFECYCLE_TREND_CONFIRMED,   // Breakout confirmed, trend established
    LIFECYCLE_PULLBACK_FORMING,  // Trend pullback forming
    LIFECYCLE_PULLBACK_ACTIVE,   // Validated pullback range
    LIFECYCLE_TREND_RESUMING,    // Pullback broken, trend resuming
    LIFECYCLE_TREND_WEAKENING,   // Trend losing momentum
    LIFECYCLE_UNKNOWN
};

//+------------------------------------------------------------------+
//| Regime Detection Result                                         |
//+------------------------------------------------------------------+
struct RegimeResult
{
    ENUM_MARKET_REGIME regime; // Primary regime classification
    double confidence;         // 0-100% confidence in detection
    double trendStrength;      // -100 to +100 (negative=bearish, positive=bullish)
    double rangeStrength;      // 0-100 strength of ranging conditions
    string description;        // Human-readable description

    // Constructor
    RegimeResult()
    {
        regime = REGIME_UNKNOWN;
        confidence = 0.0;
        trendStrength = 0.0;
        rangeStrength = 0.0;
        description = "Not analyzed";
    }

    // String representation
    string ToString() const
    {
        string regimeStr;
        switch (regime)
        {
        case REGIME_TRENDING_UP:
            regimeStr = "TRENDING_UP";
            break;
        case REGIME_TRENDING_DOWN:
            regimeStr = "TRENDING_DOWN";
            break;
        case REGIME_RANGING:
            regimeStr = "RANGING";
            break;
        default:
            regimeStr = "UNKNOWN";
            break;
        }

        return StringFormat("%s (%.0f%%) | Trend: %.1f | Range: %.1f | %s",
                            regimeStr, confidence, trendStrength, rangeStrength, description);
    }

    // Quick helper methods
    bool IsTrending() const { return regime == REGIME_TRENDING_UP || regime == REGIME_TRENDING_DOWN; }
    bool IsRanging() const { return regime == REGIME_RANGING; }
    bool IsUpTrend() const { return regime == REGIME_TRENDING_UP; }
    bool IsDownTrend() const { return regime == REGIME_TRENDING_DOWN; }
};

//+------------------------------------------------------------------+
//| Lifecycle State Structure                                       |
//+------------------------------------------------------------------+
struct LifecycleState
{
    ENUM_MARKET_LIFECYCLE state;
    datetime startTime;
    int durationBars;
    string description;

    LifecycleState()
    {
        state = LIFECYCLE_UNKNOWN;
        startTime = 0;
        durationBars = 0;
        description = "Initial state";
    }

    string ToString() const
    {
        return StringFormat("%s | Started: %s | Bars: %d | %s",
                            GetStateString(state),
                            TimeToString(startTime, TIME_DATE | TIME_MINUTES),
                            durationBars,
                            description);
    }

    static string GetStateString(ENUM_MARKET_LIFECYCLE s)
    {
        switch (s)
        {
        case LIFECYCLE_RANGE_FORMING:
            return "RANGE_FORMING";
        case LIFECYCLE_RANGE_ACTIVE:
            return "RANGE_ACTIVE";
        case LIFECYCLE_BREAKOUT_DETECTED:
            return "BREAKOUT_DETECTED";
        case LIFECYCLE_TREND_CONFIRMED:
            return "TREND_CONFIRMED";
        case LIFECYCLE_PULLBACK_FORMING:
            return "PULLBACK_FORMING";
        case LIFECYCLE_PULLBACK_ACTIVE:
            return "PULLBACK_ACTIVE";
        case LIFECYCLE_TREND_RESUMING:
            return "TREND_RESUMING";
        case LIFECYCLE_TREND_WEAKENING:
            return "TREND_WEAKENING";
        default:
            return "UNKNOWN";
        }
    }
};

//+------------------------------------------------------------------+
//| Range Information Structure                                     |
//+------------------------------------------------------------------+
struct RangeInfo
{
    double top;
    double bottom;
    double widthPct;
    datetime startTime;
    int touchCount;
    bool validated;
    int barsSinceFormation;
    double currentPosition; // 0-100% where price is within range (NEW)

    // Default constructor
    RangeInfo()
    {
        top = 0.0;
        bottom = 0.0;
        widthPct = 0.0;
        startTime = 0;
        touchCount = 0;
        validated = false;
        barsSinceFormation = 0;
        currentPosition = 50.0; // Default to middle
    }

    // Copy constructor
    RangeInfo(const RangeInfo &other)
    {
        this = other;
    }

    // Assignment operator
    void operator=(const RangeInfo &other)
    {
        top = other.top;
        bottom = other.bottom;
        widthPct = other.widthPct;
        startTime = other.startTime;
        touchCount = other.touchCount;
        validated = other.validated;
        barsSinceFormation = other.barsSinceFormation;
        currentPosition = other.currentPosition;
    }

    bool IsValid() const { return top > bottom && widthPct > 0.1; }

    bool IsPriceInside(double price) const
    {
        return price >= bottom && price <= top;
    }

    bool IsPriceAbove(double price) const
    {
        return price > top;
    }

    bool IsPriceBelow(double price) const
    {
        return price < bottom;
    }

    // NEW: Calculate where price is within the range (0-100%)
    double CalculatePosition(double price) const
    {
        if (!IsValid())
            return 50.0;

        if (price >= top)
            return 100.0;
        if (price <= bottom)
            return 0.0;

        return ((price - bottom) / (top - bottom)) * 100.0;
    }

    string ToString() const
    {
        return StringFormat("Range: %.5f-%.5f (%.2f%%) | Pos: %.1f%% | Touches: %d | Valid: %s",
                            bottom, top, widthPct, currentPosition, touchCount, validated ? "Yes" : "No");
    }
};

//+------------------------------------------------------------------+
//| Trend Information Structure                                     |
//+------------------------------------------------------------------+
struct TrendInfo
{
    ENUM_MARKET_REGIME direction;
    double startPrice;
    datetime startTime;
    double strongestPoint;
    double currentPrice;
    int trendBars;
    double totalMovePct;

    TrendInfo()
    {
        direction = REGIME_UNKNOWN;
        startPrice = 0.0;
        startTime = 0;
        strongestPoint = 0.0;
        currentPrice = 0.0;
        trendBars = 0;
        totalMovePct = 0.0;
    }

    void Update(double price)
    {
        currentPrice = price;
        trendBars++;

        if (direction == REGIME_TRENDING_UP)
        {
            strongestPoint = MathMax(strongestPoint, price);
            totalMovePct = ((price - startPrice) / startPrice) * 100.0;
        }
        else if (direction == REGIME_TRENDING_DOWN)
        {
            strongestPoint = MathMin(strongestPoint, price);
            totalMovePct = ((startPrice - price) / startPrice) * 100.0;
        }
    }

    double GetCurrentRetracementPct() const
    {
        if (direction == REGIME_TRENDING_UP)
        {
            double highest = MathMax(startPrice, strongestPoint);
            double denominator = (highest - startPrice);

            // FIX: Check for zero denominator
            if (MathAbs(denominator) < 0.00001) // Use a small epsilon instead of exact zero
                return 0.0;

            return ((highest - currentPrice) / denominator) * 100.0;
        }
        else if (direction == REGIME_TRENDING_DOWN)
        {
            double lowest = MathMin(startPrice, strongestPoint);
            double denominator = (startPrice - lowest);

            // FIX: Check for zero denominator
            if (MathAbs(denominator) < 0.00001) // Use a small epsilon instead of exact zero
                return 0.0;

            return ((currentPrice - lowest) / denominator) * 100.0;
        }
        return 0.0;
    }

    bool IsRetracementSignificant(double threshold = 20.0) const
    {
        return GetCurrentRetracementPct() > threshold;
    }

    string ToString() const
    {
        string dirStr = (direction == REGIME_TRENDING_UP) ? "UP" : (direction == REGIME_TRENDING_DOWN) ? "DOWN"
                                                                                                       : "NONE";
        return StringFormat("Trend %s | Start: %.5f | Move: %.2f%% | Retrace: %.1f%%",
                            dirStr, startPrice, totalMovePct, GetCurrentRetracementPct());
    }
};

//+------------------------------------------------------------------+
//| Market Regime Detection Class                                   |
//+------------------------------------------------------------------+
class MarketRegimeDetector
{
private:
    string m_symbol;
    ENUM_TIMEFRAMES m_timeframe;
    int m_lookbackPeriod;

    // Detection parameters
    double m_adxTrendThreshold;
    double m_adxRangeThreshold;
    double m_slopeThreshold;
    double m_atrRatioThreshold;
    double m_stdDevThreshold;

public:
    // Constructor
    MarketRegimeDetector(string symbol = NULL, ENUM_TIMEFRAMES tf = PERIOD_H1)
    {
        m_symbol = (symbol == NULL) ? Symbol() : symbol;
        m_timeframe = tf;
        m_lookbackPeriod = 24; // 24 bars default

        // Default thresholds (optimize these for your trading style)
        m_adxTrendThreshold = 25.0; // ADX above 25 = trending
        m_adxRangeThreshold = 20.0; // ADX below 20 = ranging
        m_slopeThreshold = 0.04;    // % slope per bar for trend was 0.08
        m_atrRatioThreshold = 1.1;  // Price change / ATR ratio was 2.5
        m_stdDevThreshold = 80.0;   // % of prices inside 2 std dev bands was 70
    }

    //+------------------------------------------------------------------+
    //| MAIN REGIME DETECTION FUNCTION                                 |
    //+------------------------------------------------------------------+
    RegimeResult DetectRegime()
    {
        RegimeResult result;

        // 1. Calculate all mathematical indicators
        double adxValue = CalculateADX();
        double slope = CalculateLinearRegressionSlope();
        double priceChangeRatio = CalculatePriceChangeATRRatio();
        double stdDevScore = CalculateStdDevContainment();
        double rangeTouches = CalculateRangeTouches();
        double netPriceChange = CalculateNetPriceChange();

        // 2. Calculate trend strength (-100 to +100)
        result.trendStrength = CalculateTrendStrength(adxValue, slope, priceChangeRatio, netPriceChange);

        // 3. Calculate range strength (0 to 100)
        result.rangeStrength = CalculateRangeStrength(adxValue, stdDevScore, rangeTouches, netPriceChange);

        // 4. Apply decision rules
        result = ApplyDecisionRules(result, adxValue, slope, stdDevScore, priceChangeRatio, rangeTouches);

        // 5. Set description
        result.description = GenerateDescription(result, adxValue, slope);

        return result;
    }

    //+------------------------------------------------------------------+
    //| TREND STRENGTH CALCULATION                                      |
    //+------------------------------------------------------------------+
    double CalculateTrendStrength(double adx, double slope, double priceChangeRatio, double netChange)
    {
        double strength = 0.0;

        // 1. Slope direction (primary)
        if (MathAbs(slope) > m_slopeThreshold)
        {
            strength = (slope > 0) ? 60.0 : -60.0;
        }

        // 2. ADX confirmation
        if (adx > m_adxTrendThreshold)
        {
            double adxFactor = MathMin(1.0, (adx - m_adxTrendThreshold) / 20.0);
            strength *= (1.0 + adxFactor * 0.5);
        }
        else if (adx < m_adxRangeThreshold)
        {
            // Reduce trend strength if ADX is low
            strength *= 0.7;
        }

        // 3. Price change to ATR ratio
        if (priceChangeRatio > m_atrRatioThreshold)
        {
            strength *= (1.0 + (priceChangeRatio - m_atrRatioThreshold) * 0.3);
        }

        // 4. Net price change adjustment
        if (MathAbs(netChange) > 1.0)
        {
            strength *= (1.0 + MathAbs(netChange) * 0.1);
        }

        // Cap between -100 and +100
        if (strength > 100.0)
            strength = 100.0;
        if (strength < -100.0)
            strength = -100.0;

        return strength;
    }

    //+------------------------------------------------------------------+
    //| RANGE STRENGTH CALCULATION                                      |
    //+------------------------------------------------------------------+
    double CalculateRangeStrength(double adx, double stdDevScore, double rangeTouches, double netChange)
    {
        double strength = 0.0;

        // 1. Low ADX
        if (adx < m_adxRangeThreshold)
        {
            strength += 40.0;
        }

        // 2. High standard deviation containment
        if (stdDevScore > m_stdDevThreshold)
        {
            strength += 30.0;
        }

        // 3. Range touches
        strength += rangeTouches * 0.3;

        // 4. Low net movement
        if (MathAbs(netChange) < 1.0)
        {
            strength += 20.0;
        }

        // Cap at 100
        if (strength > 100.0)
            strength = 100.0;

        return strength;
    }

    //+------------------------------------------------------------------+
    //| DECISION RULES                                                  |
    //+------------------------------------------------------------------+
    RegimeResult ApplyDecisionRules(RegimeResult &result, double adx, double slope,
                                    double stdDevScore, double priceChangeRatio, double rangeTouches)
    {
        bool isTrendingByRules = false;
        bool isRangingByRules = false;

        // RULE 1: ADX > threshold AND significant slope → TRENDING
        if (adx > m_adxTrendThreshold && MathAbs(slope) > m_slopeThreshold)
        {
            isTrendingByRules = true;
        }

        // RULE 2: Low ADX AND high std dev containment → RANGING
        if (adx < m_adxRangeThreshold && stdDevScore > m_stdDevThreshold)
        {
            isRangingByRules = true;
        }

        // RULE 3: PriceChange/ATR ratio > threshold → TRENDING
        if (priceChangeRatio > m_atrRatioThreshold)
        {
            isTrendingByRules = true;
        }

        // RULE 4: Multiple range touches → RANGING
        if (rangeTouches > 60.0) // Approximately 3 touches
        {
            isRangingByRules = true;
        }

        // Apply rules
        if (isTrendingByRules && !isRangingByRules)
        {
            // Clear trending case
            result.regime = (result.trendStrength > 0) ? REGIME_TRENDING_UP : REGIME_TRENDING_DOWN;
            result.confidence = MathAbs(result.trendStrength);
        }
        else if (!isTrendingByRules && isRangingByRules)
        {
            // Clear ranging case
            result.regime = REGIME_RANGING;
            result.confidence = result.rangeStrength;
        }
        else if (isTrendingByRules && isRangingByRules)
        {
            // Conflicting signals - use strength comparison
            if (MathAbs(result.trendStrength) > result.rangeStrength)
            {
                result.regime = (result.trendStrength > 0) ? REGIME_TRENDING_UP : REGIME_TRENDING_DOWN;
                result.confidence = MathAbs(result.trendStrength);
            }
            else
            {
                result.regime = REGIME_RANGING;
                result.confidence = result.rangeStrength;
            }
        }
        else
        {
            // Weak signals - use Golden Ratio
            double goldenScore = (slope * 100.0) + (adx * 2.0) + (priceChangeRatio * 10.0);

            if (goldenScore > 100.0 && MathAbs(result.trendStrength) > 60.0)
            {
                result.regime = (result.trendStrength > 0) ? REGIME_TRENDING_UP : REGIME_TRENDING_DOWN;
                result.confidence = MathAbs(result.trendStrength);
            }
            else if (goldenScore < 50.0 && result.rangeStrength > 60.0)
            {
                result.regime = REGIME_RANGING;
                result.confidence = result.rangeStrength;
            }
            else
            {
                // Uncertain
                result.regime = REGIME_UNKNOWN;
                result.confidence = MathMax(result.rangeStrength, MathAbs(result.trendStrength));
            }
        }

        // Ensure confidence is capped
        if (result.confidence > 100.0)
            result.confidence = 100.0;

        return result;
    }

    //+------------------------------------------------------------------+
    //| INDICATOR CALCULATION FUNCTIONS                                 |
    //+------------------------------------------------------------------+

    // Calculate ADX value
    double CalculateADX(int period = 14)
    {
        int handle = iADX(m_symbol, m_timeframe, period);
        if (handle == INVALID_HANDLE)
            return 0.0;

        double adx[];
        ArraySetAsSeries(adx, true);

        if (CopyBuffer(handle, 0, 0, 1, adx) > 0)
        {
            IndicatorRelease(handle);
            return adx[0];
        }

        IndicatorRelease(handle);
        return 0.0;
    }

    // Calculate linear regression slope (% per bar)
    double CalculateLinearRegressionSlope()
    {
        double closes[];
        ArrayResize(closes, m_lookbackPeriod);
        ArraySetAsSeries(closes, true);

        // Get close prices
        for (int i = 0; i < m_lookbackPeriod; i++)
        {
            closes[i] = iClose(m_symbol, m_timeframe, i);
        }

        // Calculate slope using least squares
        double sumX = 0.0, sumY = 0.0, sumXY = 0.0, sumX2 = 0.0;

        for (int i = 0; i < m_lookbackPeriod; i++)
        {
            sumX += i;
            sumY += closes[i];
            sumXY += i * closes[i];
            sumX2 += i * i;
        }

        double N = (double)m_lookbackPeriod;
        double slope = (N * sumXY - sumX * sumY) / (N * sumX2 - sumX * sumX);

        // FIX: Check for zero or very small close price before division
        double lastClose = closes[m_lookbackPeriod - 1];
        if (MathAbs(lastClose) < 0.00001)
            return 0.0;

        // Convert to percentage change per bar
        return (slope / lastClose) * 100.0;
    }

    // Calculate Price Change / ATR Ratio
    double CalculatePriceChangeATRRatio()
    {
        double currentPrice = iClose(m_symbol, m_timeframe, 0);
        double firstPrice = iClose(m_symbol, m_timeframe, m_lookbackPeriod - 1);

        // FIX: Check for zero or negative prices
        if (firstPrice <= 0.0 || currentPrice <= 0.0)
            return 0.0;

        // Calculate price change percentage
        double priceChangePct = MathAbs((currentPrice - firstPrice) / firstPrice) * 100.0;

        // Calculate ATR percentage
        double atr = CalculateATR(14);

        // FIX: Check for zero ATR
        if (atr <= 0.0 || currentPrice <= 0.0)
            return 0.0;

        double atrPct = (atr / currentPrice) * 100.0;

        if (atrPct > 0.0)
        {
            return priceChangePct / atrPct;
        }

        return 0.0;
    }

    // Calculate Standard Deviation Containment (% of prices inside 2 std dev bands)
    double CalculateStdDevContainment()
    {
        double closes[];
        ArrayResize(closes, m_lookbackPeriod);

        // Get close prices
        for (int i = 0; i < m_lookbackPeriod; i++)
        {
            closes[i] = iClose(m_symbol, m_timeframe, i);
        }

        // Calculate mean
        double mean = 0.0;
        for (int i = 0; i < m_lookbackPeriod; i++)
        {
            mean += closes[i];
        }
        mean /= m_lookbackPeriod;

        // Calculate standard deviation
        double variance = 0.0;
        for (int i = 0; i < m_lookbackPeriod; i++)
        {
            double diff = closes[i] - mean;
            variance += diff * diff;
        }
        variance /= m_lookbackPeriod;
        double stdDev = MathSqrt(variance);

        // Create bands at +/- 2 standard deviations
        double upperBand = mean + (2.0 * stdDev);
        double lowerBand = mean - (2.0 * stdDev);

        // Count how many closes are inside the bands
        int insideCount = 0;
        for (int i = 0; i < m_lookbackPeriod; i++)
        {
            if (closes[i] >= lowerBand && closes[i] <= upperBand)
            {
                insideCount++;
            }
        }

        // Return percentage
        return ((double)insideCount / m_lookbackPeriod) * 100.0;
    }

    // Calculate range touches (support/resistance)
    double CalculateRangeTouches()
    {
        // Find highest high and lowest low in lookback period
        double highest = 0.0;
        double lowest = 1e10;

        for (int i = 0; i < m_lookbackPeriod; i++)
        {
            double high = iHigh(m_symbol, m_timeframe, i);
            double low = iLow(m_symbol, m_timeframe, i);

            if (high > highest)
                highest = high;
            if (low < lowest)
                lowest = low;
        }

        if (highest <= lowest)
            return 0.0;

        // Calculate ATR for tolerance
        double atr = CalculateATR(14);
        double currentPrice = iClose(m_symbol, m_timeframe, 0);
        double tolerance = (atr / currentPrice) * 100.0 * 0.5;

        // Count touches of support and resistance
        int touchCount = 0;

        for (int i = 0; i < m_lookbackPeriod; i++)
        {
            double high = iHigh(m_symbol, m_timeframe, i);
            double low = iLow(m_symbol, m_timeframe, i);

            // Check if price touched resistance (within tolerance)
            if (MathAbs((high - highest) / currentPrice * 100.0) < tolerance)
            {
                touchCount++;
            }

            // Check if price touched support (within tolerance)
            if (MathAbs((low - lowest) / currentPrice * 100.0) < tolerance)
            {
                touchCount++;
            }
        }

        // Calculate score (max 100)
        double touchScore = MathMin(100.0, touchCount * 20.0);

        // Adjust for range width (narrower ranges score higher)
        double rangeWidthPct = ((highest - lowest) / currentPrice) * 100.0;
        double widthScore = MathMax(0.0, 100.0 - (rangeWidthPct * 40.0));

        // Combine scores
        return (touchScore * 0.6 + widthScore * 0.4);
    }

    // Calculate net price change percentage
    double CalculateNetPriceChange()
    {
        double currentPrice = iClose(m_symbol, m_timeframe, 0);
        double firstPrice = iClose(m_symbol, m_timeframe, m_lookbackPeriod - 1);

        if (firstPrice <= 0.0)
            return 0.0;

        return ((currentPrice - firstPrice) / firstPrice) * 100.0;
    }

    // Calculate ATR
    double CalculateATR(int period = 14)
    {
        int handle = iATR(m_symbol, m_timeframe, period);
        if (handle == INVALID_HANDLE)
            return 0.0;

        double atr[];
        ArraySetAsSeries(atr, true);

        if (CopyBuffer(handle, 0, 0, 1, atr) > 0)
        {
            IndicatorRelease(handle);
            return atr[0];
        }

        IndicatorRelease(handle);
        return 0.0;
    }

    //+------------------------------------------------------------------+
    //| HELPER FUNCTIONS                                                |
    //+------------------------------------------------------------------+

    // Generate description for the regime
    string GenerateDescription(RegimeResult &result, double adx, double slope)
    {
        string desc = "";

        switch (result.regime)
        {
        case REGIME_TRENDING_UP:
            desc = StringFormat("Strong Uptrend | ADX: %.1f > %.1f | Slope: +%.3f%%/bar",
                                adx, m_adxTrendThreshold, MathAbs(slope));
            break;

        case REGIME_TRENDING_DOWN:
            desc = StringFormat("Strong Downtrend | ADX: %.1f > %.1f | Slope: -%.3f%%/bar",
                                adx, m_adxTrendThreshold, MathAbs(slope));
            break;

        case REGIME_RANGING:
            desc = StringFormat("Sideways Market | ADX: %.1f < %.1f | Consolidation",
                                adx, m_adxRangeThreshold);
            break;

        default:
            desc = "Market unclear - mixed signals detected";
            break;
        }

        return desc;
    }

    //+------------------------------------------------------------------+
    //| SETTER METHODS                                                  |
    //+------------------------------------------------------------------+

    void SetSymbol(string symbol) { m_symbol = symbol; }
    void SetTimeframe(ENUM_TIMEFRAMES tf) { m_timeframe = tf; }
    void SetLookbackPeriod(int period) { m_lookbackPeriod = period; }

    void SetThresholds(double adxTrend = 25.0, double adxRange = 20.0,
                       double slope = 0.08, double atrRatio = 2.5,
                       double stdDev = 70.0)
    {
        m_adxTrendThreshold = adxTrend;
        m_adxRangeThreshold = adxRange;
        m_slopeThreshold = slope;
        m_atrRatioThreshold = atrRatio;
        m_stdDevThreshold = stdDev;
    }
};

//+------------------------------------------------------------------+
//| Market Lifecycle Tracker Class                                  |
//+------------------------------------------------------------------+
class MarketLifecycleTracker
{
private:
    string m_symbol;
    ENUM_TIMEFRAMES m_timeframe;

    // Current state
    LifecycleState m_currentState;
    RangeInfo m_currentRange;
    TrendInfo m_currentTrend;
    RangeInfo m_pullbackRange;

    // Detection parameters
    int m_minRangeTouches;
    int m_minPullbackTouches;
    double m_breakoutMargin; // ATR multiplier
    double m_minRangeWidthPct;
    double m_pullbackThresholdPct;

    // History
    double m_lastPrices[5];
    datetime m_lastUpdateTime;

    int m_rangeInitializationBars; // Bars to use for initial range
    bool m_rangeInitialized;       // Track if range is initialized

public:
    // Constructor
    MarketLifecycleTracker(string symbol = NULL, ENUM_TIMEFRAMES tf = PERIOD_H1)
    {
        m_symbol = (symbol == NULL) ? Symbol() : symbol;
        m_timeframe = tf;

        // Initialize with default parameters
        m_minRangeTouches = 3;
        m_minPullbackTouches = 2;
        m_breakoutMargin = 0.3;        // 0.3 * ATR
        m_minRangeWidthPct = 0.5;      // Minimum 0.5% range width
        m_pullbackThresholdPct = 20.0; // 20% retracement to consider pullback

        m_rangeInitializationBars = 5; // Use last 5 bars for initialization
        m_rangeInitialized = false;

        Reset();
    }

    //+------------------------------------------------------------------+
    //| MAIN UPDATE FUNCTION                                           |
    //+------------------------------------------------------------------+
    LifecycleState Update()
    {
        // Check if it's a new bar
        if (!IsNewBar())
            return m_currentState;

        // 1. Get current price
        double currentPrice = iClose(m_symbol, m_timeframe, 0);

        // 2. Update price history
        UpdatePriceHistory(currentPrice);

        // 3. Check for range reinitialization (NEW)
        CheckForRangeReinitialization(currentPrice);

        // 4. Process based on current state
        switch (m_currentState.state)
        {
        case LIFECYCLE_UNKNOWN:
            InitialDetection(currentPrice);
            break;

        case LIFECYCLE_RANGE_FORMING:
            ProcessRangeForming(currentPrice);
            break;

        case LIFECYCLE_RANGE_ACTIVE:
            ProcessRangeActive(currentPrice);
            break;

        case LIFECYCLE_BREAKOUT_DETECTED:
            ProcessBreakoutDetected(currentPrice);
            break;

        case LIFECYCLE_TREND_CONFIRMED:
            ProcessTrendConfirmed(currentPrice);
            break;

        case LIFECYCLE_PULLBACK_FORMING:
            ProcessPullbackForming(currentPrice);
            break;

        case LIFECYCLE_PULLBACK_ACTIVE:
            ProcessPullbackActive(currentPrice);
            break;

        case LIFECYCLE_TREND_RESUMING:
            ProcessTrendResuming(currentPrice);
            break;

        case LIFECYCLE_TREND_WEAKENING:
            ProcessTrendWeakening(currentPrice);
            break;
        }

        // 4. Update state duration
        UpdateStateDuration();

        return m_currentState;
    }

    //+------------------------------------------------------------------+
    //| STATE PROCESSING FUNCTIONS                                      |
    //+------------------------------------------------------------------+

private:
    bool ShouldReinitializeForPullback(double price)
    {
        // Only reinitialize during pullback formation if:
        // 1. We're in a pullback state
        // 2. A valid range is forming (width > minWidth)

        if (m_currentState.state == LIFECYCLE_PULLBACK_FORMING ||
            m_currentState.state == LIFECYCLE_PULLBACK_ACTIVE)
        {
            // Check if pullback range has formed with sufficient width
            if (m_pullbackRange.IsValid() &&
                m_pullbackRange.widthPct >= m_minRangeWidthPct)
            {
                // Valid pullback range detected - time to reinitialize main range
                Print("Valid pullback range detected (width=" +
                      DoubleToString(m_pullbackRange.widthPct, 2) +
                      "%), reinitializing main range");
                return true;
            }
        }

        return false;
    }

    bool ShouldReinitializeForNewRange(double price)
    {
        // Check if we're transitioning to a new range state
        if (m_currentState.state == LIFECYCLE_RANGE_FORMING)
        {
            // Wait for range to form with sufficient width
            if (m_currentRange.IsValid() &&
                m_currentRange.widthPct >= m_minRangeWidthPct &&
                m_currentRange.touchCount >= m_minRangeTouches)
            {
                // Valid new range formed
                return true;
            }
        }

        return false;
    }

    void InitialDetection(double price)
    {
        // Use MarketRegimeDetector to determine initial state
        MarketRegimeDetector detector(m_symbol, m_timeframe);
        RegimeResult regime = detector.DetectRegime();

        if (regime.IsRanging() && regime.confidence > 60)
        {
            // Start with range detection
            m_currentState.state = LIFECYCLE_RANGE_FORMING;
            InitializeRange();
            m_currentState.description = "Initial range detection started";
        }
        else if (regime.IsTrending() && regime.confidence > 70)
        {
            // Already in trend
            m_currentState.state = LIFECYCLE_TREND_CONFIRMED;
            InitializeTrend(regime);
            m_currentState.description = "Already in confirmed trend";
        }
        else
        {
            m_currentState.description = "Market unclear, waiting for formation";
        }
    }

    void ProcessRangeForming(double price)
    {
        // Update range boundaries if price extends beyond current range
        UpdateRangeBoundaries(price);

        // Check for touches
        if (IsRangeTouch(price))
        {
            m_currentRange.touchCount++;
            m_currentState.description = StringFormat("Range forming - Touch %d/%d",
                                                      m_currentRange.touchCount, m_minRangeTouches);
        }

        // Validate range after enough touches
        if (m_currentRange.touchCount >= m_minRangeTouches)
        {
            m_currentRange.validated = true;
            m_currentState.state = LIFECYCLE_RANGE_ACTIVE;
            m_currentState.description = "Range validated and active";
        }

        // Check for early breakout
        if (CheckRangeBreakout(price))
        {
            m_currentState.state = LIFECYCLE_BREAKOUT_DETECTED;
            m_currentState.description = "Early breakout from forming range";
        }
    }

    void ProcessRangeActive(double price)
    {
        // Update range if price extends it
        UpdateRangeBoundaries(price);

        // Check for breakout
        if (CheckRangeBreakout(price))
        {
            m_currentState.state = LIFECYCLE_BREAKOUT_DETECTED;
            m_currentState.description = "Breakout detected from active range";
        }
        else if (IsRangeTouch(price))
        {
            m_currentRange.touchCount++;
            m_currentState.description = StringFormat("Range active - Touch %d",
                                                      m_currentRange.touchCount);
        }
    }

    void ProcessBreakoutDetected(double price)
    {
        // Check for breakout confirmation (3 consecutive closes outside range)
        if (ConfirmBreakout(price))
        {
            // Determine trend direction
            MarketRegimeDetector detector(m_symbol, m_timeframe);
            RegimeResult regime = detector.DetectRegime();

            if (regime.IsTrending())
            {
                m_currentState.state = LIFECYCLE_TREND_CONFIRMED;
                InitializeTrend(regime);
                m_currentState.description = "Breakout confirmed, trend established";
            }
        }
        else if (CheckFalseBreakout(price))
        {
            // Price returned to range
            m_currentState.state = LIFECYCLE_RANGE_ACTIVE;
            m_currentState.description = "False breakout, range still active";
        }
    }

    void ProcessTrendConfirmed(double price)
    {
        // Update trend information
        m_currentTrend.Update(price);

        // Check for pullback
        if (m_currentTrend.IsRetracementSignificant(m_pullbackThresholdPct))
        {
            m_currentState.state = LIFECYCLE_PULLBACK_FORMING;
            InitializePullbackRange(price);
            m_currentState.description = StringFormat("Pullback forming (%.1f%% retracement)",
                                                      m_currentTrend.GetCurrentRetracementPct());
        }
        else
        {
            m_currentState.description = StringFormat("Trend continuing - Move: %.2f%%",
                                                      m_currentTrend.totalMovePct);
        }
    }

    void ProcessPullbackForming(double price)
    {
        // Update pullback range (this can expand normally)
        UpdatePullbackRange(price);

        // Check for touches
        if (IsPullbackTouch(price))
        {
            m_pullbackRange.touchCount++;
            m_currentState.description = StringFormat("Pullback forming - Touch %d/%d",
                                                      m_pullbackRange.touchCount, m_minPullbackTouches);
        }

        // Validate pullback after enough touches
        if (m_pullbackRange.touchCount >= m_minPullbackTouches)
        {
            m_pullbackRange.validated = true;
            m_currentState.state = LIFECYCLE_PULLBACK_ACTIVE;
            m_currentState.description = "Pullback range validated";

            // Check if pullback range is wide enough to reinitialize main range
            if (m_pullbackRange.widthPct >= m_minRangeWidthPct)
            {
                Print("Wide pullback detected - will reinitialize main range on breakout");
            }
        }

        // Check if trend resumes
        if (CheckPullbackBreakout(price))
        {
            m_currentState.state = LIFECYCLE_TREND_RESUMING;
            m_currentState.description = "Trend resuming from pullback";
        }
    }

    void ProcessPullbackActive(double price)
    {
        // Check for breakout from pullback range
        if (CheckPullbackBreakout(price))
        {
            m_currentState.state = LIFECYCLE_TREND_RESUMING;
            m_currentState.description = "Breakout from pullback range";

            // When breakout occurs, check if we should use pullback as new range
            if (m_pullbackRange.widthPct >= m_minRangeWidthPct)
            {
                // Pullback was significant - reinitialize main range
                m_currentRange = m_pullbackRange;
                m_currentRange.startTime = TimeCurrent();
                m_rangeInitialized = true;

                Print("Main range reinitialized from significant pullback after breakout");
            }
        }
        else if (IsPullbackTouch(price))
        {
            m_pullbackRange.touchCount++;
            m_currentState.description = StringFormat("Pullback active - Touch %d",
                                                      m_pullbackRange.touchCount);
        }
    }

    void ProcessTrendResuming(double price)
    {
        // Check for trend resumption confirmation
        if (ConfirmTrendResumption(price))
        {
            m_currentState.state = LIFECYCLE_TREND_CONFIRMED;
            // Clear pullback range
            m_pullbackRange = RangeInfo();
            m_currentState.description = "Trend fully resumed";
        }
    }

    void ProcessTrendWeakening(double price)
    {
        // Check if trend regains strength or transitions to range
        MarketRegimeDetector detector(m_symbol, m_timeframe);
        RegimeResult regime = detector.DetectRegime();

        if (regime.confidence > 70 && regime.IsTrending())
        {
            m_currentState.state = LIFECYCLE_TREND_CONFIRMED;
            m_currentState.description = "Trend regained strength";
        }
        else if (regime.IsRanging() && regime.confidence > 60)
        {
            // Transition back to range
            m_currentState.state = LIFECYCLE_RANGE_FORMING;
            InitializeRange();
            m_currentState.description = "Trend ended, new range forming";
        }
    }

    //+------------------------------------------------------------------+
    //| HELPER FUNCTIONS                                                |
    //+------------------------------------------------------------------+

    bool CheckRangeBreakout(double price)
    {
        if (!m_currentRange.IsValid())
            return false;

        double atr = CalculateATR(14);
        double margin = atr * m_breakoutMargin;

        if (price > m_currentRange.top + margin)
        {
            m_currentTrend.direction = REGIME_TRENDING_UP;
            return true;
        }
        else if (price < m_currentRange.bottom - margin)
        {
            m_currentTrend.direction = REGIME_TRENDING_DOWN;
            return true;
        }

        return false;
    }

    bool ConfirmBreakout(double price)
    {
        // Need 3 consecutive closes outside range
        int consecutive = 0;
        for (int i = 0; i < 3; i++)
        {
            double closePrice = iClose(m_symbol, m_timeframe, i);

            if (m_currentTrend.direction == REGIME_TRENDING_UP)
            {
                if (closePrice > m_currentRange.top)
                    consecutive++;
            }
            else if (m_currentTrend.direction == REGIME_TRENDING_DOWN)
            {
                if (closePrice < m_currentRange.bottom)
                    consecutive++;
            }
        }

        return consecutive >= 3;
    }

    bool CheckFalseBreakout(double price)
    {
        if (!m_currentRange.IsValid())
            return false;

        // If price returns inside the range, it's a false breakout
        return m_currentRange.IsPriceInside(price);
    }

    bool CheckPullbackBreakout(double price)
    {
        if (!m_pullbackRange.IsValid())
            return false;

        // Breakout from pullback in the direction of the main trend
        if (m_currentTrend.direction == REGIME_TRENDING_UP)
        {
            return price > m_pullbackRange.top;
        }
        else if (m_currentTrend.direction == REGIME_TRENDING_DOWN)
        {
            return price < m_pullbackRange.bottom;
        }

        return false;
    }

    bool ConfirmTrendResumption(double price)
    {
        // Price should continue in trend direction
        MarketRegimeDetector detector(m_symbol, m_timeframe);
        RegimeResult regime = detector.DetectRegime();

        if (m_currentTrend.direction == REGIME_TRENDING_UP)
        {
            return regime.IsUpTrend() && regime.confidence > 60;
        }
        else if (m_currentTrend.direction == REGIME_TRENDING_DOWN)
        {
            return regime.IsDownTrend() && regime.confidence > 60;
        }

        return false;
    }

    bool IsRangeTouch(double price)
    {
        if (!m_currentRange.IsValid())
            return false;

        double atr = CalculateATR(14);
        double tolerance = atr * 0.1; // 0.1 * ATR tolerance

        // Check if price touches range boundaries
        return (MathAbs(price - m_currentRange.top) < tolerance) ||
               (MathAbs(price - m_currentRange.bottom) < tolerance);
    }

    bool IsPullbackTouch(double price)
    {
        if (!m_pullbackRange.IsValid())
            return false;

        double atr = CalculateATR(14);
        double tolerance = atr * 0.1;

        return (MathAbs(price - m_pullbackRange.top) < tolerance) ||
               (MathAbs(price - m_pullbackRange.bottom) < tolerance);
    }

    void UpdateRangeBoundaries(double price)
    {
        if (!m_rangeInitialized)
        {
            InitializeRange();
            return;
        }

        // FIXED RANGE: Only update position, NEVER change boundaries
        m_currentRange.currentPosition = m_currentRange.CalculatePosition(price);
    }

    // NEW: Check for pullback-based reinitialization
    void ProcessStateForReinitialization()
    {
        // Only reinitialize during pullback->range transitions
        if ((m_currentState.state == LIFECYCLE_PULLBACK_ACTIVE ||
             m_currentState.state == LIFECYCLE_TREND_WEAKENING) &&
            m_pullbackRange.IsValid() &&
            m_pullbackRange.widthPct >= m_minRangeWidthPct)
        {
            // Use pullback range as new main range
            m_currentRange = m_pullbackRange;
            Print("Range reinitialized from pullback: width=" + 
                  DoubleToString(m_pullbackRange.widthPct, 2) + "%");
        }
    }

    void UpdatePullbackRange(double price)
    {
        if (!m_pullbackRange.IsValid())
        {
            m_pullbackRange.top = price;
            m_pullbackRange.bottom = price;
        }
        else
        {
            m_pullbackRange.top = MathMax(m_pullbackRange.top, price);
            m_pullbackRange.bottom = MathMin(m_pullbackRange.bottom, price);
        }

        double mid = (m_pullbackRange.top + m_pullbackRange.bottom) / 2;
        m_pullbackRange.widthPct = ((m_pullbackRange.top - m_pullbackRange.bottom) / mid) * 100.0;
    }

    void InitializeRange()
    {
        // Always use last 5 bars to find initial range
        int bars = 5;
        double highest = 0;
        double lowest = 1e10;

        for (int i = 0; i < bars; i++)
        {
            double high = iHigh(m_symbol, m_timeframe, i);
            double low = iLow(m_symbol, m_timeframe, i);

            if (high > highest)
                highest = high;
            if (low < lowest)
                lowest = low;
        }

        m_currentRange.top = highest;
        m_currentRange.bottom = lowest;
        m_currentRange.startTime = TimeCurrent();
        m_currentRange.touchCount = 1;
        m_currentRange.validated = false;

        double mid = (highest + lowest) / 2;
        if (mid > 0)
            m_currentRange.widthPct = ((highest - lowest) / mid) * 100.0;
        else
            m_currentRange.widthPct = 0.0;

        double currentPrice = iClose(m_symbol, m_timeframe, 0);
        m_currentRange.currentPosition = m_currentRange.CalculatePosition(currentPrice);

        m_rangeInitialized = true;

        Print(StringFormat("FIXED Range Initialized: Top=%.5f, Bottom=%.5f, Width=%.2f%%, Bars: 5",
                           highest, lowest, m_currentRange.widthPct));
    }

    void InitializeTrend(RegimeResult &regime)
    {
        m_currentTrend.direction = regime.regime;
        m_currentTrend.startPrice = iClose(m_symbol, m_timeframe, 0);
        m_currentTrend.startTime = TimeCurrent();
        m_currentTrend.strongestPoint = m_currentTrend.startPrice;
        m_currentTrend.currentPrice = m_currentTrend.startPrice;
        m_currentTrend.trendBars = 0;
        m_currentTrend.totalMovePct = 0.0;
    }

    void InitializePullbackRange(double price)
    {
        m_pullbackRange.top = price;
        m_pullbackRange.bottom = price;
        m_pullbackRange.startTime = TimeCurrent();
        m_pullbackRange.touchCount = 1;
        m_pullbackRange.validated = false;
        m_pullbackRange.widthPct = 0.0;
    }

    void UpdatePriceHistory(double price)
    {
        // Shift array
        for (int i = 4; i > 0; i--)
        {
            m_lastPrices[i] = m_lastPrices[i - 1];
        }
        m_lastPrices[0] = price;
    }

    void UpdateStateDuration()
    {
        m_currentState.durationBars++;
    }

    bool IsNewBar()
    {
        datetime currentTime = iTime(m_symbol, m_timeframe, 0);
        if (m_lastUpdateTime != currentTime)
        {
            m_lastUpdateTime = currentTime;
            return true;
        }
        return false;
    }

    // Utility function from original detector
    double CalculateATR(int period = 14)
    {
        int handle = iATR(m_symbol, m_timeframe, period);
        if (handle == INVALID_HANDLE)
            return 0.0;

        double atr[];
        ArraySetAsSeries(atr, true);

        if (CopyBuffer(handle, 0, 0, 1, atr) > 0)
        {
            IndicatorRelease(handle);
            return atr[0];
        }

        IndicatorRelease(handle);
        return 0.0;
    }

public:
    //+------------------------------------------------------------------+
    //| GETTER METHODS                                                  |
    //+------------------------------------------------------------------+

    LifecycleState GetCurrentState() const { return m_currentState; }
    RangeInfo GetCurrentRange() const { return m_currentRange; }
    TrendInfo GetCurrentTrend() const { return m_currentTrend; }
    RangeInfo GetPullbackRange() const { return m_pullbackRange; }

    //+------------------------------------------------------------------+
    //| SETTER METHODS                                                  |
    //+------------------------------------------------------------------+

    void SetParameters(int minRangeTouches = 3, int minPullbackTouches = 2,
                       double breakoutMargin = 0.3, double minRangeWidthPct = 0.5,
                       double pullbackThresholdPct = 20.0)
    {
        m_minRangeTouches = minRangeTouches;
        m_minPullbackTouches = minPullbackTouches;
        m_breakoutMargin = breakoutMargin;
        m_minRangeWidthPct = minRangeWidthPct;
        m_pullbackThresholdPct = pullbackThresholdPct;
    }

    void Reset()
    {
        m_currentState = LifecycleState();
        m_currentRange = RangeInfo();
        m_currentTrend = TrendInfo();
        m_pullbackRange = RangeInfo();
        m_lastUpdateTime = 0;
        ArrayInitialize(m_lastPrices, 0.0);
    }

    //+------------------------------------------------------------------+
    //| QUICK STATUS CHECK                                              |
    //+------------------------------------------------------------------+

    string GetFullStatus() const
    {
        string status = "=== MARKET LIFECYCLE STATUS ===\n";
        status += "State: " + m_currentState.ToString() + "\n";

        if (m_currentRange.IsValid())
        {
            status += "Current Range: " + m_currentRange.ToString() + "\n";
        }

        if (m_currentTrend.direction != REGIME_UNKNOWN)
        {
            status += "Current Trend: " + m_currentTrend.ToString() + "\n";
        }

        if (m_pullbackRange.IsValid())
        {
            status += "Pullback Range: " + m_pullbackRange.ToString() + "\n";
        }

        return status;
    }

    // Called from Update() to check for reinitialization
    void CheckForRangeReinitialization(double price)
    {
        // Reinitialize only in specific conditions:

        // 1. When transitioning from trend to pullback with valid range
        if (ShouldReinitializeForPullback(price))
        {
            // Use pullback range as the new main range
            m_currentRange = m_pullbackRange;
            m_currentRange.startTime = TimeCurrent();
            m_rangeInitialized = true;

            Print("Main range reinitialized from pullback: " + m_currentRange.ToString());
            return;
        }

        // 2. When a new range is actively forming (after trend ends)
        if (ShouldReinitializeForNewRange(price))
        {
            // Already have a valid range, just mark as initialized
            m_rangeInitialized = true;
            Print("New range established: " + m_currentRange.ToString());
        }

        // 3. Never reinitialize due to price moving outside range
        //    (that's the whole point of fixed ranges!)
    }
};

//+------------------------------------------------------------------+
//| Market Analysis Manager Class                                   |
//+------------------------------------------------------------------+
class MarketManager
{
private:
    MarketRegimeDetector m_regimeDetector;
    MarketLifecycleTracker m_lifecycleTracker;
    string m_symbol;
    ENUM_TIMEFRAMES m_timeframe;

public:
    MarketManager(string symbol = NULL, ENUM_TIMEFRAMES tf = PERIOD_H1) : m_regimeDetector(symbol, tf),
                                                                          m_lifecycleTracker(symbol, tf)
    {
        m_symbol = (symbol == NULL) ? Symbol() : symbol;
        m_timeframe = tf;
    }

    //+------------------------------------------------------------------+
    //| PUBLIC INTERFACE METHODS                                        |
    //+------------------------------------------------------------------+

    // Unified analysis
    struct MarketAnalysis
    {
        RegimeResult regime;      // Current regime
        LifecycleState lifecycle; // Current lifecycle state
        datetime analysisTime;
        double currentPrice;

        string ToString() const
        {
            string result = "=== MARKET ANALYSIS ===\n";
            result += "Time: " + TimeToString(analysisTime, TIME_DATE | TIME_SECONDS) + "\n";
            result += "Price: " + DoubleToString(currentPrice, 5) + "\n";
            result += "Regime: " + regime.ToString() + "\n";
            result += "Lifecycle: " + lifecycle.ToString() + "\n";
            return result;
        }
    };

    MarketAnalysis GetCompleteAnalysis()
    {
        MarketAnalysis analysis;
        analysis.regime = m_regimeDetector.DetectRegime();
        analysis.lifecycle = m_lifecycleTracker.Update();
        analysis.analysisTime = TimeCurrent();
        analysis.currentPrice = iClose(m_symbol, m_timeframe, 0);

        return analysis;
    }

    //+------------------------------------------------------------------+
    //| REGIME DETECTOR WRAPPERS                                        |
    //+------------------------------------------------------------------+

    RegimeResult DetectRegime()
    {
        return m_regimeDetector.DetectRegime();
    }

    void SetRegimeThresholds(double adxTrend = 25.0, double adxRange = 20.0,
                             double slope = 0.08, double atrRatio = 2.5,
                             double stdDev = 70.0)
    {
        m_regimeDetector.SetThresholds(adxTrend, adxRange, slope, atrRatio, stdDev);
    }

    void SetRegimeSymbol(string symbol)
    {
        m_regimeDetector.SetSymbol(symbol);
    }

    void SetRegimeTimeframe(ENUM_TIMEFRAMES tf)
    {
        m_regimeDetector.SetTimeframe(tf);
    }

    void SetRegimeLookback(int period)
    {
        m_regimeDetector.SetLookbackPeriod(period);
    }

    //+------------------------------------------------------------------+
    //| LIFECYCLE TRACKER WRAPPERS                                      |
    //+------------------------------------------------------------------+

    LifecycleState UpdateLifecycle()
    {
        return m_lifecycleTracker.Update();
    }

    string GetLifecycleStatus() const
    {
        return m_lifecycleTracker.GetFullStatus();
    }

    LifecycleState GetCurrentLifecycleState() const
    {
        return m_lifecycleTracker.GetCurrentState();
    }

    RangeInfo GetCurrentRange() const
    {
        return m_lifecycleTracker.GetCurrentRange();
    }

    TrendInfo GetCurrentTrend() const
    {
        return m_lifecycleTracker.GetCurrentTrend();
    }

    void SetLifecycleParameters(int minRangeTouches = 3, int minPullbackTouches = 2,
                                double breakoutMargin = 0.3, double minRangeWidthPct = 0.5,
                                double pullbackThresholdPct = 20.0)
    {
        m_lifecycleTracker.SetParameters(minRangeTouches, minPullbackTouches,
                                         breakoutMargin, minRangeWidthPct,
                                         pullbackThresholdPct);
    }

    void ResetLifecycle()
    {
        m_lifecycleTracker.Reset();
    }

    //+------------------------------------------------------------------+
    //| TRADING SIGNAL GENERATION                                       |
    //+------------------------------------------------------------------+

    bool IsRangeTradingSignal()
    {
        MarketAnalysis analysis = GetCompleteAnalysis();

        // Signal for range trading when:
        // 1. In active range
        // 2. Price near range boundaries
        // 3. Good regime confidence

        if (analysis.lifecycle.state == LIFECYCLE_RANGE_ACTIVE &&
            analysis.regime.IsRanging() &&
            analysis.regime.confidence > 60)
        {
            RangeInfo range = GetCurrentRange();
            double currentPrice = analysis.currentPrice;
            double atr = CalculateATR(14);

            // Check if price near range edges
            double tolerance = atr * 0.15;

            bool nearTop = MathAbs(currentPrice - range.top) < tolerance;
            bool nearBottom = MathAbs(currentPrice - range.bottom) < tolerance;

            return nearTop || nearBottom;
        }

        return false;
    }

    bool IsBreakoutSignal()
    {
        MarketAnalysis analysis = GetCompleteAnalysis();

        // Signal for breakout when:
        // 1. Breakout just detected
        // 2. Or pullback breakout (trend resumption)

        return (analysis.lifecycle.state == LIFECYCLE_BREAKOUT_DETECTED ||
                analysis.lifecycle.state == LIFECYCLE_TREND_RESUMING);
    }

    bool IsTrendFollowingSignal()
    {
        MarketAnalysis analysis = GetCompleteAnalysis();

        // Signal for trend following when:
        // 1. In confirmed trend
        // 2. Good trend strength
        // 3. Not in significant pullback

        return (analysis.lifecycle.state == LIFECYCLE_TREND_CONFIRMED &&
                analysis.regime.IsTrending() &&
                analysis.regime.confidence > 70);
    }

    //+------------------------------------------------------------------+
    //| RISK MANAGEMENT HELPERS                                         |
    //+------------------------------------------------------------------+

    double GetRangeStopLossDistance()
    {
        RangeInfo range = GetCurrentRange();
        if (!range.IsValid())
            return 0.0;

        return (range.top - range.bottom) * 0.5; // Half the range width
    }

    double GetTrendStopLossDistance()
    {
        double atr = CalculateATR(14);
        return atr * 2.0; // 2 ATR for trend trades
    }

    //+------------------------------------------------------------------+
    //| UTILITY METHODS                                                 |
    //+------------------------------------------------------------------+

    void SetSymbol(string symbol)
    {
        m_symbol = symbol;
        m_regimeDetector.SetSymbol(symbol);
        // Note: MarketLifecycleTracker needs to be recreated or have a SetSymbol method
    }

    void SetTimeframe(ENUM_TIMEFRAMES tf)
    {
        m_timeframe = tf;
        m_regimeDetector.SetTimeframe(tf);
        // Note: MarketLifecycleTracker needs to be recreated or have a SetTimeframe method
    }

    string GetSymbol() const { return m_symbol; }
    ENUM_TIMEFRAMES GetTimeframe() const { return m_timeframe; }

    RangeInfo GetPullbackRange() const
    {
        return m_lifecycleTracker.GetPullbackRange();
    }
    
    bool ShouldReinitializeRange() const
    {
        // Check if we're in a pullback with valid range
        LifecycleState state = m_lifecycleTracker.GetCurrentState();
        RangeInfo pullback = m_lifecycleTracker.GetPullbackRange();
        
        return ((state.state == LIFECYCLE_PULLBACK_FORMING || 
                 state.state == LIFECYCLE_PULLBACK_ACTIVE) &&
                pullback.IsValid() && 
                pullback.widthPct >= 0.5); // Using minRangeWidthPct
    }
    
    
    // Manual reinitialization (for debugging)
    void ForceReinitializeRange()
    {
        // You'll need to add a public method to MarketLifecycleTracker
        // m_lifecycleTracker.InitializeRange();
    }

private:
    // Helper to calculate ATR
    double CalculateATR(int period = 14)
    {
        int handle = iATR(m_symbol, m_timeframe, period);
        if (handle == INVALID_HANDLE)
            return 0.0;

        double atr[];
        ArraySetAsSeries(atr, true);

        if (CopyBuffer(handle, 0, 0, 1, atr) > 0)
        {
            IndicatorRelease(handle);
            return atr[0];
        }

        IndicatorRelease(handle);
        return 0.0;
    }
};

//+------------------------------------------------------------------+
//| GLOBAL HELPER FUNCTIONS FOR DECISION ENGINE                    |
//+------------------------------------------------------------------+

// Quick unified analysis
MarketManager::MarketAnalysis QuickMarketAnalysis(string symbol = NULL, ENUM_TIMEFRAMES tf = PERIOD_H1)
{
    static MarketManager *manager = NULL;

    if (manager == NULL || manager.GetSymbol() != symbol || manager.GetTimeframe() != tf)
    {
        if (manager != NULL)
            delete manager;
        manager = new MarketManager(symbol, tf);
    }

    return manager.GetCompleteAnalysis();
}

// Quick trading signal checks
bool IsGoodForRangeTrading(string symbol = NULL, ENUM_TIMEFRAMES tf = PERIOD_H1)
{
    static MarketManager *manager = NULL;

    if (manager == NULL || manager.GetSymbol() != symbol || manager.GetTimeframe() != tf)
    {
        if (manager != NULL)
            delete manager;
        manager = new MarketManager(symbol, tf);
    }

    return manager.IsRangeTradingSignal();
}

bool IsGoodForBreakoutTrading(string symbol = NULL, ENUM_TIMEFRAMES tf = PERIOD_H1)
{
    static MarketManager *manager = NULL;

    if (manager == NULL || manager.GetSymbol() != symbol || manager.GetTimeframe() != tf)
    {
        if (manager != NULL)
            delete manager;
        manager = new MarketManager(symbol, tf);
    }

    return manager.IsBreakoutSignal();
}

bool IsGoodForTrendTrading(string symbol = NULL, ENUM_TIMEFRAMES tf = PERIOD_H1)
{
    static MarketManager *manager = NULL;

    if (manager == NULL || manager.GetSymbol() != symbol || manager.GetTimeframe() != tf)
    {
        if (manager != NULL)
            delete manager;
        manager = new MarketManager(symbol, tf);
    }

    return manager.IsTrendFollowingSignal();
}

//+------------------------------------------------------------------+
//| Market Drawing Manager Class - ADDED TO EXISTING FILE           |
//+------------------------------------------------------------------+
class MarketDrawingManager
{
private:
    MarketManager *m_marketManager;
    long m_chartId;
    string m_symbol;
    ENUM_TIMEFRAMES m_timeframe;
    bool m_enabled;

    // Drawing settings
    color m_trendUpColor;
    color m_trendDownColor;
    color m_rangeColor;
    color m_oldRangeColor;
    color m_unknownColor;
    color m_textColor;
    color m_rangeFillColor;
    color m_positionIndicatorColor;
    int m_fontSize;
    string m_fontName;

    // Range tracking for visualization
    RangeInfo m_currentRangeDisplay;
    RangeInfo m_previousRange;
    datetime m_rangeStartTime;
    bool m_hasValidRange;
    int m_rangeHistoryCount;
    RangeInfo m_rangeHistory[5]; // Keep last 5 ranges for context

    // Drawing objects tracking
    datetime m_lastUpdateTime;
    int m_lastLifecycleState;

public:
    // Constructor
    MarketDrawingManager(string symbol = NULL, ENUM_TIMEFRAMES tf = PERIOD_H1)
    {
        m_symbol = (symbol == NULL) ? Symbol() : symbol;
        m_timeframe = tf;
        m_chartId = ChartID();
        m_enabled = true;

        // Initialize drawing settings
        m_trendUpColor = clrLimeGreen;
        m_trendDownColor = clrRed;
        m_rangeColor = clrDodgerBlue;
        m_oldRangeColor = clrGray;
        m_unknownColor = clrGray;
        m_textColor = clrWhite;
        m_rangeFillColor = (color)ColorToARGB(clrDodgerBlue, 10); // Very transparent
        m_positionIndicatorColor = clrYellow;
        m_fontSize = 10;
        m_fontName = "Arial";

        // Initialize range tracking
        m_currentRangeDisplay = RangeInfo();
        m_previousRange = RangeInfo();
        m_rangeStartTime = 0;
        m_hasValidRange = false;
        m_rangeHistoryCount = 0;

        // Create MarketManager
        m_marketManager = new MarketManager(m_symbol, m_timeframe);
        m_lastUpdateTime = 0;
        m_lastLifecycleState = LIFECYCLE_UNKNOWN;

        Print("MarketDrawingManager initialized for " + m_symbol + " on " + TimeframeToString(m_timeframe));
    }

    // Destructor
    ~MarketDrawingManager()
    {
        RemoveAllDrawings();
        delete m_marketManager;
    }

    //+------------------------------------------------------------------+
    //| PUBLIC INTERFACE                                                |
    //+------------------------------------------------------------------+

    // Enable/disable drawing
    void EnableDrawing(bool enable = true)
    {
        m_enabled = enable;
        if (!enable)
            RemoveAllDrawings();
        else
            Update(true); // Force update when enabling
    }

    bool IsDrawingEnabled() const { return m_enabled; }

    // Update and draw current market state
    void Update(bool force = false)
    {
        if (!m_enabled)
            return;

        // Only update every 30 seconds to prevent flickering
        if (!force && TimeCurrent() - m_lastUpdateTime < 30)
            return;

        m_lastUpdateTime = TimeCurrent();

        // Get current market analysis
        MarketManager::MarketAnalysis analysis = m_marketManager.GetCompleteAnalysis();

        // Get current range and update position percentage
        RangeInfo currentRange = m_marketManager.GetCurrentRange();
        if (currentRange.IsValid())
        {
            // Calculate current position in range
            currentRange.currentPosition = currentRange.CalculatePosition(analysis.currentPrice);

            // Check if this is a new range
            if (!m_currentRangeDisplay.IsValid() ||
                MathAbs(m_currentRangeDisplay.top - currentRange.top) > 0.0001 ||
                MathAbs(m_currentRangeDisplay.bottom - currentRange.bottom) > 0.0001)
            {
                // Save old range to history
                if (m_currentRangeDisplay.IsValid())
                {
                    SaveRangeToHistory(m_currentRangeDisplay);
                }

                // Update current range
                m_currentRangeDisplay = currentRange;
                m_rangeStartTime = TimeCurrent();
                m_hasValidRange = true;
            }
            else
            {
                // Update position in existing range
                m_currentRangeDisplay.currentPosition = currentRange.currentPosition;
                m_currentRangeDisplay.touchCount = currentRange.touchCount;
            }
        }
        else
        {
            m_hasValidRange = false;
        }

        // Remove old drawings
        RemoveAllDrawings();

        // Draw range visualizations if we have valid range
        if (m_hasValidRange)
        {
            DrawRangeInfo(analysis);
            DrawRangePositionIndicator(); // NEW: Shows real-time % within range
        }

        DrawTrendInfo(analysis);
        DrawRangeHistory(); // NEW: Shows previous ranges

        // Update chart
        ChartRedraw(m_chartId);
    }

    // Quick status update (less frequent)
    void OnTimer()
    {
        Update(false);
    }

    //+------------------------------------------------------------------+
    //| DRAWING METHODS                                                 |
    //+------------------------------------------------------------------+

private:
    // Draw range information with enhanced features
    void DrawRangeInfo(const MarketManager::MarketAnalysis &analysis)
    {
        if (!m_currentRangeDisplay.IsValid())
            return;

        double currentPrice = analysis.currentPrice;
        double point = SymbolInfoDouble(m_symbol, SYMBOL_POINT);

        // Draw range top
        string topName = "Range_Top_Line";
        DrawHorizontalLine(topName, m_currentRangeDisplay.top, m_rangeColor, STYLE_DASH, 2,
                           StringFormat("Range Top: %.5f (%.2f%%)", m_currentRangeDisplay.top,
                                        ((m_currentRangeDisplay.top - currentPrice) / currentPrice) * 100.0));

        // Draw range bottom
        string bottomName = "Range_Bottom_Line";
        DrawHorizontalLine(bottomName, m_currentRangeDisplay.bottom, m_rangeColor, STYLE_DASH, 2,
                           StringFormat("Range Bottom: %.5f (%.2f%%)", m_currentRangeDisplay.bottom,
                                        ((currentPrice - m_currentRangeDisplay.bottom) / currentPrice) * 100.0));

        // Draw range area (semi-transparent)
        DrawRectangleOnChart("Range_Area", TimeCurrent() - (PeriodSeconds() * 50),
                             m_currentRangeDisplay.top,
                             TimeCurrent() + (PeriodSeconds() * 10),
                             m_currentRangeDisplay.bottom,
                             ColorToARGB(m_rangeColor, 10),
                             ColorToARGB(m_rangeColor, 100));

        // Draw range info box with position indicator
        string rangeInfoText = StringFormat("Range Width: %.2f%%\nTouches: %d\nCurrent Pos: %.1f%%",
                                            m_currentRangeDisplay.widthPct,
                                            m_currentRangeDisplay.touchCount,
                                            m_currentRangeDisplay.currentPosition);

        DrawTextOnChart("Range_Info", TimeCurrent() + (PeriodSeconds() * 15),
                        m_currentRangeDisplay.bottom + ((m_currentRangeDisplay.top - m_currentRangeDisplay.bottom) * 0.7),
                        rangeInfoText,
                        m_rangeColor, m_fontSize, m_fontName, ANCHOR_LEFT_UPPER);

        // Draw percentage scale on the right side
        DrawRangePercentageScale();
    }

    // NEW: Draw real-time position indicator within range
    void DrawRangePositionIndicator()
    {
        if (!m_currentRangeDisplay.IsValid())
            return;

        double rangeHeight = m_currentRangeDisplay.top - m_currentRangeDisplay.bottom;
        if (rangeHeight <= 0)
            return;

        // Draw position indicator line at current price
        // string positionLineName = "Range_Position_Line";
        // DrawHorizontalLine(positionLineName, iClose(m_symbol, m_timeframe, 0),
        //                    m_positionIndicatorColor, STYLE_DOT, 2,
        //                    StringFormat("Current: %.1f%%", m_currentRangeDisplay.currentPosition));

        // Draw percentage markers every 25%
        for (int i = 25; i <= 75; i += 25)
        {
            double priceLevel = m_currentRangeDisplay.bottom + (rangeHeight * i / 100.0);
            string markerName = "Range_Marker_" + IntegerToString(i);
            DrawHorizontalLine(markerName, priceLevel, clrDarkGray, STYLE_DOT, 1,
                               StringFormat("%d%%", i));

            // Add percentage label
            DrawTextOnChart("Range_Marker_Label_" + IntegerToString(i),
                            TimeCurrent() + (PeriodSeconds() * 5), priceLevel,
                            StringFormat("%d%%", i),
                            clrDarkGray, m_fontSize - 2, m_fontName, ANCHOR_LEFT);
        }
    }

    // NEW: Draw percentage scale on chart
    void DrawRangePercentageScale()
    {
        if (!m_currentRangeDisplay.IsValid())
            return;

        double rangeHeight = m_currentRangeDisplay.top - m_currentRangeDisplay.bottom;
        if (rangeHeight <= 0)
            return;

        // Draw vertical percentage bar
        for (int i = 0; i <= 100; i += 10)
        {
            double priceLevel = m_currentRangeDisplay.bottom + (rangeHeight * i / 100.0);
            string scaleName = "Range_Scale_" + IntegerToString(i);

            // Draw tick mark
            DrawTextOnChart(scaleName, TimeCurrent() - (PeriodSeconds() * 40), priceLevel,
                            StringFormat("%d%%", i),
                            clrGray, m_fontSize - 1, m_fontName, ANCHOR_RIGHT);
        }
    }

    // NEW: Save range to history
    void SaveRangeToHistory(RangeInfo &range)
    {
        if (m_rangeHistoryCount >= 5)
        {
            // Shift array
            for (int i = 0; i < 4; i++)
            {
                m_rangeHistory[i] = m_rangeHistory[i + 1];
            }
            m_rangeHistoryCount = 4;
        }

        m_rangeHistory[m_rangeHistoryCount] = range;
        m_rangeHistoryCount++;
    }

    // NEW: Draw range history
    void DrawRangeHistory()
    {
        for (int i = 0; i < m_rangeHistoryCount; i++)
        {
            if (m_rangeHistory[i].IsValid())
            {
                // Draw faded range lines for historical ranges
                string topName = "Range_History_Top_" + IntegerToString(i);
                string bottomName = "Range_History_Bottom_" + IntegerToString(i);

                DrawHorizontalLine(topName, m_rangeHistory[i].top, m_oldRangeColor, STYLE_DOT, 1,
                                   StringFormat("Old Range Top (%.2f%%)", m_rangeHistory[i].widthPct));
                DrawHorizontalLine(bottomName, m_rangeHistory[i].bottom, m_oldRangeColor, STYLE_DOT, 1,
                                   StringFormat("Old Range Bottom", ""));

                // Faded area
                DrawRectangleOnChart("Range_History_Area_" + IntegerToString(i),
                                     TimeCurrent() - (PeriodSeconds() * 100), m_rangeHistory[i].top,
                                     TimeCurrent() - (PeriodSeconds() * 50), m_rangeHistory[i].bottom,
                                     ColorToARGB(m_oldRangeColor, 5),
                                     ColorToARGB(m_oldRangeColor, 30));
            }
        }
    }

    // Draw trend information
    void DrawTrendInfo(const MarketManager::MarketAnalysis &analysis)
    {
        TrendInfo trend = m_marketManager.GetCurrentTrend();
        if (trend.direction == REGIME_UNKNOWN)
            return;

        // Draw trend info box
        color trendColor = (trend.direction == REGIME_TRENDING_UP) ? m_trendUpColor : m_trendDownColor;

        DrawTextOnChart("Trend_Info", TimeCurrent() + (PeriodSeconds() * 20),
                        trend.currentPrice,
                        StringFormat("Trend: %s\nMove: %.2f%%\nBars: %d",
                                     (trend.direction == REGIME_TRENDING_UP) ? "UP" : "DOWN",
                                     trend.totalMovePct,
                                     trend.trendBars),
                        trendColor, m_fontSize, m_fontName, ANCHOR_LEFT_UPPER);

        // Draw trend line (simplified - connects start to current)
        if (trend.trendBars > 5) // Only draw after enough bars
        {
            string trendLineName = "Trend_Line";
            DrawTrendLine(trendLineName, TimeCurrent() - (PeriodSeconds() * trend.trendBars),
                          trend.startPrice, TimeCurrent(), trend.currentPrice,
                          trendColor, STYLE_SOLID, 2);
        }

        // Draw pullback info if applicable
        double retracePct = trend.GetCurrentRetracementPct();
        if (retracePct > 10.0) // Significant retracement
        {
            DrawTextOnChart("Retracement", TimeCurrent() - (PeriodSeconds() * 2), trend.currentPrice,
                            StringFormat("Retrace: %.1f%%", retracePct),
                            (retracePct > 50.0) ? clrOrangeRed : clrOrange,
                            m_fontSize - 1, m_fontName, ANCHOR_LEFT_LOWER);
        }
    }

    //+------------------------------------------------------------------+
    //| VISUALIZATION HELPERS                                           |
    //+------------------------------------------------------------------+

    void DrawRangeVisualization()
    {
        // Draw dots showing recent price action within range
        for (int i = 0; i < 20; i++)
        {
            double price = iClose(m_symbol, m_timeframe, i);
            if (m_currentRangeDisplay.IsValid() && m_currentRangeDisplay.IsPriceInside(price))
            {
                string dotName = "Range_Dot_" + IntegerToString(i);
                DrawDot(dotName, TimeCurrent() - (PeriodSeconds() * i), price,
                        m_rangeColor, 3);
            }
        }
    }

    void DrawTrendVisualization()
    {
        TrendInfo trend = m_marketManager.GetCurrentTrend();
        if (trend.direction == REGIME_UNKNOWN)
            return;

        // Draw trend momentum dots
        for (int i = 0; i < 10; i++)
        {
            double price = iClose(m_symbol, m_timeframe, i);
            string dotName = "Trend_Dot_" + IntegerToString(i);
            DrawDot(dotName, TimeCurrent() - (PeriodSeconds() * i), price,
                    (trend.direction == REGIME_TRENDING_UP) ? m_trendUpColor : m_trendDownColor,
                    2);
        }
    }

    void DrawPullbackVisualization()
    {
        RangeInfo pullback = m_marketManager.GetPullbackRange();
        if (!pullback.IsValid())
            return;

        // Draw pullback range
        string topName = "Pullback_Top_Line";
        DrawHorizontalLine(topName, pullback.top, clrOrange, STYLE_DOT, 1);

        string bottomName = "Pullback_Bottom_Line";
        DrawHorizontalLine(bottomName, pullback.bottom, clrOrange, STYLE_DOT, 1);

        // Draw pullback area
        DrawRectangleOnChart("Pullback_Area", TimeCurrent() - (PeriodSeconds() * 20),
                             pullback.top, TimeCurrent(), pullback.bottom,
                             ColorToARGB(clrOrange, 10), ColorToARGB(clrOrange, 50));
    }

    void DrawBreakoutVisualization()
    {
        if (!m_currentRangeDisplay.IsValid())
            return;

        // Highlight breakout direction
        double currentPrice = iClose(m_symbol, m_timeframe, 0);
        color breakoutColor = (currentPrice > m_currentRangeDisplay.top) ? m_trendUpColor : m_trendDownColor;

        // Draw breakout arrow
        string arrowName = "Breakout_Arrow";
        double arrowPrice = (currentPrice > m_currentRangeDisplay.top) ? m_currentRangeDisplay.top : m_currentRangeDisplay.bottom;
        DrawArrow(arrowName, TimeCurrent() - PeriodSeconds(), arrowPrice,
                  breakoutColor, (currentPrice > m_currentRangeDisplay.top) ? OBJ_ARROW_UP : OBJ_ARROW_DOWN);
    }

    void DrawTrendResumptionVisualization()
    {
        // Draw resumption confirmation dots
        for (int i = 0; i < 5; i++)
        {
            double price = iClose(m_symbol, m_timeframe, i);
            string dotName = "Resumption_Dot_" + IntegerToString(i);
            DrawDot(dotName, TimeCurrent() - (PeriodSeconds() * i), price,
                    clrLimeGreen, 3);
        }
    }

    //+------------------------------------------------------------------+
    //| DRAWING PRIMITIVES                                              |
    //+------------------------------------------------------------------+

    void DrawText(string name, int x, int y, string text, color clr, int fontSize = 10,
                  string fontName = "Arial", bool bold = false)
    {
        // Delete object if it exists
        if (ObjectFind(m_chartId, name) >= 0)
            ObjectDelete(m_chartId, name);

        ObjectCreate(m_chartId, name, OBJ_LABEL, 0, 0, 0);
        ObjectSetInteger(m_chartId, name, OBJPROP_CORNER, CORNER_LEFT_UPPER);
        ObjectSetInteger(m_chartId, name, OBJPROP_XDISTANCE, x);
        ObjectSetInteger(m_chartId, name, OBJPROP_YDISTANCE, y);
        ObjectSetString(m_chartId, name, OBJPROP_TEXT, text);
        ObjectSetInteger(m_chartId, name, OBJPROP_COLOR, clr);
        ObjectSetInteger(m_chartId, name, OBJPROP_FONTSIZE, fontSize);

        if (bold)
            ObjectSetString(m_chartId, name, OBJPROP_FONT, "Arial Bold");
        else
            ObjectSetString(m_chartId, name, OBJPROP_FONT, fontName);

        ObjectSetInteger(m_chartId, name, OBJPROP_BACK, false);
        ObjectSetInteger(m_chartId, name, OBJPROP_SELECTABLE, false);
    }

    void DrawTextOnChart(string name, datetime time, double price, string text,
                         color clr, int fontSize = 10, string fontName = "Arial",
                         ENUM_ANCHOR_POINT anchor = ANCHOR_CENTER)
    {
        ObjectCreate(m_chartId, name, OBJ_TEXT, 0, time, price);
        ObjectSetString(m_chartId, name, OBJPROP_TEXT, text);
        ObjectSetInteger(m_chartId, name, OBJPROP_COLOR, clr);
        ObjectSetInteger(m_chartId, name, OBJPROP_FONTSIZE, fontSize);
        ObjectSetString(m_chartId, name, OBJPROP_FONT, fontName);
        ObjectSetInteger(m_chartId, name, OBJPROP_ANCHOR, anchor);
        ObjectSetInteger(m_chartId, name, OBJPROP_BACK, false);
        ObjectSetInteger(m_chartId, name, OBJPROP_SELECTABLE, false);
    }

    void DrawRectangle(string name, int x1, int y1, int x2, int y2,
                       color bgColor, color borderColor)
    {
        ObjectCreate(m_chartId, name, OBJ_RECTANGLE_LABEL, 0, 0, 0);
        ObjectSetInteger(m_chartId, name, OBJPROP_CORNER, CORNER_LEFT_UPPER);
        ObjectSetInteger(m_chartId, name, OBJPROP_XDISTANCE, x1);
        ObjectSetInteger(m_chartId, name, OBJPROP_YDISTANCE, y1);
        ObjectSetInteger(m_chartId, name, OBJPROP_XSIZE, x2 - x1);
        ObjectSetInteger(m_chartId, name, OBJPROP_YSIZE, y2 - y1);
        ObjectSetInteger(m_chartId, name, OBJPROP_BGCOLOR, bgColor);
        ObjectSetInteger(m_chartId, name, OBJPROP_BORDER_TYPE, BORDER_FLAT);
        ObjectSetInteger(m_chartId, name, OBJPROP_BORDER_COLOR, borderColor);
        ObjectSetInteger(m_chartId, name, OBJPROP_BACK, true);
        ObjectSetInteger(m_chartId, name, OBJPROP_SELECTABLE, false);
    }

    void DrawRectangleOnChart(string name, datetime time1, double price1,
                              datetime time2, double price2, color bgColor, color borderColor)
    {
        ObjectCreate(m_chartId, name, OBJ_RECTANGLE, 0, time1, price1, time2, price2);
        ObjectSetInteger(m_chartId, name, OBJPROP_COLOR, borderColor);
        ObjectSetInteger(m_chartId, name, OBJPROP_BGCOLOR, bgColor);
        ObjectSetInteger(m_chartId, name, OBJPROP_BACK, true);
        ObjectSetInteger(m_chartId, name, OBJPROP_SELECTABLE, false);
    }

    void DrawHorizontalLine(string name, double price, color clr, ENUM_LINE_STYLE style = STYLE_SOLID,
                            int width = 1, string description = "")
    {
        ObjectCreate(m_chartId, name, OBJ_HLINE, 0, 0, price);
        ObjectSetDouble(m_chartId, name, OBJPROP_PRICE, price);
        ObjectSetInteger(m_chartId, name, OBJPROP_COLOR, clr);
        ObjectSetInteger(m_chartId, name, OBJPROP_STYLE, style);
        ObjectSetInteger(m_chartId, name, OBJPROP_WIDTH, width);
        ObjectSetInteger(m_chartId, name, OBJPROP_BACK, true);
        ObjectSetInteger(m_chartId, name, OBJPROP_SELECTABLE, false);

        if (description != "")
        {
            ObjectSetString(m_chartId, name, OBJPROP_TOOLTIP, description);
        }
    }

    void DrawTrendLine(string name, datetime time1, double price1,
                       datetime time2, double price2, color clr,
                       ENUM_LINE_STYLE style = STYLE_SOLID, int width = 1)
    {
        ObjectCreate(m_chartId, name, OBJ_TREND, 0, time1, price1, time2, price2);
        ObjectSetInteger(m_chartId, name, OBJPROP_COLOR, clr);
        ObjectSetInteger(m_chartId, name, OBJPROP_STYLE, style);
        ObjectSetInteger(m_chartId, name, OBJPROP_WIDTH, width);
        ObjectSetInteger(m_chartId, name, OBJPROP_BACK, true);
        ObjectSetInteger(m_chartId, name, OBJPROP_SELECTABLE, false);
        ObjectSetInteger(m_chartId, name, OBJPROP_RAY, false);
    }

    void DrawDot(string name, datetime time, double price, color clr, int size = 3)
    {
        ObjectCreate(m_chartId, name, OBJ_ARROW, 0, time, price);
        ObjectSetInteger(m_chartId, name, OBJPROP_ARROWCODE, 108);
        ObjectSetInteger(m_chartId, name, OBJPROP_COLOR, clr);
        ObjectSetInteger(m_chartId, name, OBJPROP_WIDTH, size);
        ObjectSetInteger(m_chartId, name, OBJPROP_BACK, false);
        ObjectSetInteger(m_chartId, name, OBJPROP_SELECTABLE, false);
    }

    void DrawArrow(string name, datetime time, double price, color clr, int arrowCode)
    {
        // Delete object if it exists
        if (ObjectFind(m_chartId, name) >= 0)
            ObjectDelete(m_chartId, name);

        ObjectCreate(m_chartId, name, OBJ_ARROW, 0, time, price);
        ObjectSetInteger(m_chartId, name, OBJPROP_ARROWCODE, arrowCode);
        ObjectSetInteger(m_chartId, name, OBJPROP_COLOR, clr);
        ObjectSetInteger(m_chartId, name, OBJPROP_WIDTH, 2);
        ObjectSetInteger(m_chartId, name, OBJPROP_BACK, false);
        ObjectSetInteger(m_chartId, name, OBJPROP_SELECTABLE, false);
    }

    void DrawStrengthBar(string name, int x, int y, double strength, string label, color clr)
    {
        // Draw label
        DrawText(name + "_Label", x, y, label, m_textColor, m_fontSize - 1, m_fontName);

        // Draw background bar
        int barWidth = 100;
        int barHeight = 8;
        DrawRectangle(name + "_BG", x + 90, y - 2, x + 90 + barWidth, y + barHeight + 2,
                      clrDarkGray, clrDarkGray);

        // Draw strength bar (absolute value for display)
        int fillWidth = (int)(barWidth * (MathAbs(strength) / 100.0));
        if (fillWidth > 0)
        {
            DrawRectangle(name + "_Fill", x + 90, y - 2, x + 90 + fillWidth, y + barHeight + 2,
                          clr, clr);
        }

        // Draw strength value with sign
        string sign = strength >= 0 ? "+" : "";
        DrawText(name + "_Value", x + 90 + barWidth + 5, y,
                 StringFormat("%s%.0f", sign, strength), clr, m_fontSize - 1, m_fontName);
    }

    void RemoveAllDrawings()
    {
        int total = ObjectsTotal(m_chartId);
        for (int i = total - 1; i >= 0; i--)
        {
            string name = ObjectName(m_chartId, i);
            if (StringFind(name, "Market_") == 0 ||
                StringFind(name, "Range_") == 0 ||
                StringFind(name, "Trend_") == 0 ||
                StringFind(name, "Lifecycle_") == 0 ||
                StringFind(name, "Pullback_") == 0 ||
                StringFind(name, "Breakout_") == 0 ||
                StringFind(name, "Resumption_") == 0)
            {
                ObjectDelete(m_chartId, name);
            }
        }
    }

    //+------------------------------------------------------------------+
    //| HELPER FUNCTIONS                                                |
    //+------------------------------------------------------------------+

    color GetRegimeColor(ENUM_MARKET_REGIME regime)
    {
        switch (regime)
        {
        case REGIME_TRENDING_UP:
            return m_trendUpColor;
        case REGIME_TRENDING_DOWN:
            return m_trendDownColor;
        case REGIME_RANGING:
            return m_rangeColor;
        default:
            return m_unknownColor;
        }
    }

    color GetLifecycleColor(ENUM_MARKET_LIFECYCLE state)
    {
        switch (state)
        {
        case LIFECYCLE_RANGE_FORMING:
        case LIFECYCLE_RANGE_ACTIVE:
            return m_rangeColor;

        case LIFECYCLE_BREAKOUT_DETECTED:
            return clrOrange;

        case LIFECYCLE_TREND_CONFIRMED:
        case LIFECYCLE_TREND_RESUMING:
            return m_trendUpColor;

        case LIFECYCLE_PULLBACK_FORMING:
        case LIFECYCLE_PULLBACK_ACTIVE:
            return clrOrangeRed;

        case LIFECYCLE_TREND_WEAKENING:
            return clrYellow;

        default:
            return m_unknownColor;
        }
    }

    // NEW: Get color based on position in range
    color GetPositionColor(double position)
    {
        if (position < 20)
            return clrRed; // Near bottom - oversold
        if (position > 80)
            return clrLimeGreen; // Near top - overbought
        if (position < 40)
            return clrOrange; // Lower half
        if (position > 60)
            return clrDodgerBlue; // Upper half
        return clrYellow;         // Middle
    }

    string GetRegimeString(ENUM_MARKET_REGIME regime)
    {
        switch (regime)
        {
        case REGIME_TRENDING_UP:
            return "TRENDING UP";
        case REGIME_TRENDING_DOWN:
            return "TRENDING DOWN";
        case REGIME_RANGING:
            return "RANGING";
        default:
            return "UNKNOWN";
        }
    }

    string TimeframeToString(ENUM_TIMEFRAMES tf)
    {
        switch (tf)
        {
        case PERIOD_M1:
            return "M1";
        case PERIOD_M5:
            return "M5";
        case PERIOD_M15:
            return "M15";
        case PERIOD_M30:
            return "M30";
        case PERIOD_H1:
            return "H1";
        case PERIOD_H4:
            return "H4";
        case PERIOD_D1:
            return "D1";
        case PERIOD_W1:
            return "W1";
        case PERIOD_MN1:
            return "MN";
        default:
            return IntegerToString(tf);
        }
    }

    // Convert color to ARGB with alpha transparency
    uint ColorToARGB(color clr, uchar alpha = 255)
    {
        return ((uint)alpha << 24) | ((uint)clr & 0xFFFFFF);
    }
};

//+------------------------------------------------------------------+
//| Global Market Drawing Functions - ADDED TO EXISTING FILE        |
//+------------------------------------------------------------------+

// Global instance for easy access
MarketDrawingManager *g_marketDrawer = NULL;

// Initialize market drawing
void InitializeMarketDrawing(string symbol = NULL, ENUM_TIMEFRAMES tf = PERIOD_H1)
{
    if (g_marketDrawer != NULL)
    {
        delete g_marketDrawer;
    }

    g_marketDrawer = new MarketDrawingManager(symbol, tf);
    Print("Market Drawing initialized");
}

// Update market drawing
void UpdateMarketDrawing(bool force = false)
{
    if (g_marketDrawer != NULL)
    {
        g_marketDrawer.Update(force);
    }
}

// Toggle market drawing
void ToggleMarketDrawing(bool enable)
{
    if (g_marketDrawer != NULL)
    {
        g_marketDrawer.EnableDrawing(enable);
    }
}

// Cleanup market drawing
void CleanupMarketDrawing()
{
    if (g_marketDrawer != NULL)
    {
        delete g_marketDrawer;
        g_marketDrawer = NULL;
    }
}

// Simple function to check if drawing is enabled
bool IsMarketDrawingEnabled()
{
    return g_marketDrawer != NULL && g_marketDrawer.IsDrawingEnabled();
}


++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++




Recreate this file to be simple as it now has too many things going on.
i am thinking one file rather than so many different classes. just one properly thought of but simple file.

REGIME SIMPLE LOGIC:

Complete Market Regime Framework: States, Lifecycle, Trading Rules & Display:

getMarketRegime()
    first establish a width:
        price high/low in last few bars "5 bars" using H1. 
            this then becomes a fixed range till invalidated.
            when formed the drawing is displayed and when invalidated then no drawing.
        QUICKLY:
            CHECK for state:
                1. ADX/BB/MAs/CANDLES/VOLUME/ATR/OTHER RELEVANT INDICATORS: 
                ADX:
                - < 20 → Ranging/Contraction
                - 25-40 → Trending
                - > 40 → Exhaustion Risk
                ATR/Bollinger Width:
                - Low & falling → Contraction/Squeeze
                - Low & rising → Early Trend
                - High & rising → Late Trend/Expansion
                - High & stable → Range High Vol

                2. Structure:
                - HH/HL or LH/LL → Trending
                - Horizontal S/R → Ranging
                - Failed extremes → Churn/Exhaustion

            return the state //could be trending with low volatility and rising...


        Then anticipate next most likely move instead of react, ie:
            - Tight candles after long range? → Phase 1 (ACTION_ONE)
            - Just broke out? → Phase 2 (ACTION_TWO)
            - Steady move with pullbacks? → Phase 3 (ACTION_THREE)
            - Parabolic, everyone talking? → Phase 4 (ACTION_FOUR)
            - Wide bars, no progress? → Phase 5/6 (ACTION_FIVE)

    then from the fixed width: 
    return marketState and nextMostLikelyProgression


REGIME SIMPLE LOGIC IMPLIMENTATION LOGIC:

check for the state we are in:
    1. Ranging – Low Volatility (State 1)
        * Volatility: Low & Stable
        * Character: Consolidation, equilibrium, boredom
        * Price/Indicators: 
            - Tight candles inside horizontal S/R
            - Flat MAs (50,89), ADX < 20, ATR low & flat
            - Bollinger Bands squeezed
        * Tradability: High for mean reversion
        * Direction: Neutral
        * Maturity: Late-stage - range established, awaiting catalyst
        * Lifecycle Position: Phase 1/7 - Beginning or end of cycle
        * Trading Rules:
            - Fade range extremes with tight stops
            - Prepare breakout alerts above/below range
            - Reduce position size as range matures
            - Wait for volatility contraction (next phase)

    ---

    2. Contraction/Squeeze (State 5)
        * Volatility: Low & Falling
        * Character: Coiling, compression, indecision
        * Price/Indicators:
            - Extremely tight candles (doji, spinning tops)
            - Bollinger Bands width at multi-period low
            - ADX < 15, ATR at lows
        * Tradability: Prepare, dont trade - no edge until breakout
        * Direction: Neutral (latent)
        * Maturity: Transition - energy building for next move
        * Lifecycle Position: Phase 1 → Phase 2 transition
        * Trading Rules:
            - Set breakout alerts
            - Prepare capital allocation for next move
            - Avoid fading range edges now
            - Wait for the expansion candle

    ---

    3. Expansion/Breakout Test (State 6)
        * Volatility: High & Rising
        * Character: Breakout attempt, momentum surge
        * Price/Indicators:
            - Large candle breaking key level
            - Volume spike, ATR jumps
            - ADX may still be low but rising
        * Tradability: Conditional - true vs false break decision
        * Direction: Emerging
        * Maturity: Infant stage - new trend may be born
        * Lifecycle Position: Phase 2 - Breakout Decision Point
        * Trading Rules:
            - Wait for close beyond range (not just wick)
            - Dont chase - let price retest breakout level
            - Confirm with follow-through candle
            - Beware low-volume breaks (traps)

    ---

    4. Trending – Low Volatility (State 3)
        * Volatility: Low & Rising
        * Character: Healthy, institutional trend
        * Price/Indicators:
            - Steady candles in one direction
            - MAs aligned as dynamic S/R
            - ADX rising (25-40), ATR gradually increasing
        * Tradability: Very High - ideal conditions
        * Direction: Strong (Up/Down)
        * Maturity: Early to Mid-stage - established but not crowded
        * Lifecycle Position: Phase 3 - Trend Birth & Acceptance
        * Trading Rules:
            - Enter on first pullback to breakout level
            - Add to position on subsequent higher lows
            - Use loose trailing stops
            - This is highest probability phase

    ---

        5. Trending – High Volatility (State 4)
        * Volatility: High & Expanding
        * Character: Climactic, emotional, parabolic
        * Price/Indicators:
            - Large candles, gaps, exhaustion patterns
            - ADX > 40, ATR spiking
            - MAs far from price
        * Tradability: Low - caution, reversal risk high
        * Direction: Strong but weakening
        * Maturity: Late-stage - overextended, smart money distributing
        * Lifecycle Position: Phase 4 - Trend Maturity & Euphoria
        * Trading Rules:
            - Take partial profits (25-50%)
            - Tighten stops to protect gains
            - No new entries
            - Watch for exhaustion patterns

    ---

    6. Churn/Exhaustion (State 7)
        * Volatility: High & Unstable
        * Character: Directionless volatility, distribution
        * Price/Indicators:
            - Large, overlapping candles, no progress
            - ADX falling from >40, ATR erratic
            - Broadening formation patterns
        * Tradability: Avoid - whipsaw kills accounts
        * Direction: Neutralizing
        * Maturity: Terminal stage - old trend dying
        * Lifecycle Position: Phase 5 - Distribution & Exhaustion
        * Trading Rules:
            - Exit remaining positions
            - Prepare to fade extremes (small size only)
            - Shift to range/reversal mindset
            - Set alerts for key breaks

    ---

    7. Ranging – High Volatility (State 2)
        * Volatility: High & Stable
        * Character: Choppy, emotional swings within bounds
        * Price/Indicators:
            - Wide candles but failing at S/R
            - High ATR, ADX still low (<25)
            - Volume spikes at edges
        * Tradability: Low - dangerous, stop hunts frequent
        * Direction: Neutral
        * Maturity: Mid-stage - unstable, may precede expansion/contraction
        * Lifecycle Position: Phase 6 Path B or Phase 7 early
        * Trading Rules:
            - Fade extremes but expect stop runs
            - Very tight stops if trading
            - Better to wait for stabilization
            - This is the "trap zone"

    ---

ONCE STATE IS UNDERSTOOD THEN CRITICAL TRANSITIONS & DECISION POINTS

    ---

    NEXT MOST LIKELY PROGRESSION

Root State        | Current State         | Direction if applicable | Next Likely States                            | YOUR RECOMMENDATION       | Position Size | Stop Placement |RR-recomendation
|---------------  |---------------        |-----------------        |-----------------                              |-------------              |---------------|----------------|----------------
REGIME_RANGING    | Ranging Low Vol       | Direction if applicable | Contraction (5) or Range High Vol (2)         | Fade edges                | Small         | Very tight     |RR-recomendation
REGIME_UNKNOWN    | Contraction           | Direction if applicable | Expansion (6)                                 | Wait for break            | Zero          | N/A            |RR-recomendation
REGIME_TRENDING   | Expansion             | Direction if applicable | Trending Low Vol (3) or Range High Vol (2)    | Test entry                | Medium        | Below breakout |RR-recomendation
REGIME_TRENDING   | Trending Low Vol      | Direction if applicable | Trending High Vol (4)                         | Add to winners            | Large         | Loose trailing |RR-recomendation
REGIME_TRENDING   | Trending High Vol     | Direction if applicable | Churn (7)                                     | Take profits              | Reducing      | Tightening     |RR-recomendation
REGIME_UNKNOWN    | Churn                 | Direction if applicable | Range High Vol (2) or Trending Low Vol (3)    | Wait for clarity          | Zero          | N/A            |RR-recomendation
REGIME_RANGING    | Range High Vol        | Direction if applicable | Range Low Vol (1)                             | Fade carefully            | Very small    | Extremely tight|RR-recomendation

    ---






    
so the regime basically comes with:
0. rootState
1. state.
2. next most likely state.
3. action recommendattion.
4. position size recommendation, also base this on acount size and symbol.
5. tp/sl recommendation, also base this on acount size and symbol.
6. risk to reward recommendation.
7. Direction recommendation















//+------------------------------------------------------------------+
//|                                                   MarketRegime.mq5|
//|             Simplified Market Regime & Lifecycle Detection       |
//+------------------------------------------------------------------+
// Logger Integration Status: FULLY INTEGRATED
// All logging uses static Logger::Log() calls with debug wrapper
//+------------------------------------------------------------------+

#property copyright "Copyright 2024"
#property version "1.00"
#property strict

#include <Math\Stat\Math.mqh>

#include "../Headers/Enums.mqh"
#include "../Headers/Structures.mqh"
#include "../Data/IndicatorManager.mqh"
#include "../Utils/Logger.mqh"

// ====================== DEBUG SETTINGS ======================
bool DEBUG_REGIME_ENABLED = true;

// Simple debug function using Logger
void DebugRegimeLogFile(string context, string message)
{
   if (DEBUG_REGIME_ENABLED)
   {
      Logger::Log("MarketRegime", StringFormat("%s: %s", context, message), true, false);
   }
}

//+------------------------------------------------------------------+
//| Market Regime Detector Class                                     |
//+------------------------------------------------------------------+
class MarketRegimeDetector
{
private:
   string m_symbol;
   ENUM_TIMEFRAMES m_timeframe;
   double m_fixedRangeTop;
   double m_fixedRangeBottom;
   datetime m_rangeStartTime;
   bool m_rangeActive;
   int m_lookbackBars;

   // Account info for position sizing
   double m_accountBalance;
   double m_accountRiskPercent;

   // Indicator Manager instance
   IndicatorManager *m_indicatorManager;
   bool m_useIndicatorManager;

   int m_rangeLookbackBars;       // How many bars to look back for the range
   int m_barsSinceLastRangeInit;  // Bars since last range initialization
   datetime m_lastRangeCheckTime; // Last time we checked for reinitialization

   // NEW: Range invalidation tracking
   datetime m_lastInvalidationTime; // Track when range was invalidated
   int m_invalidationCooldownBars;  // Bars to wait before new range

public:
   MarketRegimeDetector(string symbol = NULL,
                        ENUM_TIMEFRAMES tf = PERIOD_H1,
                        double accountBalance = 10000,
                        double riskPercent = 1.0,
                        bool useIndicatorManager = true)
   {
      m_symbol = (symbol == NULL) ? Symbol() : symbol;
      m_timeframe = tf;
      m_rangeActive = false;
      m_lookbackBars = 10;
      m_accountBalance = accountBalance;
      m_accountRiskPercent = riskPercent;
      m_useIndicatorManager = useIndicatorManager;

      m_rangeLookbackBars = 10; // Default: reinitialize every 8 bars
      m_barsSinceLastRangeInit = 0;
      m_lastRangeCheckTime = 0;

      // NEW: Initialize invalidation tracking
      m_lastInvalidationTime = 0;
      m_invalidationCooldownBars = 8; // Wait 8 bars after invalidation

      DebugRegimeLogFile("Constructor", StringFormat("Initializing detector for %s timeframe %d",
                                                     m_symbol, m_timeframe));

      // Initialize indicator manager if requested
      if (m_useIndicatorManager)
      {
         m_indicatorManager = new IndicatorManager(m_symbol);
         if (!m_indicatorManager.Initialize())
         {
            Logger::LogError("MarketRegime", "IndicatorManager failed to initialize. Falling back to direct indicators.");
            m_useIndicatorManager = false;
         }
         else
         {
            DebugRegimeLogFile("Constructor", "IndicatorManager initialized successfully");
         }
      }
      else
      {
         m_indicatorManager = NULL;
         DebugRegimeLogFile("Constructor", "Using direct indicators (no IndicatorManager)");
      }

      // Initialize fixed range
      InitializeFixedRange();
   }

   ~MarketRegimeDetector()
   {
      DebugRegimeLogFile("Destructor", "Cleaning up MarketRegimeDetector");

      if (m_indicatorManager != NULL)
      {
         m_indicatorManager.Deinitialize();
         delete m_indicatorManager;
         DebugRegimeLogFile("Destructor", "IndicatorManager cleaned up");
      }

      Logger::Log("MarketRegime", "Detector shutdown complete", true, false);
   }

   //+------------------------------------------------------------------+
   //| MAIN PUBLIC METHOD: Get complete market analysis                |
   //+------------------------------------------------------------------+
   MarketAnalysis GetMarketRegime()
   {
      DebugRegimeLogFile("GetMarketRegime", "Starting market analysis");

      MarketAnalysis analysis;

      // 1. Check if we should reinitialize the range
      CheckAndReinitializeRange();

      // 2. Initialize fixed range if not active AND conditions are right
      if (!m_rangeActive && ShouldCreateNewRange())
      {
         InitializeFixedRange();
      }

      // 3. Get current state using indicators
      analysis.state = DetectCurrentState();
      analysis.rootState = GetRootState(analysis.state);
      analysis.confidence = CalculateConfidence(analysis.state);

      DebugRegimeLogFile("GetMarketRegime", StringFormat("Detected state: %s with %.1f%% confidence",
                                                         MarketAnalysis::GetStateString(analysis.state), analysis.confidence));

      // 4. Determine next likely state
      analysis.nextLikelyState = PredictNextState(analysis.state);

      DebugRegimeLogFile("GetMarketRegime", StringFormat("Next likely state: %s",
                                                         MarketAnalysis::GetStateString(analysis.nextLikelyState)));

      // 5. Generate trading recommendations
      GenerateRecommendations(analysis);

      DebugRegimeLogFile("GetMarketRegime", StringFormat("Recommendation: %s (Position: %s)",
                                                         analysis.action, GetPositionSizeString(analysis.positionSize)));

      // 6. Set description
      analysis.description = GenerateDescription(analysis);

      DebugRegimeLogFile("GetMarketRegime", "Analysis complete");

      // Log final analysis summary
      Logger::Log("MarketRegime", StringFormat("ANALYSIS: %s | Confidence: %.0f%% | Action: %s | R/R: %.1f", MarketAnalysis::GetStateString(analysis.state), analysis.confidence, analysis.action, analysis.riskRewardRatio),
                  true, true);

      return analysis;
   }

private:
   string GetPositionSizeString(ENUM_POSITION_SIZE size)
   {
      switch (size)
      {
      case SIZE_ZERO:
         return "ZERO";
      case SIZE_VERY_SMALL:
         return "VERY_SMALL";
      case SIZE_SMALL:
         return "SMALL";
      case SIZE_MEDIUM:
         return "MEDIUM";
      case SIZE_LARGE:
         return "LARGE";
      default:
         return "UNKNOWN";
      }
   }

   void ReinitializeRange()
   {
      DebugRegimeLogFile("ReinitializeRange", "Reinitializing fixed range...");
      Logger::Log("MarketRegime", "Reinitializing fixed range", true, false);
      InitializeFixedRange();
      m_barsSinceLastRangeInit = 0;
   }

   //+------------------------------------------------------------------+
   //| SMART RANGE REINITIALIZATION                                    |
   //+------------------------------------------------------------------+
   bool ShouldSmartReinitialize()
   {
      DebugRegimeLogFile("ShouldSmartReinitialize", "Checking if smart reinitialization is needed");

      // Get current market state
      ENUM_MARKET_STATE state = DetectCurrentState();
      double confidence = CalculateConfidence(state);

      // Condition 1: Clear ranging state with high confidence
      bool isClearRanging = (state == STATE_RANGING_LOW_VOL || state == STATE_RANGING_HIGH_VOL) &&
                            confidence > 50;

      // Condition 2: Range is too old (more than X bars)
      bool rangeIsOld = m_barsSinceLastRangeInit >= m_rangeLookbackBars;

      // Condition 3: Price has moved slightly outside the range
      bool priceOutOfRange = false;
      if (m_rangeActive)
      {
         double currentPrice = iClose(m_symbol, m_timeframe, 0);
         double rangeWidth = m_fixedRangeTop - m_fixedRangeBottom;
         double atr = GetATR(14);

         if (currentPrice > m_fixedRangeTop + (rangeWidth * 0.3) ||
             currentPrice < m_fixedRangeBottom - (rangeWidth * 0.3))
         {
            priceOutOfRange = true;
         }
      }

      // Decision matrix
      if (rangeIsOld)
      {
         if (isClearRanging || priceOutOfRange)
         {
            DebugRegimeLogFile("ShouldSmartReinitialize", "Conditions met for smart reinitialization");
            return true;
         }

         // Reinitialize if market is in contraction/expansion phase
         if (state == STATE_CONTRACTION || state == STATE_EXPANSION)
         {
            DebugRegimeLogFile("ShouldSmartReinitialize", "Contraction/Expansion phase detected");
            return true;
         }
      }

      DebugRegimeLogFile("ShouldSmartReinitialize", "No need for smart reinitialization");
      return false;
   }

   void CheckAndReinitializeRange()
   {
      datetime currentTime = iTime(m_symbol, m_timeframe, 0);

      if (m_lastRangeCheckTime == currentTime)
         return;

      m_lastRangeCheckTime = currentTime;
      m_barsSinceLastRangeInit++;

      DebugRegimeLogFile("CheckAndReinitializeRange",
                         StringFormat("Bar count since last init: %d", m_barsSinceLastRangeInit));

      // Use smart reinitialization logic
      if (ShouldSmartReinitialize())
      {
         ReinitializeRange();
      }
   }

   //+------------------------------------------------------------------+
   //| SHOULD CREATE NEW RANGE METHOD                                   |
   //+------------------------------------------------------------------+
   bool ShouldCreateNewRange()
   {
      DebugRegimeLogFile("ShouldCreateNewRange", "Checking if new range should be created");

      // 1. Check cooldown period
      if (m_lastInvalidationTime > 0)
      {
         int barsSinceInvalidation = iBarShift(m_symbol, m_timeframe, m_lastInvalidationTime);
         if (barsSinceInvalidation < m_invalidationCooldownBars)
         {
            DebugRegimeLogFile("ShouldCreateNewRange",
                               StringFormat("Still in cooldown: %d/%d bars",
                                            barsSinceInvalidation, m_invalidationCooldownBars));
            return false; // Still in cooldown
         }
      }

      // 2. Market must be clearly ranging (not trending)
      double adx = GetADX(14);
      if (adx >= 25) // ADX too high = trending
      {
         DebugRegimeLogFile("ShouldCreateNewRange",
                            StringFormat("ADX too high for new range: %.1f (>=25)", adx));
         return false;
      }

      // 3. Check MA alignment - price should be near MA50
      double ma50 = GetMAValue(50, 0);
      double currentPrice = iClose(m_symbol, m_timeframe, 0);

      // FIX: Add validation for MA50
      if (ma50 <= 0)
      {
         DebugRegimeLogFile("ShouldCreateNewRange", "Invalid MA50 value");
         return false; // Invalid MA value
      }

      double priceFromMA = MathAbs(currentPrice - ma50) / ma50 * 100;

      if (priceFromMA > 0.8) // Price too far from MA50
      {
         DebugRegimeLogFile("ShouldCreateNewRange",
                            StringFormat("Price too far from MA50: %.2f%% (>0.8%%)", priceFromMA));
         return false;
      }

      // 4. Check for consolidation (low volatility)
      double atr = GetATR(14);
      double volatilityScore = CalculateVolatilityScore(atr, GetBollingerWidth(20, 2));
      if (volatilityScore > 0.5) // Too volatile
      {
         DebugRegimeLogFile("ShouldCreateNewRange",
                            StringFormat("Volatility too high: %.2f (>0.5)", volatilityScore));
         return false;
      }

      // 5. Look for price compression (last 5 bars)
      double high = iHigh(m_symbol, m_timeframe, 0);
      double low = iLow(m_symbol, m_timeframe, 0);

      for (int i = 1; i < 5; i++)
      {
         high = MathMax(high, iHigh(m_symbol, m_timeframe, i));
         low = MathMin(low, iLow(m_symbol, m_timeframe, i));
      }

      double rangeHeight = high - low;
      double avgPrice = (high + low) / 2;

      // FIX: Add validation for avgPrice
      if (avgPrice <= 0)
      {
         DebugRegimeLogFile("ShouldCreateNewRange", "Invalid average price");
         return false;
      }

      double rangePercent = (rangeHeight / avgPrice) * 100;

      if (rangePercent > 0.5) // Range too wide
      {
         DebugRegimeLogFile("ShouldCreateNewRange",
                            StringFormat("Range too wide: %.2f%% (>0.5%%)", rangePercent));
         return false;
      }

      DebugRegimeLogFile("ShouldCreateNewRange", "All conditions met for new range creation");
      return true; // All conditions met, can create new range
   }

   double GetMAValue(int period, int shift = 0)
   {
      if (m_useIndicatorManager && m_indicatorManager != NULL && m_indicatorManager.IsInitialized())
      {
         // Use IndicatorManager for MA values
         double ma9, ma21, ma50, ma89;
         if (m_indicatorManager.GetMAValuesForRange(m_timeframe, ma9, ma21, ma50, ma89, shift))
         {
            if (period == 50)
               return ma50;
            if (period == 89)
               return ma89;
         }
         else
         {
            double ma_fast, ma_slow, ma_medium;
            if (m_indicatorManager.GetMAValues(m_timeframe, ma_fast, ma_slow, ma_medium, shift))
            {
               if (period == 9)
                  return ma_fast;
               if (period == 21)
                  return ma_medium;
               if (period == 89)
                  return ma_slow;
            }
         }
      }

      // Fallback to direct MA calculation
      int handle = iMA(m_symbol, m_timeframe, period, 0, MODE_SMA, PRICE_CLOSE);
      if (handle == INVALID_HANDLE)
         return 0;

      double values[];
      ArraySetAsSeries(values, true);

      if (CopyBuffer(handle, 0, shift, 1, values) > 0)
      {
         IndicatorRelease(handle);
         return values[0];
      }

      IndicatorRelease(handle);
      return 0;
   }

   bool IsMATrendAligned()
   {
      if (m_useIndicatorManager && m_indicatorManager != NULL && m_indicatorManager.IsInitialized())
      {
         // Use IndicatorManager for MA values
         double ma9, ma21, ma50, ma89;
         if (m_indicatorManager.GetMAValuesForRange(m_timeframe, ma9, ma21, ma50, ma89, 0))
         {
            double ma50_10 = GetMAValue(50, 8);
            double ma89_10 = GetMAValue(89, 8);

            if (ma50 <= 0 || ma89 <= 0)
               return false;

            // Calculate separation percentage
            double separation = MathAbs(ma50 - ma89) / ((ma50 + ma89) / 2) * 100;

            // Check alignment and slope
            bool maAlignedBullish = (ma50 > ma89) && (ma50_10 > ma89_10);
            bool maAlignedBearish = (ma50 < ma89) && (ma50_10 < ma89_10);
            bool maSlopeBullish = (ma50 > ma50_10);
            bool maSlopeBearish = (ma50 < ma50_10);

            // Significant separation with same slope = strong trend
            if (separation > 0.5) // 0.5% separation threshold
            {
               if ((maAlignedBullish && maSlopeBullish) ||
                   (maAlignedBearish && maSlopeBearish))
               {
                  DebugRegimeLogFile("IsMATrendAligned", "MA trend alignment confirmed");
                  return true;
               }
            }
         }
      }

      return false;
   }

   void InitializeFixedRange()
   {
      DebugRegimeLogFile("InitializeFixedRange", "Initializing new fixed range");

      int bars = 10; // Fixed 5-bar lookback for initial range
      double highest = 0;
      double lowest = DBL_MAX;

      for (int i = 0; i < bars; i++)
      {
         double high = iHigh(m_symbol, m_timeframe, i);
         double low = iLow(m_symbol, m_timeframe, i);

         if (high > highest)
            highest = high;
         if (low < lowest)
            lowest = low;
      }

      if (highest > lowest)
      {
         m_fixedRangeTop = highest;
         m_fixedRangeBottom = lowest;
         m_rangeStartTime = iTime(m_symbol, m_timeframe, 0);
         m_rangeActive = true;
         m_barsSinceLastRangeInit = 0;

         string rangeInfo = StringFormat("Fixed Range Initialized: %.5f - %.5f (Width: %.2f%%)",
                                         lowest, highest,
                                         ((highest - lowest) / ((highest + lowest) / 2)) * 100);

         Print(rangeInfo);
         Logger::Log("MarketRegime", rangeInfo, true, true);
         DebugRegimeLogFile("InitializeFixedRange", rangeInfo);

         // Log range details
         Logger::LogTrade("Range", m_symbol, "RANGE", 0, (highest + lowest) / 2);
      }
      else
      {
         Logger::LogError("MarketRegime", "Failed to initialize fixed range");
         DebugRegimeLogFile("InitializeFixedRange", "Failed to initialize fixed range");
      }
   }

   bool IsPriceInFixedRange(double price)
   {
      if (!m_rangeActive)
         return false;
      return price >= m_fixedRangeBottom && price <= m_fixedRangeTop;
   }

   //+------------------------------------------------------------------+
   //| CHECK AND INVALIDATE RANGE METHOD                                |
   //+------------------------------------------------------------------+
   void CheckAndInvalidateRange()
   {
      if (!m_rangeActive)
      {
         DebugRegimeLogFile("CheckAndInvalidateRange", "Range not active, nothing to invalidate");
         return;
      }

      DebugRegimeLogFile("CheckAndInvalidateRange", "Checking if range should be invalidated");

      double currentPrice = iClose(m_symbol, m_timeframe, 0);

      // Rule 1: Consecutive closes outside range
      int consecutiveOutside = 0;
      for (int i = 0; i < 3; i++) // Check last 3 bars
      {
         double price = iClose(m_symbol, m_timeframe, i);
         if (!IsPriceInFixedRange(price))
            consecutiveOutside++;
         else
            break; // Break streak if price returns to range
      }

      if (consecutiveOutside >= 2)
      {
         InvalidateRange("2+ consecutive closes outside range");
         return;
      }

      // Rule 2: Strong momentum break with ATR expansion
      double atr = GetATR(14);
      double atr_5 = GetATRValue(14, 5);
      double priceChange = GetPriceChange(3); // Last 3 bars change

      bool strongBreakout = HasRangeBroken(currentPrice) &&
                            (atr > atr_5 * 1.2) &&            // ATR expanded 20%
                            MathAbs(priceChange) > atr * 1.5; // Strong momentum

      if (strongBreakout)
      {
         InvalidateRange("Strong momentum breakout with ATR expansion");
         return;
      }

      // Rule 3: MA confirms trend (not range)
      double ma50 = GetMAValue(50, 0);
      double ma200 = GetMAValue(200, 0);
      double adx = GetADX(14);

      // FIX: Add validation check for MA values
      // if (ma50 > 0 && ma200 > 0) // Only proceed if we have valid MA values
      // {
      //    double maDistance = MathAbs(ma50 - ma200) / ((ma50 + ma200) / 2) * 100;

      //    bool maTrending = (maDistance > 0.5) &&                     // MAs separated
      //                      ((ma50 > ma200 && currentPrice > ma50) || // Bullish alignment
      //                       (ma50 < ma200 && currentPrice < ma50));  // Bearish alignment

      //    if (maTrending && adx >= 20)
      //    {
      //       InvalidateRange("MA alignment confirms trending market");
      //       return;
      //    }
      // }

      DebugRegimeLogFile("CheckAndInvalidateRange", "Range validation passed, keeping range active");
   }

   //+------------------------------------------------------------------+
   //| INVALIDATE RANGE HELPER                                          |
   //+------------------------------------------------------------------+
   void InvalidateRange(string reason)
   {
      DebugRegimeLogFile("InvalidateRange", StringFormat("Invalidating range: %s", reason));

      m_rangeActive = false;
      m_lastInvalidationTime = iTime(m_symbol, m_timeframe, 0);

      string invalidationMsg = StringFormat("Range Invalidated: %s (Cooldown: %d bars)",
                                            reason, m_invalidationCooldownBars);

      Print(invalidationMsg);
      Logger::Log("MarketRegime", invalidationMsg, true, true);
      DebugRegimeLogFile("InvalidateRange", invalidationMsg);

      // Log trade event for range invalidation
      Logger::LogTrade("Range", m_symbol, "INVALIDATE", 0, iClose(m_symbol, m_timeframe, 0));
   }

   //+------------------------------------------------------------------+
   //| MODIFIED HasRangeBroken()                                        |
   //+------------------------------------------------------------------+
   bool HasRangeBroken(double price)
   {
      if (!m_rangeActive)
         return false;

      double atr = GetATR(14);
      double margin = atr * 0.5; // INCREASED buffer from 0.3 ATR to 0.5 ATR

      bool broken = (price > m_fixedRangeTop + margin) ||
                    (price < m_fixedRangeBottom - margin);

      if (broken)
      {
         DebugRegimeLogFile("HasRangeBroken", StringFormat("Range broken at price %.5f (ATR margin: %.5f)", price, margin));
      }

      return broken;
   }

   //+------------------------------------------------------------------+
   //| STATE DETECTION LOGIC - NO UNKNOWN STATES                        |
   //+------------------------------------------------------------------+
   ENUM_MARKET_STATE DetectCurrentState()
   {
      DebugRegimeLogFile("DetectCurrentState", "Starting state detection - NO UNKNOWN states");

      // NEW: Check if current range should be invalidated
      CheckAndInvalidateRange();

      // Get key indicator values
      double adx = GetADX(14);
      double atr = GetATR(14);
      double bbWidth = GetBollingerWidth(20, 2);
      double priceChange = GetPriceChange(8);
      double rangeTouches = CountRangeTouches();

      DebugRegimeLogFile("DetectCurrentState",
                         StringFormat("Indicators: ADX=%.1f, ATR=%.5f, BBWidth=%.4f, PriceChange=%.2f%%, RangeTouches=%d",
                                      adx, atr, bbWidth, priceChange, rangeTouches));

      // ========== FIXED: MA SEPARATION as CONFIDENCE BOOST, not VETO ==========
      double ma50 = GetMAValue(50, 0);
      double ma89 = GetMAValue(89, 0);
      double maDistancePercent = 0;

      if (ma50 > 0 && ma89 > 0)
      {
         maDistancePercent = MathAbs(ma50 - ma89) / ((ma50 + ma89) / 2) * 100;
         DebugRegimeLogFile("DetectCurrentState",
                            StringFormat("MA50=%.5f, MA89=%.5f, Distance=%.2f%%",
                                         ma50, ma89, maDistancePercent));
      }

      // ========== TREND BIAS: If MAs are far apart, INCREASE trend confidence ==========
      double trendBias = 0; // 0 = no bias, 1.0 = strong trend bias
      double rangeBias = 0; // 0 = no bias, 1.0 = strong range bias

      if (maDistancePercent > 0.5) // 0.5% separation = moderate trend bias
      {
         trendBias = MathMin(1.0, (maDistancePercent - 0.5) / 1.0); // Scales 0.5-1.5% to 0-1.0

         // Get MA slope (5 bars ago vs now)
         double ma50_10 = GetMAValue(50, 10);
         bool maSlopeUp = (ma50 > ma50_10);
         bool maSlopeDown = (ma50 < ma50_10);

         // If MAs are diverging (getting further apart), stronger trend bias
         if ((maDistancePercent > 0.75) && (maSlopeUp || maSlopeDown))
         {
            trendBias = MathMin(1.0, trendBias + 0.3); // Extra bias for strong separation with slope
         }

         DebugRegimeLogFile("DetectCurrentState",
                            StringFormat("Trend bias: %.2f, Range bias: %.2f", trendBias, rangeBias));
      }

      double volatilityScore = CalculateVolatilityScore(atr, bbWidth);
      bool volatilityLow = volatilityScore < 0.3;
      bool volatilityHigh = volatilityScore > 0.7;
      bool volatilityRising = IsVolatilityRising();

      DebugRegimeLogFile("DetectCurrentState",
                         StringFormat("Volatility: Score=%.2f, Low=%s, High=%s, Rising=%s",
                                      volatilityScore, volatilityLow ? "true" : "false",
                                      volatilityHigh ? "true" : "false", volatilityRising ? "true" : "false"));

      // Price structure analysis
      bool isTrendingStructure = IsTrendingStructure();
      bool isRangingStructure = IsRangingStructure();

      DebugRegimeLogFile("DetectCurrentState",
                         StringFormat("Structure: Trending=%s, Ranging=%s",
                                      isTrendingStructure ? "true" : "false",
                                      isRangingStructure ? "true" : "false"));

      double currentPrice = iClose(m_symbol, m_timeframe, 0);

      // NEW: State confidence check before ranging logic
      if (m_rangeActive)
      {
         // Check if range is still valid before using it
         if (rangeTouches < 2) // Not enough touches
         {
            // Range exists but isn't being respected
            DebugRegimeLogFile("DetectCurrentState",
                               StringFormat("Range weak: only %d touches, likely breaking out", rangeTouches));
            CheckAndInvalidateRange(); // Mark as invalid
            // Return expansion (trend-like state) instead of unknown
            DebugRegimeLogFile("DetectCurrentState", "Range weak, returning STATE_EXPANSION (trend-like)");
            Logger::Log("MarketRegime", "Market State: EXPANSION (Weak range breaking out)", true, false);
            return STATE_EXPANSION;
         }
      }

      // Decision matrix - FIRST PASS: Clear cases
      // 1. Check for Contraction/Squeeze (STATE_CONTRACTION) - treat as RANGE
      if (volatilityLow && !volatilityRising && adx < 15 && rangeTouches < 2)
      {
         DebugRegimeLogFile("DetectCurrentState", "Detected STATE_CONTRACTION (treat as RANGE)");
         Logger::Log("MarketRegime", "Market State: CONTRACTION (Volatility squeeze - RANGE)", true, false);
         return STATE_CONTRACTION; // Contraction is a ranging state
      }

      // 2. Check for Expansion/Breakout (STATE_EXPANSION) - treat as TREND
      if (HasRangeBroken(currentPrice) &&
          MathAbs(priceChange) > atr * 2)
      {
         DebugRegimeLogFile("DetectCurrentState", "Detected STATE_EXPANSION (treat as TREND)");
         Logger::Log("MarketRegime", "Market State: EXPANSION (Breakout detected - TREND)", true, false);
         return STATE_EXPANSION; // Expansion is a trend state
      }

      // 3. Check for Churn/Exhaustion (STATE_CHURN) - treat as RANGE
      if (volatilityHigh && volatilityRising && adx > 40 &&
          MathAbs(priceChange) < atr * 0.5)
      {
         DebugRegimeLogFile("DetectCurrentState", "Detected STATE_CHURN (treat as RANGE)");
         Logger::Log("MarketRegime", "Market State: CHURN (Exhaustion phase - RANGE)", true, false);
         return STATE_CHURN; // Churn is a ranging state
      }

      // ========== ADD RANGE STRENGTH CHECK ==========
      // Check if price is respecting range boundaries
      bool priceNearRangeBoundary = false;
      if (m_rangeActive)
      {
         double rangeHeight = m_fixedRangeTop - m_fixedRangeBottom;
         double pricePosition = (currentPrice - m_fixedRangeBottom) / rangeHeight;

         // Price near top or bottom (within 15% of boundary)
         if (pricePosition < 0.15 || pricePosition > 0.85)
         {
            priceNearRangeBoundary = true;
            rangeBias = MathMin(1.0, rangeBias + 0.4);
         }

         // Multiple range touches = stronger range
         if (rangeTouches >= 3)
         {
            rangeBias = MathMin(1.0, rangeBias + 0.3);
         }
      }

      // ========== BALANCED DECISION MAKING ==========

      // 4. Check trending states WITH BIAS CONSIDERATION
      if (isTrendingStructure)
      {
         // RESTORE ADX THRESHOLD to 25 (from 22)
         if (adx >= 25) // increase makes trends harder to qualify
         {
            // Apply trend bias if MAs are separated
            double trendStrength = adx / 100.0;                                               // Normalize ADX 0-1
            double finalTrendScore = trendStrength + (trendBias * 0.25) - (rangeBias * 0.35); // consider trend biases.

            // Need reasonable trend strength even with MA bias
            if (finalTrendScore > 0.45) // higher score to make harder for trend to qualify
            {
               // Healthy trend
               if (volatilityLow || volatilityScore < 0.5)
               {
                  DebugRegimeLogFile("DetectCurrentState", "Detected STATE_TRENDING_LOW_VOL");
                  Logger::Log("MarketRegime", "Market State: TRENDING_LOW_VOL (Healthy trend)", true, false);
                  return STATE_TRENDING_LOW_VOL;
               }
               else
               {
                  DebugRegimeLogFile("DetectCurrentState", "Detected STATE_TRENDING_HIGH_VOL");
                  Logger::Log("MarketRegime", "Market State: TRENDING_HIGH_VOL (High vol trend)", true, false);
                  return STATE_TRENDING_HIGH_VOL;
               }
            }
         }
         else if (adx > 40) // Keep this for exhaustion trend
         {
            // Exhaustion trend
            DebugRegimeLogFile("DetectCurrentState", "Detected STATE_TRENDING_HIGH_VOL (Exhaustion)");
            Logger::Log("MarketRegime", "Market State: TRENDING_HIGH_VOL (Exhaustion)", true, false);
            return STATE_TRENDING_HIGH_VOL;
         }
      }

      // 5. Check ranging states WITH BIAS CONSIDERATION
      // SIMPLIFIED RANGING LOGIC
      if (m_rangeActive && IsPriceInFixedRange(currentPrice))
      {
         // Check range strength
         if (rangeTouches >= 2) // Range has been respected
         {
            // Simple: If in range and price is within it, we're ranging
            if (volatilityHigh)
            {
               DebugRegimeLogFile("DetectCurrentState", "Detected STATE_RANGING_HIGH_VOL");
               Logger::Log("MarketRegime", "Market State: RANGING_HIGH_VOL (High volatility range)", true, false);
               return STATE_RANGING_HIGH_VOL;
            }
            else
            {
               DebugRegimeLogFile("DetectCurrentState", "Detected STATE_RANGING_LOW_VOL");
               Logger::Log("MarketRegime", "Market State: RANGING_LOW_VOL (Low volatility range)", true, false);
               return STATE_RANGING_LOW_VOL;
            }
         }
      }

      // ========== TIE-BREAKER: If both trend and range have similar scores ==========
      if (isTrendingStructure && m_rangeActive)
      {
         DebugRegimeLogFile("DetectCurrentState", "Running tie-breaker logic (both trend and range possible)");

         // ========== BALANCED SCORING ==========
         // Trend factors (0-1 scale)
         double trendADX = MathMin(1.0, adx / 100.0);              // ADX contribution
         double trendMA = MathMin(0.3, maDistancePercent / 500.0); // REDUCED: MA contributes max 0.3 (was 0.75)
         double trendStructure = isTrendingStructure ? 0.4 : 0.1;  // Price structure

         // Range factors (0-1 scale)
         double rangeActive = m_rangeActive ? 0.5 : 0.0;              // Range is active
         double rangeTouchesScore = MathMin(0.3, rangeTouches * 0.1); // Range touches
         double rangePositionScore = 0.0;

         // Calculate price position in range (if in range)
         if (m_rangeActive)
         {
            double rangeHeight = m_fixedRangeTop - m_fixedRangeBottom;
            double pricePosition = (currentPrice - m_fixedRangeBottom) / rangeHeight;
            // Price in middle of range = stronger range evidence
            if (pricePosition > 0.3 && pricePosition < 0.7)
               rangePositionScore = 0.2;
         }

         // Composite scores with BALANCED weights
         double trendScore = (trendADX * 0.5) + (trendMA * 0.3) + (trendStructure * 0.2);
         double rangeScore = (rangeActive * 0.4) + (rangeTouchesScore * 0.3) + (rangePositionScore * 0.3);

         DebugRegimeLogFile("DetectCurrentState",
                            StringFormat("Tie-breaker scores: Trend=%.2f, Range=%.2f", trendScore, rangeScore));

         // ========== DECISION WITH THRESHOLDS ==========
         // Both need reasonable minimum scores
         bool trendValid = trendScore >= 0.3;
         bool rangeValid = rangeScore >= 0.3;

         if (trendValid && !rangeValid)
         {
            // Clear trend
            if (volatilityLow || volatilityScore < 0.5)
            {
               DebugRegimeLogFile("DetectCurrentState", "Tie-breaker: Clear trend (TRENDING_LOW_VOL)");
               return STATE_TRENDING_LOW_VOL;
            }
            else
            {
               DebugRegimeLogFile("DetectCurrentState", "Tie-breaker: Clear trend (TRENDING_HIGH_VOL)");
               return STATE_TRENDING_HIGH_VOL;
            }
         }
         else if (rangeValid && !trendValid)
         {
            // Clear range
            if (volatilityHigh)
            {
               DebugRegimeLogFile("DetectCurrentState", "Tie-breaker: Clear range (RANGING_HIGH_VOL)");
               return STATE_RANGING_HIGH_VOL;
            }
            else
            {
               DebugRegimeLogFile("DetectCurrentState", "Tie-breaker: Clear range (RANGING_LOW_VOL)");
               return STATE_RANGING_LOW_VOL;
            }
         }
         else if (trendValid && rangeValid)
         {
            // Both valid - check dominance
            double scoreDifference = trendScore - rangeScore;

            // Trend needs to be SIGNIFICANTLY stronger (>0.15) to win
            if (scoreDifference > 0.15) // Changed from 0.2 to 0.15
            {
               if (volatilityLow || volatilityScore < 0.5)
               {
                  DebugRegimeLogFile("DetectCurrentState", "Tie-breaker: Trend dominant (TRENDING_LOW_VOL)");
                  return STATE_TRENDING_LOW_VOL;
               }
               else
               {
                  DebugRegimeLogFile("DetectCurrentState", "Tie-breaker: Trend dominant (TRENDING_HIGH_VOL)");
                  return STATE_TRENDING_HIGH_VOL;
               }
            }
            // Range needs to be SIGNIFICANTLY stronger to win
            else if (scoreDifference < -0.15) // Range needs larger numbers for advantage
            {
               if (volatilityHigh)
               {
                  DebugRegimeLogFile("DetectCurrentState", "Tie-breaker: Range dominant (RANGING_HIGH_VOL)");
                  return STATE_RANGING_HIGH_VOL;
               }
               else
               {
                  DebugRegimeLogFile("DetectCurrentState", "Tie-breaker: Range dominant (RANGING_LOW_VOL)");
                  return STATE_RANGING_LOW_VOL;
               }
            }
            // Close scores = check additional factors
            else
            {
               DebugRegimeLogFile("DetectCurrentState", "Tie-breaker: Close scores, checking additional factors");

               // Close call - check additional factors
               // 1. Check ADX strength
               if (adx >= 30)
               {
                  // Strong ADX favors trend
                  if (volatilityLow || volatilityScore < 0.5)
                  {
                     DebugRegimeLogFile("DetectCurrentState", "Tie-breaker: ADX favors trend (TRENDING_LOW_VOL)");
                     return STATE_TRENDING_LOW_VOL;
                  }
                  else
                  {
                     DebugRegimeLogFile("DetectCurrentState", "Tie-breaker: ADX favors trend (TRENDING_HIGH_VOL)");
                     return STATE_TRENDING_HIGH_VOL;
                  }
               }
               // 2. Check volatility context
               else if (volatilityHigh)
               {
                  // High volatility favors range (noisy)
                  DebugRegimeLogFile("DetectCurrentState", "Tie-breaker: High vol favors range (RANGING_HIGH_VOL)");
                  return STATE_RANGING_HIGH_VOL;
               }
               // 3. Check recent price action
               else
               {
                  // Default to range for safety (as requested)
                  DebugRegimeLogFile("DetectCurrentState", "Tie-breaker: Default to range (RANGING_LOW_VOL)");
                  return STATE_RANGING_LOW_VOL;
               }
            }
         }
      }

      // ========== FINAL DECISION: NO UNKNOWN STATES ==========
      // If we get here, we need to make a definitive choice

      // Check if we have ANY range evidence
      bool hasRangeEvidence = m_rangeActive || rangeTouches > 0 || volatilityHigh;

      // Check if we have ANY trend evidence
      bool hasTrendEvidence = adx > 20 || isTrendingStructure || maDistancePercent > 0.3;

      DebugRegimeLogFile("DetectCurrentState",
                         StringFormat("Final decision: RangeEvidence=%s, TrendEvidence=%s, Volatility=%.2f, ADX=%.1f",
                                      hasRangeEvidence ? "YES" : "NO",
                                      hasTrendEvidence ? "YES" : "NO",
                                      volatilityScore, adx));

      // DECISION RULES (prioritize RANGE when unclear):
      // 1. If we have ANY range evidence, favor RANGE
      // 2. If ADX is low (<25) and volatility is not high, favor RANGE
      // 3. Only return TREND if we have strong evidence

      if (hasRangeEvidence || adx < 25 || volatilityScore < 0.6)
      {
         // Favor RANGE (safer default)
         if (volatilityHigh)
         {
            DebugRegimeLogFile("DetectCurrentState", "Ambiguous: Favoring STATE_RANGING_HIGH_VOL (safety default)");
            Logger::Log("MarketRegime", "Market State: RANGING_HIGH_VOL (ambiguous, safety default)", true, false);
            return STATE_RANGING_HIGH_VOL;
         }
         else
         {
            DebugRegimeLogFile("DetectCurrentState", "Ambiguous: Favoring STATE_RANGING_LOW_VOL (safety default)");
            Logger::Log("MarketRegime", "Market State: RANGING_LOW_VOL (ambiguous, safety default)", true, false);
            return STATE_RANGING_LOW_VOL;
         }
      }
      else
      {
         // Strong trend evidence
         if (volatilityLow || volatilityScore < 0.5)
         {
            DebugRegimeLogFile("DetectCurrentState", "Ambiguous but strong trend evidence: STATE_TRENDING_LOW_VOL");
            Logger::Log("MarketRegime", "Market State: TRENDING_LOW_VOL (strong evidence)", true, false);
            return STATE_TRENDING_LOW_VOL;
         }
         else
         {
            DebugRegimeLogFile("DetectCurrentState", "Ambiguous but strong trend evidence: STATE_TRENDING_HIGH_VOL");
            Logger::Log("MarketRegime", "Market State: TRENDING_HIGH_VOL (strong evidence)", true, false);
            return STATE_TRENDING_HIGH_VOL;
         }
      }
   }

   ENUM_ROOT_REGIME GetRootState(ENUM_MARKET_STATE state)
   {
      ENUM_ROOT_REGIME rootState;

      switch (state)
      {
      case STATE_TRENDING_LOW_VOL:
      case STATE_TRENDING_HIGH_VOL:
         rootState = REGIME_TRENDING;
         break;

      case STATE_RANGING_LOW_VOL:
      case STATE_RANGING_HIGH_VOL:
         rootState = REGIME_RANGING;
         break;

      default:
         rootState = REGIME_UNKNOWN;
      }

      DebugRegimeLogFile("GetRootState",
                         StringFormat("Root state for %s: %s",
                                      MarketAnalysis::GetStateString(state),
                                      rootState == REGIME_TRENDING ? "TRENDING" : rootState == REGIME_RANGING ? "RANGING"
                                                                                                              : "UNKNOWN"));

      return rootState;
   }

   //+------------------------------------------------------------------+
   //| INDICATOR CALCULATIONS (using IndicatorManager when available)  |
   //+------------------------------------------------------------------+
   double GetADX(int period)
   {
      if (m_useIndicatorManager && m_indicatorManager != NULL && m_indicatorManager.IsInitialized())
      {
         double adx, plus_di, minus_di;
         if (m_indicatorManager.GetADXValues(m_timeframe, adx, plus_di, minus_di))
         {
            return adx;
         }
      }

      // Fallback to direct calculation
      int handle = iADX(m_symbol, m_timeframe, period);
      if (handle == INVALID_HANDLE)
         return 0;

      double adx[];
      ArraySetAsSeries(adx, true);

      if (CopyBuffer(handle, 0, 0, 1, adx) > 0)
      {
         IndicatorRelease(handle);
         return adx[0];
      }

      IndicatorRelease(handle);
      return 0;
   }

   double GetATR(int period)
   {
      if (m_useIndicatorManager && m_indicatorManager != NULL && m_indicatorManager.IsInitialized())
      {
         return m_indicatorManager.GetATR(m_timeframe);
      }

      // Fallback to direct calculation
      int handle = iATR(m_symbol, m_timeframe, period);
      if (handle == INVALID_HANDLE)
         return 0;

      double atr[];
      ArraySetAsSeries(atr, true);

      if (CopyBuffer(handle, 0, 0, 1, atr) > 0)
      {
         IndicatorRelease(handle);
         return atr[0];
      }

      IndicatorRelease(handle);
      return 0;
   }

   double GetBollingerWidth(int period, double deviations)
   {
      if (m_useIndicatorManager && m_indicatorManager != NULL && m_indicatorManager.IsInitialized())
      {
         double upper, middle, lower;
         if (m_indicatorManager.GetBollingerBandsValues(m_timeframe, upper, middle, lower))
         {
            double currentPrice = iClose(m_symbol, m_timeframe, 0);
            if (currentPrice > 0)
               return (upper - lower) / currentPrice;
         }
      }

      // Fallback to direct calculation
      int handle = iBands(m_symbol, m_timeframe, period, 0, deviations, PRICE_CLOSE);
      if (handle == INVALID_HANDLE)
         return 0;

      double upper[], lower[];
      ArraySetAsSeries(upper, true);
      ArraySetAsSeries(lower, true);

      if (CopyBuffer(handle, 1, 0, 1, upper) > 0 &&
          CopyBuffer(handle, 2, 0, 1, lower) > 0)
      {
         IndicatorRelease(handle);
         double currentPrice = iClose(m_symbol, m_timeframe, 0);
         if (currentPrice > 0)
            return (upper[0] - lower[0]) / currentPrice;
      }

      IndicatorRelease(handle);
      return 0;
   }

   double GetPriceChange(int bars)
   {
      double current = iClose(m_symbol, m_timeframe, 0);
      double past = iClose(m_symbol, m_timeframe, bars);

      if (past > 0)
         return ((current - past) / past) * 100;

      return 0;
   }

   int CountRangeTouches()
   {
      if (!m_rangeActive)
         return 0;

      int touches = 0;
      double atr = GetATR(14);
      double tolerance = atr * 0.1;

      for (int i = 0; i < 20; i++)
      {
         double high = iHigh(m_symbol, m_timeframe, i);
         double low = iLow(m_symbol, m_timeframe, i);

         if (MathAbs(high - m_fixedRangeTop) < tolerance ||
             MathAbs(low - m_fixedRangeBottom) < tolerance)
         {
            touches++;
         }
      }

      DebugRegimeLogFile("CountRangeTouches", StringFormat("Range touches: %d", touches));
      return touches;
   }

   // In CalculateVolatilityScore() function:
   double CalculateVolatilityScore(double atr, double bbWidth)
   {
      // Use longer period for smoothing (increase from 20 to 50)
      double atrValues[10];
      int lookback = 10; // Increased from 20

      for (int i = 0; i < lookback; i++)
      {
         double price = iClose(m_symbol, m_timeframe, i);
         if (price > 0)
            atrValues[i] = GetATRValue(14, i) / price;
      }

      double currentAtrNorm = atr / iClose(m_symbol, m_timeframe, 0);
      double avgAtrNorm = ArrayAverage(atrValues, lookback);

      if (avgAtrNorm > 0)
      {
         // Apply smoothing to reduce noise
         static double smoothedScore = 0.5;
         double rawScore = MathMin(1.0, currentAtrNorm / avgAtrNorm);
         smoothedScore = smoothedScore * 0.3 + rawScore * 0.7; // 50% weight to previous value
         return smoothedScore;
      }

      return 0.5;
   }

   bool IsVolatilityRising()
   {
      double atrNow = GetATR(14);
      double atrBefore = GetATRValue(14, 5);

      bool rising = atrNow > atrBefore * 1.1;
      DebugRegimeLogFile("IsVolatilityRising", StringFormat("ATR now=%.5f, before=%.5f, rising=%s",
                                                            atrNow, atrBefore, rising ? "true" : "false"));
      return rising;
   }

   double GetATRValue(int period, int shift)
   {
      if (m_useIndicatorManager && m_indicatorManager != NULL && m_indicatorManager.IsInitialized())
      {
         return m_indicatorManager.GetATR(m_timeframe, shift);
      }

      // Fallback to direct calculation
      int handle = iATR(m_symbol, m_timeframe, period);
      if (handle == INVALID_HANDLE)
         return 0;

      double atr[];
      ArraySetAsSeries(atr, true);

      if (CopyBuffer(handle, 0, shift, 1, atr) > 0)
      {
         IndicatorRelease(handle);
         return atr[0];
      }

      IndicatorRelease(handle);
      return 0;
   }

   bool IsTrendingStructure()
   {
      DebugRegimeLogFile("IsTrendingStructure", "Checking for trending price structure");

      // ========== NEW: ADD MOVING AVERAGE CONFIRMATION ==========
      // Get MA values using IndicatorManager or direct
      double ma50 = GetMAValue(50, 0);
      double ma50_10 = GetMAValue(50, 8);
      double ma89 = GetMAValue(89, 0);
      double ma89_10 = GetMAValue(89, 8);
      double ma200 = GetMAValue(200, 0);

      // Check if we got valid values
      if (ma50 <= 0 || ma89 <= 0 || ma200 <= 0)
      {
         DebugRegimeLogFile("IsTrendingStructure", "Invalid MA values, falling back to basic structure check");
         // Fall back to original price structure logic
         return CheckBasicTrendStructure();
      }

      // Calculate MA separation percentage
      double maDistancePercent = 0;
      double avgPrice = (ma50 + ma89) / 2;
      if (avgPrice > 0)
      {
         maDistancePercent = MathAbs(ma50 - ma89) / avgPrice * 100;
      }

      // Check MA alignment
      bool maAlignedBullish = (ma50 > ma89) && (ma50_10 > ma89_10);
      bool maAlignedBearish = (ma50 < ma89) && (ma50_10 < ma89_10);

      // ========== ENHANCED PRICE STRUCTURE (more forgiving) ==========
      // Look at 8 bars instead of 5
      int lookback = 10;
      double highs[], lows[];
      ArrayResize(highs, lookback);
      ArrayResize(lows, lookback);

      for (int i = 0; i < lookback; i++)
      {
         highs[i] = iHigh(m_symbol, m_timeframe, i);
         lows[i] = iLow(m_symbol, m_timeframe, i);
      }

      // More forgiving trend detection (60% rule instead of 100%)
      int uptrendSignals = 0;
      int downtrendSignals = 0;

      for (int i = 1; i < lookback; i++)
      {
         // Check HH pattern
         if (highs[i] > highs[i - 1])
            uptrendSignals++;
         if (lows[i] > lows[i - 1])
            uptrendSignals++;

         // Check LH pattern
         if (highs[i] < highs[i - 1])
            downtrendSignals++;
         if (lows[i] < lows[i - 1])
            downtrendSignals++;
      }

      int totalChecks = (lookback - 1) * 2;
      double uptrendRatio = totalChecks > 0 ? (double)uptrendSignals / totalChecks : 0;
      double downtrendRatio = totalChecks > 0 ? (double)downtrendSignals / totalChecks : 0;

      DebugRegimeLogFile("IsTrendingStructure",
                         StringFormat("Price structure: Uptrend ratio=%.2f, Downtrend ratio=%.2f",
                                      uptrendRatio, downtrendRatio));

      // ========== DECISION LOGIC (BALANCED) ==========

      // CRITICAL: If MAs are significantly separated (>0.75%), GENTLE trend bias
      // Changed from 0.5% to 0.75% for more selective bias
      if (maDistancePercent > 0.6) // Increase to 0.5 - need clearer MA separation
      {
         // Strong MA separation = trend gets MODERATE priority
         // Changed from 0.4 to 0.5 threshold - need clearer trend structure
         if (maAlignedBullish && uptrendRatio > 0.45)
         {
            DebugRegimeLogFile("IsTrendingStructure", "MA aligned bullish with uptrend structure");
            return true; // Need 50% confirmation (was 40%)
         }
         if (maAlignedBearish && downtrendRatio > 0.45)
         {
            DebugRegimeLogFile("IsTrendingStructure", "MA aligned bearish with downtrend structure");
            return true;
         }
      }

      // Original logic - keep as primary
      if (uptrendRatio > 0.6 || downtrendRatio > 0.6) // 70% threshold
      {
         DebugRegimeLogFile("IsTrendingStructure", "Strong price structure detected");
         return true;
      }

      // Additional check: If price consistently above/below key MAs
      double currentPrice = iClose(m_symbol, m_timeframe, 0);

      // Price far from 200 MA suggests trend
      double distanceFromMA = MathAbs(currentPrice - ma200) / ma200 * 100;
      if (distanceFromMA > 1.0) // Changed from 1.0% to 1.5% - more conservative
      {
         DebugRegimeLogFile("IsTrendingStructure",
                            StringFormat("Price far from MA200 (%.2f%%), forcing trend classification", distanceFromMA));
         return true; // Force trend classification
      }

      DebugRegimeLogFile("IsTrendingStructure", "No trending structure detected");
      return false;
   }

   bool CheckBasicTrendStructure()
   {
      DebugRegimeLogFile("CheckBasicTrendStructure", "Running basic 5-bar trend structure check");

      // Your original 5-bar HH/HL or LH/LL logic
      double highs[5], lows[5];

      for (int i = 0; i < 5; i++)
      {
         highs[i] = iHigh(m_symbol, m_timeframe, i);
         lows[i] = iLow(m_symbol, m_timeframe, i);
      }

      // Check for uptrend structure
      bool uptrend = true;
      for (int i = 1; i < 5; i++)
      {
         if (highs[i] <= highs[i - 1] || lows[i] <= lows[i - 1])
         {
            uptrend = false;
            break;
         }
      }

      // Check for downtrend structure
      bool downtrend = true;
      for (int i = 1; i < 5; i++)
      {
         if (highs[i] >= highs[i - 1] || lows[i] >= lows[i - 1])
         {
            downtrend = false;
            break;
         }
      }

      bool result = uptrend || downtrend;
      DebugRegimeLogFile("CheckBasicTrendStructure",
                         StringFormat("Basic trend check result: %s (Uptrend: %s, Downtrend: %s)",
                                      result ? "true" : "false", uptrend ? "true" : "false", downtrend ? "true" : "false"));

      return result;
   }

   bool IsRangingStructure()
   {
      if (!m_rangeActive)
      {
         DebugRegimeLogFile("IsRangingStructure", "Range not active");
         return false;
      }

      double currentPrice = iClose(m_symbol, m_timeframe, 0);
      bool inRange = IsPriceInFixedRange(currentPrice);

      DebugRegimeLogFile("IsRangingStructure",
                         StringFormat("Price %.5f in range %.5f-%.5f: %s",
                                      currentPrice, m_fixedRangeBottom, m_fixedRangeTop, inRange ? "true" : "false"));

      return inRange;
   }

   double CalculateConfidence(ENUM_MARKET_STATE state)
   {
      DebugRegimeLogFile("CalculateConfidence",
                         StringFormat("Calculating confidence for state: %s", MarketAnalysis::GetStateString(state)));

      double adx = GetADX(14);
      double atrScore = CalculateVolatilityScore(GetATR(14), GetBollingerWidth(20, 2));
      double structureConfidence = 0;

      if (state == STATE_TRENDING_LOW_VOL || state == STATE_TRENDING_HIGH_VOL)
      {
         structureConfidence = IsTrendingStructure() ? 80 : 40;
      }
      else if (state == STATE_RANGING_LOW_VOL || state == STATE_RANGING_HIGH_VOL)
      {
         structureConfidence = IsRangingStructure() ? 80 : 40;
      }

      double confidence = 0;

      switch (state)
      {
      case STATE_CONTRACTION:
         confidence = (1.0 - atrScore) * 100 * 0.7 + (adx < 20 ? 30 : 0);
         break;

      case STATE_EXPANSION:
         confidence = (HasRangeBroken(iClose(m_symbol, m_timeframe, 0)) ? 80 : 30);
         break;

      case STATE_TRENDING_LOW_VOL:
         confidence = MathMin(100, adx * 2) * 0.6 + structureConfidence * 0.4;
         break;

      case STATE_TRENDING_HIGH_VOL:
         confidence = MathMin(100, adx) * 0.7 + structureConfidence * 0.3;
         break;

      case STATE_RANGING_LOW_VOL:
      case STATE_RANGING_HIGH_VOL:
         confidence = (m_rangeActive ? 70 : 30) + (CountRangeTouches() * 5);
         break;

      case STATE_CHURN:
         confidence = (atrScore > 0.7 ? 70 : 30) + (adx > 40 ? 30 : 0);
         break;
      }

      confidence = MathMin(100, MathMax(0, confidence));

      DebugRegimeLogFile("CalculateConfidence",
                         StringFormat("Final confidence: %.1f%% (ADX=%.1f, ATRScore=%.2f, StructureConf=%.0f)",
                                      confidence, adx, atrScore, structureConfidence));

      return confidence;
   }

   //+------------------------------------------------------------------+
   //| STATE PREDICTION                                                |
   //+------------------------------------------------------------------+
   ENUM_MARKET_STATE PredictNextState(ENUM_MARKET_STATE currentState)
   {
      DebugRegimeLogFile("PredictNextState",
                         StringFormat("Predicting next state from: %s", MarketAnalysis::GetStateString(currentState)));

      ENUM_MARKET_STATE nextState;

      // State transition probabilities
      switch (currentState)
      {
      case STATE_RANGING_LOW_VOL:
         // Could compress further or expand into high volatility range
         nextState = STATE_CONTRACTION;
         break;

      case STATE_CONTRACTION:
         // Compression leads to expansion
         nextState = STATE_EXPANSION;
         break;

      case STATE_EXPANSION:
         // Breakout leads to trending or failed breakout leads back to range
         nextState = STATE_TRENDING_LOW_VOL;
         break;

      case STATE_TRENDING_LOW_VOL:
         // Healthy trend matures into high volatility trend
         nextState = STATE_TRENDING_HIGH_VOL;
         break;

      case STATE_TRENDING_HIGH_VOL:
         // Exhaustion leads to churn
         nextState = STATE_CHURN;
         break;

      case STATE_CHURN:
         // Churn leads to new range (high vol)
         nextState = STATE_RANGING_HIGH_VOL;
         break;

      case STATE_RANGING_HIGH_VOL:
         // High vol range stabilizes into low vol range
         nextState = STATE_RANGING_LOW_VOL;
         break;

      default:
         nextState = STATE_UNKNOWN;
      }

      DebugRegimeLogFile("PredictNextState",
                         StringFormat("Predicted next state: %s", MarketAnalysis::GetStateString(nextState)));

      return nextState;
   }

   //+------------------------------------------------------------------+
   //| TRADING RECOMMENDATIONS                                         |
   //+------------------------------------------------------------------+
   void GenerateRecommendations(MarketAnalysis &analysis)
   {
      DebugRegimeLogFile("GenerateRecommendations", "Generating trading recommendations");

      double currentPrice = iClose(m_symbol, m_timeframe, 0);
      double atr = GetATR(14);

      // Default values
      analysis.action = "Wait for clarity";
      analysis.positionSize = SIZE_ZERO;
      analysis.stopDistance = atr * 1.5;
      analysis.takeProfitDistance = atr * 3.0;
      analysis.riskRewardRatio = 2.0;
      analysis.direction = "Neutral";

      // State-specific recommendations
      switch (analysis.state)
      {
      case STATE_RANGING_LOW_VOL:
         analysis.action = "Fade range extremes";
         analysis.positionSize = SIZE_SMALL;
         analysis.stopDistance = (m_fixedRangeTop - m_fixedRangeBottom) * 0.2;
         analysis.takeProfitDistance = (m_fixedRangeTop - m_fixedRangeBottom) * 0.6;
         analysis.riskRewardRatio = 3.0;
         analysis.direction = "Mean reversion";
         DebugRegimeLogFile("GenerateRecommendations", "RANGING_LOW_VOL: Fade extremes, mean reversion");
         break;

      case STATE_RANGING_HIGH_VOL:
         analysis.action = "Fade carefully with tight stops";
         analysis.positionSize = SIZE_VERY_SMALL;
         analysis.stopDistance = atr * 0.8;
         analysis.takeProfitDistance = atr * 1.5;
         analysis.riskRewardRatio = 2.0;
         analysis.direction = "Neutral (dangerous)";
         DebugRegimeLogFile("GenerateRecommendations", "RANGING_HIGH_VOL: Fade carefully, tight stops");
         break;

      case STATE_CONTRACTION:
         analysis.action = "Prepare for breakout - no trades";
         analysis.positionSize = SIZE_ZERO;
         analysis.stopDistance = 0;
         analysis.takeProfitDistance = 0;
         analysis.riskRewardRatio = 0;
         analysis.direction = "Neutral (waiting)";
         DebugRegimeLogFile("GenerateRecommendations", "CONTRACTION: No trades, prepare for breakout");
         break;

      case STATE_EXPANSION:
         analysis.action = "Test breakout entry";
         analysis.positionSize = SIZE_MEDIUM;
         analysis.stopDistance = atr * 1.2;
         analysis.takeProfitDistance = atr * 3.6;
         analysis.riskRewardRatio = 3.0;

         if (currentPrice > m_fixedRangeTop)
         {
            analysis.direction = "Bullish breakout";
            DebugRegimeLogFile("GenerateRecommendations", "EXPANSION: Bullish breakout entry");
         }
         else if (currentPrice < m_fixedRangeBottom)
         {
            analysis.direction = "Bearish breakout";
            DebugRegimeLogFile("GenerateRecommendations", "EXPANSION: Bearish breakout entry");
         }
         else
         {
            analysis.direction = "Testing";
            DebugRegimeLogFile("GenerateRecommendations", "EXPANSION: Testing breakout");
         }
         break;

      case STATE_TRENDING_LOW_VOL:
         analysis.action = "Add to winning positions";
         analysis.positionSize = SIZE_LARGE;
         analysis.stopDistance = atr * 2.0;
         analysis.takeProfitDistance = atr * 6.0;
         analysis.riskRewardRatio = 3.0;
         analysis.direction = IsTrendingStructure() ? "With trend" : "Neutral";
         DebugRegimeLogFile("GenerateRecommendations", "TRENDING_LOW_VOL: Add to winning positions");
         break;

      case STATE_TRENDING_HIGH_VOL:
         analysis.action = "Take partial profits";
         analysis.positionSize = SIZE_MEDIUM;
         analysis.stopDistance = atr * 1.0;
         analysis.takeProfitDistance = atr * 2.0;
         analysis.riskRewardRatio = 2.0;
         analysis.direction = "With trend (cautious)";
         DebugRegimeLogFile("GenerateRecommendations", "TRENDING_HIGH_VOL: Take partial profits");
         break;

      case STATE_CHURN:
         analysis.action = "Exit positions, wait for clarity";
         analysis.positionSize = SIZE_ZERO;
         analysis.stopDistance = 0;
         analysis.takeProfitDistance = 0;
         analysis.riskRewardRatio = 0;
         analysis.direction = "Neutral (avoid)";
         DebugRegimeLogFile("GenerateRecommendations", "CHURN: Exit positions, wait for clarity");
         break;
      }

      // Adjust position size based on account
      analysis.positionSize = AdjustPositionSize(analysis.positionSize);

      DebugRegimeLogFile("GenerateRecommendations",
                         StringFormat("Final recommendation: %s, Position: %s, Stop: %.2f, TP: %.2f, R/R: %.1f",
                                      analysis.action, GetPositionSizeString(analysis.positionSize),
                                      analysis.stopDistance, analysis.takeProfitDistance, analysis.riskRewardRatio));

      // Log trading recommendation
      Logger::LogTrade("Recommendation", m_symbol,
                       analysis.direction == "Bullish breakout" ? "BUY" : analysis.direction == "Bearish breakout" ? "SELL"
                                                                                                                   : "HOLD",
                       0, currentPrice);

      // Convert to pips for display if needed
      double point = SymbolInfoDouble(m_symbol, SYMBOL_POINT);
      if (point > 0)
      {
         analysis.stopDistance = analysis.stopDistance / point;
         analysis.takeProfitDistance = analysis.takeProfitDistance / point;
      }
   }

   ENUM_POSITION_SIZE AdjustPositionSize(ENUM_POSITION_SIZE baseSize)
   {
      // Adjust based on account size and risk
      double riskAmount = m_accountBalance * (m_accountRiskPercent / 100);

      ENUM_POSITION_SIZE adjustedSize = baseSize;

      if (riskAmount < 100) // Very small account
      {
         switch (baseSize)
         {
         case SIZE_LARGE:
            adjustedSize = SIZE_MEDIUM;
            DebugRegimeLogFile("AdjustPositionSize", "Account small: LARGE -> MEDIUM");
            break;
         case SIZE_MEDIUM:
            adjustedSize = SIZE_SMALL;
            DebugRegimeLogFile("AdjustPositionSize", "Account small: MEDIUM -> SMALL");
            break;
         default:
            adjustedSize = baseSize;
         }
      }

      DebugRegimeLogFile("AdjustPositionSize",
                         StringFormat("Position size: %s -> %s (Account: $%.2f, Risk: $%.2f)",
                                      GetPositionSizeString(baseSize),
                                      GetPositionSizeString(adjustedSize),
                                      m_accountBalance, riskAmount));

      return adjustedSize;
   }

   //+------------------------------------------------------------------+
   //| UTILITY FUNCTIONS                                               |
   //+------------------------------------------------------------------+
   double ArrayAverage(double &arr[], int count)
   {
      double sum = 0;
      int validCount = 0;

      for (int i = 0; i < count && i < ArraySize(arr); i++)
      {
         sum += arr[i];
         validCount++;
      }

      double avg = validCount > 0 ? sum / validCount : 0;
      return avg;
   }

   string GenerateDescription(MarketAnalysis &analysis)
   {
      string desc = "";

      switch (analysis.state)
      {
      case STATE_RANGING_LOW_VOL:
         desc = "Low volatility consolidation. Market is balanced, awaiting catalyst.";
         break;

      case STATE_RANGING_HIGH_VOL:
         desc = "High volatility range. Emotional swings, frequent stop hunts.";
         break;

      case STATE_CONTRACTION:
         desc = "Volatility squeeze/compression. Energy building for next move.";
         break;

      case STATE_EXPANSION:
         desc = "Breakout/expansion phase. New trend may be forming.";
         break;

      case STATE_TRENDING_LOW_VOL:
         desc = "Healthy institutional trend. Ideal trading conditions.";
         break;

      case STATE_TRENDING_HIGH_VOL:
         desc = "Parabolic/exhaustion trend. Late stage, reversal risk high.";
         break;

      case STATE_CHURN:
         desc = "Exhaustion/distribution. Directionless volatility, avoid trading.";
         break;

      default:
         desc = "Market state unclear. Wait for better signals.";
      }

      DebugRegimeLogFile("GenerateDescription", StringFormat("Description: %s", desc));

      return desc;
   }

public:
   //+------------------------------------------------------------------+
   //| MANUAL RANGE REINITIALIZATION                                   |
   //+------------------------------------------------------------------+
   void ResetRange()
   {
      DebugRegimeLogFile("ResetRange", "Manual range reset requested");
      Logger::Log("MarketRegime", "Manual range reset", true, true);
      ReinitializeRange();
   }

   //+------------------------------------------------------------------+
   //| GET/SET RANGE CONFIGURATION                                     |
   //+------------------------------------------------------------------+
   int GetBarsSinceLastRangeInit() const { return m_barsSinceLastRangeInit; }
   int GetRangeLookbackBars() const { return m_rangeLookbackBars; }

   void SetRangeLookbackBars(int bars)
   {
      m_rangeLookbackBars = bars;
      string msg = "Range lookback bars set to: " + IntegerToString(bars);
      Print(msg);
      Logger::Log("MarketRegime", msg, true, false);
      DebugRegimeLogFile("SetRangeLookbackBars", msg);
   }

   //+------------------------------------------------------------------+
   //| GETTER METHODS                                                  |
   //+------------------------------------------------------------------+
   double GetRangeTop() const { return m_fixedRangeTop; }
   double GetRangeBottom() const { return m_fixedRangeBottom; }
   bool IsRangeActive() const { return m_rangeActive; }

   void SetAccountInfo(double balance, double riskPercent)
   {
      m_accountBalance = balance;
      m_accountRiskPercent = riskPercent;

      DebugRegimeLogFile("SetAccountInfo",
                         StringFormat("Account info updated: Balance=$%.2f, Risk=%.1f%%",
                                      balance, riskPercent));
   }

   // Access to IndicatorManager for other uses
   IndicatorManager *GetIndicatorManager() const { return m_indicatorManager; }

   // Method to get IndicatorManager initialization status
   bool IsIndicatorManagerInitialized() const
   {
      bool initialized = (m_indicatorManager != NULL && m_indicatorManager.IsInitialized());
      DebugRegimeLogFile("IsIndicatorManagerInitialized",
                         StringFormat("IndicatorManager initialized: %s", initialized ? "true" : "false"));
      return initialized;
   }
};

//+------------------------------------------------------------------+
//| SIMPLE CHART DRAWING FUNCTIONS                                   |
//+------------------------------------------------------------------+
void DrawMarketRegime(MarketAnalysis &analysis, string symbol = NULL, ENUM_TIMEFRAMES tf = PERIOD_H1)
{
   if (symbol == NULL)
      symbol = Symbol();

   long chartId = ChartID();

   // Remove previous drawings
   RemoveRegimeDrawings(chartId);

   // Draw fixed range if active
   MarketRegimeDetector detector(symbol, tf);
   if (detector.IsRangeActive())
   {
      double top = detector.GetRangeTop();
      double bottom = detector.GetRangeBottom();

      // Draw range lines
      DrawHorizontalLine(chartId, "Regime_Range_Top", top, clrBlue, STYLE_DASH, 2);
      DrawHorizontalLine(chartId, "Regime_Range_Bottom", bottom, clrBlue, STYLE_DASH, 2);
   }

   // Draw regime info box
   string infoText = StringFormat(
       "State: %s (%.0f%%)\n" +
           "Action: %s\n" +
           "Position: %s\n" +
           "Direction: %s\n" +
           "R/R: %.1f",
       MarketAnalysis::GetStateString(analysis.state),
       analysis.confidence,
       analysis.action,
       MarketAnalysis::GetStateString(analysis.nextLikelyState),
       analysis.direction,
       analysis.riskRewardRatio);

   DrawText(chartId, "Regime_Info", TimeCurrent() + PeriodSeconds(tf) * 15,
            iClose(symbol, tf, 0), infoText, clrWhite, 10, "Arial");

   ChartRedraw(chartId);
}

void RemoveRegimeDrawings(long chartId)
{
   int total = ObjectsTotal(chartId);
   for (int i = total - 1; i >= 0; i--)
   {
      string name = ObjectName(chartId, i);
      if (StringFind(name, "Regime_") == 0)
      {
         ObjectDelete(chartId, name);
      }
   }
}

void DrawHorizontalLine(long chartId, string name, double price, color clr,
                        ENUM_LINE_STYLE style, int width)
{
   if (ObjectFind(chartId, name) >= 0)
      ObjectDelete(chartId, name);

   ObjectCreate(chartId, name, OBJ_HLINE, 0, 0, price);
   ObjectSetInteger(chartId, name, OBJPROP_COLOR, clr);
   ObjectSetInteger(chartId, name, OBJPROP_STYLE, style);
   ObjectSetInteger(chartId, name, OBJPROP_WIDTH, width);
   ObjectSetInteger(chartId, name, OBJPROP_BACK, true);
}

void DrawText(long chartId, string name, datetime time, double price,
              string text, color clr, int fontSize, string font)
{
   if (ObjectFind(chartId, name) >= 0)
      ObjectDelete(chartId, name);

   ObjectCreate(chartId, name, OBJ_TEXT, 0, time, price);
   ObjectSetString(chartId, name, OBJPROP_TEXT, text);
   ObjectSetInteger(chartId, name, OBJPROP_COLOR, clr);
   ObjectSetInteger(chartId, name, OBJPROP_FONTSIZE, fontSize);
   ObjectSetString(chartId, name, OBJPROP_FONT, font);
   ObjectSetInteger(chartId, name, OBJPROP_BACK, false);
}

//+------------------------------------------------------------------+
//| ENHANCED CHART DISPLAY FUNCTIONS                                |
//+------------------------------------------------------------------+
//+------------------------------------------------------------------+
//| SIMPLIFIED: Show range ONLY when market is ranging              |
//+------------------------------------------------------------------+
void DisplayMarketRegimeOnChart(MarketAnalysis &analysis,
                                string symbol = NULL,
                                ENUM_TIMEFRAMES tf = PERIOD_H1,
                                int corner = CORNER_RIGHT_UPPER)
{
   DebugRegimeLogFile("DisplayMarketRegimeOnChart", "Updating chart display");

   if (symbol == NULL)
      symbol = Symbol();

   long chartId = ChartID();
   string prefix = "MarketRegime_";

   // Remove ALL previous drawings first
   RemoveAllRegimeDrawings(chartId, prefix);

   // ==================== CRITICAL CHECK: Only show in ranging states ====================
   bool shouldShowRange = false;

   switch (analysis.state)
   {
   case STATE_RANGING_LOW_VOL:
   case STATE_RANGING_HIGH_VOL:
   case STATE_CONTRACTION: // Contraction/squeeze is also a ranging state
      shouldShowRange = true;
      DebugRegimeLogFile("DisplayMarketRegimeOnChart", "Showing range (ranging state detected)");
      break;

   case STATE_TRENDING_LOW_VOL:
   case STATE_TRENDING_HIGH_VOL:
   case STATE_EXPANSION: // Expansion/breakout = trending
   case STATE_CHURN:     // Churn/exhaustion = unclear
   case STATE_UNKNOWN:
   default:
      shouldShowRange = false;
      DebugRegimeLogFile("DisplayMarketRegimeOnChart", "Hiding range (non-ranging state)");
   }

   // Only proceed if we should show range
   if (!shouldShowRange)
   {
      // Remove any existing drawings and exit
      ChartRedraw(chartId);
      return;
   }

   // Draw fixed range on chart if active
   MarketRegimeDetector detector(symbol, tf);

   // Double-check: Is range actually active?
   if (detector.IsRangeActive() && shouldShowRange)
   {
      double top = detector.GetRangeTop();
      double bottom = detector.GetRangeBottom();

      // Only draw if range is meaningful (not too narrow)
      double rangeHeight = top - bottom;
      double avgPrice = (top + bottom) / 2;

      if (avgPrice > 0 && (rangeHeight / avgPrice) * 100 > 0.05) // 0.05% minimum width
      {
         // Draw ONLY the range lines (no text, no labels)
         DrawHorizontalLine(chartId, prefix + "RangeTop", top, clrDodgerBlue, STYLE_DASH, 2);
         DrawHorizontalLine(chartId, prefix + "RangeBottom", bottom, clrDodgerBlue, STYLE_DASH, 2);

         DebugRegimeLogFile("DisplayMarketRegimeOnChart",
                            StringFormat("Range drawn: Top=%.5f, Bottom=%.5f, Height=%.2f%%",
                                         top, bottom, (rangeHeight / avgPrice) * 100));
      }
   }

   // If we get here and range shouldn't be shown, the RemoveAllRegimeDrawings
   // at the beginning already cleared any existing drawings

   ChartRedraw(chartId);
}

//+------------------------------------------------------------------+
//| PIXEL-BASED DRAWING FUNCTIONS (for panel)                       |
//+------------------------------------------------------------------+

void DrawTextPixel(long chartId, string name, int x, int y, string text,
                   color clr, int fontSize, string font, int corner)
{
   if (ObjectFind(chartId, name) >= 0)
      ObjectDelete(chartId, name);

   ObjectCreate(chartId, name, OBJ_LABEL, 0, 0, 0);
   ObjectSetInteger(chartId, name, OBJPROP_CORNER, corner);
   ObjectSetInteger(chartId, name, OBJPROP_XDISTANCE, x);
   ObjectSetInteger(chartId, name, OBJPROP_YDISTANCE, y);
   ObjectSetString(chartId, name, OBJPROP_TEXT, text);
   ObjectSetInteger(chartId, name, OBJPROP_COLOR, clr);
   ObjectSetInteger(chartId, name, OBJPROP_FONTSIZE, fontSize);
   ObjectSetString(chartId, name, OBJPROP_FONT, font);
   ObjectSetInteger(chartId, name, OBJPROP_BACK, false);
   ObjectSetInteger(chartId, name, OBJPROP_SELECTABLE, false);
}

void DrawLinePixel(long chartId, string name, int x1, int y1, int x2, int y2,
                   color clr, int width)
{
   if (ObjectFind(chartId, name) >= 0)
      ObjectDelete(chartId, name);

   ObjectCreate(chartId, name, OBJ_TREND, 0, 0, 0);
   ObjectSetInteger(chartId, name, OBJPROP_CORNER, CORNER_LEFT_UPPER);
   ObjectSetInteger(chartId, name, OBJPROP_XDISTANCE, x1);
   ObjectSetInteger(chartId, name, OBJPROP_YDISTANCE, y1);
   ObjectSetInteger(chartId, name, OBJPROP_XSIZE, x2 - x1);
   ObjectSetInteger(chartId, name, OBJPROP_YSIZE, y2 - y1);
   ObjectSetInteger(chartId, name, OBJPROP_COLOR, clr);
   ObjectSetInteger(chartId, name, OBJPROP_WIDTH, width);
   ObjectSetInteger(chartId, name, OBJPROP_BACK, false);
   ObjectSetInteger(chartId, name, OBJPROP_SELECTABLE, false);
   ObjectSetInteger(chartId, name, OBJPROP_RAY, false);
}

void DrawTextOnChart(long chartId, string name, datetime time, double price,
                     string text, color clr, int fontSize, string font,
                     ENUM_ANCHOR_POINT anchor = ANCHOR_LEFT_UPPER)
{
   if (ObjectFind(chartId, name) >= 0)
      ObjectDelete(chartId, name);

   ObjectCreate(chartId, name, OBJ_TEXT, 0, time, price);
   ObjectSetString(chartId, name, OBJPROP_TEXT, text);
   ObjectSetInteger(chartId, name, OBJPROP_COLOR, clr);
   ObjectSetInteger(chartId, name, OBJPROP_FONTSIZE, fontSize);
   ObjectSetString(chartId, name, OBJPROP_FONT, font);
   ObjectSetInteger(chartId, name, OBJPROP_ANCHOR, anchor);
   ObjectSetInteger(chartId, name, OBJPROP_BACK, false);
   ObjectSetInteger(chartId, name, OBJPROP_SELECTABLE, false);
}

void RemoveAllRegimeDrawings(long chartId, string prefix = "MarketRegime_")
{
   int total = ObjectsTotal(chartId);
   for (int i = total - 1; i >= 0; i--)
   {
      string name = ObjectName(chartId, i);
      if (StringFind(name, prefix) == 0)
      {
         ObjectDelete(chartId, name);
      }
   }
}

//+------------------------------------------------------------------+
//| COLOR FUNCTIONS FOR VISUAL FEEDBACK                             |
//+------------------------------------------------------------------+

color GetStateColor(ENUM_MARKET_STATE state)
{
   switch (state)
   {
   case STATE_RANGING_LOW_VOL:
      return clrSteelBlue; // Blue for consolidation
   case STATE_RANGING_HIGH_VOL:
      return clrOrangeRed; // Red-orange for dangerous range
   case STATE_TRENDING_LOW_VOL:
      return clrLimeGreen; // Green for healthy trend
   case STATE_TRENDING_HIGH_VOL:
      return clrGold; // Gold/yellow for exhaustion
   case STATE_CONTRACTION:
      return clrDarkSlateGray; // Dark gray for compression
   case STATE_EXPANSION:
      return clrDodgerBlue; // Bright blue for expansion
   case STATE_CHURN:
      return clrDarkOrange; // Orange for churn
   default:
      return clrGray;
   }
}

color GetTransitionColor(ENUM_MARKET_STATE current, ENUM_MARKET_STATE next)
{
   // Color transitions based on expected move
   if ((current == STATE_CONTRACTION && next == STATE_EXPANSION) ||
       (current == STATE_EXPANSION && next == STATE_TRENDING_LOW_VOL))
      return clrLime; // Green for positive transitions

   if ((current == STATE_TRENDING_HIGH_VOL && next == STATE_CHURN) ||
       (current == STATE_CHURN && next == STATE_RANGING_HIGH_VOL))
      return clrOrangeRed; // Red for negative transitions

   return clrYellow; // Yellow for neutral transitions
}

color GetActionColor(string action)
{
   if (StringFind(action, "Fade") >= 0)
      return clrDodgerBlue; // Blue for mean reversion
   if (StringFind(action, "Add") >= 0 || StringFind(action, "Test") >= 0)
      return clrLime; // Green for trend following
   if (StringFind(action, "Take") >= 0)
      return clrGold; // Yellow for profit taking
   if (StringFind(action, "Exit") >= 0 || StringFind(action, "Wait") >= 0)
      return clrOrangeRed; // Red for exiting/avoiding

   return clrWhite;
}

color GetPositionSizeColor(ENUM_POSITION_SIZE size)
{
   switch (size)
   {
   case SIZE_ZERO:
      return clrGray;
   case SIZE_VERY_SMALL:
      return clrOrangeRed;
   case SIZE_SMALL:
      return clrOrange;
   case SIZE_MEDIUM:
      return clrYellow;
   case SIZE_LARGE:
      return clrLime;
   default:
      return clrWhite;
   }
}

color GetDirectionColor(string direction)
{
   if (StringFind(direction, "Bullish") >= 0 || StringFind(direction, "Up") >= 0)
      return clrLime;
   if (StringFind(direction, "Bearish") >= 0 || StringFind(direction, "Down") >= 0)
      return clrRed;
   if (StringFind(direction, "With trend") >= 0)
      return clrLimeGreen;
   if (StringFind(direction, "Mean reversion") >= 0)
      return clrDodgerBlue;
   if (StringFind(direction, "Neutral") >= 0)
      return clrGray;

   return clrYellow;
}

color GetRRColor(double rrRatio)
{
   if (rrRatio >= 3.0)
      return clrLime;
   if (rrRatio >= 2.0)
      return clrYellow;
   if (rrRatio > 0)
      return clrOrange;

   return clrGray;
}

color GetRangePositionColor(double positionPercent)
{
   if (positionPercent < 20)
      return clrRed; // Oversold near bottom
   if (positionPercent > 80)
      return clrLime; // Overbought near top
   if (positionPercent < 40)
      return clrOrange; // Lower half
   if (positionPercent > 60)
      return clrDodgerBlue; // Upper half

   return clrYellow; // Middle
}

//+------------------------------------------------------------------+
//| ENHANCED GLOBAL HELPER FUNCTION                                  |
//+------------------------------------------------------------------+
MarketAnalysis GetMarketRegimeWithDisplay(string symbol = NULL,
                                          ENUM_TIMEFRAMES tf = PERIOD_H1,
                                          bool showPanel = true,
                                          int panelCorner = CORNER_RIGHT_UPPER,
                                          bool useIndicatorManager = true)
{
   static MarketRegimeDetector *detector = NULL;

   if (detector == NULL)
   {
      detector = new MarketRegimeDetector(symbol, tf, 10000, 1.0, useIndicatorManager);
      Logger::Log("MarketRegime", StringFormat("Global detector initialized for %s", symbol), true, false);
   }

   // Get analysis
   MarketAnalysis analysis = detector.GetMarketRegime();

   // Display on chart if requested
   if (showPanel)
   {
      DisplayMarketRegimeOnChart(analysis, symbol, tf, panelCorner);
   }

   return analysis;
}
//+------------------------------------------------------------------+























































































































































given this file:

//+------------------------------------------------------------------+
//|                                                   MarketRegime.mq5|
//|             Simplified Market Regime & Lifecycle Detection       |
//+------------------------------------------------------------------+
// Logger Integration Status: FULLY INTEGRATED
// All logging uses static Logger::Log() calls with debug wrapper
//+------------------------------------------------------------------+

#property copyright "Copyright 2024"
#property version "1.00"
#property strict

#include <Math\Stat\Math.mqh>

#include "../Headers/Enums.mqh"
#include "../Headers/Structures.mqh"
#include "../Data/IndicatorManager.mqh"
#include "../Utils/Logger.mqh"

// ====================== DEBUG SETTINGS ======================
bool DEBUG_REGIME_ENABLED = true;

// Simple debug function using Logger
void DebugRegimeLogFile(string context, string message)
{
   if (DEBUG_REGIME_ENABLED)
   {
      Logger::Log("MarketRegime", StringFormat("%s: %s", context, message), true, false);
   }
}

//+------------------------------------------------------------------+
//| Market Regime Detector Class                                     |
//+------------------------------------------------------------------+
class MarketRegimeDetector
{
private:
   string m_symbol;
   ENUM_TIMEFRAMES m_timeframe;
   double m_fixedRangeTop;
   double m_fixedRangeBottom;
   datetime m_rangeStartTime;
   bool m_rangeActive;
   int m_lookbackBars;

   // Account info for position sizing
   double m_accountBalance;
   double m_accountRiskPercent;

   // Indicator Manager instance
   IndicatorManager *m_indicatorManager;
   bool m_useIndicatorManager;

   int m_rangeLookbackBars;       // How many bars to look back for the range
   int m_barsSinceLastRangeInit;  // Bars since last range initialization
   datetime m_lastRangeCheckTime; // Last time we checked for reinitialization

   // NEW: Range invalidation tracking
   datetime m_lastInvalidationTime; // Track when range was invalidated
   int m_invalidationCooldownBars;  // Bars to wait before new range

public:
   MarketRegimeDetector(string symbol = NULL,
                        ENUM_TIMEFRAMES tf = PERIOD_H1,
                        double accountBalance = 10000,
                        double riskPercent = 1.0,
                        bool useIndicatorManager = true)
   {
      m_symbol = (symbol == NULL) ? Symbol() : symbol;
      m_timeframe = tf;
      m_rangeActive = false;
      m_lookbackBars = 10;
      m_accountBalance = accountBalance;
      m_accountRiskPercent = riskPercent;
      m_useIndicatorManager = useIndicatorManager;

      m_rangeLookbackBars = 10; // Default: reinitialize every 8 bars
      m_barsSinceLastRangeInit = 0;
      m_lastRangeCheckTime = 0;

      // NEW: Initialize invalidation tracking
      m_lastInvalidationTime = 0;
      m_invalidationCooldownBars = 8; // Wait 8 bars after invalidation

      DebugRegimeLogFile("Constructor", StringFormat("Initializing detector for %s timeframe %d",
                                                     m_symbol, m_timeframe));

      // Initialize indicator manager if requested
      if (m_useIndicatorManager)
      {
         m_indicatorManager = new IndicatorManager(m_symbol);
         if (!m_indicatorManager.Initialize())
         {
            Logger::LogError("MarketRegime", "IndicatorManager failed to initialize. Falling back to direct indicators.");
            m_useIndicatorManager = false;
         }
         else
         {
            DebugRegimeLogFile("Constructor", "IndicatorManager initialized successfully");
         }
      }
      else
      {
         m_indicatorManager = NULL;
         DebugRegimeLogFile("Constructor", "Using direct indicators (no IndicatorManager)");
      }

      // Initialize fixed range
      InitializeFixedRange();
   }

   ~MarketRegimeDetector()
   {
      DebugRegimeLogFile("Destructor", "Cleaning up MarketRegimeDetector");

      if (m_indicatorManager != NULL)
      {
         m_indicatorManager.Deinitialize();
         delete m_indicatorManager;
         DebugRegimeLogFile("Destructor", "IndicatorManager cleaned up");
      }

      Logger::Log("MarketRegime", "Detector shutdown complete", true, false);
   }

   //+------------------------------------------------------------------+
   //| MAIN PUBLIC METHOD: Get complete market analysis                |
   //+------------------------------------------------------------------+
   MarketAnalysis GetMarketRegime()
   {
      DebugRegimeLogFile("GetMarketRegime", "Starting market analysis");

      MarketAnalysis analysis;

      // 1. Check if we should reinitialize the range
      CheckAndReinitializeRange();

      // 2. Initialize fixed range if not active AND conditions are right
      if (!m_rangeActive && ShouldCreateNewRange())
      {
         InitializeFixedRange();
      }

      // 3. Get current state using indicators
      analysis.state = DetectCurrentState();
      analysis.rootState = GetRootState(analysis.state);
      analysis.confidence = CalculateConfidence(analysis.state);

      DebugRegimeLogFile("GetMarketRegime", StringFormat("Detected state: %s with %.1f%% confidence",
                                                         MarketAnalysis::GetStateString(analysis.state), analysis.confidence));

      // 4. Determine next likely state
      analysis.nextLikelyState = PredictNextState(analysis.state);

      DebugRegimeLogFile("GetMarketRegime", StringFormat("Next likely state: %s",
                                                         MarketAnalysis::GetStateString(analysis.nextLikelyState)));

      // 5. Generate trading recommendations
      GenerateRecommendations(analysis);

      DebugRegimeLogFile("GetMarketRegime", StringFormat("Recommendation: %s (Position: %s)",
                                                         analysis.action, GetPositionSizeString(analysis.positionSize)));

      // 6. Set description
      analysis.description = GenerateDescription(analysis);

      DebugRegimeLogFile("GetMarketRegime", "Analysis complete");

      // Log final analysis summary
      Logger::Log("MarketRegime", StringFormat("ANALYSIS: %s | Confidence: %.0f%% | Action: %s | R/R: %.1f", MarketAnalysis::GetStateString(analysis.state), analysis.confidence, analysis.action, analysis.riskRewardRatio),
                  true, true);

      return analysis;
   }

private:
   void CheckAndReinitializeRange()
   {
      datetime currentTime = iTime(m_symbol, m_timeframe, 0);

      if (m_lastRangeCheckTime == currentTime)
         return;

      m_lastRangeCheckTime = currentTime;
      m_barsSinceLastRangeInit++;

      DebugRegimeLogFile("CheckAndReinitializeRange",
                         StringFormat("Bar count since last init: %d", m_barsSinceLastRangeInit));

      // Use smart reinitialization logic
      if (ShouldSmartReinitialize())
      {
         ReinitializeRange();
      }
   }

   //+------------------------------------------------------------------+
   //| SMART RANGE REINITIALIZATION                                    |
   //+------------------------------------------------------------------+
   bool ShouldSmartReinitialize()
   {
      DebugRegimeLogFile("ShouldSmartReinitialize", "Checking if smart reinitialization is needed");

      // Get current market state
      ENUM_MARKET_STATE state = DetectCurrentState();
      double confidence = CalculateConfidence(state);

      // Condition 1: Clear ranging state with high confidence
      bool isClearRanging = (state == STATE_RANGING_LOW_VOL || state == STATE_RANGING_HIGH_VOL) &&
                            confidence > 50;

      // Condition 2: Range is too old (more than X bars)
      bool rangeIsOld = m_barsSinceLastRangeInit >= m_rangeLookbackBars;

      // Condition 3: Price has moved slightly outside the range
      bool priceOutOfRange = false;
      if (m_rangeActive)
      {
         double currentPrice = iClose(m_symbol, m_timeframe, 0);
         double rangeWidth = m_fixedRangeTop - m_fixedRangeBottom;
         double atr = GetATR(14);

         if (currentPrice > m_fixedRangeTop + (rangeWidth * 0.3) ||
             currentPrice < m_fixedRangeBottom - (rangeWidth * 0.3))
         {
            priceOutOfRange = true;
         }
      }

      // Decision matrix
      if (rangeIsOld)
      {
         if (isClearRanging || priceOutOfRange)
         {
            DebugRegimeLogFile("ShouldSmartReinitialize", "Conditions met for smart reinitialization");
            return true;
         }

         // Reinitialize if market is in contraction/expansion phase
         if (state == STATE_CONTRACTION || state == STATE_EXPANSION)
         {
            DebugRegimeLogFile("ShouldSmartReinitialize", "Contraction/Expansion phase detected");
            return true;
         }
      }

      DebugRegimeLogFile("ShouldSmartReinitialize", "No need for smart reinitialization");
      return false;
   }

   void ReinitializeRange()
   {
      DebugRegimeLogFile("ReinitializeRange", "Reinitializing fixed range...");
      Logger::Log("MarketRegime", "Reinitializing fixed range", true, false);
      InitializeFixedRange();
      m_barsSinceLastRangeInit = 0;
   }

   void InitializeFixedRange()
   {
      DebugRegimeLogFile("InitializeFixedRange", "Initializing new fixed range");

      int bars = 10; // Fixed 5-bar lookback for initial range
      double highest = 0;
      double lowest = DBL_MAX;

      for (int i = 0; i < bars; i++)
      {
         double high = iHigh(m_symbol, m_timeframe, i);
         double low = iLow(m_symbol, m_timeframe, i);

         if (high > highest)
            highest = high;
         if (low < lowest)
            lowest = low;
      }

      if (highest > lowest)
      {
         m_fixedRangeTop = highest;
         m_fixedRangeBottom = lowest;
         m_rangeStartTime = iTime(m_symbol, m_timeframe, 0);
         m_rangeActive = true;
         m_barsSinceLastRangeInit = 0;

         string rangeInfo = StringFormat("Fixed Range Initialized: %.5f - %.5f (Width: %.2f%%)",
                                         lowest, highest,
                                         ((highest - lowest) / ((highest + lowest) / 2)) * 100);

         Print(rangeInfo);
         Logger::Log("MarketRegime", rangeInfo, true, true);
         DebugRegimeLogFile("InitializeFixedRange", rangeInfo);

         // Log range details
         Logger::LogTrade("Range", m_symbol, "RANGE", 0, (highest + lowest) / 2);
      }
      else
      {
         Logger::LogError("MarketRegime", "Failed to initialize fixed range");
         DebugRegimeLogFile("InitializeFixedRange", "Failed to initialize fixed range");
      }
   }

   //+------------------------------------------------------------------+
   //| STATE DETECTION LOGIC - NO UNKNOWN STATES                        |
   //+------------------------------------------------------------------+
   ENUM_MARKET_STATE DetectCurrentState()
   {
      DebugRegimeLogFile("DetectCurrentState", "Starting state detection - NO UNKNOWN states");

      // NEW: Check if current range should be invalidated
      CheckAndInvalidateRange();

      // Get key indicator values
      double adx = GetADX(14);
      double atr = GetATR(14);
      double bbWidth = GetBollingerWidth(20, 2);
      double priceChange = GetPriceChange(8);
      double rangeTouches = CountRangeTouches();

      DebugRegimeLogFile("DetectCurrentState",
                         StringFormat("Indicators: ADX=%.1f, ATR=%.5f, BBWidth=%.4f, PriceChange=%.2f%%, RangeTouches=%d",
                                      adx, atr, bbWidth, priceChange, rangeTouches));

      // ========== MA50 vs PRICE ANALYSIS for TREND BIAS ==========
      double ma50 = GetMAValue(50, 0);
      double ma50_5 = GetMAValue(50, 5); // MA 5 bars ago for slope
      double currentPrice = iClose(m_symbol, m_timeframe, 0);

      // Calculate price position relative to MA50
      double priceVsMA = 0;
      double trendBias = 0;
      double rangeBias = 0;

      if (ma50 > 0)
      {
         // Price distance from MA50 as percentage
         priceVsMA = MathAbs(currentPrice - ma50) / ma50 * 100;

         // Calculate MA slope (rising or falling)
         double maSlope = (ma50 - ma50_5) / 5.0; // Approximate slope per bar

         // Strong trend bias if:
         // 1. Price is far from MA (>1% away) AND
         // 2. MA has clear slope (trending)
         bool priceFarFromMA = priceVsMA > 1.0;
         bool maHasSlope = MathAbs(maSlope) > 0.0001;
         bool priceAboveMA = currentPrice > ma50;
         bool priceBelowMA = currentPrice < ma50;

         if (priceFarFromMA && maHasSlope)
         {
            // Check if price and MA alignment suggests trend
            if ((priceAboveMA && maSlope > 0) || (priceBelowMA && maSlope < 0))
            {
               // Strong uptrend or downtrend
               trendBias = MathMin(1.0, priceVsMA / 2.0); // Scales 1-2% distance to 0.5-1.0 bias
               trendBias = MathMax(0.3, trendBias);       // Minimum 0.3 bias for clear trends

               // Extra bias for very strong moves
               if (priceVsMA > 1.5 && MathAbs(maSlope) > 0.0002)
               {
                  trendBias = MathMin(1.0, trendBias + 0.2);
               }
            }
            else if ((priceAboveMA && maSlope < 0) || (priceBelowMA && maSlope > 0))
            {
               // Price is pulling back to MA - possible range or correction
               rangeBias = MathMin(1.0, 0.3 + (priceVsMA / 3.0));
            }
         }
         else if (priceVsMA < 0.5)
         {
            // Price close to MA50 suggests ranging
            rangeBias = MathMin(1.0, 0.5 - (priceVsMA * 0.8));
         }

         DebugRegimeLogFile("DetectCurrentState",
                            StringFormat("MA50 Analysis: Price=%.5f, MA50=%.5f, Distance=%.2f%%, Slope=%.6f, TrendBias=%.2f, RangeBias=%.2f",
                                         currentPrice, ma50, priceVsMA, maSlope, trendBias, rangeBias));
      }

      double volatilityScore = CalculateVolatilityScore(atr, bbWidth);
      bool volatilityLow = volatilityScore < 0.3;
      bool volatilityHigh = volatilityScore > 0.7;
      bool volatilityRising = IsVolatilityRising();

      DebugRegimeLogFile("DetectCurrentState",
                         StringFormat("Volatility: Score=%.2f, Low=%s, High=%s, Rising=%s",
                                      volatilityScore, volatilityLow ? "true" : "false",
                                      volatilityHigh ? "true" : "false", volatilityRising ? "true" : "false"));

      // Price structure analysis
      bool isTrendingStructure = IsTrendingStructure();
      bool isRangingStructure = IsRangingStructure();

      DebugRegimeLogFile("DetectCurrentState",
                         StringFormat("Structure: Trending=%s, Ranging=%s",
                                      isTrendingStructure ? "true" : "false",
                                      isRangingStructure ? "true" : "false"));

      // State confidence check before ranging logic
      if (m_rangeActive)
      {
         // Check if range is still valid before using it
         if (rangeTouches < 2) // Not enough touches
         {
            // Range exists but isn't being respected
            DebugRegimeLogFile("DetectCurrentState",
                               StringFormat("Range weak: only %d touches, likely breaking out", rangeTouches));
            CheckAndInvalidateRange(); // Mark as invalid
            // Return expansion (trend-like state) instead of unknown
            DebugRegimeLogFile("DetectCurrentState", "Range weak, returning STATE_EXPANSION (trend-like)");
            Logger::Log("MarketRegime", "Market State: EXPANSION (Weak range breaking out)", true, false);
            return STATE_EXPANSION;
         }
      }

      // Decision matrix - FIRST PASS: Clear cases
      // 1. Check for Contraction/Squeeze (STATE_CONTRACTION) - treat as RANGE
      if (volatilityLow && !volatilityRising && adx < 15 && rangeTouches < 2)
      {
         DebugRegimeLogFile("DetectCurrentState", "Detected STATE_CONTRACTION (treat as RANGE)");
         Logger::Log("MarketRegime", "Market State: CONTRACTION (Volatility squeeze - RANGE)", true, false);
         return STATE_CONTRACTION; // Contraction is a ranging state
      }

      // 2. Check for Expansion/Breakout (STATE_EXPANSION) - treat as TREND
      if (HasRangeBroken(currentPrice) &&
          MathAbs(priceChange) > atr * 2)
      {
         DebugRegimeLogFile("DetectCurrentState", "Detected STATE_EXPANSION (treat as TREND)");
         Logger::Log("MarketRegime", "Market State: EXPANSION (Breakout detected - TREND)", true, false);
         return STATE_EXPANSION; // Expansion is a trend state
      }

      // 3. Check for Churn/Exhaustion (STATE_CHURN) - treat as RANGE
      if (volatilityHigh && volatilityRising && adx > 40 &&
          MathAbs(priceChange) < atr * 0.5)
      {
         DebugRegimeLogFile("DetectCurrentState", "Detected STATE_CHURN (treat as RANGE)");
         Logger::Log("MarketRegime", "Market State: CHURN (Exhaustion phase - RANGE)", true, false);
         return STATE_CHURN; // Churn is a ranging state
      }

      // ========== ADD RANGE STRENGTH CHECK ==========
      // Check if price is respecting range boundaries
      bool priceNearRangeBoundary = false;
      if (m_rangeActive)
      {
         double rangeHeight = m_fixedRangeTop - m_fixedRangeBottom;
         double pricePosition = (currentPrice - m_fixedRangeBottom) / rangeHeight;

         // Price near top or bottom (within 15% of boundary)
         if (pricePosition < 0.15 || pricePosition > 0.85)
         {
            priceNearRangeBoundary = true;
            rangeBias = MathMin(1.0, rangeBias + 0.4);
         }

         // Multiple range touches = stronger range
         if (rangeTouches >= 3)
         {
            rangeBias = MathMin(1.0, rangeBias + 0.3);
         }
      }

      // ========== BALANCED DECISION MAKING ==========

      // 4. Check trending states WITH BIAS CONSIDERATION
      if (isTrendingStructure)
      {
         // RESTORE ADX THRESHOLD to 25 (from 22)
         if (adx >= 25) // increase makes trends harder to qualify
         {
            // Apply trend bias from MA50 analysis
            double trendStrength = adx / 100.0; // Normalize ADX 0-1
            double finalTrendScore = trendStrength + (trendBias * 0.25) - (rangeBias * 0.35);

            // Need reasonable trend strength even with MA bias
            if (finalTrendScore > 0.45) // higher score to make harder for trend to qualify
            {
               // Healthy trend
               if (volatilityLow || volatilityScore < 0.5)
               {
                  DebugRegimeLogFile("DetectCurrentState", "Detected STATE_TRENDING_LOW_VOL");
                  Logger::Log("MarketRegime", "Market State: TRENDING_LOW_VOL (Healthy trend)", true, false);
                  return STATE_TRENDING_LOW_VOL;
               }
               else
               {
                  DebugRegimeLogFile("DetectCurrentState", "Detected STATE_TRENDING_HIGH_VOL");
                  Logger::Log("MarketRegime", "Market State: TRENDING_HIGH_VOL (High vol trend)", true, false);
                  return STATE_TRENDING_HIGH_VOL;
               }
            }
         }
         else if (adx > 40) // Keep this for exhaustion trend
         {
            // Exhaustion trend
            DebugRegimeLogFile("DetectCurrentState", "Detected STATE_TRENDING_HIGH_VOL (Exhaustion)");
            Logger::Log("MarketRegime", "Market State: TRENDING_HIGH_VOL (Exhaustion)", true, false);
            return STATE_TRENDING_HIGH_VOL;
         }
      }

      // 5. Check ranging states WITH BIAS CONSIDERATION
      // SIMPLIFIED RANGING LOGIC
      if (m_rangeActive && IsPriceInFixedRange(currentPrice))
      {
         // Check range strength
         if (rangeTouches >= 2) // Range has been respected
         {
            // Simple: If in range and price is within it, we're ranging
            if (volatilityHigh)
            {
               DebugRegimeLogFile("DetectCurrentState", "Detected STATE_RANGING_HIGH_VOL");
               Logger::Log("MarketRegime", "Market State: RANGING_HIGH_VOL (High volatility range)", true, false);
               return STATE_RANGING_HIGH_VOL;
            }
            else
            {
               DebugRegimeLogFile("DetectCurrentState", "Detected STATE_RANGING_LOW_VOL");
               Logger::Log("MarketRegime", "Market State: RANGING_LOW_VOL (Low volatility range)", true, false);
               return STATE_RANGING_LOW_VOL;
            }
         }
      }

      // ========== TIE-BREAKER: If both trend and range have similar scores ==========
      if (isTrendingStructure && m_rangeActive)
      {
         DebugRegimeLogFile("DetectCurrentState", "Running tie-breaker logic (both trend and range possible)");

         // ========== BALANCED SCORING ==========
         // Trend factors (0-1 scale)
         double trendADX = MathMin(1.0, adx / 100.0);             // ADX contribution
         double trendPriceMA = MathMin(0.3, priceVsMA / 3.0);     // Price distance from MA50
         double trendStructure = isTrendingStructure ? 0.4 : 0.1; // Price structure

         // Range factors (0-1 scale)
         double rangeActive = m_rangeActive ? 0.5 : 0.0;              // Range is active
         double rangeTouchesScore = MathMin(0.3, rangeTouches * 0.1); // Range touches
         double rangePositionScore = 0.0;

         // Calculate price position in range (if in range)
         if (m_rangeActive)
         {
            double rangeHeight = m_fixedRangeTop - m_fixedRangeBottom;
            double pricePosition = (currentPrice - m_fixedRangeBottom) / rangeHeight;
            // Price in middle of range = stronger range evidence
            if (pricePosition > 0.3 && pricePosition < 0.7)
               rangePositionScore = 0.2;
         }

         // Composite scores with BALANCED weights
         double trendScore = (trendADX * 0.5) + (trendPriceMA * 0.3) + (trendStructure * 0.2);
         double rangeScore = (rangeActive * 0.4) + (rangeTouchesScore * 0.3) + (rangePositionScore * 0.3);

         DebugRegimeLogFile("DetectCurrentState",
                            StringFormat("Tie-breaker scores: Trend=%.2f, Range=%.2f", trendScore, rangeScore));

         // ========== DECISION WITH THRESHOLDS ==========
         // Both need reasonable minimum scores
         bool trendValid = trendScore >= 0.3;
         bool rangeValid = rangeScore >= 0.3;

         if (trendValid && !rangeValid)
         {
            // Clear trend
            if (volatilityLow || volatilityScore < 0.5)
            {
               DebugRegimeLogFile("DetectCurrentState", "Tie-breaker: Clear trend (TRENDING_LOW_VOL)");
               return STATE_TRENDING_LOW_VOL;
            }
            else
            {
               DebugRegimeLogFile("DetectCurrentState", "Tie-breaker: Clear trend (TRENDING_HIGH_VOL)");
               return STATE_TRENDING_HIGH_VOL;
            }
         }
         else if (rangeValid && !trendValid)
         {
            // Clear range
            if (volatilityHigh)
            {
               DebugRegimeLogFile("DetectCurrentState", "Tie-breaker: Clear range (RANGING_HIGH_VOL)");
               return STATE_RANGING_HIGH_VOL;
            }
            else
            {
               DebugRegimeLogFile("DetectCurrentState", "Tie-breaker: Clear range (RANGING_LOW_VOL)");
               return STATE_RANGING_LOW_VOL;
            }
         }
         else if (trendValid && rangeValid)
         {
            // Both valid - check dominance
            double scoreDifference = trendScore - rangeScore;

            // Trend needs to be SIGNIFICANTLY stronger (>0.15) to win
            if (scoreDifference > 0.15) // Changed from 0.2 to 0.15
            {
               if (volatilityLow || volatilityScore < 0.5)
               {
                  DebugRegimeLogFile("DetectCurrentState", "Tie-breaker: Trend dominant (TRENDING_LOW_VOL)");
                  return STATE_TRENDING_LOW_VOL;
               }
               else
               {
                  DebugRegimeLogFile("DetectCurrentState", "Tie-breaker: Trend dominant (TRENDING_HIGH_VOL)");
                  return STATE_TRENDING_HIGH_VOL;
               }
            }
            // Range needs to be SIGNIFICANTLY stronger to win
            else if (scoreDifference < -0.15) // Range needs larger numbers for advantage
            {
               if (volatilityHigh)
               {
                  DebugRegimeLogFile("DetectCurrentState", "Tie-breaker: Range dominant (RANGING_HIGH_VOL)");
                  return STATE_RANGING_HIGH_VOL;
               }
               else
               {
                  DebugRegimeLogFile("DetectCurrentState", "Tie-breaker: Range dominant (RANGING_LOW_VOL)");
                  return STATE_RANGING_LOW_VOL;
               }
            }
            // Close scores = check additional factors
            else
            {
               DebugRegimeLogFile("DetectCurrentState", "Tie-breaker: Close scores, checking additional factors");

               // Close call - check additional factors
               // 1. Check ADX strength
               if (adx >= 30)
               {
                  // Strong ADX favors trend
                  if (volatilityLow || volatilityScore < 0.5)
                  {
                     DebugRegimeLogFile("DetectCurrentState", "Tie-breaker: ADX favors trend (TRENDING_LOW_VOL)");
                     return STATE_TRENDING_LOW_VOL;
                  }
                  else
                  {
                     DebugRegimeLogFile("DetectCurrentState", "Tie-breaker: ADX favors trend (TRENDING_HIGH_VOL)");
                     return STATE_TRENDING_HIGH_VOL;
                  }
               }
               // 2. Check volatility context
               else if (volatilityHigh)
               {
                  // High volatility favors range (noisy)
                  DebugRegimeLogFile("DetectCurrentState", "Tie-breaker: High vol favors range (RANGING_HIGH_VOL)");
                  return STATE_RANGING_HIGH_VOL;
               }
               // 3. Check recent price action
               else
               {
                  // Default to range for safety (as requested)
                  DebugRegimeLogFile("DetectCurrentState", "Tie-breaker: Default to range (RANGING_LOW_VOL)");
                  return STATE_RANGING_LOW_VOL;
               }
            }
         }
      }

      // ========== FINAL DECISION: NO UNKNOWN STATES ==========
      // If we get here, we need to make a definitive choice

      // Check if we have ANY range evidence
      bool hasRangeEvidence = m_rangeActive || rangeTouches > 0 || volatilityHigh || priceVsMA < 0.5;

      // Check if we have ANY trend evidence
      bool hasTrendEvidence = adx > 20 || isTrendingStructure || priceVsMA > 1.0;

      DebugRegimeLogFile("DetectCurrentState",
                         StringFormat("Final decision: RangeEvidence=%s, TrendEvidence=%s, PriceVsMA=%.2f%%, Volatility=%.2f, ADX=%.1f",
                                      hasRangeEvidence ? "YES" : "NO",
                                      hasTrendEvidence ? "YES" : "NO",
                                      priceVsMA, volatilityScore, adx));

      // DECISION RULES (prioritize RANGE when unclear):
      // 1. If we have ANY range evidence, favor RANGE
      // 2. If ADX is low (<25) and volatility is not high, favor RANGE
      // 3. Only return TREND if we have strong evidence

      if (hasRangeEvidence || adx < 25 || volatilityScore < 0.6)
      {
         // Favor RANGE (safer default)
         if (volatilityHigh)
         {
            DebugRegimeLogFile("DetectCurrentState", "Ambiguous: Favoring STATE_RANGING_HIGH_VOL (safety default)");
            Logger::Log("MarketRegime", "Market State: RANGING_HIGH_VOL (ambiguous, safety default)", true, false);
            return STATE_RANGING_HIGH_VOL;
         }
         else
         {
            DebugRegimeLogFile("DetectCurrentState", "Ambiguous: Favoring STATE_RANGING_LOW_VOL (safety default)");
            Logger::Log("MarketRegime", "Market State: RANGING_LOW_VOL (ambiguous, safety default)", true, false);
            return STATE_RANGING_LOW_VOL;
         }
      }
      else
      {
         // Strong trend evidence
         if (volatilityLow || volatilityScore < 0.5)
         {
            DebugRegimeLogFile("DetectCurrentState", "Ambiguous but strong trend evidence: STATE_TRENDING_LOW_VOL");
            Logger::Log("MarketRegime", "Market State: TRENDING_LOW_VOL (strong evidence)", true, false);
            return STATE_TRENDING_LOW_VOL;
         }
         else
         {
            DebugRegimeLogFile("DetectCurrentState", "Ambiguous but strong trend evidence: STATE_TRENDING_HIGH_VOL");
            Logger::Log("MarketRegime", "Market State: TRENDING_HIGH_VOL (strong evidence)", true, false);
            return STATE_TRENDING_HIGH_VOL;
         }
      }
   }

   //+------------------------------------------------------------------+
   //| CHECK AND INVALIDATE RANGE METHOD                                |
   //+------------------------------------------------------------------+
   void CheckAndInvalidateRange()
   {
      if (!m_rangeActive)
      {
         DebugRegimeLogFile("CheckAndInvalidateRange", "Range not active, nothing to invalidate");
         return;
      }

      DebugRegimeLogFile("CheckAndInvalidateRange", "Checking if range should be invalidated");

      double currentPrice = iClose(m_symbol, m_timeframe, 0);

      // Rule 1: Consecutive closes outside range
      int consecutiveOutside = 0;
      for (int i = 0; i < 3; i++) // Check last 3 bars
      {
         double price = iClose(m_symbol, m_timeframe, i);
         if (!IsPriceInFixedRange(price))
            consecutiveOutside++;
         else
            break; // Break streak if price returns to range
      }

      if (consecutiveOutside >= 2)
      {
         InvalidateRange("2+ consecutive closes outside range");
         return;
      }

      // Rule 2: Strong momentum break with ATR expansion
      double atr = GetATR(14);
      double atr_5 = GetATRValue(14, 5);
      double priceChange = GetPriceChange(3); // Last 3 bars change

      bool strongBreakout = HasRangeBroken(currentPrice) &&
                            (atr > atr_5 * 1.2) &&            // ATR expanded 20%
                            MathAbs(priceChange) > atr * 1.5; // Strong momentum

      if (strongBreakout)
      {
         InvalidateRange("Strong momentum breakout with ATR expansion");
         return;
      }

      // Rule 3: MA confirms trend (not range)
      double ma50 = GetMAValue(50, 0);
      double adx = GetADX(14);

      if (ma50 > 0)
      {
         // Calculate ATR-based dynamic threshold
         double atr = GetATR(14);
         double priceDistance = MathAbs(currentPrice - ma50);

         // Use 1x ATR as threshold for "away from MA50"
         bool awayFromMA50 = priceDistance > atr;

         // Check previous bar for confirmation
         double prevPrice = iClose(m_symbol, m_timeframe, 1);
         double prevMA50 = GetMAValue(50, 1);

         bool prevAwayFromMA50 = false;
         if (prevMA50 > 0)
         {
            prevAwayFromMA50 = MathAbs(prevPrice - prevMA50) > (atr * 0.8);
         }

         // Check direction
         bool bullishTrend = (currentPrice > ma50) && awayFromMA50;
         bool bearishTrend = (currentPrice < ma50) && awayFromMA50;

         // Invalidate range if price is away from MA50 in either direction
         if ((bullishTrend || bearishTrend) && awayFromMA50 && prevAwayFromMA50 && adx >= 20)
         {
            string trendType = bullishTrend ? "bullish" : "bearish";
            string message = StringFormat("Price %s away from MA50 (Distance: %.2f%%, ATR: %.5f)",
                                          trendType,
                                          (priceDistance / ma50 * 100),
                                          atr);
            InvalidateRange(message);
            return;
         }
      }

      DebugRegimeLogFile("CheckAndInvalidateRange", "Range validation passed, keeping range active");
   }

   bool IsPriceInFixedRange(double price)
   {
      if (!m_rangeActive)
         return false;
      return price >= m_fixedRangeBottom && price <= m_fixedRangeTop;
   }

   //+------------------------------------------------------------------+
   //| MODIFIED HasRangeBroken()                                        |
   //+------------------------------------------------------------------+
   bool HasRangeBroken(double price)
   {
      if (!m_rangeActive)
         return false;

      double atr = GetATR(14);
      double margin = atr * 0.5; // INCREASED buffer from 0.3 ATR to 0.5 ATR

      bool broken = (price > m_fixedRangeTop + margin) ||
                    (price < m_fixedRangeBottom - margin);

      if (broken)
      {
         DebugRegimeLogFile("HasRangeBroken", StringFormat("Range broken at price %.5f (ATR margin: %.5f)", price, margin));
      }

      return broken;
   }

   //+------------------------------------------------------------------+
   //| INVALIDATE RANGE HELPER                                          |
   //+------------------------------------------------------------------+
   void InvalidateRange(string reason)
   {
      DebugRegimeLogFile("InvalidateRange", StringFormat("Invalidating range: %s", reason));

      m_rangeActive = false;
      m_lastInvalidationTime = iTime(m_symbol, m_timeframe, 0);

      string invalidationMsg = StringFormat("Range Invalidated: %s (Cooldown: %d bars)",
                                            reason, m_invalidationCooldownBars);

      Print(invalidationMsg);
      Logger::Log("MarketRegime", invalidationMsg, true, true);
      DebugRegimeLogFile("InvalidateRange", invalidationMsg);

      // Log trade event for range invalidation
      Logger::LogTrade("Range", m_symbol, "INVALIDATE", 0, iClose(m_symbol, m_timeframe, 0));
   }

   //+------------------------------------------------------------------+
   //| SHOULD CREATE NEW RANGE METHOD                                   |
   //+------------------------------------------------------------------+
   bool ShouldCreateNewRange()
   {
      DebugRegimeLogFile("ShouldCreateNewRange", "Checking if new range should be created");

      // 1. Check cooldown period
      if (m_lastInvalidationTime > 0)
      {
         int barsSinceInvalidation = iBarShift(m_symbol, m_timeframe, m_lastInvalidationTime);
         if (barsSinceInvalidation < m_invalidationCooldownBars)
         {
            DebugRegimeLogFile("ShouldCreateNewRange",
                               StringFormat("Still in cooldown: %d/%d bars",
                                            barsSinceInvalidation, m_invalidationCooldownBars));
            return false; // Still in cooldown
         }
      }

      // 2. Market must be clearly ranging (not trending)
      double adx = GetADX(14);
      if (adx >= 25) // ADX too high = trending
      {
         DebugRegimeLogFile("ShouldCreateNewRange",
                            StringFormat("ADX too high for new range: %.1f (>=25)", adx));
         return false;
      }

      // 3. Check MA alignment - price should be near MA50
      double ma50 = GetMAValue(50, 0);
      double currentPrice = iClose(m_symbol, m_timeframe, 0);

      // FIX: Add validation for MA50
      if (ma50 <= 0)
      {
         DebugRegimeLogFile("ShouldCreateNewRange", "Invalid MA50 value");
         return false; // Invalid MA value
      }

      double priceFromMA = MathAbs(currentPrice - ma50) / ma50 * 100;

      if (priceFromMA > 0.8) // Price too far from MA50
      {
         DebugRegimeLogFile("ShouldCreateNewRange",
                            StringFormat("Price too far from MA50: %.2f%% (>0.8%%)", priceFromMA));
         return false;
      }

      // 4. Check for consolidation (low volatility)
      double atr = GetATR(14);
      double volatilityScore = CalculateVolatilityScore(atr, GetBollingerWidth(20, 2));
      if (volatilityScore > 0.5) // Too volatile
      {
         DebugRegimeLogFile("ShouldCreateNewRange",
                            StringFormat("Volatility too high: %.2f (>0.5)", volatilityScore));
         return false;
      }

      // 5. Look for price compression (last 5 bars)
      double high = iHigh(m_symbol, m_timeframe, 0);
      double low = iLow(m_symbol, m_timeframe, 0);

      for (int i = 1; i < 5; i++)
      {
         high = MathMax(high, iHigh(m_symbol, m_timeframe, i));
         low = MathMin(low, iLow(m_symbol, m_timeframe, i));
      }

      double rangeHeight = high - low;
      double avgPrice = (high + low) / 2;

      // FIX: Add validation for avgPrice
      if (avgPrice <= 0)
      {
         DebugRegimeLogFile("ShouldCreateNewRange", "Invalid average price");
         return false;
      }

      double rangePercent = (rangeHeight / avgPrice) * 100;

      if (rangePercent > 0.5) // Range too wide
      {
         DebugRegimeLogFile("ShouldCreateNewRange",
                            StringFormat("Range too wide: %.2f%% (>0.5%%)", rangePercent));
         return false;
      }

      DebugRegimeLogFile("ShouldCreateNewRange", "All conditions met for new range creation");
      return true; // All conditions met, can create new range
   }

   string GetPositionSizeString(ENUM_POSITION_SIZE size)
   {
      switch (size)
      {
      case SIZE_ZERO:
         return "ZERO";
      case SIZE_VERY_SMALL:
         return "VERY_SMALL";
      case SIZE_SMALL:
         return "SMALL";
      case SIZE_MEDIUM:
         return "MEDIUM";
      case SIZE_LARGE:
         return "LARGE";
      default:
         return "UNKNOWN";
      }
   }

   double GetMAValue(int period, int shift = 0)
   {
      if (m_useIndicatorManager && m_indicatorManager != NULL && m_indicatorManager.IsInitialized())
      {
         // Use IndicatorManager for MA values
         double ma9, ma21, ma50, ma89;
         if (m_indicatorManager.GetMAValuesForRange(m_timeframe, ma9, ma21, ma50, ma89, shift))
         {
            if (period == 50)
               return ma50;
            if (period == 89)
               return ma89;
         }
         else
         {
            double ma_fast, ma_slow, ma_medium;
            if (m_indicatorManager.GetMAValues(m_timeframe, ma_fast, ma_slow, ma_medium, shift))
            {
               if (period == 9)
                  return ma_fast;
               if (period == 21)
                  return ma_medium;
               if (period == 89)
                  return ma_slow;
            }
         }
      }

      // Fallback to direct MA calculation
      int handle = iMA(m_symbol, m_timeframe, period, 0, MODE_SMA, PRICE_CLOSE);
      if (handle == INVALID_HANDLE)
         return 0;

      double values[];
      ArraySetAsSeries(values, true);

      if (CopyBuffer(handle, 0, shift, 1, values) > 0)
      {
         IndicatorRelease(handle);
         return values[0];
      }

      IndicatorRelease(handle);
      return 0;
   }

   bool IsMATrendAligned()
   {
      if (m_useIndicatorManager && m_indicatorManager != NULL && m_indicatorManager.IsInitialized())
      {
         // Use IndicatorManager for MA values
         double ma9, ma21, ma50, ma89;
         if (m_indicatorManager.GetMAValuesForRange(m_timeframe, ma9, ma21, ma50, ma89, 0))
         {
            double ma50_10 = GetMAValue(50, 8);
            double ma89_10 = GetMAValue(89, 8);

            if (ma50 <= 0 || ma89 <= 0)
               return false;

            // Calculate separation percentage
            double separation = MathAbs(ma50 - ma89) / ((ma50 + ma89) / 2) * 100;

            // Check alignment and slope
            bool maAlignedBullish = (ma50 > ma89) && (ma50_10 > ma89_10);
            bool maAlignedBearish = (ma50 < ma89) && (ma50_10 < ma89_10);
            bool maSlopeBullish = (ma50 > ma50_10);
            bool maSlopeBearish = (ma50 < ma50_10);

            // Significant separation with same slope = strong trend
            if (separation > 0.5) // 0.5% separation threshold
            {
               if ((maAlignedBullish && maSlopeBullish) ||
                   (maAlignedBearish && maSlopeBearish))
               {
                  DebugRegimeLogFile("IsMATrendAligned", "MA trend alignment confirmed");
                  return true;
               }
            }
         }
      }

      return false;
   }

   ENUM_ROOT_REGIME GetRootState(ENUM_MARKET_STATE state)
   {
      ENUM_ROOT_REGIME rootState;

      switch (state)
      {
      case STATE_TRENDING_LOW_VOL:
      case STATE_TRENDING_HIGH_VOL:
         rootState = REGIME_TRENDING;
         break;

      case STATE_RANGING_LOW_VOL:
      case STATE_RANGING_HIGH_VOL:
         rootState = REGIME_RANGING;
         break;

      default:
         rootState = REGIME_UNKNOWN;
      }

      DebugRegimeLogFile("GetRootState",
                         StringFormat("Root state for %s: %s",
                                      MarketAnalysis::GetStateString(state),
                                      rootState == REGIME_TRENDING ? "TRENDING" : rootState == REGIME_RANGING ? "RANGING"
                                                                                                              : "UNKNOWN"));

      return rootState;
   }

   //+------------------------------------------------------------------+
   //| INDICATOR CALCULATIONS (using IndicatorManager when available)  |
   //+------------------------------------------------------------------+
   double GetADX(int period)
   {
      if (m_useIndicatorManager && m_indicatorManager != NULL && m_indicatorManager.IsInitialized())
      {
         double adx, plus_di, minus_di;
         if (m_indicatorManager.GetADXValues(m_timeframe, adx, plus_di, minus_di))
         {
            return adx;
         }
      }

      // Fallback to direct calculation
      int handle = iADX(m_symbol, m_timeframe, period);
      if (handle == INVALID_HANDLE)
         return 0;

      double adx[];
      ArraySetAsSeries(adx, true);

      if (CopyBuffer(handle, 0, 0, 1, adx) > 0)
      {
         IndicatorRelease(handle);
         return adx[0];
      }

      IndicatorRelease(handle);
      return 0;
   }

   double GetATR(int period)
   {
      if (m_useIndicatorManager && m_indicatorManager != NULL && m_indicatorManager.IsInitialized())
      {
         return m_indicatorManager.GetATR(m_timeframe);
      }

      // Fallback to direct calculation
      int handle = iATR(m_symbol, m_timeframe, period);
      if (handle == INVALID_HANDLE)
         return 0;

      double atr[];
      ArraySetAsSeries(atr, true);

      if (CopyBuffer(handle, 0, 0, 1, atr) > 0)
      {
         IndicatorRelease(handle);
         return atr[0];
      }

      IndicatorRelease(handle);
      return 0;
   }

   double GetBollingerWidth(int period, double deviations)
   {
      if (m_useIndicatorManager && m_indicatorManager != NULL && m_indicatorManager.IsInitialized())
      {
         double upper, middle, lower;
         if (m_indicatorManager.GetBollingerBandsValues(m_timeframe, upper, middle, lower))
         {
            double currentPrice = iClose(m_symbol, m_timeframe, 0);
            if (currentPrice > 0)
               return (upper - lower) / currentPrice;
         }
      }

      // Fallback to direct calculation
      int handle = iBands(m_symbol, m_timeframe, period, 0, deviations, PRICE_CLOSE);
      if (handle == INVALID_HANDLE)
         return 0;

      double upper[], lower[];
      ArraySetAsSeries(upper, true);
      ArraySetAsSeries(lower, true);

      if (CopyBuffer(handle, 1, 0, 1, upper) > 0 &&
          CopyBuffer(handle, 2, 0, 1, lower) > 0)
      {
         IndicatorRelease(handle);
         double currentPrice = iClose(m_symbol, m_timeframe, 0);
         if (currentPrice > 0)
            return (upper[0] - lower[0]) / currentPrice;
      }

      IndicatorRelease(handle);
      return 0;
   }

   double GetPriceChange(int bars)
   {
      double current = iClose(m_symbol, m_timeframe, 0);
      double past = iClose(m_symbol, m_timeframe, bars);

      if (past > 0)
         return ((current - past) / past) * 100;

      return 0;
   }

   int CountRangeTouches()
   {
      if (!m_rangeActive)
         return 0;

      int touches = 0;
      double atr = GetATR(14);
      double tolerance = atr * 0.1;

      for (int i = 0; i < 20; i++)
      {
         double high = iHigh(m_symbol, m_timeframe, i);
         double low = iLow(m_symbol, m_timeframe, i);

         if (MathAbs(high - m_fixedRangeTop) < tolerance ||
             MathAbs(low - m_fixedRangeBottom) < tolerance)
         {
            touches++;
         }
      }

      DebugRegimeLogFile("CountRangeTouches", StringFormat("Range touches: %d", touches));
      return touches;
   }

   // In CalculateVolatilityScore() function:
   double CalculateVolatilityScore(double atr, double bbWidth)
   {
      // Use longer period for smoothing (increase from 20 to 50)
      double atrValues[10];
      int lookback = 10; // Increased from 20

      for (int i = 0; i < lookback; i++)
      {
         double price = iClose(m_symbol, m_timeframe, i);
         if (price > 0)
            atrValues[i] = GetATRValue(14, i) / price;
      }

      double currentAtrNorm = atr / iClose(m_symbol, m_timeframe, 0);
      double avgAtrNorm = ArrayAverage(atrValues, lookback);

      if (avgAtrNorm > 0)
      {
         // Apply smoothing to reduce noise
         static double smoothedScore = 0.5;
         double rawScore = MathMin(1.0, currentAtrNorm / avgAtrNorm);
         smoothedScore = smoothedScore * 0.3 + rawScore * 0.7; // 50% weight to previous value
         return smoothedScore;
      }

      return 0.5;
   }

   bool IsVolatilityRising()
   {
      double atrNow = GetATR(14);
      double atrBefore = GetATRValue(14, 5);

      bool rising = atrNow > atrBefore * 1.1;
      DebugRegimeLogFile("IsVolatilityRising", StringFormat("ATR now=%.5f, before=%.5f, rising=%s",
                                                            atrNow, atrBefore, rising ? "true" : "false"));
      return rising;
   }

   double GetATRValue(int period, int shift)
   {
      if (m_useIndicatorManager && m_indicatorManager != NULL && m_indicatorManager.IsInitialized())
      {
         return m_indicatorManager.GetATR(m_timeframe, shift);
      }

      // Fallback to direct calculation
      int handle = iATR(m_symbol, m_timeframe, period);
      if (handle == INVALID_HANDLE)
         return 0;

      double atr[];
      ArraySetAsSeries(atr, true);

      if (CopyBuffer(handle, 0, shift, 1, atr) > 0)
      {
         IndicatorRelease(handle);
         return atr[0];
      }

      IndicatorRelease(handle);
      return 0;
   }

   bool IsTrendingStructure()
   {
      DebugRegimeLogFile("IsTrendingStructure", "Checking for trending price structure");

      // ========== NEW: ADD MOVING AVERAGE CONFIRMATION ==========
      // Get MA values using IndicatorManager or direct
      double ma50 = GetMAValue(50, 0);
      double ma50_10 = GetMAValue(50, 8);
      double ma89 = GetMAValue(89, 0);
      double ma89_10 = GetMAValue(89, 8);
      double ma200 = GetMAValue(200, 0);

      // Check if we got valid values
      if (ma50 <= 0 || ma89 <= 0 || ma200 <= 0)
      {
         DebugRegimeLogFile("IsTrendingStructure", "Invalid MA values, falling back to basic structure check");
         // Fall back to original price structure logic
         return CheckBasicTrendStructure();
      }

      // Calculate MA separation percentage
      double maDistancePercent = 0;
      double avgPrice = (ma50 + ma89) / 2;
      if (avgPrice > 0)
      {
         maDistancePercent = MathAbs(ma50 - ma89) / avgPrice * 100;
      }

      // Check MA alignment
      bool maAlignedBullish = (ma50 > ma89) && (ma50_10 > ma89_10);
      bool maAlignedBearish = (ma50 < ma89) && (ma50_10 < ma89_10);

      // ========== ENHANCED PRICE STRUCTURE (more forgiving) ==========
      // Look at 8 bars instead of 5
      int lookback = 10;
      double highs[], lows[];
      ArrayResize(highs, lookback);
      ArrayResize(lows, lookback);

      for (int i = 0; i < lookback; i++)
      {
         highs[i] = iHigh(m_symbol, m_timeframe, i);
         lows[i] = iLow(m_symbol, m_timeframe, i);
      }

      // More forgiving trend detection (60% rule instead of 100%)
      int uptrendSignals = 0;
      int downtrendSignals = 0;

      for (int i = 1; i < lookback; i++)
      {
         // Check HH pattern
         if (highs[i] > highs[i - 1])
            uptrendSignals++;
         if (lows[i] > lows[i - 1])
            uptrendSignals++;

         // Check LH pattern
         if (highs[i] < highs[i - 1])
            downtrendSignals++;
         if (lows[i] < lows[i - 1])
            downtrendSignals++;
      }

      int totalChecks = (lookback - 1) * 2;
      double uptrendRatio = totalChecks > 0 ? (double)uptrendSignals / totalChecks : 0;
      double downtrendRatio = totalChecks > 0 ? (double)downtrendSignals / totalChecks : 0;

      DebugRegimeLogFile("IsTrendingStructure",
                         StringFormat("Price structure: Uptrend ratio=%.2f, Downtrend ratio=%.2f",
                                      uptrendRatio, downtrendRatio));

      // ========== DECISION LOGIC (BALANCED) ==========

      // CRITICAL: If MAs are significantly separated (>0.75%), GENTLE trend bias
      // Changed from 0.5% to 0.75% for more selective bias
      if (maDistancePercent > 0.6) // Increase to 0.5 - need clearer MA separation
      {
         // Strong MA separation = trend gets MODERATE priority
         // Changed from 0.4 to 0.5 threshold - need clearer trend structure
         if (maAlignedBullish && uptrendRatio > 0.45)
         {
            DebugRegimeLogFile("IsTrendingStructure", "MA aligned bullish with uptrend structure");
            return true; // Need 50% confirmation (was 40%)
         }
         if (maAlignedBearish && downtrendRatio > 0.45)
         {
            DebugRegimeLogFile("IsTrendingStructure", "MA aligned bearish with downtrend structure");
            return true;
         }
      }

      // Original logic - keep as primary
      if (uptrendRatio > 0.6 || downtrendRatio > 0.6) // 70% threshold
      {
         DebugRegimeLogFile("IsTrendingStructure", "Strong price structure detected");
         return true;
      }

      // Additional check: If price consistently above/below key MAs
      double currentPrice = iClose(m_symbol, m_timeframe, 0);

      // Price far from 200 MA suggests trend
      double distanceFromMA = MathAbs(currentPrice - ma200) / ma200 * 100;
      if (distanceFromMA > 1.0) // Changed from 1.0% to 1.5% - more conservative
      {
         DebugRegimeLogFile("IsTrendingStructure",
                            StringFormat("Price far from MA200 (%.2f%%), forcing trend classification", distanceFromMA));
         return true; // Force trend classification
      }

      DebugRegimeLogFile("IsTrendingStructure", "No trending structure detected");
      return false;
   }

   bool CheckBasicTrendStructure()
   {
      DebugRegimeLogFile("CheckBasicTrendStructure", "Running basic 5-bar trend structure check");

      // Your original 5-bar HH/HL or LH/LL logic
      double highs[5], lows[5];

      for (int i = 0; i < 5; i++)
      {
         highs[i] = iHigh(m_symbol, m_timeframe, i);
         lows[i] = iLow(m_symbol, m_timeframe, i);
      }

      // Check for uptrend structure
      bool uptrend = true;
      for (int i = 1; i < 5; i++)
      {
         if (highs[i] <= highs[i - 1] || lows[i] <= lows[i - 1])
         {
            uptrend = false;
            break;
         }
      }

      // Check for downtrend structure
      bool downtrend = true;
      for (int i = 1; i < 5; i++)
      {
         if (highs[i] >= highs[i - 1] || lows[i] >= lows[i - 1])
         {
            downtrend = false;
            break;
         }
      }

      bool result = uptrend || downtrend;
      DebugRegimeLogFile("CheckBasicTrendStructure",
                         StringFormat("Basic trend check result: %s (Uptrend: %s, Downtrend: %s)",
                                      result ? "true" : "false", uptrend ? "true" : "false", downtrend ? "true" : "false"));

      return result;
   }

   bool IsRangingStructure()
   {
      if (!m_rangeActive)
      {
         DebugRegimeLogFile("IsRangingStructure", "Range not active");
         return false;
      }

      double currentPrice = iClose(m_symbol, m_timeframe, 0);
      bool inRange = IsPriceInFixedRange(currentPrice);

      DebugRegimeLogFile("IsRangingStructure",
                         StringFormat("Price %.5f in range %.5f-%.5f: %s",
                                      currentPrice, m_fixedRangeBottom, m_fixedRangeTop, inRange ? "true" : "false"));

      return inRange;
   }

   double CalculateConfidence(ENUM_MARKET_STATE state)
   {
      DebugRegimeLogFile("CalculateConfidence",
                         StringFormat("Calculating confidence for state: %s", MarketAnalysis::GetStateString(state)));

      double adx = GetADX(14);
      double atrScore = CalculateVolatilityScore(GetATR(14), GetBollingerWidth(20, 2));
      double structureConfidence = 0;

      if (state == STATE_TRENDING_LOW_VOL || state == STATE_TRENDING_HIGH_VOL)
      {
         structureConfidence = IsTrendingStructure() ? 80 : 40;
      }
      else if (state == STATE_RANGING_LOW_VOL || state == STATE_RANGING_HIGH_VOL)
      {
         structureConfidence = IsRangingStructure() ? 80 : 40;
      }

      double confidence = 0;

      switch (state)
      {
      case STATE_CONTRACTION:
         confidence = (1.0 - atrScore) * 100 * 0.7 + (adx < 20 ? 30 : 0);
         break;

      case STATE_EXPANSION:
         confidence = (HasRangeBroken(iClose(m_symbol, m_timeframe, 0)) ? 80 : 30);
         break;

      case STATE_TRENDING_LOW_VOL:
         confidence = MathMin(100, adx * 2) * 0.6 + structureConfidence * 0.4;
         break;

      case STATE_TRENDING_HIGH_VOL:
         confidence = MathMin(100, adx) * 0.7 + structureConfidence * 0.3;
         break;

      case STATE_RANGING_LOW_VOL:
      case STATE_RANGING_HIGH_VOL:
         confidence = (m_rangeActive ? 70 : 30) + (CountRangeTouches() * 5);
         break;

      case STATE_CHURN:
         confidence = (atrScore > 0.7 ? 70 : 30) + (adx > 40 ? 30 : 0);
         break;
      }

      confidence = MathMin(100, MathMax(0, confidence));

      DebugRegimeLogFile("CalculateConfidence",
                         StringFormat("Final confidence: %.1f%% (ADX=%.1f, ATRScore=%.2f, StructureConf=%.0f)",
                                      confidence, adx, atrScore, structureConfidence));

      return confidence;
   }

   //+------------------------------------------------------------------+
   //| STATE PREDICTION                                                |
   //+------------------------------------------------------------------+
   ENUM_MARKET_STATE PredictNextState(ENUM_MARKET_STATE currentState)
   {
      DebugRegimeLogFile("PredictNextState",
                         StringFormat("Predicting next state from: %s", MarketAnalysis::GetStateString(currentState)));

      ENUM_MARKET_STATE nextState;

      // State transition probabilities
      switch (currentState)
      {
      case STATE_RANGING_LOW_VOL:
         // Could compress further or expand into high volatility range
         nextState = STATE_CONTRACTION;
         break;

      case STATE_CONTRACTION:
         // Compression leads to expansion
         nextState = STATE_EXPANSION;
         break;

      case STATE_EXPANSION:
         // Breakout leads to trending or failed breakout leads back to range
         nextState = STATE_TRENDING_LOW_VOL;
         break;

      case STATE_TRENDING_LOW_VOL:
         // Healthy trend matures into high volatility trend
         nextState = STATE_TRENDING_HIGH_VOL;
         break;

      case STATE_TRENDING_HIGH_VOL:
         // Exhaustion leads to churn
         nextState = STATE_CHURN;
         break;

      case STATE_CHURN:
         // Churn leads to new range (high vol)
         nextState = STATE_RANGING_HIGH_VOL;
         break;

      case STATE_RANGING_HIGH_VOL:
         // High vol range stabilizes into low vol range
         nextState = STATE_RANGING_LOW_VOL;
         break;

      default:
         nextState = STATE_UNKNOWN;
      }

      DebugRegimeLogFile("PredictNextState",
                         StringFormat("Predicted next state: %s", MarketAnalysis::GetStateString(nextState)));

      return nextState;
   }

   //+------------------------------------------------------------------+
   //| TRADING RECOMMENDATIONS                                         |
   //+------------------------------------------------------------------+
   void GenerateRecommendations(MarketAnalysis &analysis)
   {
      DebugRegimeLogFile("GenerateRecommendations", "Generating trading recommendations");

      double currentPrice = iClose(m_symbol, m_timeframe, 0);
      double atr = GetATR(14);

      // Default values
      analysis.action = "Wait for clarity";
      analysis.positionSize = SIZE_ZERO;
      analysis.stopDistance = atr * 1.5;
      analysis.takeProfitDistance = atr * 3.0;
      analysis.riskRewardRatio = 2.0;
      analysis.direction = "Neutral";

      // State-specific recommendations
      switch (analysis.state)
      {
      case STATE_RANGING_LOW_VOL:
         analysis.action = "Fade range extremes";
         analysis.positionSize = SIZE_SMALL;
         analysis.stopDistance = (m_fixedRangeTop - m_fixedRangeBottom) * 0.2;
         analysis.takeProfitDistance = (m_fixedRangeTop - m_fixedRangeBottom) * 0.6;
         analysis.riskRewardRatio = 3.0;
         analysis.direction = "Mean reversion";
         DebugRegimeLogFile("GenerateRecommendations", "RANGING_LOW_VOL: Fade extremes, mean reversion");
         break;

      case STATE_RANGING_HIGH_VOL:
         analysis.action = "Fade carefully with tight stops";
         analysis.positionSize = SIZE_VERY_SMALL;
         analysis.stopDistance = atr * 0.8;
         analysis.takeProfitDistance = atr * 1.5;
         analysis.riskRewardRatio = 2.0;
         analysis.direction = "Neutral (dangerous)";
         DebugRegimeLogFile("GenerateRecommendations", "RANGING_HIGH_VOL: Fade carefully, tight stops");
         break;

      case STATE_CONTRACTION:
         analysis.action = "Prepare for breakout - no trades";
         analysis.positionSize = SIZE_ZERO;
         analysis.stopDistance = 0;
         analysis.takeProfitDistance = 0;
         analysis.riskRewardRatio = 0;
         analysis.direction = "Neutral (waiting)";
         DebugRegimeLogFile("GenerateRecommendations", "CONTRACTION: No trades, prepare for breakout");
         break;

      case STATE_EXPANSION:
         analysis.action = "Test breakout entry";
         analysis.positionSize = SIZE_MEDIUM;
         analysis.stopDistance = atr * 1.2;
         analysis.takeProfitDistance = atr * 3.6;
         analysis.riskRewardRatio = 3.0;

         if (currentPrice > m_fixedRangeTop)
         {
            analysis.direction = "Bullish breakout";
            DebugRegimeLogFile("GenerateRecommendations", "EXPANSION: Bullish breakout entry");
         }
         else if (currentPrice < m_fixedRangeBottom)
         {
            analysis.direction = "Bearish breakout";
            DebugRegimeLogFile("GenerateRecommendations", "EXPANSION: Bearish breakout entry");
         }
         else
         {
            analysis.direction = "Testing";
            DebugRegimeLogFile("GenerateRecommendations", "EXPANSION: Testing breakout");
         }
         break;

      case STATE_TRENDING_LOW_VOL:
         analysis.action = "Add to winning positions";
         analysis.positionSize = SIZE_LARGE;
         analysis.stopDistance = atr * 2.0;
         analysis.takeProfitDistance = atr * 6.0;
         analysis.riskRewardRatio = 3.0;
         analysis.direction = IsTrendingStructure() ? "With trend" : "Neutral";
         DebugRegimeLogFile("GenerateRecommendations", "TRENDING_LOW_VOL: Add to winning positions");
         break;

      case STATE_TRENDING_HIGH_VOL:
         analysis.action = "Take partial profits";
         analysis.positionSize = SIZE_MEDIUM;
         analysis.stopDistance = atr * 1.0;
         analysis.takeProfitDistance = atr * 2.0;
         analysis.riskRewardRatio = 2.0;
         analysis.direction = "With trend (cautious)";
         DebugRegimeLogFile("GenerateRecommendations", "TRENDING_HIGH_VOL: Take partial profits");
         break;

      case STATE_CHURN:
         analysis.action = "Exit positions, wait for clarity";
         analysis.positionSize = SIZE_ZERO;
         analysis.stopDistance = 0;
         analysis.takeProfitDistance = 0;
         analysis.riskRewardRatio = 0;
         analysis.direction = "Neutral (avoid)";
         DebugRegimeLogFile("GenerateRecommendations", "CHURN: Exit positions, wait for clarity");
         break;
      }

      // Adjust position size based on account
      analysis.positionSize = AdjustPositionSize(analysis.positionSize);

      DebugRegimeLogFile("GenerateRecommendations",
                         StringFormat("Final recommendation: %s, Position: %s, Stop: %.2f, TP: %.2f, R/R: %.1f",
                                      analysis.action, GetPositionSizeString(analysis.positionSize),
                                      analysis.stopDistance, analysis.takeProfitDistance, analysis.riskRewardRatio));

      // Log trading recommendation
      Logger::LogTrade("Recommendation", m_symbol,
                       analysis.direction == "Bullish breakout" ? "BUY" : analysis.direction == "Bearish breakout" ? "SELL"
                                                                                                                   : "HOLD",
                       0, currentPrice);

      // Convert to pips for display if needed
      double point = SymbolInfoDouble(m_symbol, SYMBOL_POINT);
      if (point > 0)
      {
         analysis.stopDistance = analysis.stopDistance / point;
         analysis.takeProfitDistance = analysis.takeProfitDistance / point;
      }
   }

   ENUM_POSITION_SIZE AdjustPositionSize(ENUM_POSITION_SIZE baseSize)
   {
      // Adjust based on account size and risk
      double riskAmount = m_accountBalance * (m_accountRiskPercent / 100);

      ENUM_POSITION_SIZE adjustedSize = baseSize;

      if (riskAmount < 100) // Very small account
      {
         switch (baseSize)
         {
         case SIZE_LARGE:
            adjustedSize = SIZE_MEDIUM;
            DebugRegimeLogFile("AdjustPositionSize", "Account small: LARGE -> MEDIUM");
            break;
         case SIZE_MEDIUM:
            adjustedSize = SIZE_SMALL;
            DebugRegimeLogFile("AdjustPositionSize", "Account small: MEDIUM -> SMALL");
            break;
         default:
            adjustedSize = baseSize;
         }
      }

      DebugRegimeLogFile("AdjustPositionSize",
                         StringFormat("Position size: %s -> %s (Account: $%.2f, Risk: $%.2f)",
                                      GetPositionSizeString(baseSize),
                                      GetPositionSizeString(adjustedSize),
                                      m_accountBalance, riskAmount));

      return adjustedSize;
   }

   //+------------------------------------------------------------------+
   //| UTILITY FUNCTIONS                                               |
   //+------------------------------------------------------------------+
   double ArrayAverage(double &arr[], int count)
   {
      double sum = 0;
      int validCount = 0;

      for (int i = 0; i < count && i < ArraySize(arr); i++)
      {
         sum += arr[i];
         validCount++;
      }

      double avg = validCount > 0 ? sum / validCount : 0;
      return avg;
   }

   string GenerateDescription(MarketAnalysis &analysis)
   {
      string desc = "";

      switch (analysis.state)
      {
      case STATE_RANGING_LOW_VOL:
         desc = "Low volatility consolidation. Market is balanced, awaiting catalyst.";
         break;

      case STATE_RANGING_HIGH_VOL:
         desc = "High volatility range. Emotional swings, frequent stop hunts.";
         break;

      case STATE_CONTRACTION:
         desc = "Volatility squeeze/compression. Energy building for next move.";
         break;

      case STATE_EXPANSION:
         desc = "Breakout/expansion phase. New trend may be forming.";
         break;

      case STATE_TRENDING_LOW_VOL:
         desc = "Healthy institutional trend. Ideal trading conditions.";
         break;

      case STATE_TRENDING_HIGH_VOL:
         desc = "Parabolic/exhaustion trend. Late stage, reversal risk high.";
         break;

      case STATE_CHURN:
         desc = "Exhaustion/distribution. Directionless volatility, avoid trading.";
         break;

      default:
         desc = "Market state unclear. Wait for better signals.";
      }

      DebugRegimeLogFile("GenerateDescription", StringFormat("Description: %s", desc));

      return desc;
   }

public:
   //+------------------------------------------------------------------+
   //| MANUAL RANGE REINITIALIZATION                                   |
   //+------------------------------------------------------------------+
   void ResetRange()
   {
      DebugRegimeLogFile("ResetRange", "Manual range reset requested");
      Logger::Log("MarketRegime", "Manual range reset", true, true);
      ReinitializeRange();
   }

   //+------------------------------------------------------------------+
   //| GET/SET RANGE CONFIGURATION                                     |
   //+------------------------------------------------------------------+
   int GetBarsSinceLastRangeInit() const { return m_barsSinceLastRangeInit; }
   int GetRangeLookbackBars() const { return m_rangeLookbackBars; }

   void SetRangeLookbackBars(int bars)
   {
      m_rangeLookbackBars = bars;
      string msg = "Range lookback bars set to: " + IntegerToString(bars);
      Print(msg);
      Logger::Log("MarketRegime", msg, true, false);
      DebugRegimeLogFile("SetRangeLookbackBars", msg);
   }

   //+------------------------------------------------------------------+
   //| GETTER METHODS                                                  |
   //+------------------------------------------------------------------+
   double GetRangeTop() const { return m_fixedRangeTop; }
   double GetRangeBottom() const { return m_fixedRangeBottom; }
   bool IsRangeActive() const { return m_rangeActive; }

   void SetAccountInfo(double balance, double riskPercent)
   {
      m_accountBalance = balance;
      m_accountRiskPercent = riskPercent;

      DebugRegimeLogFile("SetAccountInfo",
                         StringFormat("Account info updated: Balance=$%.2f, Risk=%.1f%%",
                                      balance, riskPercent));
   }

   // Access to IndicatorManager for other uses
   IndicatorManager *GetIndicatorManager() const { return m_indicatorManager; }

   // Method to get IndicatorManager initialization status
   bool IsIndicatorManagerInitialized() const
   {
      bool initialized = (m_indicatorManager != NULL && m_indicatorManager.IsInitialized());
      DebugRegimeLogFile("IsIndicatorManagerInitialized",
                         StringFormat("IndicatorManager initialized: %s", initialized ? "true" : "false"));
      return initialized;
   }
};

//+------------------------------------------------------------------+
//| SIMPLE CHART DRAWING FUNCTIONS                                   |
//+------------------------------------------------------------------+
void DrawMarketRegime(MarketAnalysis &analysis, string symbol = NULL, ENUM_TIMEFRAMES tf = PERIOD_H1)
{
   if (symbol == NULL)
      symbol = Symbol();

   long chartId = ChartID();

   // Remove previous drawings
   RemoveRegimeDrawings(chartId);

   // Draw fixed range if active
   MarketRegimeDetector detector(symbol, tf);
   if (detector.IsRangeActive())
   {
      double top = detector.GetRangeTop();
      double bottom = detector.GetRangeBottom();

      // Draw range lines
      DrawHorizontalLine(chartId, "Regime_Range_Top", top, clrBlue, STYLE_DASH, 2);
      DrawHorizontalLine(chartId, "Regime_Range_Bottom", bottom, clrBlue, STYLE_DASH, 2);
   }

   // Draw regime info box
   string infoText = StringFormat(
       "State: %s (%.0f%%)\n" +
           "Action: %s\n" +
           "Position: %s\n" +
           "Direction: %s\n" +
           "R/R: %.1f",
       MarketAnalysis::GetStateString(analysis.state),
       analysis.confidence,
       analysis.action,
       MarketAnalysis::GetStateString(analysis.nextLikelyState),
       analysis.direction,
       analysis.riskRewardRatio);

   DrawText(chartId, "Regime_Info", TimeCurrent() + PeriodSeconds(tf) * 15,
            iClose(symbol, tf, 0), infoText, clrWhite, 10, "Arial");

   ChartRedraw(chartId);
}

void RemoveRegimeDrawings(long chartId)
{
   int total = ObjectsTotal(chartId);
   for (int i = total - 1; i >= 0; i--)
   {
      string name = ObjectName(chartId, i);
      if (StringFind(name, "Regime_") == 0)
      {
         ObjectDelete(chartId, name);
      }
   }
}

void DrawHorizontalLine(long chartId, string name, double price, color clr,
                        ENUM_LINE_STYLE style, int width)
{
   if (ObjectFind(chartId, name) >= 0)
      ObjectDelete(chartId, name);

   ObjectCreate(chartId, name, OBJ_HLINE, 0, 0, price);
   ObjectSetInteger(chartId, name, OBJPROP_COLOR, clr);
   ObjectSetInteger(chartId, name, OBJPROP_STYLE, style);
   ObjectSetInteger(chartId, name, OBJPROP_WIDTH, width);
   ObjectSetInteger(chartId, name, OBJPROP_BACK, true);
}

void DrawText(long chartId, string name, datetime time, double price,
              string text, color clr, int fontSize, string font)
{
   if (ObjectFind(chartId, name) >= 0)
      ObjectDelete(chartId, name);

   ObjectCreate(chartId, name, OBJ_TEXT, 0, time, price);
   ObjectSetString(chartId, name, OBJPROP_TEXT, text);
   ObjectSetInteger(chartId, name, OBJPROP_COLOR, clr);
   ObjectSetInteger(chartId, name, OBJPROP_FONTSIZE, fontSize);
   ObjectSetString(chartId, name, OBJPROP_FONT, font);
   ObjectSetInteger(chartId, name, OBJPROP_BACK, false);
}

//+------------------------------------------------------------------+
//| ENHANCED CHART DISPLAY FUNCTIONS                                |
//+------------------------------------------------------------------+
//+------------------------------------------------------------------+
//| SIMPLIFIED: Show range ONLY when market is ranging              |
//+------------------------------------------------------------------+
void DisplayMarketRegimeOnChart(MarketAnalysis &analysis,
                                string symbol = NULL,
                                ENUM_TIMEFRAMES tf = PERIOD_H1,
                                int corner = CORNER_RIGHT_UPPER)
{
   DebugRegimeLogFile("DisplayMarketRegimeOnChart", "Updating chart display");

   if (symbol == NULL)
      symbol = Symbol();

   long chartId = ChartID();
   string prefix = "MarketRegime_";

   // Remove ALL previous drawings first
   RemoveAllRegimeDrawings(chartId, prefix);

   // ==================== CRITICAL CHECK: Only show in ranging states ====================
   bool shouldShowRange = false;

   switch (analysis.state)
   {
   case STATE_RANGING_LOW_VOL:
   case STATE_RANGING_HIGH_VOL:
   case STATE_CONTRACTION: // Contraction/squeeze is also a ranging state
      shouldShowRange = true;
      DebugRegimeLogFile("DisplayMarketRegimeOnChart", "Showing range (ranging state detected)");
      break;

   case STATE_TRENDING_LOW_VOL:
   case STATE_TRENDING_HIGH_VOL:
   case STATE_EXPANSION: // Expansion/breakout = trending
   case STATE_CHURN:     // Churn/exhaustion = unclear
   case STATE_UNKNOWN:
   default:
      shouldShowRange = false;
      DebugRegimeLogFile("DisplayMarketRegimeOnChart", "Hiding range (non-ranging state)");
   }

   // Only proceed if we should show range
   if (!shouldShowRange)
   {
      // Remove any existing drawings and exit
      ChartRedraw(chartId);
      return;
   }

   // Draw fixed range on chart if active
   MarketRegimeDetector detector(symbol, tf);

   // Double-check: Is range actually active?
   if (detector.IsRangeActive() && shouldShowRange)
   {
      double top = detector.GetRangeTop();
      double bottom = detector.GetRangeBottom();

      // Only draw if range is meaningful (not too narrow)
      double rangeHeight = top - bottom;
      double avgPrice = (top + bottom) / 2;

      if (avgPrice > 0 && (rangeHeight / avgPrice) * 100 > 0.05) // 0.05% minimum width
      {
         // Draw ONLY the range lines (no text, no labels)
         DrawHorizontalLine(chartId, prefix + "RangeTop", top, clrDodgerBlue, STYLE_DASH, 2);
         DrawHorizontalLine(chartId, prefix + "RangeBottom", bottom, clrDodgerBlue, STYLE_DASH, 2);

         DebugRegimeLogFile("DisplayMarketRegimeOnChart",
                            StringFormat("Range drawn: Top=%.5f, Bottom=%.5f, Height=%.2f%%",
                                         top, bottom, (rangeHeight / avgPrice) * 100));
      }
   }

   // If we get here and range shouldn't be shown, the RemoveAllRegimeDrawings
   // at the beginning already cleared any existing drawings

   ChartRedraw(chartId);
}

//+------------------------------------------------------------------+
//| PIXEL-BASED DRAWING FUNCTIONS (for panel)                       |
//+------------------------------------------------------------------+

void DrawTextPixel(long chartId, string name, int x, int y, string text,
                   color clr, int fontSize, string font, int corner)
{
   if (ObjectFind(chartId, name) >= 0)
      ObjectDelete(chartId, name);

   ObjectCreate(chartId, name, OBJ_LABEL, 0, 0, 0);
   ObjectSetInteger(chartId, name, OBJPROP_CORNER, corner);
   ObjectSetInteger(chartId, name, OBJPROP_XDISTANCE, x);
   ObjectSetInteger(chartId, name, OBJPROP_YDISTANCE, y);
   ObjectSetString(chartId, name, OBJPROP_TEXT, text);
   ObjectSetInteger(chartId, name, OBJPROP_COLOR, clr);
   ObjectSetInteger(chartId, name, OBJPROP_FONTSIZE, fontSize);
   ObjectSetString(chartId, name, OBJPROP_FONT, font);
   ObjectSetInteger(chartId, name, OBJPROP_BACK, false);
   ObjectSetInteger(chartId, name, OBJPROP_SELECTABLE, false);
}

void DrawLinePixel(long chartId, string name, int x1, int y1, int x2, int y2,
                   color clr, int width)
{
   if (ObjectFind(chartId, name) >= 0)
      ObjectDelete(chartId, name);

   ObjectCreate(chartId, name, OBJ_TREND, 0, 0, 0);
   ObjectSetInteger(chartId, name, OBJPROP_CORNER, CORNER_LEFT_UPPER);
   ObjectSetInteger(chartId, name, OBJPROP_XDISTANCE, x1);
   ObjectSetInteger(chartId, name, OBJPROP_YDISTANCE, y1);
   ObjectSetInteger(chartId, name, OBJPROP_XSIZE, x2 - x1);
   ObjectSetInteger(chartId, name, OBJPROP_YSIZE, y2 - y1);
   ObjectSetInteger(chartId, name, OBJPROP_COLOR, clr);
   ObjectSetInteger(chartId, name, OBJPROP_WIDTH, width);
   ObjectSetInteger(chartId, name, OBJPROP_BACK, false);
   ObjectSetInteger(chartId, name, OBJPROP_SELECTABLE, false);
   ObjectSetInteger(chartId, name, OBJPROP_RAY, false);
}

void DrawTextOnChart(long chartId, string name, datetime time, double price,
                     string text, color clr, int fontSize, string font,
                     ENUM_ANCHOR_POINT anchor = ANCHOR_LEFT_UPPER)
{
   if (ObjectFind(chartId, name) >= 0)
      ObjectDelete(chartId, name);

   ObjectCreate(chartId, name, OBJ_TEXT, 0, time, price);
   ObjectSetString(chartId, name, OBJPROP_TEXT, text);
   ObjectSetInteger(chartId, name, OBJPROP_COLOR, clr);
   ObjectSetInteger(chartId, name, OBJPROP_FONTSIZE, fontSize);
   ObjectSetString(chartId, name, OBJPROP_FONT, font);
   ObjectSetInteger(chartId, name, OBJPROP_ANCHOR, anchor);
   ObjectSetInteger(chartId, name, OBJPROP_BACK, false);
   ObjectSetInteger(chartId, name, OBJPROP_SELECTABLE, false);
}

void RemoveAllRegimeDrawings(long chartId, string prefix = "MarketRegime_")
{
   int total = ObjectsTotal(chartId);
   for (int i = total - 1; i >= 0; i--)
   {
      string name = ObjectName(chartId, i);
      if (StringFind(name, prefix) == 0)
      {
         ObjectDelete(chartId, name);
      }
   }
}

//+------------------------------------------------------------------+
//| COLOR FUNCTIONS FOR VISUAL FEEDBACK                             |
//+------------------------------------------------------------------+

color GetStateColor(ENUM_MARKET_STATE state)
{
   switch (state)
   {
   case STATE_RANGING_LOW_VOL:
      return clrSteelBlue; // Blue for consolidation
   case STATE_RANGING_HIGH_VOL:
      return clrOrangeRed; // Red-orange for dangerous range
   case STATE_TRENDING_LOW_VOL:
      return clrLimeGreen; // Green for healthy trend
   case STATE_TRENDING_HIGH_VOL:
      return clrGold; // Gold/yellow for exhaustion
   case STATE_CONTRACTION:
      return clrDarkSlateGray; // Dark gray for compression
   case STATE_EXPANSION:
      return clrDodgerBlue; // Bright blue for expansion
   case STATE_CHURN:
      return clrDarkOrange; // Orange for churn
   default:
      return clrGray;
   }
}

color GetTransitionColor(ENUM_MARKET_STATE current, ENUM_MARKET_STATE next)
{
   // Color transitions based on expected move
   if ((current == STATE_CONTRACTION && next == STATE_EXPANSION) ||
       (current == STATE_EXPANSION && next == STATE_TRENDING_LOW_VOL))
      return clrLime; // Green for positive transitions

   if ((current == STATE_TRENDING_HIGH_VOL && next == STATE_CHURN) ||
       (current == STATE_CHURN && next == STATE_RANGING_HIGH_VOL))
      return clrOrangeRed; // Red for negative transitions

   return clrYellow; // Yellow for neutral transitions
}

color GetActionColor(string action)
{
   if (StringFind(action, "Fade") >= 0)
      return clrDodgerBlue; // Blue for mean reversion
   if (StringFind(action, "Add") >= 0 || StringFind(action, "Test") >= 0)
      return clrLime; // Green for trend following
   if (StringFind(action, "Take") >= 0)
      return clrGold; // Yellow for profit taking
   if (StringFind(action, "Exit") >= 0 || StringFind(action, "Wait") >= 0)
      return clrOrangeRed; // Red for exiting/avoiding

   return clrWhite;
}

color GetPositionSizeColor(ENUM_POSITION_SIZE size)
{
   switch (size)
   {
   case SIZE_ZERO:
      return clrGray;
   case SIZE_VERY_SMALL:
      return clrOrangeRed;
   case SIZE_SMALL:
      return clrOrange;
   case SIZE_MEDIUM:
      return clrYellow;
   case SIZE_LARGE:
      return clrLime;
   default:
      return clrWhite;
   }
}

color GetDirectionColor(string direction)
{
   if (StringFind(direction, "Bullish") >= 0 || StringFind(direction, "Up") >= 0)
      return clrLime;
   if (StringFind(direction, "Bearish") >= 0 || StringFind(direction, "Down") >= 0)
      return clrRed;
   if (StringFind(direction, "With trend") >= 0)
      return clrLimeGreen;
   if (StringFind(direction, "Mean reversion") >= 0)
      return clrDodgerBlue;
   if (StringFind(direction, "Neutral") >= 0)
      return clrGray;

   return clrYellow;
}

color GetRRColor(double rrRatio)
{
   if (rrRatio >= 3.0)
      return clrLime;
   if (rrRatio >= 2.0)
      return clrYellow;
   if (rrRatio > 0)
      return clrOrange;

   return clrGray;
}

color GetRangePositionColor(double positionPercent)
{
   if (positionPercent < 20)
      return clrRed; // Oversold near bottom
   if (positionPercent > 80)
      return clrLime; // Overbought near top
   if (positionPercent < 40)
      return clrOrange; // Lower half
   if (positionPercent > 60)
      return clrDodgerBlue; // Upper half

   return clrYellow; // Middle
}

//+------------------------------------------------------------------+
//| ENHANCED GLOBAL HELPER FUNCTION                                  |
//+------------------------------------------------------------------+
MarketAnalysis GetMarketRegimeWithDisplay(string symbol = NULL,
                                          ENUM_TIMEFRAMES tf = PERIOD_H1,
                                          bool showPanel = true,
                                          int panelCorner = CORNER_RIGHT_UPPER,
                                          bool useIndicatorManager = true)
{
   static MarketRegimeDetector *detector = NULL;

   if (detector == NULL)
   {
      detector = new MarketRegimeDetector(symbol, tf, 10000, 1.0, useIndicatorManager);
      Logger::Log("MarketRegime", StringFormat("Global detector initialized for %s", symbol), true, false);
   }

   // Get analysis
   MarketAnalysis analysis = detector.GetMarketRegime();

   // Display on chart if requested
   if (showPanel)
   {
      DisplayMarketRegimeOnChart(analysis, symbol, tf, panelCorner);
   }

   return analysis;
}
//+------------------------------------------------------------------+




+++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++


give me a recreated file that:
1. 

GetMarketRegime() (Public Entry Point)
    │
    ├── CheckRangeAge()  ← Simple: if (m_barsSinceLastRangeInit > 10) reinitialize
    ├── CheckRangeBreak()  ← Simple: if (small breaks <=0.2? is added to range) reinitialize
    │    │
    │    └── ReinitializeRange() → FindBoundaries()  ← Just high/low, NO activation
    │
    ├── ShouldCreateNewRange()  ← Simple conditions only:
    │    ├── Cooldown passed? (m_lastInvalidationTime)
    │    ├── ADX < 25? (not trending)
    │    ├── Price near/touching MA50?
    │    ├── Low volatility?
    │    └── Price compression? (narrow 5-bar range)
    │
    ├── FindBoundaries()  ← If ShouldCreateNewRange() returns true
    │    └── Sets m_fixedRangeTop/Bottom but NOT m_rangeActive
    │
    └── DetectCurrentState()  ← SIMPLE hierarchy with conflict resolution
         │
         ├── [PHASE 1: ACTIVE RANGE PERSISTENCE] (Highest Priority)
         │    │
         │    ├── if (m_rangeActive)
         │    │    │
         │    │    ├── RangeStillValid()?  ← Simple tolerance check
         │    │    │    ├── Price within (range ± 0.3*ATR)?
         │    │    │    └── Not broken for 2+ consecutive bars?
         │    │    │
         │    │    ├── YES → Return RANGING_LOW_VOL or RANGING_HIGH_VOL
         │    │    │
         │    │    └── NO → InvalidateRange() → Continue to validation
         │    │
         │    └── [Range not active or invalidated]
         │
         ├── [PHASE 2: VALIDATION]
         │    │
         │    ├── ValidateRange()  ← Needs evidence
         │    │    ├── Touches on BOTH boundaries? (≥1 each)
         │    │    ├── NO clear HH/HL or LL/LH structure?
         │    │    └── MAs relatively flat? (<0.3% slope)
         │    │
         │    ├── ValidateTrend()  ← Needs STRONG evidence  
         │    │    ├── Clear HH/HL or LL/LH structure? (5+ bars)
         │    │    ├── MA separation > 0.5%?
         │    │    └── ADX ≥ 25?
         │    │
         │    └── ScoreRangeEvidence() / ScoreTrendEvidence()  ← For tie-breaking
         │
         ├── [PHASE 3: DECISION MATRIX WITH CONFLICT RESOLUTION]
         │    │
         │    ├── CASE 1: Clear Range (Range=YES, Trend=NO)
         │    │    ├── ActivateRange()  ← Sets m_rangeActive = true
         │    │    └── Return RANGING state (low/high vol based on volatility)
         │    │
         │    ├── CASE 2: Clear Trend (Range=NO, Trend=YES)
         │    │    ├── CheckAndInvalidateTrend()?  ← Time expiration & structure
         │    │    │    ├── Trend expired (>30 bars)?
         │    │    │    ├── Checj also ShouldRenewTrend()
         │    │    │    ├── Trend structure broken?
         │    │    │    └── MAs 50 and price are too close?
         │    │    │
         │    │    ├── NO Invalidation → Return TRENDING state
         │    │    │
         │    │    └── YES Invalidation → Fall to TRANSITION
         │    │
         │    ├── CASE 3: Conflict (Range=YES, Trend=YES)  ← BOTH possible
         │    │    │
         │    │    ├── Calculate scores (0-100):
         │    │    │    RangeScore = Touches(40) + Structure(30) + MA(30)
         │    │    │    TrendScore = Structure(40) + MA(30) + ADX(30)
         │    │    │
         │    │    ├── Decision rules:
         │    │    │    ├── If RangeScore > TrendScore + 10 → ActivateRange() → RANGING
         │    │    │    ├── If TrendScore > RangeScore + 15 → Check invalidation → TRENDING
         │    │    │    └── Else (too close) → TRANSITION state
         │    │    │
         │    │    └── [Example: RangeScore=65, TrendScore=60 → Range wins]
         │    │         [Example: RangeScore=70, TrendScore=85 → Trend wins]
         │    │         [Example: RangeScore=68, TrendScore=72 → Too close, TRANSITION]
         │    │
         │    └── CASE 4: Neither (Range=NO, Trend=NO) or unclear
         │         │
         │         └── DetermineTransitionState()
         │              ├── Volatility low + ADX low → STATE_CONTRACTION
         │              ├── Volatility high + ADX high → STATE_CHURN
         │              └── Default → STATE_EXPANSION
         │
         └── [RETURN FINAL STATE]


